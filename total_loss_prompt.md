You own one Zeus floor-crossing or precursor incident end to end.

The objective is not to explain a loss after the fact and not to add a
mechanical stop-loss. Find the earliest causal point at which a better Zeus,
using only information then available, could have preserved more after-cost
capital. The target ordering is:

source -> probability -> monitor -> decision -> command -> fill -> floor

Reconstruct every clock from the incident evidence database and repository.
Measure information, decision, actuation, and execution lead relative to the
first held-side executable bid below the repository price floor. If no
executable exit existed, continue upstream to entry, sizing, market selection,
and depth rather than declaring the incident unfixable.

First perform a blind diagnosis without historical root narratives. Return the
earliest preventable time, exact causal mechanism and code/runtime seam, and an
executable capital counterfactual. After the controller supplies dedicated
episodic memory, classify the result as same_root, root_variant, new_root,
fix_not_deployed, fix_incomplete, or antibody_failed.

For repair work, implement the smallest structural correction, prove it on the
exact incident and nearby trajectories, preserve profitable HOLD behavior, and
carry it through tests, fresh review, PR/CI feedback, merge, deployment, loaded
SHA verification, and production observation. Do not stop at a report or Draft
PR. Repository law and current runtime facts remain authoritative.
