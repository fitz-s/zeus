# M5 — collection-lifecycle timestamps stamped at REAL code points

Created: 2026-06-16
Last audited: 2026-06-16
Authority basis: M5 task brief (timing-semantics fix worktree); operator BASIS RULE — every replacement timestamp must capture a REAL distinct instant; SIMPLIFY (additive instrumentation only); preserve existing C1-AVAIL-CLOCK fix.

## The defect (recap)
ECMWF collection telemetry stamped every collection-plane instant
(`fetch_started_at`, `fetch_finished_at`, `captured_at`, `imported_at`) with
`computed_at` — the model run-init / valid-from time of the FORECAST, not when
WE fetched it. That fabricated collection latency = 0 in every `source_run` row.

## Per-event resolution

All wall-clock reads use `datetime.now(timezone.utc)` at the actual code point
(the established producer in this file; identical to `utc_iso_now()` — see note
on `forecasts_append.py`). `write_source_run` accepts `datetime | str | None`
and routes through `_to_iso()`, so passing a datetime or `None` is correct.

### fetch_started
- REAL code point: `src/data/ecmwf_open_data.py:1441` — `_fetch_started_at = datetime.now(timezone.utc)`, stamped immediately before the parallel download batch loop (`for offset in range(...)`) that submits `fetch_fn` -> `session.get` (the first HTTP GET). Initialized `None` at `:1437` (left `None` on the `skip_download` test seam — no HTTP issued).
- Threaded into all three persist sites: success-path authority chain `:1837`; FAILED branch `:1527`; NOT_RELEASED branch `:1596`.

### fetch_finished
- REAL code point: `src/data/ecmwf_open_data.py:1474` — `_fetch_finished_at = datetime.now(timezone.utc)`, stamped immediately after the batch loop closes (every future resolved: bytes received, timed out, or failed), before `ok_steps = sorted(...)`.
- Threaded into all three persist sites: `:1838` (success), `:1528` (FAILED), `:1597` (NOT_RELEASED).

### captured (decode -> in-memory possession)
- REAL code point: reuses the EXISTING real possession read `snapshot_possession_at = datetime.now(timezone.utc)` at `src/data/ecmwf_open_data.py:1766`, taken inside the ingest block immediately before the snapshot write (decode-into-memory complete). It is in scope at the authority-chain call (same function frame) and passed as `captured_at=snapshot_possession_at` at `:1839`.
- This value is NOT a new read I invented — it is the same real wall-clock the C1 fix already takes for proof-of-possession. Reusing it is correct: "captured" and "possession" are the same real instant.
- FAILED / NOT_RELEASED branches: `captured_at=None` (`:1529`, `:1598`) — decode never ran on those branches, so there is no real captured instant. Honest NULL, not a guess.

### imported (persisted to store)
- REAL code point: `src/data/ecmwf_open_data.py:959` — `imported_at=datetime.now(timezone.utc)` inside `_write_source_authority_chain`'s `write_source_run` call, i.e. now() at the exact persist write of the `source_run` row. This is the real import-completion instant.
- FAILED / NOT_RELEASED branches: `imported_at=None` (`:1530`, `:1599`). Those branches write only a FAILED/SKIPPED-status `source_run` row — no forecast DATA is imported — so there is no real data-import instant. Honest NULL.
  (`authority_computed_at` at `:1820` remains the `computed_at` authority-write clock for the chain; it is unchanged and is NOT a collection-plane field.)

### retrieved_at  (`src/data/forecasts_append.py` — Open-Meteo Previous Runs)
- REAL code point: `src/data/forecasts_append.py:387` — `retrieved_at = utc_iso_now()`, stamped immediately before the `try:` that calls `_fetch_previous_runs_chunk(...)` (`:389`), which issues the Open-Meteo HTTP request via `openmeteo_fetch`. This is the real PRE-HTTP "when we asked for it" instant.
- The prior code already stamped at this exact pre-HTTP point using `datetime.now(timezone.utc).isoformat()`; M5 switched it to the canonical `utc_iso_now()` producer (instruction #3, consistency) — byte-identical output, no behavior change. Added `from src.state.db import utc_iso_now` at `:79`.
- This is the SINGLE `retrieved_at` assignment in the file. `_rows_from_payload` (`:258`, `:262`) and the catch-up entrypoints (`append_forecasts_window` window-tick `:493`, `catch_up_missing` `:502`) all flow through this one stamp; the other `datetime.now(timezone.utc)` calls in the file (`:478`, `:516`, `:519`) build `rebuild_run_id` strings / date-window math, not collection-plane timestamps.

## `src/data/ecmwf_open_data_ingest.py` — NO M5 EDIT (deliberate)
This module has ZERO `computed_at` usage. It is a READER: it reconstructs a
`ForecastBundle` from already-persisted `ensemble_snapshots` rows, reading the
real persisted possession times (`fetch_time` / `recorded_at`) into `captured_at`
at `:335`. That is a faithful read-through of a real instant recorded by the
upstream writer — not a collection-lifecycle fabrication. Stamping a fresh now()
here would be the WRONG instant (it would record reconstruct/read time, not the
original capture time). The `fallback_dt = datetime.now(timezone.utc)` (`:333`)
is only a missing-value reconstruction default, not a lifecycle stamp.
Therefore: no edit. (Verified: `grep M5-COLLECTION-CLOCK` on this file is empty.)

## C1-AVAIL-CLOCK fix — preserved, edited AROUND (not regressed)
`proof_of_possession_available_at(...)` calls intact and untouched:
- `src/data/ecmwf_open_data.py:68` (import)
- `:950`  source_available_at (success authority chain) — I edited the lines BELOW it (fetch/captured/imported), not it.
- `:1522` source_available_at (FAILED branch) — same.
- `:1591` source_available_at (NOT_RELEASED branch) — same.
- `:1793` proof_of_possession_available_at(snapshot_possession_at) (snapshot SourceRunContext) — untouched.
In `src/data/ecmwf_open_data_ingest.py`: the dropped `run_init_dt` cycle fallback
for `available_at` remains dropped — `:354` still reads `provenance["available_at"]`
directly (no edit). `snapshot_possession_at` / `_source_cycle_expires_at` semantics
unchanged.

## Verification
- `python3 -m py_compile src/data/ecmwf_open_data.py src/data/ecmwf_open_data_ingest.py src/data/forecasts_append.py` -> PY_COMPILE_OK (all three).
- `import src.data.forecasts_append` -> OK (new `utc_iso_now` import resolves, no circular import).
- `import src.data.ecmwf_open_data` -> OK; `collect_open_ens_cycle` loads; `snapshot_possession_at` reference after the with-block resolves.
- Grep confirms NO collection-plane field still stamped with `computed_at`.
- Targeted tests (`-q`), directly exercising the changed lifecycle / source_run construction — ALL GREEN:
  - test_ecmwf_open_data_parallel_fetch.py
  - test_opendata_observed_members_aggregation.py
  - test_source_run_schema.py
  - test_ecmwf_open_data_step_hours.py
  - test_phase3_routing_ecmwf_open_data.py
  - test_opendata_future_target_contract.py
  => 36 passed (combined run), 31 passed (first subset). No timestamp-field assertion broke.

### Pre-existing failures (NOT caused by M5 — both in code I never touched)
- `tests/test_ecmwf_open_data_ingest_metric_independence.py::test_high_only_db_high_metric_succeeds`
  -> `sqlite3.OperationalError: no such column: contributes_to_target_extrema` at `ecmwf_open_data_ingest.py:400` (the reader SELECT). Test fixture schema lacks the column; I made zero edits to this file (line 400 untouched).
- `tests/test_forecast_fetch_plan.py::test_track_metric_mapping_is_strict`
  -> asserts `data_version_for_track('mx2t6_high_full_horizon')` starts with `ecmwf_opendata_mx2t6`, but it returns `ecmwf_opendata_mx2t3_local_calendar_day_max` (track->data_version naming drift). I touched no track-mapping / data_version code.
  Both are unrelated config/fixture drift on the clean base, in the known-skip spirit of the brief.
