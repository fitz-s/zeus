# Lifecycle rest→taker conversion fix — 2026-06-20

Created: 2026-06-20
Authority basis: root AGENTS.md money-path law (no caps, settlement-only truth, single-flight),
docs/authority/replacement_final_form_2026_06_09.md, regime_unification_2026-06-12.md.
Branch: live/lifecycle-conversion-fix-20260620 (off keeper head 561dc44). Live code at fix time: c568b8fcf7.

## Symptom (real live-DB evidence, read-only)
Certified +EV orders did not transact. GTC post_only maker rests fill ~5%; taker fills 75–100%.
Daily MATCHED 06-17=7, 06-18=**19**, 06-19=3, 06-20=**0** (cancels flat ~33/day). Taker-cross order
envelopes/day 06-17=12, 06-18=18, 06-19=2, 06-20=0. 217/301 positions voided; profit_audit P&L NULL.

## Confirmed death-line (verify-first; the void-reconciler hypothesis was REFUTED)
The continuous-redecision **screen** cancels-and-reposts rests BEFORE the 20-min escalation deadline:
- Age of terminal-unfilled ENTRY rests (zeus_trades, 06-19+): **5–20min = 60, 20–60min = 4**. 60/64
  cancelled before the 20-min deadline.
- Cancel reasons (venue_command_events): **CONFIRMED_VALUE_REFRESH = 59** (screen value-refresh @5min,
  REST_VALUE_REFRESH_MIN_AGE_SECONDS), **BOOK_MOVED = 7** (1-tick drift) — the screen, not the deadline job.
- `_family_rest_state` arms `escalated_after_rest` only at age ≥ MAKER_REST_ESCALATION_DEADLINE_MINUTES
  (20min) → the 60/64 screen-pulled rests can NEVER arm → re-decided as fresh REST_DEFAULT → infinite
  rest→pull→re-rest loop → 0 crosses. escalation_cross opportunity_events/day: 06-18=12 → 06-20=0.
- VENUE_WIPED is only 1/69 latest facts (dominant terminal = CANCEL_CONFIRMED, already escalation-eligible);
  the 81034c36fa void reconciler does NOT bury the cancel evidence.

## Fix (3 minimal, law-compliant changes)
1. **Restore escalation arming** — `_family_rest_state` (event_reactor_adapter.py): arm floor =
   `min(deadline_seconds, REST_VALUE_REFRESH_MIN_AGE_SECONDS)`. A screen-pulled cancelled-UNFILLED rest
   becomes cross-eligible, so the next decision crosses (if admissible) or no-trades, instead of re-resting.
   Reuses the screen's own floor (no new constant); never crosses above q_lcb (FIX-B unchanged); first
   rest still REST_DEFAULTs (Karachi antibody); a concurrent LIVE rest still HOLDs (single-flight).
2. **Collapse the taker-quality shadow gate** — `_build_event_bound_taker_quality_proof`: the
   0.03/0.05/1.20/0.60 thresholds were an unlicensed cap over the conservative law. Demoted to telemetry
   (`legacy_threshold_pass`); `passed = conservative after-cost surplus ≥ 0` (ask+fee ≤ q_lcb). Operator
   law NO-CAPS; fail-closed on negative edge preserved.
3. **No identical re-rest** — `select_rest_then_cross_mode` (mode_consistent_ev.py): escalated-but-
   inadmissible returns no-trade (chosen_ev=-inf) instead of re-posting the identical unfillable rest;
   the next cycle re-evaluates on a fresh book.

## Verification
- RED-on-revert antibodies (proven by stashing the src fixes → 3 tests FAIL; restored → pass):
  `test_screen_pulled_unfilled_rest_between_floor_and_deadline_escalates`,
  `test_escalated_but_taker_forbidden_does_not_re_rest`,
  `TestTakerQualityShadowGateCollapsed::test_positive_surplus_below_legacy_thresholds_passes`.
- Safety-preservation tests: live competing rest still HOLDs; negative-edge taker still fails closed;
  first rest still REST_DEFAULT; filled order never arms escalation.
- Targeted suite green; broader regression green (test_runtime_guards pre-existing failures verified
  identical on the unfixed tree, unrelated).

## NOTE — behavioral change (not a pure bug-restore)
Because the 20-min deadline is never reached (screen always pulls at 5min), arming at the 5-min screen
floor is the minimal change that restores ANY crossing. Effect: the maker window is ~5min, then cross-if-
admissible / else no-trade — the operator-approved single-flight direction. The optimal arm floor remains
a measurement question (KM curve × settlement EV, same open item as the 20-min deadline).

## Acceptance bar (post-restart, operator-gated)
REAL market chain only: MATCHED returns from 0 toward 7–19/day; escalation_cross events > 0/day; GTC fill
fraction rises off ~5%. Not test-green alone.
