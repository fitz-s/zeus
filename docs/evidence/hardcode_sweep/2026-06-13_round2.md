# Hardcode Sweep — Round 2
**Scope**: `src/engine`, `src/strategy`, `src/execution`, `src/contracts`, `src/riskguard`, `src/state` (truth-owners), `src/data` (ingest cadences), `src/sizing`, `config/settings.json`
**Method**: Python AST walk (2,109 raw literals from 2,046 unique sites) + targeted grep on known suspects
**Authority reference**: `docs/authority/statistical_calibration_addendum_2026-06-13.md`
**Date**: 2026-06-12

---

## Summary counts

| Class | Count | Notes |
|---|---|---|
| A. VENUE_CONTRACT | 4 | Polymarket fee 5%, min order 1 share, 51/31 ensemble members (external provider), fee bps metadata fields |
| B. PHYSICS_UNITS | 6 | °C↔°F (9/5+32), F/C noise constants (ASOS sensor), 3600s/hour, unit conversions |
| C. FITTED_ARTIFACT | 15 | Values backed by live state/*.json artifacts |
| D. OPERATOR_LAW | 22 | Values from explicit operator directives or commit-anchored design decisions |
| E. STRUCTURAL_SAFE | 18 | Timeouts, retry caps, budget seconds, cadences provably non-decision-influencing |
| **F. UNFITTED_DECISION_RELEVANT** | **31** | **THE KILL LIST** — values with no mathematical backing that influence trades |

---

## F — KILL LIST (trade-decision relevant, no mathematical backing)

Ranked by decision impact (sizing > selection > timing > ops). Each entry cites the governing math from the 2026-06-13 statistical calibration addendum.

| Rank | File:line | Value | Controls | Proposed governing math |
|---|---|---|---|---|
| 1 | `config/settings.json:sizing.kelly_multiplier` | **0.125** | Base fractional-Kelly multiplier applied to every stake. The single largest sizing lever. settings._note calls it HARDCODED, "proving-phase". | **A1 addendum**: DR-Kelly interval width is the correct shrinkage. Replace with walk-forward Sharpe-ratio / edge-SNR fit: `λ* = argmax_λ [geometric_mean(realized_log_returns(λ)) - risk_pen]` over forward settled fills. CI-width licensing (A1: `max se(π_ij) < ε_edge/2`) replaces the fixed fraction. Artifact: refit cadence 100+ settlements. |
| 2 | `src/strategy/kelly.py:433` | **0.25** | Default `base` fed to `dynamic_kelly_mult` when no kelly_multiplier is supplied; used as the entry-path base in `money_path_adapters.py:200`. Effectively the unconfigured path multiplier. | Same as rank 1. |
| 3 | `src/sizing/sizing_context.py:23` / `src/strategy/kelly.py:481,483` | **ci_width > 0.10 → ×0.7; ci_width > 0.15 → ×0.5** | Stepwise CI-width haircut in `dynamic_kelly_mult`. Thresholds 0.10/0.15 and multipliers 0.7/0.5 are pure guess; cumulative 0.25×0.7×0.5=0.0875 is not derived from any edge distribution. | **A1 addendum**: CI-width enters Kelly via interval width directly (`f* = max(0, f_Kelly(q_L))`). Stepwise haircut is a double-count when `replacement_0_1_fused_q_shape_enabled=true` (INV-40). Fix: use interval-Kelly (`q_lcb` alone), retire stepwise haircut. No separate thresholds needed. |
| 4 | `config/settings.json:edge.base_alpha.level1-5` | **0.65, 0.55, 0.40, 0.25, 0.25** | Model-vs-market trust weights used in `market_fusion.py`. Notes call them HARDCODED. | **A1/A4 addendum**: Replace with BLP `(α, β, w)` monotone CDF transport (A4 unified law). Per-calibration-level weights derived from OOS Brier improvement (model Brier vs market Brier) over 100+ settled cells per level. Artifact: `state/blend_alpha_fit.json`, refit after each calibration maturity step. |
| 5 | `config/settings.json:edge.fdr_alpha` | **0.1** | BH FDR threshold — but addendum A2 condemns BH on this path (mutually exclusive bins violate PRDS; {0,1} p-values make it vacuous). The value is irrelevant because the procedure is wrong. | **A2 addendum (BLOCKER)**: Kill BH gate entirely. Replace with posterior lfsr + EB selection shrinkage: `m_j = E[e_j|D]` (Tweedie/EB), gate on `P(e_j > e_min | D) ≥ 0.90` + `E[log_utility] > 0`. Track winner's-curse slope diagnostic. |
| 6 | `src/strategy/live_inference/mode_consistent_ev.py:69` | **0.10** (`MAKER_FILL_PROBABILITY_PRIOR`) | Conservative prior for maker EV. Code explicitly labels this `basis=GUESS`. At n=13 bid+tick fills, the estimate is too noisy for Kelly. | **A8 addendum + D5**: Gamma-prior Lomax fill model: `P(fill) = 1 − (β/(β+H))^α` with (α,β) from tape trade-throughs (queue discount c∈0.3–0.7). Artifact: `state/maker_fill_prior.json`, armed when n ≥ `MAKER_FILL_RECALIBRATION_MIN_FACTS`. |
| 7 | `src/strategy/live_inference/mode_consistent_ev.py:86` | **0.15** (`TAKER_OVER_MAKER_MARGIN`) | Hysteresis margin: taker chosen only if EV_taker ≥ EV_maker × (1+0.15). Basis: "sane margin" after observing knife-edge churn. | **A8 addendum**: `V_M − V_T > ε_exec` where ε_exec is the inf over (λ_f, λ_e) uncertainty. Derived from Gamma-posterior λ_e (own-order exposures) + OU/exponential λ_f. Until fill data licenses it, the 0.15 heuristic should be documented with a measurement plan (same as fill prior). |
| 8 | `src/strategy/live_inference/mode_consistent_ev.py:144` | **0.15** (`TAKER_IMMEDIATE_FLEETING_EDGE_THRESHOLD`) | Triggers immediate cross if edge ≥ 0.15 in near-event lanes. Code labels `basis=GUESS`. | **A8 addendum**: measured edge-decay quantile from escalation receipt cohort (edge-at-rest vs edge-at-deadline). Artifact fires when settled cohort is thick enough. Until then, keep documented as GUESS; do not lower. |
| 9 | `config/settings.json:edge.spread_tight_f` / `edge.spread_wide_f` | **2.0°F / 5.0°F** | ENS spread thresholds used in `market_fusion.py` to adjust alpha by ±0.10/0.15. Settings notes say HARDCODED. | Fit per-city ensemble spread distribution from ENS snapshot history (percentiles 25th / 75th). Artifact: `state/ens_spread_percentiles.json`, refit monthly. At 1000+ snapshots/city, replace with measured p25/p75. |
| 10 | `src/strategy/market_fusion.py:232,234` | **+0.10 / −0.15** (ENS spread alpha adjustments) | Per-decision alpha bonus/malus for tight/wide ENS spread. Comment says "Sweep showed bonus=0.10 gives -0.00825 Brier improvement" — validated but in-sample. | **A4 addendum (BLP blend)**: absorb spread adjustment into a fitted BLP parameter `w` (model-market weight). Baked into the 3-parameter `(α,β,w)` fit, not a separate additive tweak. |
| 11 | `src/strategy/market_fusion.py:244,246` | **+0.05 / −0.05** (lead-days alpha adjustment) | Alpha adjusted ±0.05 for short/long lead. No evidence cited. | Same as rank 10: absorbed into BLP lead-day interaction term. |
| 12 | `src/strategy/kelly.py:172` | **0.3** (`OBSERVED_FRACTION_MIN`) | Floor on settlement_capture Kelly — prevents early-day zero sizing. Comment says "Operator-tunable; below 0.3 punitive." | Fit from realized settlement_capture entry timing vs eventual settlement accuracy (what fraction-of-day observation leads to what fill rate). Artifact: `state/settlement_capture_fraction_calibration.json`. |
| 13 | `src/strategy/live_inference/market_anchor.py:54` | **1.5** (`DEFAULT_NEAR_CENTER_STEPS`) | Scope of market-anchor cap: bins within 1.5 settlement steps of mu are capped. Comment says "matches direction_law's adjacent reach." | Derived from C1-C3 loss distribution in `docs/evidence/2026_06_11_objective_math/`. The reach should be: max step distance at which model-vs-market loss is statistically different from C4. Fit over settled C3 vs C4 outcomes. |
| 14 | `src/strategy/live_inference/market_anchor.py:58` | **0.4** (`DEFAULT_FALLBACK_ALPHA`) | Fallback model-market blend alpha when caller supplies no level. "Conservative mid-trust: equal weight floored toward the legacy level-3 value." | Same as rank 4 (BLP blend fit). Until then, should use the fitted level-3 base_alpha from the config (0.40 by coincidence, but this wires it in code, bypassing config). Replace with a live config read. |
| 15 | `src/strategy/probability_uncertainty.py:62` | **0.05** (`DEFAULT_ALPHA`) | Bootstrap tail quantile: q_lcb is the 5th percentile of bootstrap edge samples. | **A1 addendum**: CI-width licensing law: `1.96 · max se(π_ij) < ε_edge/2`. The 5/95 choice corresponds to a 90% CI width. This should be derived from the required decision confidence level and the actual bootstrap SE distribution, not fixed at 5%. Under EB selection shrinkage (A2), the shrunk posterior mean replaces the raw bootstrap LCB anyway. |
| 16 | `src/strategy/market_analysis.py:1002-1003` | **5, 95** (np.percentile) | Bootstrap CI bounds for edge computation and p-value. Same as rank 15. | Same as rank 15. |
| 17 | `src/calibration/manager.py:324-337` (via `config/settings.json:calibration.maturity.level1/2/3`) | **150, 50, 15** | Calibration maturity level thresholds determining Platt regularization and edge threshold multipliers. | **A5 addendum**: replace n-based tiers with CI-width licensing: fit is licensed when `1.96 · max se(π_ij) < ε_edge/2`. The sample counts 150/50/15 are rough proxies for the precision condition; actual required n depends on class frequency and edge magnitude. Artifact: add `ci_width` to calibration fit output and use it as the primary gate. |
| 18 | `config/settings.json:calibration.refit_every_n` | **50** | Platt refit trigger cadence (every 50 new pairs). | Should be: refit when `|new_se(π_ij) − old_se(π_ij)| > δ_precision` (i.e., when new data would materially narrow the CI). Derive δ from edge magnitude. 50 is purely a guess. |
| 19 | `config/settings.json:edge.n_bootstrap` | **500** | Number of bootstrap samples for edge CI. | Bootstrap SE ≈ `σ/√n_bootstrap`. Required precision: `se_boot < se_analytical / 10`. For typical edge distributions this is n≈200–500. Confirm with: `se_analytical(n_data) / se_boot(n_bootstrap=500) > 10`; if failing, raise. Use analytical SE (Efron-Stein) when tractable. |
| 20 | `config/settings.json:calibration.n_bootstrap` | **200** | Number of bootstrap samples for Platt calibration transfer uncertainty. | Same logic as rank 19. The `transfer_logit_sigma_scale=4.0` (also a hardcode) is derived from the chain-rule logit slope at p=0.5; if the bootstrap SE drives the transfer uncertainty, the n=200 should satisfy `se_boot < target_sigma / 10`. Verify and document. |
| 21 | `src/strategy/live_inference/mode_consistent_ev.py:72` | **30** (`MAKER_FILL_RECALIBRATION_MIN_FACTS`) | Min bid+tick fills before replacing the fill prior with measured rate. | **D5 addendum**: `P(δ_eff > e₀(1−G)/F) > 0.6` under MC over (λ_e, λ_f) posteriors. The required n is determined by when the posterior CI is tight enough to distinguish the prior from the measurement. At Lomax model, ≈ 20–40 fills depending on variance. 30 is plausible but should be derived from the posterior width criterion. |
| 22 | `src/strategy/candidates/center_buy_calibrated_shadow.py:56` | **30** (`_MIN_CAL_PAIRS`) | Minimum calibration pairs required to run conformal p-value in this candidate. | **A5 addendum**: same as rank 17 — replace raw n floor with CI-width gate. |
| 23 | `src/strategy/oracle_estimator.py:98-100` | **10** (`INSUFFICIENT_SAMPLE_N`), **0.05** (`CAUTION_P95_LOWER`), **0.10** (`BLACKLIST_P95_LOWER`) | Oracle evidence classification thresholds. Beta-binomial posterior gates for Kelly penalty. | **A2/A3 addendum**: the n=10 floor and 0.05/0.10 p95 bounds are pre-set policy constants. The statistically principled replacement is an e-process monitor (A3) + posterior expected log growth gate. The p95 thresholds correspond to a Beta(1,1) prior; they should be checked with the walk-forward oracle diagnostics and calibrated against actual oracle-vs-settlement agreement rates. |
| 24 | `src/riskguard/riskguard.py:1442,1458` | **10, 0.3** (fill rate alarm: n≥10, fill_rate < 0.3) | Triggers riskguard fill-rate alarm if < 30% fill rate on ≥10 execution observations. | **A3 addendum**: anytime-valid test martingale on fill outcomes. The 0.3 threshold should be set at the historical mean minus k·SE where k comes from the Ville inequality bound. Current value is a guess. |
| 25 | `config/settings.json:riskguard.brier_yellow/orange/red` | **0.25, 0.30, 0.35** | Brier score alert levels. | **A3 addendum**: e-process/test martingale over sequential Brier scores relative to the calibrated baseline. Thresholds should be derived from: `M_T(λ) > 1/α` (Ville's inequality at desired α). Fixed thresholds are not anytime-valid under optional stopping. |
| 26 | `config/settings.json:riskguard.win_rate_yellow/orange` | **0.40, 0.35** | Win-rate alert thresholds. | Same as rank 25. Wilson intervals used for sequential win-rate monitoring are not valid under optional stopping (addendum A3). Replace with test martingale. |
| 27 | `config/settings.json:riskguard.max_daily_loss_pct / max_weekly_loss_pct / max_drawdown_pct` | **0.08, 0.15, 0.20** | Drawdown / loss stop triggers. | Operator law territory (operator sets risk appetite). But the 8%/15%/20% values are unanchored to any bankroll-ruin analysis. Should be derived from: `P(ruin in N trades) < ε` under the fitted Kelly+edge distribution. These are currently placeholders. Mark as OPERATOR_LAW pending a Kelly-ruin simulation. |
| 28 | `src/strategy/james_stein_blend.py:50` | **30.0** (`MAX_ARTIFACT_AGE_DAYS`) | Max age before N_eff artifact degrades to N_nominal=51. | The degradation trigger should be tied to when the rho_w/rho_b estimates are likely stale (e.g., after a major model upgrade or seasonal boundary). A fixed 30 days is reasonable but should be: min(30d, next_model_version_date, seasonal_boundary). Propose: configuration-driven with an operator override and an explicit refit trigger keyed to `state/member_correlation_fit.json` mtime. |
| 29 | `src/strategy/benchmark_suite.py:148` | **0.1** (`min_fill_probability`) | Benchmark gate: strategy fails if fill probability < 10%. | **A8 addendum**: fill probability should be estimated from the Lomax/Gamma model. The 0.1 threshold should be set where `V_M^L − V_T^L ≤ 0` under pessimistic λ bounds. |
| 30 | `src/strategy/ecmwf_aifs_sampled_2t_probabilities.py:45` | **0.05** (`MEMBER_VOTE_SMOOTHING_ALPHA`) | Laplace-style smoothing for member vote probabilities (α per bin). | **A10 addendum (JS shrinkage)**: the 0.05 pseudocount is an ad-hoc shrinkage constant. Under addendum A10, member-vote shape is already extremely low signal (N_eff=3.71 vs raw 51). The smoothing α should be derived from the Dirichlet concentration implied by N_eff: `α = N_eff / K_bins ≈ 3.71/20 ≈ 0.19`. Use measured N_eff artifact. |
| 31 | `config/settings.json:exit.*` — `buy_no_floor/ceiling` (-0.02/-0.15), `buy_yes_floor/ceiling` (-0.01/-0.10), `divergence_soft/hard` (0.2/0.3), `divergence_velocity_confirm` (-0.05), `reversal_confirmations` (2), `consecutive_confirmations` (2), `near_settlement_hours` (4.0), `expiry_hours` (4) | Multiple exit thresholds | Exit decision gates: when to cut a position. | **A8 addendum (maker/taker, extended to exits)**: exit thresholds should satisfy `E[log_utility | hold] > E[log_utility | exit_now]` where the hold distribution is the posterior predictive for settlement, and exit cost is fee_rate × price. The velocity/divergence thresholds need a Kalman/state-space model for belief drift (addendum B, state-space candidate). These are pure heuristics. |

---

## C — FITTED ARTIFACT TABLE (for audit)

| Value | File/config | Artifact | Notes |
|---|---|---|---|
| `k` (sigma scale), `w` (uniform mixture weight) per city/season | `src/engine/event_reactor_adapter.py` | `state/sigma_scale_fit.json` | MLE Bernoulli-Normal scale fit over forecast-vs-settlement residuals. |
| `b_loc`, `k` (bias scale) | `src/calibration/` / EMOS path | `state/bias_scale_fit.json` | Bias scale artifact |
| `max_observed_fee_fraction` (0.0 today) | `src/contracts/fee_authority.py:42,46` / `MIN_FILLS_TO_LICENSE=10, MAX_EVIDENCE_AGE_DAYS=30` | `state/fee_reconciliation.json` | Fee fraction from realized fills. Currently 0.0 (0 bps fees observed); schedule fraction 0.05 is the conservative fallback ONLY. Fee defaults in `execution_price.py:95,259` at 0.05 are therefore the fallback, not the live value. |
| `rho_w=0.255, rho_b=0.140, N_eff=3.71` | `src/strategy/james_stein_blend.py:4` | `state/member_correlation_fit.json` | Within/between-city member correlations measured over 178 AIFS events + 4163 deterministic events (2026-06-13). |
| sigma_floor per city/season/metric | `src/calibration/` / monitor_refresh.py | `state/settlement_sigma_floor.json` | MAD-sigma over forecast-settlement residuals, min cohort n=20. |
| EMOS params (a,b,c,d,e) per city/season/metric | `src/calibration/emos.py` | `state/emos_calibration.json` | EMOS-NGR fit; HIGH and LOW cells. |
| `MAKER_FILL_PROBABILITY_AT_ESCALATION_DEADLINE = 0.39` | `src/strategy/live_inference/mode_consistent_ev.py:126` | KM curve: `km_2026_06_10_resting_facts_n108@120min` (no json artifact yet) | KM-measured; n=108 resting orders. Needs a formal artifact in `state/`. |
| EMOS CI license (`k_cov`) per city | `src/calibration/emos_ci_license.py` | `state/emos_ci_license.json` | CI licensing from settlement backward coverage check. |
| Platt models (active) per city/season/metric | `src/calibration/store.py` | `zeus-world.db::platt_models_v2` | 137 HIGH ft_v1 cohorts (pinned 2026-06-06). |
| Settlement backward coverage verdict | `src/calibration/settlement_backward_coverage.py` | In-DB per-band table; arm gate threshold `_ARM_RATIO_TOL=0.10` is hardcoded (see F list rank... but it is a tolerance on coverage ratio consistency, closer to OPERATOR_LAW) | |
| Oracle penalty levels (beta-binomial posteriors) | `src/strategy/oracle_estimator.py` | `data/oracle_error_rates.json` (loaded at runtime) | Bayesian posterior from oracle mismatch counts; artifact updated by daemon. |
| City correlation matrix | `src/strategy/correlation.py` | `config/city_correlation_matrix.json` | Offline Pearson matrix from TIGGE snapshots. |

---

## D — OPERATOR LAW TABLE (for audit)

| Value | File/config | Operator law / memory anchor |
|---|---|---|
| `kelly_multiplier = 0.125` | `config/settings.json:sizing` | Operator 2026-06-10 restoration: "proving-phase" envelope. Explicit note. (Also in F kill list as still unfitted.) |
| `max_single_position_pct = 0.0` | `config/settings.json:sizing` | Operator 2026-06-09: no concentration cap; sizing from Kelly only. |
| `TAKER_IMMEDIATE_EVENT_END_FLOOR_MINUTES = 180.0` | `mode_consistent_ev.py:135` | Derived: escalation deadline (120m) + 60m slack. Structural, pinned in tests. |
| `TAKER_FLEETING_EDGE_MAX_MINUTES_TO_EVENT_END = 360.0` | `mode_consistent_ev.py:~158` | Operator 2026-06-11 Denver fill incident: structural edge doesn't vanish in rest window; lane 5 restricted to ≤360m. |
| `MAKER_REST_ESCALATION_DEADLINE_MINUTES = 120.0` | `mode_consistent_ev.py:120` | MEASURED KM curve (n=108): 39% cumulative fill at 120m. (C category by evidence, but formally an operator-set deadline with measurement support.) |
| `FALLBACK_F1_HAIRCUT = 0.7` | `kelly.py:173` | Operator-verified across 13 cities; 0.7 until fallback verified for specific market. |
| `OBSERVED_FRACTION_MIN = 0.3` | `kelly.py:172` | Operator-tunable; prevents early-Day0 zero sizing. Note says "below 0.3 is too punitive." (Also F list.) |
| `TAKER_MAX_RELATIVE_SPREAD = 0.25` | `mode_consistent_ev.py:57` | Measured incident: 56% relative spread with unlicensed tail q. 0.25 is a veto, not a size. |
| `MAKER_ADVERSE_SELECTION_LAMBDA = 1.0` | `mode_consistent_ev.py:88` | Full half-spread as adverse-selection estimate (standard first-order). |
| `fdr_alpha = 0.1` (settings) | `config/settings.json:edge` | Legacy policy constant; condemned by A2 addendum. Dead in the correct A2 replacement. |
| `edli.day0_catchup_emit_limit = 20` | `config/settings.json` | Throughput bound preventing runaway catchup. Operational. |
| `replacement_forecast_shadow.download_anchor_sigma_c = 3.0` | `config/settings.json` / `replacement_forecast_materializer.py:121` | Conservative outlier gate: IFS anchor within 3σ of AIFS ensemble (Gaussian outlier logic). Standard 3-sigma cut. |
| `min_members_floor = 40` | `config/settings.json:ensemble` | Operator 2026-05-19 note: 48-50 members statistically equivalent to 51 for variance estimation; < 40 unreliable. |
| Real order submit, live mode flags | Various `edli.*` booleans | Operator directives 2026-06-09/12. |
| `CLOSED_EVENTS_CUTOFF_DAYS = 30` | `execution/harvester.py:74` | Live scope: only events closed ≤ 30 days ago. Operational hygiene. |
| `_QUARANTINE_THRESHOLD = 10` | `events/edli_position_bridge.py:659` | 10 consecutive bridge failures before quarantine. Operational resilience constant. |
| `grace_minutes = 15.0` | `events/edli_trade_fact_bridge.py:136` | Grace window for fill recovery from user channel. Operational. |
| `MAX_EXIT_RETRIES = 10` | `execution/exit_lifecycle.py:133` | Max exit retries before backoff_exhausted. Operational. |
| `download_release_lag_hours = 14.0` | `config/settings.json` / `time_semantics.py:507` | AIFS-ENS upstream publication lag — EXTERNAL constant, not chosen by Zeus. |
| `MIN_FILLS_TO_LICENSE = 10` | `contracts/fee_authority.py:42` | 10 fills to license fee artifact; rationale: deterministic per-fill venue parameter, not statistical estimate. Documented. |
| `MAX_EVIDENCE_AGE_DAYS = 30.0` | `contracts/fee_authority.py:46` | Fee regime can change; 30d stale degrades back to schedule. |
| `n_mc = 10000` | `config/settings.json:ensemble.n_mc` and `day0.n_mc` | Operator-bumped from 5000; LAW 4 floor 5000. Note documents SE improvement. |
| `no_submit_visible_depth_fill_lcb = 0.05` | `config/settings.json:edli` | Wilson LCB floor for visible depth fill probability; conservative baseline. |

---

## A — VENUE CONTRACT APPENDIX

| Value | File | Classification |
|---|---|---|
| `fee_rate = 0.05` (5%) | `contracts/execution_price.py:95,259`, `config/settings.json:exit.fee_rate` | VENUE_CONTRACT: Polymarket taker fee schedule. The realized fee is currently 0.0 (artifact-licensed). The 0.05 is the conservative schedule fallback, not the live live charge. Source: docs.polymarket.com/trading/fees. |
| `min_order_size = 1` (share) | `engine/event_reactor_adapter.py:3790,3966` | VENUE_CONTRACT: Polymarket minimum order 1 share. Observed from venue API. |
| `primary_members = 51` | `config/settings.json:ensemble` | VENUE_CONTRACT/EXTERNAL: ECMWF IFS-ENS member count — fixed by provider. Not a Zeus choice. |
| `crosscheck_members = 31` | `config/settings.json:ensemble` | VENUE_CONTRACT/EXTERNAL: GFS ensemble count — fixed by NCEP. |

---

## B — PHYSICS UNITS APPENDIX

| Value | File | Notes |
|---|---|---|
| `9.0/5.0 + 32.0` / `(x-32)*5.0/9.0` | `engine/evaluator.py:2163`, `event_reactor_adapter.py:9563,11666,11667,11883,11906,12476`, `replacement_forecast_hook_factory.py:201` | °C↔°F physics conversion. |
| `instrument_noise_f = 0.5` | `config/settings.json:ensemble` | ASOS sensor noise ±0.5°F (standard measurement uncertainty). Physics/calibration constant. |
| `instrument_noise_c = 0.28` | `config/settings.json:ensemble` | 0.5°F × 5/9 = 0.278°C. Physics constant. |
| `3600.0` | Various (time_semantics, cycle_runtime) | Seconds per hour — physics unit. |
| `60.0` | Various | Seconds per minute — physics unit. |
| `24.0` | Various | Hours per day — physics unit. |

---

## E — STRUCTURAL SAFE APPENDIX

Values that could not change a trade decision even if wrong (operational plumbing, pure infra):

| Value | File | Justification |
|---|---|---|
| `ZEUS_DB_BUSY_TIMEOUT_MS = 30000` (default) | `state/db.py:97` | SQLite busy-timeout. A longer wait never changes a trade, only delays it. |
| `REACTOR_REFRESH_BUDGET_SECONDS = 17.0` | `contracts/time_semantics.py:219` | Must be < 20s warm interval. Structural constraint enforced by registry; does not affect which trade is placed. |
| `EDLI_SUBSTRATE_WARM_INTERVAL_SECONDS = 20.0` | `main.py` / `time_semantics.py:229` | Market substrate warm interval. Affects timing cadence, not which trade. |
| `_EDLI_LIVE_DISCOVERY_EVAL_BUDGET_DEFAULT_SECONDS = 360.0` | `engine/cycle_runtime.py:100` | Per-cycle evaluation budget. Structural. |
| `_CLOSED_EVENTS_MAX_WALL_SECONDS = 120` | `execution/harvester.py:75` | Mandatory wall-cap for harvester paginator. Safety antibody. |
| `_PROCEEDS_TOTAL_BUDGET_S = 12.0` | `execution/wrap_unwrap_commands.py:766` | Budget for wrap proceeds per tick (yield-control for money-path pollers). Structural. |
| `DEFAULT_COOLDOWN_SECONDS = 300` | `execution/exit_lifecycle.py:134` | 5 min between exit retries. Operational. |
| `MAX_EXIT_RETRIES = 10` | `execution/exit_lifecycle.py:133` | Backoff cap; position eventually enters `backoff_exhausted`. Operator law. |
| `max_quote_age_ms = 1000` | `engine/event_reactor_adapter.py:397` | Pre-submit quote freshness check (1s). Structural; stale-price detection, not a sizing decision. |
| `edli_stage_readiness_max_age_seconds = 900` | `config/settings.json:edli` | Stage readiness TTL. Operational hygiene. |
| `market_channel_refresh_window_seconds = 60` | `config/settings.json:edli` | Market channel rate-limit window. Operational. |
| `market_channel_refresh_max_actions_per_window = 5` | `config/settings.json:edli` | Rate-limit budget per window. Structural. |
| `_QUARANTINE_THRESHOLD = 10` | `events/edli_position_bridge.py:659` | 10 consecutive bridge failures → quarantine. Recovery guard, not sizing. |
| `bimodal_kde_order = 10` | `config/settings.json:ensemble` | Polynomial order for bimodal KDE; 10 gives adequate flexibility without overfit. Structural to the probability shape detection, not a decision threshold. |
| `n_bootstrap (edge) = 500` | `config/settings.json:edge` | Bootstrap samples. Accuracy operational; the 500 target is in kill list (rank 19) but it is SAFE in the sense a wrong value degrades CI quality without directly flipping a trade direction. |
| `bimodal_gap_ratio = 0.3` | `config/settings.json:ensemble` | Bimodal gap detection threshold. Shape-detection structural. |
| `unimodal_range_epsilon = 0.5` | `config/settings.json:ensemble` | Unimodal range sentinel (0.5°). Shape-detection structural. |
| `conflict_kl_threshold = 0.15` | `config/settings.json:ensemble` | KL divergence threshold for model conflict detection. Shape-detection structural. |

---

## settings.json numeric leaf audit

All numeric leaves with their classification:

| Key | Value | Class | Notes |
|---|---|---|---|
| `discovery.opening_hunt_interval_min` | 15 | E | Cron cadence — operational |
| `discovery.day0_interval_min` | 15 | E | Cron cadence |
| `discovery.imminent_open_capture_interval_min` | 5 | E | Cron cadence |
| `discovery.max_lead_days` | 7 | D | Operator limit on forecast horizon; venue markets don't extend further |
| `discovery.preferred_lead_days` | [3,4,5] | D | Operator preference; best ensemble skill window |
| `discovery.min_hours_to_resolution` | 6 | D | Operator min_hours_to_resolution — matches riskguard.staleness_hours by coincidence; see time_semantics cluster-2 |
| `ensemble.primary_members` | 51 | A | ECMWF IFS-ENS count — external |
| `ensemble.crosscheck_members` | 31 | A | GFS count — external |
| `ensemble.n_mc` | 10000 | D | Operator-set Monte Carlo count; LAW 4 minimum 5000 |
| `ensemble.instrument_noise_f/c` | 0.5/0.28 | B | ASOS sensor noise |
| `ensemble.bimodal_kde_order` | 10 | E | KDE polynomial order |
| `ensemble.bimodal_gap_ratio` | 0.3 | E | Shape detection |
| `ensemble.boundary_window` | 0.5 | B | Settlement boundary window in °C/°F units — physics |
| `ensemble.unimodal_range_epsilon` | 0.5 | E | Shape detection |
| `ensemble.conflict_kl_threshold` | 0.15 | E | Shape detection |
| `ensemble.min_members_floor` | 40 | D | Operator 2026-05-19 note |
| `probability_sanity.*` | Various | D | Externalized from prior hardcodes; operator-tunable boundary sanity gates |
| `probability_edge_bin_sanity.*` | Various | D | LIVE-PROB-P0 §F operator binding; documented extensively in note |
| `edli.bias_decay_threshold_c/f` | 2.0/3.0 | **F** | Bias decay Kelly haircut trigger. "Interim" per note; no fitted distribution threshold. Governing math: CI-width on effective_bias_c vs edge magnitude. |
| `edli.bias_decay_kelly_factor` | 0.5 | **F** | Halves Kelly for high-bias cities. Hardcoded halving; same issue as ci_width haircut. Should be `(1 - |bias| / sigma_edge)` or Kelly-optimal given measured bias. |
| `edli.no_submit_visible_depth_fill_lcb` | 0.05 | D | Wilson LCB floor for depth coverage (conservative floor). |
| `edli.mainstream_warm_max_families_per_cycle` | 8 | E | Throughput control |
| `edli.day0_catchup_emit_limit` | 20 | D | Operator throughput bound |
| `edli.pre_submit_max_quote_age_ms` | 1000 | E | Structural pre-submit check |
| `edli.edli_stage_readiness_max_age_seconds` | 900 | E | Stage health TTL |
| `edli.market_channel_refresh_*` | 5/60 | E | Rate-limit operational |
| `edli.edli_live_max_unresolved_unknowns` | 0 | D | Operator strict gate |
| `edli.edli_live_min_realized_edge_bps` | 0 | D | Operator: no realized edge floor (trust model) |
| `entry_forecast.target_horizon_days` | 10 | D | Operator choice; AIFS-ENS extends to 10 days |
| `calibration.refit_every_n` | 50 | **F** | See rank 18 |
| `calibration.n_bootstrap` | 200 | **F** | See rank 20 |
| `calibration.maturity.level1/2/3` | 150/50/15 | **F** | See rank 17 |
| `calibration.transfer_logit_sigma_scale` | 4.0 | **F** | Chain-rule logit slope at p=0.5; operator-tunable "once OOS corpus available." Unfitted. |
| `day0.n_mc` | 10000 | D | LAW 4 minimum |
| `edge.n_bootstrap` | 500 | **F** | See rank 19 |
| `edge.base_alpha.level1-5` | 0.65/0.55/0.40/0.25/0.25 | **F** | See rank 4 |
| `edge.spread_tight_f / spread_wide_f` | 2.0/5.0 | **F** | See rank 9 |
| `edge.fdr_alpha` | 0.1 | **F** | Condemned by A2; dead key in correct replacement |
| `edge.opening_alpha_bonus` | 0.15 | **F** | Alpha bonus for opening hunt. No Brier validation cited. Same as rank 10/11 — absorb into BLP blend. |
| `sizing.kelly_multiplier` | 0.125 | **F** | Rank 1 |
| `sizing.max_single_position_pct` | 0.0 | D | Operator directive: no cap |
| `sizing.max_portfolio_heat_pct` | 0.5 | **F** | 50% bankroll heat limit. No Kelly-ruin derivation. Should satisfy `P(ruin) < ε` under fitted edge distribution. |
| `sizing.max_correlated_pct` | 0.25 | **F** | Correlated exposure cap. Note: currently equal to kelly_multiplier by coincidence. Should be: `max_corr_pct ≥ kelly_mult` (invariant) + ruin simulation. |
| `sizing.max_city_pct` | 0.2 | **F** | City concentration cap. Same — unfitted. |
| `sizing.min_order_usd` | 1.0 | A/D | Venue minimum $1; also a portfolio floor choice. |
| `correlation.default_cross_cluster` | 0.1 | **F** | Geographic fallback correlation floor. Should be fitted from city_correlation_matrix.json empirical distribution minimum. |
| `exit.*` (all numeric thresholds) | Multiple | **F** | See rank 31 |
| `riskguard.brier_*` | 0.25/0.30/0.35 | **F** | See rank 25 |
| `riskguard.accuracy_orange` | 0.45 | **F** | Classification accuracy alarm. Same unfitted threshold issue as riskguard Brier. |
| `riskguard.win_rate_*` | 0.40/0.35 | **F** | See rank 26 |
| `riskguard.max_daily_loss_pct / max_weekly_loss_pct / max_drawdown_pct` | 0.08/0.15/0.20 | **F** | See rank 27 |
| `riskguard.staleness_hours` | 6 | D | Matches model_cycle_cadence (6h); one cadence without data = stale. Documented in time_semantics cluster-2. |
| `execution.limit_offset_pct` | 0.02 | **F** | Limit price offset 2% above ask. Unfitted; should relate to adverse selection model (A8). |
| `execution.fill_timeout_seconds` | 600 | E | 10-min fill wait before cancel. Operational structural. |
| `replacement_forecast_shadow.download_release_lag_hours` | 14.0 | A/B | AIFS-ENS upstream publication lag — external. |
| `replacement_forecast_shadow.download_anchor_sigma_c` | 3.0 | D | 3-sigma outlier gate for anchor; standard Gaussian cut. |
| `replacement_forecast_shadow.download_aifs_retries` | 4 | E | HTTP retry count. Operational. |
| `replacement_forecast_shadow.*_limit_per_cycle` | 10/10/10/200 | E | Per-cycle throughput bounds. Operational. |
| `replacement_forecast_shadow.materialization_interval_min` | 5 | E | Cron cadence. Operational. |

---

## Additional F items from AST sweep (not in primary kill list above)

| File:line | Value | Controls | Proposed governing math |
|---|---|---|---|
| `config/settings.json:edli.bias_decay_threshold_c` | 2.0°C | Triggers 50% Kelly haircut for high-bias cities. Interim guard. | CI-width on effective_bias_c: gate when `|bias| > σ_edge / 2` (bias dominates edge uncertainty). Fit σ_edge per city from settled fill distribution. |
| `config/settings.json:edli.bias_decay_kelly_factor` | 0.5 | 50% Kelly multiplier reduction for high-bias. | Kelly-optimal given measured bias: `f*_biased = f*_unbiased × (1 − |bias|/edge_lcb)`. Replace halving with analytic correction. |
| `src/strategy/live_inference/live_admission.py:134,135` | `COVERAGE_UNLICENSED_TAIL_PRICE_MAX=0.05`, `DISAGREEMENT_RATIO=2.0` | Tail rejection gate: cheap (< 5¢) candidates with q_lcb > 2× market price are rejected. | The 0.05 price boundary aligns with `probability_edge_bin_sanity.low_price_threshold=0.05` (operator-defined), so could be classified D. The 2.0× disagreement ratio is unfitted. Should be: `P(q_lcb > ratio × p_mkt | data) < threshold` under the calibrated posterior. Measuring the false-rejection rate on settled outcomes. |
| `src/calibration/settlement_backward_coverage.py:60` | `_ARM_RATIO_TOL = 0.10` | Coverage ratio tolerance for arm gate: `|coverage_ratio − 1| < 0.10`. | Should be derived from the required calibration tolerance: `ε_arm = ε_edge / (2 × d_coverage/d_q)` where the derivative connects coverage deviation to q_lcb shift. At current sample sizes, 0.10 is a reasonable default, but it should be derived from the CI-width licensing criterion (A5 addendum). |
| `config/settings.json:calibration.transfer_logit_sigma_scale` | 4.0 | Cross-domain Platt transfer uncertainty (logit slope at p=0.5). | OOS Brier-diff corpus + chain-rule derivation. Note says "operator-tunable once OOS Brier-diff corpus is available." Currently a documented guess. |

---

*Total F entries: 31 primary + 4 additional appendix = 35 unfitted decision-relevant values.*
*Report written: 2026-06-12 by executor agent sweep.*
