# Independent money-path review — commits #124 and #126

- Reviewer: independent code-reviewer pass (writer != reviewer discipline)
- Date: 2026-06-15
- Mode: READ-ONLY (no edit / commit / merge)
- Target branch: `live/iteration-2026-06-13` (HEAD `7edb38d6c7`)
- Both commits branch from merge-base `6b428b5087` ("route both gates through one
  `_direction_admitted` predicate"). Live HEAD is one commit ahead of that base
  (`7edb38d6c7` adds the read-only `SELECT_GATE_DIAG`). NOT divergent — clean lineage.
- Method: each commit reviewed in a detached `git worktree` checked out AT its SHA
  (the new test/source does NOT exist in the main working tree, so a naive in-tree
  `pytest` would silently test the WRONG code — caught and corrected during review).
  RED-on-revert claims were proven EMPIRICALLY by reverting the predicate in the
  throwaway worktree and observing the test flip to failing.

---

## COMMIT 1 — #124 test  (sha `1ac53714294aeeed37bb5bf13affbd565cacf4a7`, branch `claude/agent-ae0db7372bface8c7`)

### VERDICT: SHIP

Test-only regression-lock for the NO-on-modal favorite-longshot relaxation. Genuinely
RED-on-revert (proven two independent ways). No `src/` behavior change.

### Findings (all checks PASS)

1. **Test-only — CONFIRMED.** `git show --name-only` lists exactly two paths:
   `tests/decision/test_family_decision_engine.py` and the evidence doc
   `docs/evidence/qkernel_rebuild/test_no_on_modal_reproof_2026-06-15.md`.
   No `src/` file touched. +125 test lines / +65 doc lines (claimed +190 — matches).

2. **Pins the real invariant — CONFIRMED.** The new test
   `test_no_on_modal_survives_select_via_edge_gated_relaxation` hand-builds a
   `CandidateDecision(direction_law_ok=False, route.side="NO", economics.edge_lcb=0.08)`
   on the modal bin `b25` and calls `engine._select([cand])` directly. It asserts
   POSITIVE SELECTION (`reason is None`, `selected is not None`, `selected.route.side=="NO"`,
   `selected.route.bin_id=="b25"`, and critically `selected.direction_law_ok is False`) —
   i.e. it asserts the candidate is SELECTED despite being direction-law-illegal, not
   merely "no exception". The NO-side-only guard is also pinned: a
   `direction_law_ok=False, side="YES", edge_lcb=0.10` candidate is asserted NOT selected
   with `reason == NO_TRADE_NO_DIRECTION_LAW`.
   - `_select` (src/decision/family_decision_engine.py:848) reads `d.direction_law_ok`,
     `d.route.side`, `d.economics.edge_lcb` directly off the hand-built candidate — it does
     NOT re-derive direction law — so the test exercises exactly `_direction_admitted`
     (family_decision_engine.py:904-907) and the `live_candidate_passes` re-proof
     (family_decision_engine.py:929-936).
   - `live_candidate_passes` (src/decision/payoff_vector.py:716-749) hard-ANDs
     `direction_law_proof_present` (line 747), so reverting that arg to bare
     `d.direction_law_ok` (False here) drops the candidate.
   - `_hand_route` builds a real `executable=True` route via `build_candidate_route`, so
     the candidate clears the `after_executable` precondition and reaches the gate under test.

3. **RED-on-revert — PROVEN EMPIRICALLY (two sites).** In a detached worktree at the SHA:
   - Revert the re-proof `direction_law_proof_present=_direction_admitted(d)` →
     `=d.direction_law_ok` (the exact `3c4aeecc75` divergence bug that `6b428b5087` fixed):
     test FAILS with `reason == 'NO_POSITIVE_EDGE_CANDIDATE'` (= `NO_TRADE_NO_POSITIVE_EDGE`,
     returned at family_decision_engine.py:977). EXACTLY the commit message's claim.
   - Revert the shared `_direction_admitted` predicate body to `return d.direction_law_ok`:
     test FAILS with `reason == 'NO_DIRECTION_LAW_CANDIDATE'`.
   Both reverts go RED → the test robustly distinguishes fixed vs broken predicate.
   It is a genuine RED test, not a tautology.

4. **Run — 7 passed** (at SHA `1ac5371`, the full file):
   `pytest tests/decision/test_family_decision_engine.py -q` → `7 passed, 2 warnings`.
   (The 2 numpy RuntimeWarnings are pre-existing in sibling tests, not from the new test.)
   NOTE: the new test is NOT present in the main working tree (commit 124 is not an
   ancestor of `live/iteration-2026-06-13` HEAD); a naive in-tree run reports only the
   6 baseline tests and would NOT exercise the new one. Verified at SHA.

5. **Provenance:** `_direction_admitted` already exists at the merge-base `6b428b5087`
   (4 occurrences). Commit 124 is therefore a regression-LOCK on already-shipped fixed
   behavior — the correct pattern (future reverts of `6b428b5087` go RED). Does not gate
   any new caps/haircuts/throttles. No money-path constraint violated.

---

## COMMIT 2 — #126 venue-close sweep  (sha `2dd35cf1a13f648ef78399c2c9dece7aea643c2a`, branch `claude/agent-a4458885764eea077`)

### VERDICT: SHIP

Extends `archive_expired_candidates` to also drain venue-closed (POST_TRADING) FSR/DAY0
families. Append-only preserved, fail-closed, mirrors the reactor authority exactly,
band is pre-filter only, the changed test strengthens (not weakens) coverage. All six
hunt items PASS; proven empirically.

### Findings

1. **APPEND-ONLY law — PASS.** The only write in `archive_expired_candidates` is
   `UPDATE opportunity_event_processing SET processing_status = 'expired' ...`
   (src/events/event_store.py:~440-456), idempotent (`AND processing_status IN
   ('pending','processing')`). The append-only `opportunity_events` log is only ever
   READ (`SELECT ... FROM opportunity_events e ...`). No DELETE/UPDATE on the event log.
   Test (a) pins this: `_event_row_still_present()` queries `opportunity_events` directly
   and asserts the row survives the sweep (test_archive_expired_sweep.py:397).

2. **FAIL-CLOSED — PASS.** Per-row gate is
   `_strictly_past_in_tz(...) OR _venue_closed_in_phase(...)` (event_store.py:437-438).
   `_venue_closed_in_phase` (event_store.py:964-1017) returns `False` (KEEP active) on:
   missing city/target_date; `runtime_cities_by_name().get(city)` is None; missing tz;
   and ANY exception (`except Exception: return False`). `_strictly_past_in_tz` is likewise
   fail-closed. The `decision_time` fed in is `_parse_utc(...)` → `.astimezone(timezone.utc)`
   (zero-offset aware), so `market_phase_for_decision`'s `_require_zero_utc_offset` does NOT
   raise — the sweep is not inadvertently always-False/disabled.
   Proven empirically: injecting a fail-OPEN bug (unknown city → return True) makes test (d)
   `test_failclosed_missing_city_and_target_kept_active` FAIL (`'expired' == 'pending'`).
   The fail-closed contract is genuinely test-protected.

3. **Mirrors the reactor authority — PASS (byte-equivalent).** `_venue_closed_in_phase`
   and `reactor._venue_market_closed_horizon` (src/events/reactor.py:1017-1077) use the
   SAME imports (`runtime_cities_by_name`, `MarketPhase`, `_f1_fallback_end_utc`,
   `market_phase_for_decision`), the SAME call shape (`polymarket_start_utc=None`,
   `polymarket_end_utc=_f1_fallback_end_utc(target_local_date)`), the SAME terminal set
   `(POST_TRADING, RESOLVED)`, and the SAME fail-closed default. No re-derived/divergent
   close time. `market_phase_for_decision` returns POST_TRADING iff
   `decision_time >= polymarket_end_utc` (market_phase.py:165-166); `_f1_fallback_end_utc`
   is `12:00 UTC of target_date` (market_phase.py:204-213) — the F1 anchor. Scope is a
   SAFE SUBSET: the sweep SQL restricts to `{FORECAST_SNAPSHOT_READY, DAY0_EXTREME_UPDATED}`,
   while the reactor's `_VENUE_CLOSE_HORIZON_EVENT_TYPES` also includes
   `EDLI_REDECISION_PENDING` (a re-decision trigger, not a per-city family) — narrower
   sweep cannot terminalize a type the reactor would not.

4. **Changed test claim — CORRECT; coverage STRENGTHENED.**
   `test_day0_frontier_band_settled_is_swept_fsr_live_preserved` (renamed from
   `..._fsr_margin_preserved`), Auckland (UTC+12), decision `2026-06-05T12:30Z`:
   - OLD `band_fsr` = Auckland/2026-06-05: local day ends 2026-06-05T12:00Z (≤ 12:30Z →
     strictly-past TRUE) AND F1 close 2026-06-05T12:00Z (≤ 12:30Z → POST_TRADING). The
     family was genuinely past+closed; the old `== "pending"` assertion pinned the old
     cheap-string band EXCLUSION defect, not a real invariant. Author's claim is correct.
   - NEW `live_fsr` = Auckland/2026-06-06: strictly-past floor =
     `settlement_day_entry_utc(2026-06-07, +12)` = 2026-06-06T12:00Z; `12:30Z(06-05) >=`?
     NO → kept. Venue close 2026-06-06T12:00Z; `12:30Z(06-05) >=`? NO → SETTLEMENT_DAY,
     not POST_TRADING → kept. Correctly pins "live FSR (venue open, local day open)
     preserved". The change REPLACES a defect-pinning assertion with a genuine
     live-preservation invariant — strengthens coverage, hides no regression.

5. **Widened SQL band is pre-filter only — PASS.** Band widened to
   `target_date < frontier_floor OR <= venue_close_ceiling` (FSR) /
   `< day0_floor OR <= venue_close_ceiling` (DAY0), where
   `venue_close_ceiling = (decision_time - 12h).date()` (`_venue_close_target_ceiling`,
   event_store.py:1312-1335, with a `"0001-01-01"` past-sentinel fail-safe on arithmetic
   error → never over-archives). The actual expiry decision is STILL the per-row
   `_strictly_past_in_tz(...) OR _venue_closed_in_phase(...)` gate. A band-included row
   that is neither past nor POST_TRADING is NOT added to `expired_ids`. The per-row
   predicate is an EXACT comparison independent of the band, so a too-wide band cannot
   cause a false sweep. Verified by test (b) `test_genuinely_live_venue_open_not_swept`
   (target tomorrow → venue open → kept).

6. **Run — 25 passed** (at SHA `2dd35cf1`):
   `pytest tests/events/test_archive_expired_sweep.py tests/events/test_archive_day0_events_swept.py -q`
   → `25 passed`. Full `tests/events/` dir at SHA: `514 passed, 8 skipped, 2 xfailed` —
   no collateral breakage. `py_compile` + `pyflakes` clean on event_store.py.
   - RED-on-revert proven: removing the venue-close OR-clause from the per-row gate makes
     test (a) `test_venue_closed_local_open_swept_to_expired` FAIL (`assert 0 >= 1`).
   - The flagged baseline failures (`tests/money_path/test_finding_b_free_cash_bound.py`
     ×3) reproduce on the merge-base `6b428b5087` — pre-existing, NOT introduced by #126,
     correctly out of scope. (`test_edli_online_invariants.py` not separately re-run; the
     prompt pre-attributed it to baseline and #126 does not touch that surface.)

### Constraint ledger (#126)
- No new caps / haircuts / throttles — CONFIRMED (only a status transition + a band widen).
- Append-only `opportunity_events` — CONFIRMED (read-only; processing row mutated).
- Fail-closed MECE — CONFIRMED (both predicates keep-on-unknown; proven via fail-OPEN injection).
- Does not terminate a still-live family (venue open AND local day open) — CONFIRMED
  (tests (b),(c) + arithmetic on the changed test).

---

## Coverage statement (what was / was not reviewed)

REVIEWED in full:
- #124: the entire new test + the `_select` / `_direction_admitted` / `live_candidate_passes`
  source it pins; empirical GREEN at SHA + RED on two independent reverts.
- #126: the entire `event_store.py` diff (band widen, per-row gate, `_venue_closed_in_phase`,
  `_venue_close_target_ceiling`); the reactor authority it mirrors; the F1/market_phase
  authority; all 4 new tests + the 1 changed test; empirical GREEN (25 + full events 514)
  at SHA + RED on revert + fail-OPEN injection catch; baseline-failure attribution on the
  merge-base.

NOT reviewed (out of scope / not load-bearing for these two commits):
- The pre-existing `tests/money_path/test_edli_online_invariants.py` failures (prompt
  pre-attributed to baseline; #126 does not touch that surface).
- The two evidence/markdown docs were confirmed present in their commits but their prose
  was not line-audited (non-code).
- Downstream readers of `processing_status='expired'` were confirmed by docstring claim +
  the `tests/events/` green suite, not each independently re-read.

## Bottom line
- #124: **SHIP** — genuine RED-on-revert regression lock, test-only, no money-path risk.
- #126: **SHIP** — append-only safe, fail-closed, reactor-mirrored, band is pre-filter,
  changed test strengthens coverage. No live family at risk.
