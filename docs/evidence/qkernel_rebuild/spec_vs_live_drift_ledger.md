# Q-Kernel Rebuild — Spec-vs-Live Drift Ledger

One consolidated reconciliation of the `consult_build_spec.md` references against the live worktree tip across all four rebuild subsystems (`reactor_seams`, `calibration_forecast`, `execution_routes`, `exit_lifecycle`). Read this before touching any cited path — where spec and live disagree, **the Actual-live column is authoritative**.

---

## VENUE-PRIMITIVE VERDICT (gates Stage-7 conversion routes)

> ### **ABSENT — on-chain neg-risk convert/merge/split primitives are NOT WIRED.**
>
> `grep -rn "def convert|mergePositions(|convertPositions(|splitPosition(|def merge_positions|def split_position" src/venue/ src/execution/` → **ZERO executable hits (exit 1).**
>
> **Only wired venue position primitives:** redeem / `redeemPositions` (standard-CTF + NegRiskAdapter) at `src/venue/polymarket_v2_adapter.py:226` (protocol), `:872` (impl `redeem`), `:1496` (`_redeem_via_negrisk_safe`), `:2669`/`:2733` (calldata encoders), plus submit / cancel.
>
> The `merge`/`split` strings a broad grep finds are **unrelated in-memory Python helpers**: `src/strategy/family_exclusive_dedup.py:748` `merge(*exposure_groups)` (exposure-group union) and `src/runtime/bankroll_provider.py:363` `_split_positions_by_domain` (position partitioning) — neither touches chain.
>
> **CONSEQUENCE (spec lines 728-732):** Stage-7 `RouteCost.route_type = CONVERSION_SELL_BASKET` and `NegRiskRouteSet.conversion_routes` have **no venue primitive to execute** and **MUST remain shadow**. `DIRECT_YES` / `DIRECT_NO` / `SYNTHETIC_NOT_I_YES_BASKET` / `PAIR_ARB` / `FULL_YES_BASKET_ARB` may go live via existing `submit()` (multiple independent native orders, no conversion). Conversion routes can go live **only after** a convert/merge/split primitive is added to `PolymarketV2Adapter` + `NegRiskAdapter` calldata encoders.

Other subsystems' venue verdicts: `reactor_seams` N/A (forecast/decision seam, not execution); `calibration_forecast` N/A; `exit_lifecycle` N/A (live exit path does only direct token sells; `conversion_basket_sell_value` is a net-new dependency of the not-yet-created `liquidation_value.py`, no existing exit-side wiring to confirm against).

---

## Drift table (BLOCKER → MAJOR → MINOR)

CONFIRMED_OK rows are collapsed to a per-subsystem count below the table (all cited paths/symbols/lines resolve exactly as the spec claims; nothing to reconcile).

| Spec ref | Spec claims | Actual live | Resolution | Severity |
|---|---|---|---|---|
| `consult_build_spec.md:728-732` (execution_routes) — CRITICAL GATE: verify venue primitives before live conversion routes | If convert/merge/split not wired, conversion routes stay shadow; only direct/synthetic routes proceed | **convert/merge/split ABSENT.** `grep -rn` for executable methods across `src/venue/` + `src/execution/` = ZERO hits. Only wired: redeem/`redeemPositions` (`polymarket_v2_adapter.py:226`/`:872`/`:1496`/`:2669`/`:2733`) + submit/cancel/get_*. No `PolymarketV2AdapterProtocol` convert/merge/split method (`:196-235`). The merge/split grep hits are non-venue helpers (`family_exclusive_dedup.py:748`, `bankroll_provider.py:363`). | Stage-7 `CONVERSION_SELL_BASKET` / `NegRiskRouteSet.conversion_routes` **MUST STAY SHADOW**. `DIRECT_YES`, `DIRECT_NO`, `SYNTHETIC_NOT_I_YES_BASKET`, `PAIR_ARB`, `FULL_YES_BASKET_ARB` may go live via existing `submit()`. To enable conversion later, add a convert/merge/split method to `PolymarketV2Adapter` + `NegRiskAdapter` calldata encoder first. | **BLOCKER** |
| `consult_build_spec.md:1002,1136` (execution_routes) — schema path `src/events/no_trade_events_schema.py` | Stage-0 receipt-spine file at `src/events/no_trade_events_schema.py` | **`src/events/no_trade_events_schema.py` DOES NOT EXIST.** Real path is `src/state/schema/no_trade_events_schema.py` (verified via `find`). Sibling `src/state/no_trade_events.py` also exists. No `src/events/` hit. | Stage-0 implementers must wire receipt fields into `src/state/schema/no_trade_events_schema.py` (NOT `src/events/...`). Spec path is wrong; substitute the real path. | **MAJOR** |
| `subsystem brief` (calibration_forecast) — `src/strategy/probability_uncertainty.py`: `no_side_samples` + `lower_quantile` as rebuild seam | Brief named these as the NO-complement rebuild seam | **BRIEF-vs-SPEC MISMATCH** (not code drift): helpers exist live (`probability_uncertainty.py:105` `no_side_samples`, `:83` `lower_quantile`, `:90` `upper_quantile`, `:368` `edge_lcb`) but `consult_build_spec.md` does NOT reference this file anywhere (grep: NOT FOUND). | Do NOT expect `probability_uncertainty.no_side_samples`/`lower_quantile` to be the rebuild seam. The live NO seam is `1 - q_ucb_yes` at `event_reactor_adapter.py:9955`; rebuild target is `q_no_samples = 1 - q_yes_samples` from `JointQBand.samples` (new module `src/probability/joint_q_band.py`). Helpers remain available if reused. | **MAJOR** |
| `consult_build_spec.md:1212,1234` (exit_lifecycle) — `src/strategy/exit_family_optimizer.py` ('no in-tree caller imports it yet') | Optimizer exists but no live caller; possibly resurrect/replace | **ALIVE-BUT-DEAD-IN-SRC.** File exists (18214 B, last touched 2026-06-07). Exports `optimize_exit_family` (`:233`), `ExitLegInput` (`:88`), `ExitLegDecision` (`:118`), `ExitFamilyDecision` (`:134`). NO live `src/` import (grep empty); only tests import it; `src/` mentions are docstrings/comments only. | **RESURRECTABLE, not dead-delete.** Recent, test-covered, pure-math; `optimize_exit_family(legs: Sequence[ExitLegInput])` already consumes a family vector. RESURRECT-AS-INPUT: call it from the new engine for direct/hold legs, layer `conversion_basket_sell_value` on top. If `LiquidationRoute` diverges, REPLACE but salvage `_per_leg_sell_value`/`_per_leg_hold_value`/`_held_probability`. Delete only under Stage 11 once subsumed. | **MAJOR** |
| `consult_build_spec.md:934-938,928` (exit_lifecycle) — `LiquidationValueEngine` over the family position vector; `position_vector_hash` | Engine reads a family position vector (`direct_sell_value`/`conversion_basket_sell_value`/`hold_to_redeem_value` over `position`, `family_book`, `venue_primitives`, `joint_q`); `LiquidationDecision` carries `position_vector_hash` | **DRIFT — no family position-vector exists at the exit decision site.** Live exit is strictly PER-POSITION single-token: `Position.evaluate_exit` (`portfolio.py:842`) called per position at `cycle_runtime.py:3559`. `ExitContext` (`portfolio.py:147`) is single-token (fresh_prob/price/bid/ask/vig/toxicity) — NO family leg vector, family_book, joint_q, or position_vector_hash. Only family grouping in repo is `family_exclusive_dedup.py` (entry-side), not threaded into exit. `optimize_exit_family` DOES take a family vector but is never invoked live. | The rebuild must **CREATE** the family position-vector read for exit — it is ABSENT, not drifted. Group `PortfolioState` positions by `family_key` (primitive in `family_exclusive_dedup.py`), shape as `ExitLegInput[]` (what `optimize_exit_family` consumes). Thread family_book/joint_q/venue_primitives into a NEW family-scoped exit context (today's `ExitContext` is insufficient). `position_vector_hash` is net-new. Wire `LiquidationValueEngine` at the `cycle_runtime.py:3559` exit tick. | **MAJOR** |
| `brief + line 25 BLOCKER` (reactor_seams) — `_maybe_apply_edli_bias_correction` (call `:11084` / def `:11526`) | Spec cites single call `:11084` + def `:11526` (body 11526-1608) | CONFIRMED both: call `:11084` inside day0/q-build branch; def `:11526`; returns `:11609`. **SECOND live call at `:12594`** (the `_snapshot_p_raw` FULL FORWARD PREDICTIVE path, guarded by `members_already_corrected`) — the spec's single `:11084` cite does not mention it. | Stage-2/Stage-11 target. Both spec sites resolve. **Stage-11 deletion must neutralize BOTH call sites** (`:11084` AND `:12594`), not just `:11084`. | **MINOR** |
| `consult_build_spec.md:73-77` (execution_routes) — `utility_ranker.py` header: 'DEFAULT-OFF / SHADOW … NOT wired into live … until payoff-matrix shadow passes' | Header claims module is DEFAULT-OFF/SHADOW, not wired live (operator directive 2026-06-08, §14.8) | **STALE provenance.** Header at `utility_ranker.py:73`/`:77` still says SHADOW/DEFAULT-OFF, but module IS live-wired: imported `event_reactor_adapter.py:248`, `FamilyPayoffMatrix.over_bins` `:8313`, `rank_candidates` `:8329`. Spec line 57 itself confirms the reactor uses it live. | Treat the SHADOW/DEFAULT-OFF/'NOT wired' header as STALE — do NOT trust it. Live-wiring authority is `event_reactor_adapter.py:248/8313/8329`. Update the header when touching the file. | **MINOR** |
| `consult_build_spec.md:34` (calibration_forecast) — `emos_q_builder.py:108` (μ-offset application) | `:108` activated offsets shift μ via `mu_c = mu_c - offset` | **Off-by-one.** Offset READ at `:107`; guard `if _mu_off_c is not None and float(_mu_off_c) < 0.0:` at `:108`; subtraction `mu_c = mu_c - float(_mu_off_c)` at `:109`. The μ=μ*−offset mechanic is exactly as described. | Implement against `emos_q_builder.py:107-109` (not the single `:108`). | **MINOR** |
| `consult_build_spec.md:47,1043-1062` (calibration_forecast) — `emos_q_builder.py:132-140` rounding-rule omission | `build_emos_q` calls `bin_probability_settlement` WITHOUT the city's rounding rule (defaults WMO, ignoring HK `oracle_truncate`) | **CONFIRMED omission, with a nuance.** `emos_q_builder.py:132-136` omits `rounding_rule`. But `bin_probability_settlement` (`emos.py:587`) DOES support `rounding_rule` (defaults `'wmo_half_up'`) — the integrator has the capability; the seam silently defaults to WMO. So HK families get `wmo_half_up` instead of `oracle_truncate`. | Thread `OutcomeSpace.resolution.rounding_rule` into the `bin_probability_settlement` call at `emos_q_builder.py:133`. Cited `:132-140` window is exact. | **MINOR** |
| `consult_build_spec.md:41,587,1135,1319` (calibration_forecast) — `replacement_forecast_materializer.py:1419-1441` `_build_fused_q_bounds` | `np.percentile(probs,5,axis=0)` over raw per-bin masses (not row-normalized joint draws) | **CONFIRMED.** def header at `:1359` (not 1419), but percentile band is at `:1425-1426` (`q_lcb_vec=np.percentile(probs,5.0,axis=0)`, `q_ucb_vec=...95.0...`); `probs` (`:1423`) is (N draws × M bins) per-draw mass with NO per-row Σq=1 renormalization before the marginal quantile. Cited `:1419-1441` brackets the band. | Rebuild = `JointQBand` that renormalizes each draw row to simplex before any marginal quantile. Implement against `_build_fused_q_bounds` (def `:1359`, percentile `:1425-1426`). | **MINOR** |
| `consult_build_spec.md:467,530` (calibration_forecast) — `settlement_preimage` symbol name | Prose `:467` names `settlement_preimage_offsets`; pseudocode `:530` calls `settlement_preimage(bin, omega.resolution)` | Live symbol is `settlement_preimage_offsets` (`settlement_semantics.py:57`). A bare `settlement_preimage` does NOT exist. Spec `:530` is NEW-function pseudocode (`build_joint_q`), so `settlement_preimage(...)` is a to-be-created wrapper, not a drift. | Rebuild must wrap/call the existing `settlement_preimage_offsets:57`; there is no bare `settlement_preimage` to import. Prose `:467` uses the correct existing name. | **MINOR** |
| `consult_build_spec.md:654,734,1004` (execution_routes) — new files `src/execution/negrisk_routes.py`, `src/decision/payoff_vector.py`, `src/decision/decision_receipt.py` | Create these new files | ABSENT-as-expected for to-create files, but **DIRECTORY DRIFT**: parent dir `src/decision/` does NOT EXIST at all (`ls`: No such file or directory). `negrisk_routes.py` is fine under existing `src/execution/`. | Create the `src/decision/` package first (with `__init__.py`) before `payoff_vector.py` / `decision_receipt.py`. The spec assumes a `src/decision/` package not yet in the tree. (Same absence noted in reactor_seams Stage-8 for `family_decision_engine.py`.) | **MINOR** |
| `consult_build_spec.md:64 vs :1216` (exit_lifecycle) — RED test name for conversion-basket route | `tests/execution/test_liquidation_value_engine.py` net-new RED tests | Net-new (correctly absent). **INTERNAL SPEC DRIFT:** `:64` names `test_no_position_chooses_conversion_basket_when_direct_bid_is_worse`; `:1216` names `test_no_position_chooses_conversion_basket_when_more_valuable` — same intent, two names in the spec itself. | Author the test once; pick ONE canonical name. Recommend `:1216`'s `..._when_more_valuable`. | **MINOR** |

---

## CONFIRMED_OK counts (all cited refs resolve exactly; no reconciliation needed)

| Subsystem | CONFIRMED_OK rows |
|---|---|
| `reactor_seams` | 10 (q-producer seam `_live_yes_probabilities` def `:9283`; q-build branch tree `:10940-11091`; BLOCKER `:10944-11091`; day0 branch `:11074-11091`; sizing-consumer seam `_score_family_candidates_by_robust_marginal_utility` def `:8276`; scalar binary-Kelly `family_total = bankroll_usd * mult * f_star` `:8632`; NO lower bound `q_lcb_no = 1.0 - q_ucb_yes` `:9955`; per-sample seam `_side_q_lcb_from_yes_samples` def `:10286` / use `:10461`; Stage-8 target `src/decision/family_decision_engine.py` correctly absent; `utility_ranker.py` `FamilyPayoffMatrix` `:115`) |
| `calibration_forecast` | 7 (`emos.py` symbols; `build_emos_q` at `emos_q_builder.py:41`; `bin_probability_settlement` `emos.py:580`; settlement σ-floor branch `emos_q_builder.py:116-121`/`186-191`; `replacement_forecast_materializer.py:1119` `predictive_sigma_c=max(1.0,...)`; soft-anchor q_shape fallback; all `settlement_semantics.py` symbols `for_city:257`/`settlement_preimage_offsets:57`/both rounding rules) |
| `execution_routes` | 7 (`executable_cost._levels_for_direction` `:158`; `with_taker_fee` `:78`; midpoint/last bans `assert_not_midpoint_cost:88`/`:104`; `market_anchor.market_anchored_no_lcb:89`; `trade_score.robust_trade_score:36`; `utility_ranker.FamilyPayoffMatrix:115`/ΔU live-wired; reactor sizing seam `event_reactor_adapter.py:8625-8632`; `neg_risk` passthrough in `executable_cost`) |
| `exit_lifecycle` | 4 (direct-token sell path `exit_lifecycle.py:1242-1342`, `place_sell_order` call `:1333` in `_execute_live_exit:1153`; `ExitIntent:146`/`build_exit_intent:652`/`place_sell_order` wrapper `:157`→`executor.py:2184`; net-new `liquidation_value.py` correctly absent; modify-locus `exit_lifecycle.py` confirmed) |

---

## For implement agents

- **Always prefer the Actual-live column over the spec.** Every "Spec claims" cell that disagrees with "Actual live" is a spec error or omission; the live worktree is ground truth.
- **Stage-7 gate is hard:** conversion routes stay shadow until a convert/merge/split venue primitive lands. Do not wire `CONVERSION_SELL_BASKET` / `conversion_routes` to live `submit()`.
- **`src/decision/` package does not exist** — create it (with `__init__.py`) before any `src/decision/*.py` target (`family_decision_engine.py`, `payoff_vector.py`, `decision_receipt.py`).
- **Stage-0 schema path correction:** use `src/state/schema/no_trade_events_schema.py`, never `src/events/...`.
- **EDLI bias correction has TWO call sites** (`event_reactor_adapter.py:11084` and `:12594`) — Stage-11 must neutralize both.
- **`exit_family_optimizer.py` is resurrectable, not dead** — reuse `optimize_exit_family(legs)` as input to the new `LiquidationValueEngine`; do not rewrite from scratch.
- **The family position-vector for exit is ABSENT** — it must be created (group by `family_key`, shape `ExitLegInput[]`, thread family_book/joint_q/venue_primitives into a new family-scoped exit context); today's exit is strictly per-position single-token.
- **Treat the `utility_ranker.py` SHADOW/DEFAULT-OFF header as stale** — the module is live-wired; trust `event_reactor_adapter.py:248/8313/8329`.
