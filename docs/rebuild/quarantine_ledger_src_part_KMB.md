## KM-B: portfolio.py, market_scanner.py, lifecycle_manager.py, fill_tracker.py

Investigator: main (re-driven inline after original KM-B sub-agent died on API 400). portfolio.py + market_scanner.py: targeted extraction (too large for full read, 4048L/6014L). lifecycle_manager.py (554L) + fill_tracker.py (1670L): read in full.

| File | Lines | Mechanism | Bucket | New name (if B2) | Consumers | DB artifacts |
|---|---|---|---|---|---|---|
| lifecycle_manager.py | 31-102 (LEGAL_LIFECYCLE_FOLDS), 122-126 (TERMINAL_STATES derivation) | state-machine fold table: QUARANTINED legal transitions {QUARANTINED,SETTLED,VOIDED} + entry from None; NOT terminal (P0c 2026-07-04 widened it) | B1 dies w/ T5 — **this IS the canonical enum + transition table T5 must rewrite** | — | fold_lifecycle_phase, is_terminal_state, every enter_*_runtime_state fn below | — |
| lifecycle_manager.py | 174-203 (`_legacy_runtime_phase_dispatch`, FROZEN oracle, not live), 232-269 (`derive_runtime_position_phase`) | dispatch state string→LifecyclePhase.QUARANTINED (explicit + chain-fallback); delegates to canonical_projections.py `derive_position_phase` | B1 dies w/ T5, re-point to new enum member | — | phase_for_runtime_position (canonical runtime authority) | — |
| lifecycle_manager.py | 288-346 (`enter_pending_exit_runtime_state`), 392-394 (`enter_chain_quarantined_runtime_state`), 416-441 (`enter_settled_runtime_state`), 463-491 (`enter_voided_runtime_state`) | 4 explicit fold-guard writers that special-case QUARANTINED origin (pending_exit escape hatch for entry_authority_quarantined/chain_absent_confirmed; direct quarantine entry; settle-from-quarantine; void-from-quarantine force-resolve) | B1 dies w/ T5, **all 4 must be re-pointed together — same packet**, this is the live transition surface | — | `enter_chain_quarantined_runtime_state` called from portfolio.py:61 (import only; no live call site found in files read — verify); tests/test_b066_quarantine_sentinel_ids.py, tests/test_live_safety_invariants.py | — |
| portfolio.py | 315-317,879-884 | `QUARANTINE_SENTINEL="QUARANTINE_UNRESOLVED"` city placeholder + `is_quarantine_placeholder` property (True when city==sentinel) — flags chain-only stub w/ unresolved market metadata, must not enter lifecycle/risk/monitor logic | B2 | sentinel→`UNRESOLVED_CHAIN_STUB`, property→`is_unresolved_chain_stub` | cycle_runtime.py:5084 belt-and-suspenders skip (KM-A); fill_tracker.py doesn't reference it | — |
| portfolio.py | 650-651 | `quarantined_at: str` field on Position dataclass — ISO timestamp when quarantined | B1 dies w/ T5, rename field to match new state (`disputed_at`/`review_flagged_at`) | — | set throughout chain_reconciliation.py (KM-A) | position_current column? (verify — likely not persisted directly, check w/ canonical projection builder) |
| portfolio.py | 2510-2550 (`_chain_only_fact_from_row`), 2553-2591 (`_derive_chain_only_review_state`) | build typed `ChainOnlyFact` (legit, PR E2 replacement for old synthetic-Position path) from a `token_suppression` row; review-state derived from `suppression_reason` (`chain_only_quarantined`→unresolved/expired-after-48h, `operator_quarantine_clear`/`settled_position`→resolved) | B2, shared vocab w/ db.py TOKEN_SUPPRESSION_REASONS (KM-A) and chain_reconciliation.py (KM-A) | rename literals together, same packet | called by load_portfolio (below) | reads token_suppression rows |
| portfolio.py | 2603-2650 (`load_runtime_open_portfolio`, "POISON projection row quarantined" log), 2789-2812 (`load_portfolio`, same log wording) | **misleading log text**: a row whose enum-coercion fails is SKIPPED (not added to positions, error logged) — no quarantine state is actually minted here, just word used loosely for "excluded/contained" | B4 text-only | log→"POISON projection row **excluded**" / "**skipped**" | — | — |
| portfolio.py | 2680-2828 (`load_portfolio` main body) | calls db.py `query_chain_only_quarantine_rows`/`query_token_suppression_tokens` (KM-A shared mechanism), builds `chain_only_facts` list from suppression rows not already represented by a real local position | B2, same as db.py entry (KM-A) — this is the READ side of the T2 scoped-block pattern | — | `_has_quarantined_positions` in cycle_runner.py consults chain_only_facts (per comment; verify w/ Tail-3) | — |
| portfolio.py | 2905-2908 | write-side filter for positions-{mode}.json cache: strips terminal + `quarantined` rows before serializing (defence-in-depth alongside explicit pop() calls) | B1 dies w/ T5 literal | — | — | — |
| portfolio.py | 3058-3067 (`INACTIVE_RUNTIME_STATES`) | frozenset = TERMINAL_POSITION_STATES ∪ {"economically_closed","quarantined"} — explicit comment: consumed by chain_reconciliation.py, cycle_runtime.py, evaluator.py entry-dedup, exchange_reconcile.py | B1 dies w/ T5, **wide blast radius — 4+ consumer files must be re-pointed together** | — | chain_reconciliation.py (KM-A), cycle_runtime.py (KM-A), evaluator.py (not yet classified — cross-ref Tail-3/other), exchange_reconcile.py (Tail-3 scope) | — |
| portfolio.py | 3088-3100 (`_is_runtime_open_position`) | treats state=="quarantined" + current-money-risk chain_state + positive chain_shares as runtime-open (same redecision-eligibility shape as cycle_runtime.py `_quarantined_position_can_redecision`, KM-A — **3rd independent copy of similar logic**, flag for consolidation) | B1 dies w/ T5, consolidate w/ KM-A's flagged duplication | — | — | — |
| portfolio.py | 3646-3690 (`has_same_token_open_db`) | decision-time DB dedup gate: direct SQL `phase = 'quarantined' AND chain_state IN (...)` / `phase IN ('voided','quarantined')` fallback | B1 dies w/ T5 | — | called at decision/entry time (dedup gate) | position_current.phase/chain_state literal in raw SQL |
| market_scanner.py | 540-693,811-953,1254-1256 (whole `source_contract_quarantine` subsystem) | city-scoped JSON-file (not DB) block: `source_contract_quarantine.json` records per-city active blocks w/ reason+evidence; `is_city_source_quarantined()` gates Gamma market scan skip; `upsert_source_contract_quarantine()`/`release_source_contract_quarantine()` — release REQUIRES all 6 evidence keys present+completed (`REQUIRED_SOURCE_CONVERSION_EVIDENCE`: config_updated, source_validity_updated, backfill_completed, settlements_rebuilt, calibration_rebuilt, verification_passed) — **textbook match for mission doc's own T2 worked example** ("city-scoped source block with release evidence") | B2 — doc's own named target, straightforward mechanical rename | file→`source_contract_block.json`, fn prefix `*_quarantine*`→`*_block*` throughout (source_contract_quarantine_path→source_contract_block_path, is_city_source_quarantined→is_city_source_blocked, upsert/release_source_contract_quarantine→upsert/release_source_contract_block, SOURCE_CONTRACT_QUARANTINE_SCHEMA_VERSION→SOURCE_CONTRACT_BLOCK_SCHEMA_VERSION) | scripts/source_contract_auto_convert.py, scripts/watch_source_contract.py (only 2 external consumers, no other src/ file) | JSON file only, no DB table. **Cross-ref**: db.py `_SOURCE_CONTRACT_AUDIT_STATUSES` incl "QUARANTINED" (db.py:6620-6626, KM-A) is a SEPARATE `source_contract_audit_events` DB table — likely fed by scripts/watch_source_contract.py, same rename theme but distinct artifact — verify relationship before assuming they're one migration |
| fill_tracker.py | 57-60 (4 reason consts), 875-919 (`_quarantine_missing_fill_economics`,`_quarantine_missing_fill_authority`), 926-934 (`_mark_entry_quarantined`,`_is_entry_quarantined`), 988,1050,1114,1165,1240,1287 (7 call sites, matches mission doc's own line list exactly) | **T4 target, confirmed exact match to doc's disease description**: missing venue fill economics / missing authority fields / ledger write failure / canonical write failure / void canonical write failure — all folded into `pos.state="quarantined"` via one shared setter | B1 dies w/ T4 | per doc's target form: missing-economics/authority→stay pending_entry w/ typed blocker reason (re-polled); local write failure→loud ERROR+retry, no state change; consts drop entirely | — | pos.state (Position dataclass, projects to position_current.phase) |
| fill_tracker.py | 1456-1473 (`_pending_entry_no_order_id_timeout` handler) | pending-entry-with-no-order-id timeout path sets `pos.state = LifecycleState.QUARANTINED.value` directly (does NOT go through `_mark_entry_quarantined`, separate write site not in doc's 7-site list — **8th site, undercounted by doc**) | B1 dies w/ T4/T5 — per doc's target form this is the "pending timeout → hand to chain reconciliation" case | — | — | — |

### Summary (KM-B only)
- Mechanism count: 15 distinct mechanisms across 4 files.
- Buckets: B1=11, B2=3, B3=0, B4=1.
- **Hardest reshape**: market_scanner.py source-contract-block subsystem is actually the EASIEST of the reshapes (doc already names the target shape) — the harder call is portfolio.py's `INACTIVE_RUNTIME_STATES` frozenset, which has 4+ file consumers (chain_reconciliation.py, cycle_runtime.py, evaluator.py, exchange_reconcile.py) that must all be re-pointed to the new phase name in the same packet or the system will have inconsistent open/closed classification mid-migration.
- **New finding**: fill_tracker.py has an 8th `pos.state="quarantined"` write site (line 1471, the no-order-id timeout handler) not in the mission doc's list of 7 call sites for T4 — doc's blast-radius count is undercounted by one.
- **Duplication flagged** (3rd instance, adds to KM-A's finding): portfolio.py `_is_runtime_open_position` (3088-3100) is a THIRD independent implementation of the same "quarantined + current-money-risk chain_state + positive shares = still counts as open" predicate, alongside cycle_runtime.py's `_quarantined_position_can_redecision` and `_canonical_monitor_position_rows`'s inline copy (both KM-A). Three call sites computing the same thing — high risk of drift during reshape, should consolidate to ONE shared predicate function all three modules import.
