## KM-A: decision_integrity_quarantine.py, chain_reconciliation.py, db.py, cycle_runtime.py

Investigator: main (self, not sub-dispatched). Files read IN FULL: decision_integrity_quarantine.py (940L), chain_reconciliation.py (2364L). db.py (13245L) and cycle_runtime.py (6355L) too big for full read — targeted extraction of every quarantine-bearing block w/ wide context, all cited line ranges verified against current code.

| File | Lines | Mechanism | Bucket | New name (if B2) | Consumers | DB artifacts |
|---|---|---|---|---|---|---|
| decision_integrity_quarantine.py | 311-457,459-560,567-716,739-940 | tag fact rows (opportunity_fact,calibration_pairs,probability_trace_fact,selection_family_fact,selection_hypothesis_fact,decision_events) linked via snapshot_id to non-contributing/UNKNOWN-attribution ensemble snapshot. non-destructive tag, idempotent INSERT OR IGNORE. | B2 (doc's own worked example — "re-implement as first-class fact invalidation") | table→fact-row validity: prefer `is_admissible` computed view or `fact_invalidations`; reason `QUARANTINED_NON_CONTRIBUTING_FORECAST_EXTREMA`→`NONCONTRIBUTING_FORECAST_EXTREMA_EXCLUDED` | scripts/quarantine_bad_forecast_decisions.py (CLI), src/analysis/evidence_report.py (reader), tests/test_decision_integrity_quarantine.py | table `decision_integrity_quarantine` (zeus_trades.db) UNIQUE(table_name,row_id,reason_code) |
| decision_integrity_quarantine.py | 64-184,187-308 | tag LIVE `decision_certificates` rows (table_name='decision_certificates') failing money-law semantics: invalid payload verify OR parent-ancestry mode != LIVE. row remains immutable; tag = durable live-reader exclusion. | B2 — HARDEST reshape (doc's own erratum #8 case) | certificate revocation: `is_certificate_revoked()` on cert row or dedicated `certificate_revocations` table; reasons `QUARANTINED_INVALID_LIVE_ACTIONABLE_CERTIFICATE`→`CERTIFICATE_REVOKED_INVALID_PAYLOAD`, `QUARANTINED_INVALID_LIVE_MONEY_PARENT_MODE`→`CERTIFICATE_REVOKED_INVALID_PARENT_MODE` | LIVE CALLERS (money-path): executor.py:1384 `_decision_certificate_is_quarantined()` (pre-submit gate), command_recovery.py:3042 `_decision_certificate_is_quarantined()` (recovery pre-submit gate) — **both reimplement the same check independently, not shared import**; reshape should consolidate to ONE checker both import | same table, table_name='decision_certificates' |
| decision_integrity_quarantine.py | 448-456,557-560 | `_quarantine_ref`/`_snap_ref`: cross-DB schema-qualifier helpers | B1 dies w/ table rename | — | internal only | — |
| schema/decision_integrity_quarantine_schema.py | 1-100 (full) | CREATE TABLE DDL, SCHEMA_VERSION 32 bump, `ensure_table()` | B2, dies together w/ table rename above | — | db.py:5951-5953 `init_schema` call | table DDL itself |
| chain_reconciliation.py | 1136-1142,1806-1851 | `_is_current_money_risk_quarantine`: state==QUARANTINED + current-money-risk chain_state check; refresh block re-syncs chain economics for such positions every cycle w/o resolving | B1 dies w/ T5 | — | called throughout reconcile() main loop | position_current.phase='quarantined' |
| chain_reconciliation.py | 1274-1319,1719,2067,1755-1803 | `_preserve_confirmed_fill_chain_absence_conflict` + VOIDED-w/-positive-chain-evidence correction: sets state=QUARANTINED,chain_state='entry_authority_quarantined' to PRESERVE monitorable exposure on confirmed-fill-vs-chain-absence conflict — legit "flag for review, don't drop" wearing quarantine clothes | B2 | phase/flag `entry_authority_disputed`, resolved via chain-mirror reconciler grading (doc confirms this path exists) | chain-mirror reconciler (grading consumer, not read here) | position_current.phase, chain_state |
| chain_reconciliation.py | 1373-1436 | `_restore_terminal_chain_exposure_if_available`: same pattern — restores chain-observed exposure into QUARANTINED/'entry_authority_quarantined' when terminal-phase position still shows chain balance | B2, same new name as row above | — | — | same fields |
| chain_reconciliation.py | 1531-1591 | `_materialize_chain_only_position_if_resolvable`: **CONTRADICTS doc's own T5 target** ("chain-only unknown assets live as ChainOnlyFact... never as Position rows") — materializes fake Position w/ state=QUARANTINED for chain-only token | B1 dies w/ T5, but **flag: must be DELETED not renamed** — ChainOnlyFact already covers this case, doc's own design says so | — | called from Rule-3 block below | position_current row minted (should not exist per doc) |
| chain_reconciliation.py | 1321-1352,2250-2287 | `_persist_chain_only_quarantine_fact` + Rule-3 loop: `record_token_suppression(...,suppression_reason="chain_only_quarantined",...)` (scoped per-token block) + appends typed `ChainOnlyFact` to `portfolio.chain_only_facts` — genuinely-needed scoped source block, matches T2 pattern #1 exactly | B2 | suppression_reason `chain_only_quarantined`→`chain_only_unattributed` (shared const w/ db.py TOKEN_SUPPRESSION_REASONS) | db.py record_token_suppression/query_chain_only_quarantine_rows (shared vocab, see below) | token_suppression, token_suppression_history tables |
| chain_reconciliation.py | 906-944 | `_append_canonical_review_required`: generic "write REVIEW_REQUIRED event + phase=QUARANTINED projection", used by 3 rows above | B1 dies w/ T5 (phase literal); event type REVIEW_REQUIRED + reason string are B2-legit, keep | called by rows above | position_events, position_current | 
| chain_reconciliation.py | 2292-2364 | `check_quarantine_timeouts`/`quarantine_resolution_reason` + QUARANTINE_REVIEW_REQUIRED/QUARANTINE_EXPIRED_REVIEW_REQUIRED: doc's OWN comment confirms 48h position-quarantine timer ALREADY RETIRED (P0b 2026-07-04); fn now ONLY does ChainOnlyFact 48h review escalation (log-only) | B2 | fn→`chain_only_review_escalation_check`, consts→`CHAIN_ONLY_REVIEW_REQUIRED`/`_EXPIRED` | cycle_runtime.py:4798 imports `quarantine_resolution_reason`; cycle_runner.py calls `check_quarantine_timeouts` unconditionally per this file's own docstring (verify w/ Tail-3) | — |
| db.py | 1601-1609 | TOKEN_SUPPRESSION_REASONS/RESOLVED_TOKEN_SUPPRESSION_REASONS consts: frozenset{"operator_quarantine_clear","chain_only_quarantined","settled_position"} | B2, shared vocab w/ chain_reconciliation.py row above | rename both quarantine literals | — | — |
| db.py | 5835-5862 | token_suppression / token_suppression_history DDL, CHECK(suppression_reason IN(...)) same 3 literals | B2 | same rename, needs schema migration | — | 2 tables, CHECK literal |
| db.py | ~11500-11642 (record_token_suppression), ~11645-11710 (query_token_suppression_tokens), 11845-11896 (query_chain_only_quarantine_rows), ~11800-11823 (chain_only_entry_block_scope) | THE legit scoped-source-block mechanism (T2 pattern #1: block only for own condition_id) | B2 | fn `query_chain_only_quarantine_rows`→`query_chain_only_unattributed_rows` (name itself carries word) | chain_reconciliation.py (writer), cycle_runtime.py `_apply_acknowledged_quarantine_clears` (writer via deps), control_plane.py (ack tokens, KM-C covers) | token_suppression* |
| db.py | 11238-11258 | runtime-exposure load query: phase='quarantined' treated as "current risk-review set," included unless chain_state in TERMINAL_NO_CURRENT_MONEY_RISK_CHAIN_STATES | B1 dies w/ T5 | — | (need enclosing fn name — inside a get/load-positions query builder) | position_current.phase |
| db.py | 5099-5109 | position_events CHECK: phase_before/phase_after IN(...,'quarantined',...) | B1 dies w/ T5 schema migration | — | — | position_events CHECK |
| db.py | 5102 | `occurred_at CHECK (... OR occurred_at = 'QUARANTINE')` — **exact T7 target**, state word inside timestamp type | B4/B3 — no live writer found for literal 'QUARANTINE' occurred_at in files read so far (verify full-repo grep before declaring dead) | drop the OR clause | — | position_events CHECK |
| db.py | 5147-5150,5433 | position_current.phase CHECK AND **separate** position_lots.state CHECK (UPPERCASE enum incl 'QUARANTINED') — **two distinct tables/enums, both need T5 migration; position_lots easy to miss if T5 scope only checks position_current** | B1 dies w/ T5, flag position_lots as second migration target | — | — | 2 separate CHECK constraints |
| db.py | 1739,1802,3633,~7716, trigger ~3122-3145 | **NOT in T1-T7, newly found mass**: `settlements.authority` CHECK IN('VERIFIED','UNVERIFIED','QUARANTINED') + `settlements_authority_monotonic` trigger enforcing QUARANTINED→VERIFIED requires non-empty `provenance_json.reactivated_by`, blocks VERIFIED→UNVERIFIED | B2 — textbook LEGITIMATE case (reject-at-boundary + evidence-backed release), doc's own definition fits exactly | authority value `QUARANTINED`→`DISPUTED` or `INVALIDATED` | settlement writers/readers repo-wide (not traced here — separate sweep needed) | settlements.authority column + CHECK + trigger. **Live count: 92 QUARANTINED rows per code comment probe — not in doc's Live Blast Radius section, needs own count before migration** |
| db.py | 6620-6626 | `_SOURCE_CONTRACT_AUDIT_STATUSES` incl "QUARANTINED" — source-contract audit status enum | B1/B2, cross-ref market_scanner.py T2 source-block target (KM-B agent) | — | needs market_scanner.py cross-check | — |
| db.py | 5951-5953,3376-3378 | schema-init wiring calls (`_ensure_decision_integrity_quarantine_table`, edli_fill_bridge_dispositions comment) | B1, mechanical, dies w/ owning module rename | — | — | — |
| cycle_runtime.py | 32,58-59,121,3896-3900,3927-3954,4001-4013 | `_quarantined_position_can_redecision` + support fns (`_chain_absent_quarantine_is_fresh_enough_for_redecision`,`_quarantine_target_date_still_actionable`,`_requires_quarantine_monitor_resolution`) — **CENTRAL mechanism of THIS branch (p2-pending-exit-restart-redecision)**. Doc line 61 cites this fn by name as proof quarantine facts are "already redecision-eligible." 5-condition gate: state==quarantined, chain_state in allow-list, direction valid, real exposure>0.01, not placeholder, (entry_authority_quarantined OR fresh timestamp) | B1 dies w/ T5 **but flag HIGH-RISK**: this IS the truth-resolution path the disease-definition requires already existing — must be PRESERVED and re-pointed to new state name, not deleted, when T5 executes | — | called from `_family_monitor_positions`,`_day0_hard_fact_position_eligible`,monitor-loop dispatch (below) | position_current.phase,chain_state,chain_verified_at,last_chain_absence_observed_at |
| cycle_runtime.py | 3844-3877 | `_apply_acknowledged_quarantine_clears`: T6 target, reads `deps.has_acknowledged_quarantine_clear`, writes suppression_reason="operator_quarantine_clear" on ack, appends to ignored_tokens | B1 dies w/ T6, must update SAME packet as control_plane.py ack machinery (KM-C) | — | control_plane.py (ack source, KM-C) | token_suppression |
| cycle_runtime.py | 102-109,2050-2060 | `_ORDER_OWNERSHIP_TERMINAL_POSITION_PHASES` = TERMINAL_STATES\|{"quarantined"}; log "quarantining instead of cancelling" at 2057 — **misleading log, no actual quarantine mint happens here**, just skips order cancellation | B1 (frozenset literal) dies w/ T5; B4 for log wording only | log→"leaving order in place — durable local ownership" | — | — |
| cycle_runtime.py | 3471-3513,3957-3964 | `_family_monitor_positions`/`_family_monitor_position_has_live_risk`, `_day0_hard_fact_position_eligible` — both fold in `_quarantined_position_can_redecision` as OR-branch | B1 dies w/ T5, re-point together w/ core fn above | — | — | — |
| cycle_runtime.py | 3993-3998,4016-4089 | `_canonical_monitor_position_rows`: `_CANONICAL_MONITOR_PHASE_PRIORITY{"quarantined":3}` + inline re-implementation of the SAME 3 gates as `_quarantined_position_can_redecision` (**duplicated logic, 2nd hardcoded copy — maintenance risk independent of excision, worth consolidating during reshape**) | B1 dies w/ T5 | consolidate w/ core predicate | — | — |
| cycle_runtime.py | 4900,5025-5048,5082-5087,5256,5313 | monitor-loop dispatch: terminal-skip guard excludes quarantined; `_requires_quarantine_monitor_resolution` routes to redecision OR admin-exit-mark-and-skip; `is_quarantine_placeholder` belt-and-suspenders skip (synthetic/incomplete position marker, comment explains cities_by_name lookup would fail); closed-market hard-fact hold-to-settlement explicitly SKIPS mutation when phase==quarantined | B1 dies w/ T5 (re-point all); `is_quarantine_placeholder` flag itself is B2 — rename→`is_unresolved_placeholder` | — | — | — |
| cycle_runtime.py | 3077-3078 | state→phase mapper: `if state=="quarantined": return LifecyclePhase.QUARANTINED.value` | B1 dies w/ T5 enum removal | — | — | — |

### Summary (KM-A only)
- Mechanism count: 21 distinct mechanisms across 4 files (2 fully read, 2 targeted-extraction).
- Buckets: B1=12, B2=9, B3=0 standalone (1 B3/B4 mixed), B4=1 (log text) + 1 (occurred_at CHECK, B3/B4 mixed).
- **Hardest reshape**: decision_integrity_quarantine.py certificate-revocation pair (lines 64-308) — two LIVE money-path callers (executor.py, command_recovery.py) each reimplement the read-check independently; reshape must consolidate to one shared checker AND re-point both in the same packet, per doc's own R1-b erratum #8 flag.
- **New finding not in T1-T7**: `settlements.authority` QUARANTINED state + monotonic trigger (db.py) — legitimate reject/evidence-release pattern, 92 live rows, needs its own blast-radius count and repo-wide settlement-writer sweep before migration.
- **Contradiction flagged**: chain_reconciliation.py `_materialize_chain_only_position_if_resolvable` (1531-1591) actively violates T5's stated target design (materializes Position rows for chain-only tokens that should only exist as ChainOnlyFact) — must be deleted, not renamed, when T5 executes.
- **Duplication flagged**: cycle_runtime.py has TWO independent hardcoded copies of the quarantined-position-redecision-eligibility gate (`_quarantined_position_can_redecision` vs inline logic in `_canonical_monitor_position_rows`) — consolidate during reshape.
