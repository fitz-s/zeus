# Quarantine excision census — TAIL4 (23 files, src/state + src/strategy + src/main.py + src/observability + src/ops + src/risk_allocator + src/riskguard)

Read-only classification per docs/rebuild/quarantine_excision_2026-07-11.md. No edits made.

## Corrections to task brief (verify against doc claims)

- **T3 line range**: doc says `src/riskguard/riskguard.py:352-446`. Actual mechanism
  `_load_riskguard_portfolio_truth` spans **352-454** (func body incl. return dict).
  `consistency_lock` computed at line **449**, past doc's stated end (446). Doc range
  undercuts by ~8 lines; core disease (excluded-rows-added-back → `"pass"`) is at
  427-449, still inside doc's cited span, just the closing brace is later.
- **T5 CHECK-constraint literal is NOT in `src/state/schema/v2_schema.py`.** Grepped
  full file (1608 lines): zero hits for `position_current`. The actual
  `position_current.phase` CHECK lives in **`src/state/db.py:5146-5150`**:
  `phase TEXT NOT NULL CHECK (phase IN ('pending_entry','active','day0_window','pending_exit','economically_closed','settled','voided','quarantined','admin_closed'))`.
  `chain_state` on the same table is **unconstrained TEXT** (no CHECK) — confirmed no
  hidden enum. v2_schema.py instead contains a *different, unrelated* mechanism: a
  recurring `authority IN ('VERIFIED','UNVERIFIED','QUARANTINED'[,...])` tri-state CHECK
  repeated across 5 tables (settlement_outcomes, ensemble_snapshots, calibration_pairs,
  a tigge/day0 fit table, an ICAO observation table) — this is the
  `ensemble_snapshot_provenance` boundary-rejection family the doc names separately
  under RESHAPE-AND-RENAME, not the T5 lifecycle-phase target. Task brief's file
  assignment for T5 was wrong; T5's real schema site is out-of-scope file `src/state/db.py`.

## Ledger

| File | Lines | Mechanism | Bucket | New name if B2 | Consumers | DB artifacts |
|---|---|---|---|---|---|---|
| src/state/AGENTS.md | 50-54 | Doc lists lifecycle phases, calls `quarantined` "Terminal:" — contradicts P0c code (chain_mirror_reconciler.py, projection.py) which explicitly widened the fold and treats it as still-open/drainable, not terminal. Same contradiction T7 names for zeus_current_architecture.md §8.2. | B4 (rewrite; also flags a live doc/code contradiction matching T7) | — | doc only | none |
| src/state/attribution_drift.py | 208 | Comment "Quarantine: unknown persisted strategy_key" over code that just returns `AttributionVerdict(kind="insufficient_signal", ...)`. No state minted, no exclusion beyond the return value. | B4 | — | none (comment) | none |
| src/state/canonical_write.py | 170-192 | `transition_phase()`: computes `redecision_quarantine_exit` (phase=="quarantined" AND chain_state=="entry_authority_quarantined" AND positive exposure) to allow a quarantined position to re-enter `pending_exit` instead of being blocked as terminal. This is the P2 redecision release-path (current branch topic), parallel to projection.py's copy of the same gate. | B1 dies with T5 (once T1-T4 stop minting + T5 retires the phase, no row ever reaches phase="quarantined" again — this branch becomes structurally unreachable) | — | caller: src/execution/exit_lifecycle.py:2843 `transition_phase(...)` | position_current.phase='quarantined', chain_state='entry_authority_quarantined' (free-text, no CHECK) |
| src/state/chain_mirror_reconciler.py | 34-42 (docstring), 66, 103, 801-805, 914, 1043, 1262-1265 | (a) `_OPEN_LIKE_PHASES` frozenset (L102-104) includes `"quarantined"` — reconciler treats quarantined rows as still-open and reclassifies them against chain truth every 10-min cycle, folding to `SETTLED` (via `enter_settled_runtime_state`, L801-805) or `VOIDED`/`CLOSED_EXITED` (via `_apply_closed_exited_finding`, L1035-1046) — this IS the T5-doc-cited drain backstop ("no row stays quarantined past one reconcile cycle", L1262-1265 docstring). Confirms chain_mirror_state_model_2026-07-04 claim: already folds quarantined→settled/voided. (b) remaining hits are comments referencing `LEGAL_LIFECYCLE_FOLDS[QUARANTINED]` and P0c history. | B1 dies with T5 for the `"quarantined"` literal in `_OPEN_LIKE_PHASES` and the fold branches that key off it (mechanically unreachable once T5 retires the phase — reconcile()/`_apply_settlement_finding`/`_apply_closed_exited_finding` themselves SURVIVE as the general chain-truth reconciler, only the vocabulary dies); B4 for docstring/comments | — | reconcile() called from run_cycle() (main.py registers 10-min job) | position_current.phase (reads/writes via 'quarantined' literal), chain_state='closed_exited' |
| src/state/chain_state.py | 43-49 | `_CHAIN_LOCAL_NON_ACTIVE_STATES` frozenset explicitly re-adds `LifecyclePhase.QUARANTINED.value` after it "dropped out of TERMINAL_STATES" (P0c) — treats quarantined as non-active for `classify_chain_state()`'s call site. | B1 dies with T5 | consumer: chain_state.py:122 `if pos_state in _CHAIN_LOCAL_NON_ACTIVE_STATES` (same file, `classify_chain_state`) | position_current.phase |
| src/state/collateral_ledger.py | 866-889 | `_has_active_ctf_exposure()`: SQL excludes phase IN ('settled','economically_closed','voided','quarantined','admin_closed') from "still needs CTF inventory" check. **Risk finding**: since quarantined rows CAN carry positive chain_shares (that's the whole premise of the P2 redecision path), excluding them here means the collateral cache can accept a stale/empty token-balance snapshot over a quarantined position's real chain exposure — potential real-fact-hidden pattern, worth flagging to the owning packet even though it's a downstream *consumer* of the phase literal, not a quarantine-minting site itself. | B1 dies with T5 (list simply drops the literal); flag the masking risk for the T5 packet to re-verify post-drain | consumer of `_has_active_ctf_exposure`: collateral_ledger.py:747 (`_load_latest_snapshot`) | position_current.phase, chain_shares/shares |
| src/state/db_writer_lock.py | 639-642, 672, 721, 788, 823, 827 | Allowlist registry comments (not executable branches): (a) L639-642, L788, L823 — historical write-lock-audit bookkeeping that used "QUARANTINED"/"STALE_REWRITE" as *audit review-status labels* (orthogonal meaning: "still needs write-guard resolution", unrelated to position/settlement quarantine). (b) L672 — describes `scripts/quarantine_invalid_live_actionable_certificates.py` writing `trade.decision_integrity_quarantine` (the R1-b decision_integrity_quarantine side-table named in the doc's RESHAPE-AND-RENAME list). (c) L721 — describes `scripts/drain_settlement_quarantine.py` writing `settlement_outcomes` authority (the v2_schema.py tri-state family). (d) L827 — "quarantine DDL + SAVEPOINT" for an era-flip migration script, unverified further. | B4 for (a); B1/B2 dies-with (b) decision_integrity_quarantine reshape, (c) settlement authority reshape — comments need rewriting when those scripts/tables rename; (d) unclassified without deeper script read (out of file scope) | — | scripts/ referenced (out of scope), trade.decision_integrity_quarantine, forecasts.settlement_outcomes.authority |
| src/state/domains.py | 69 | `CANONICAL_OWNER['decision_integrity_quarantine'] = Domain.TRADE` — table-ownership registry entry for the side-table the doc names explicitly ("R1-b erratum #8 found live callers... cannot be deleted blind... re-implement as first-class certificate/fact invalidation... callers re-pointed in same packet"). | B2 — rename dict key when table renamed (e.g. to `certificate_invalidation`/`fact_revocation`) | none (pure registry mapping, read wherever CANONICAL_OWNER is consulted — not grepped, out of scope) | trade.decision_integrity_quarantine |
| src/state/edge_observation.py | 160-164 | Comment "Quarantine: do not include outside the governed buckets" over an ordinary `if strategy not in per_strategy: continue` filter in edge-sum aggregation. No persisted state, no exclusion beyond this loop. | B4 | — | none (comment) | none |
| src/state/ledger.py | 88-115 | `_ensure_token_suppression_reason_schema()`: rebuilds `token_suppression` table if its CHECK doesn't yet include `'chain_only_quarantined'`; new CHECK is `suppression_reason IN ('operator_quarantine_clear','chain_only_quarantined','settled_position')`. `chain_only_quarantined` = T2's ChainOnlyFact scoped-block reason; `operator_quarantine_clear` = T6's control-plane ack literal. Both confirmed live via src/control/control_plane.py, src/engine/cycle_runtime.py, src/state/portfolio.py, src/execution/command_recovery.py, src/execution/harvester.py, src/state/chain_reconciliation.py. | B1/B2 dies-with T2 (scoped-block rename) + T6 (ack-lane rename); this file's migration helper needs its target CHECK literal updated in lockstep | consumers (cross-file, not this file): control_plane.py:505-591, cycle_runtime.py:3854-3860, portfolio.py:2511-2577/2743, chain_reconciliation.py:1324-1331, command_recovery.py:4175-4199, harvester.py:2597, db.py:11505-11696 (record_token_suppression/query_token_suppression_tokens) | trade.token_suppression.suppression_reason CHECK, trade.token_suppression_history (same CHECK, db.py:5850-5866) |
| src/state/ledger.py | 309 | Position-events rebuild migration copies rows `WHERE occurred_at LIKE '____-__-__T%' OR occurred_at = 'QUARANTINE'` — confirms T7's named target (kernel.sql `occurred_at` accepting literal `'QUARANTINE'`, a state word inside a timestamp column) has REAL historical rows with `occurred_at='QUARANTINE'` that this migration preserves during a table rebuild. | B1 dies with T7 (once kernel.sql shape is fixed, this OR-clause becomes either a one-time data-repair step or dead) | — | trade.position_events.occurred_at (historical literal string 'QUARANTINE') |
| src/state/market_topology_repo.py | 15-30 | `SOURCE_CONTRACT_STATUSES = frozenset({"MATCH","MISMATCH","UNKNOWN","QUARANTINED"})` — exact match for doc's named target "market_scanner source-contract mismatch: city-scoped source block with release evidence → rename to source-contract block". | B2 — rename `QUARANTINED` → e.g. `SOURCE_BLOCKED` (doc proposes `source_block` naming) | market_topology_repo.py:94 `if source_contract_status not in SOURCE_CONTRACT_STATUSES` (same file) | world.market_topology_current (or equivalent) `source_contract_status` column |
| src/state/projection.py | 257-266 | `_ABSORBING_POSITION_PHASES = TERMINAL_STATES ∪ {ECONOMICALLY_CLOSED, QUARANTINED}` — `upsert_position_current`'s reopen guard treats quarantined as absorbing (cannot silently reopen into active), consistent with chain-mirror-only drain. | B1 dies with T5 | projection.py:586,592,632 (same file, `upsert_position_current`) | position_current.phase |
| src/state/projection.py | 267-279, 548-564, 592-595 | `_REDECISION_QUARANTINE_CHAIN_STATES={"entry_authority_quarantined"}`, `_REDECISION_QUARANTINE_EXIT_EVENTS`, `_projection_allows_redecision_quarantine_pending_exit()` — the projection-layer twin of canonical_write.py's redecision gate: lets a quarantined+positive-exposure row re-enter `pending_exit` on EXIT_INTENT/EXIT_ORDER_* events. This is the core P2 pending-exit-restart-redecision mechanism (current branch). | B1 dies with T5 (unreachable once nothing mints phase='quarantined'; the redecision CAPABILITY itself is not disease, it's exactly the release path T5 depends on, but its vocabulary/gate dies with the phase) | upsert_position_current (projection.py:594); analogous separate impl in canonical_write.py:170-192; unrelated same-named `_REDECISION_QUARANTINE_CHAIN_STATES` also independently defined in cycle_runtime.py:58 pointing at `REDECISION_ELIGIBLE_QUARANTINE_CHAIN_STATES` from src/contracts/position_truth.py:55 (out of scope, cross-reference only) | position_current.phase, chain_state='entry_authority_quarantined' |
| src/state/schema/v2_schema.py | 97-110 | `settlement_outcomes.authority TEXT ... CHECK (authority IN ('VERIFIED','UNVERIFIED','QUARANTINED'))`. Default write in src/state/settlement_writers.py:197 is `authority=settlement.get("authority","QUARANTINED")` — unconfirmed settlement rows default to this state; drained via `scripts/drain_settlement_quarantine.py` (Gamma API confirmation, referenced db_writer_lock.py:721). Matches doc's boundary-rejection-with-scoped-release pattern (analogous to ensemble_snapshot_provenance). | B2 — rename `QUARANTINED` → e.g. `UNCONFIRMED`/`PENDING_VERIFICATION`; verify downstream reader behavior (out-of-scope files db.py/harvester.py/observation_instants_writer.py hint some readers "silently exclude" QUARANTINED rows — that reader behavior needs checking against disease def (c) before the rename packet ships) | src/state/db.py:3138 (trigger allowing QUARANTINED→VERIFIED transition), settlement_writers.py:197 | forecasts.settlement_outcomes.authority CHECK + default |
| src/state/schema/v2_schema.py | 133-140 | Comment: "Legacy/UNVERIFIED/QUARANTINED rows are not constrained [by W2 unit-check triggers]" — explains trigger scope, no new mechanism. | B4 | — | none | none |
| src/state/schema/v2_schema.py | 260-273 | `ensemble_snapshots.authority CHECK (authority IN ('VERIFIED','UNVERIFIED','QUARANTINED'))` — THE literal schema site for doc's named `ensemble_snapshot_provenance` target ("known-bad data-version rejection → boundary validation named as such, e.g. rejected_data_versions/is_admissible; reject-at-ingest semantics, no quarantine state"). | B2 — per doc's own proposed rename | out of file scope (ensemble_snapshot_provenance.py, harvester.py) | forecasts.ensemble_snapshots.authority CHECK |
| src/state/schema/v2_schema.py | 862-869 | `calibration_pairs.authority CHECK (... 'QUARANTINED')` — same tri-state family, different table. | B2 — same rename as above, applied consistently | out of file scope | forecasts.calibration_pairs.authority CHECK |
| src/state/schema/v2_schema.py | 1166-1173 | Tigge/day0 Platt-fit table `authority CHECK (... 'QUARANTINED')` — same tri-state family. | B2 — same rename | out of file scope | forecasts table (day0_horizon_platt_fits or sibling).authority CHECK |
| src/state/schema/v2_schema.py | 1303-1310 | ICAO observation table `authority CHECK (authority IN ('VERIFIED','UNVERIFIED','QUARANTINED','ICAO_STATION_NATIVE'))` — same family, extra 4th literal. | B2 — same rename, preserve ICAO_STATION_NATIVE | out of file scope | forecasts observation table.authority CHECK |
| src/state/schema/v2_schema.py | 1509-1519 | Comment (rescue_events design rationale) mentions "a quarantine placeholder whose track identity was never set" as one of 3 hypothetical failure modes the new table disambiguates. No code. | B4 | — | none | none |
| src/state/settlement_writers.py | 108-118 | Comment "Dispatcher must raise or quarantine; never write settlement row" — describes the ERA_DEAD fail-closed branch of `dispatch_era_basis()`; the actual behavior is `raise`/return ERA_DEAD, no quarantine call happens here. | B4 (comment overstates; code is plain fail-closed rejection) | — | none | none |
| src/state/settlement_writers.py | 197 | `authority=settlement.get("authority", "QUARANTINED")` default in `write_settlement_with_era_provenance._execute()` — see v2_schema.py:97-110 entry above, same mechanism. | B2 (same rename as settlement_outcomes.authority) | log_settlement (src/state/db.py, out of scope) | forecasts.settlement_outcomes.authority default value |
| src/state/venue_command_repo.py | 216-226 | `_POSITION_LOT_STATES` frozenset includes `"QUARANTINED"` as a legal `position_lots.state` value — whitelist for `append_position_lot()`. | B2 — rename to reflect "rolled-back/voided lot" semantics (e.g. `ROLLED_BACK`) | venue_command_repo.py:3170 (state validation in append_position_lot) | trade.position_lots.state CHECK (db.py:1483-1486, :5430-5433) |
| src/state/venue_command_repo.py | 330-359 | `_optimistic_source_trade_fact_ids_for_failed_trade()`: NOT EXISTS subquery excludes trade_fact_ids that already have a `position_lots.state='QUARANTINED'` rollback row for a FAILED trade — idempotency guard for the rollback below. | B2 — same rename as L216-226 (consumer of the literal) | called by `_rollback_optimistic_lots_for_failed_trade` (L362, same file) | trade.position_lots.state='QUARANTINED' |
| src/state/venue_command_repo.py | 3284-3334 | `rollback_optimistic_lot_for_failed_trade()`: when a previously OPTIMISTIC_EXPOSURE lot's underlying trade later fails, appends a new `position_lots` row with `state='QUARANTINED'` (append-only ledger, cannot mutate the original). Legitimate scoped local correction — the exposure genuinely didn't materialize; not a global freeze, not silent (the failed trade_fact_id is FAILED-state, traceable). Per canonical_projections.py comment, 0 live rows in current DB. | B2 RESHAPE-AND-RENAME — keep function, rename lot state literal to reflect "rollback of failed trade" (e.g. `ROLLED_BACK_FAILED_TRADE`) rather than `QUARANTINED` | src/ingest/polymarket_user_channel.py:1157, venue_command_repo.py:3141 (`_rollback_optimistic_lots_for_failed_trade`) | trade.position_lots (state, source_trade_fact_id) |
| src/state/ws_poll_reaction.py | 211-214 | Comment "Unknown strategy_key (legacy data) — quarantine" over plain `if strategy_key not in per_strategy: continue` skip in tick-latency aggregation. | B4 | — | none (comment) | none |
| src/strategy/family_exclusive_dedup.py | 197-240 | `_BLOCKING_EXPOSURE_PHASES` (lowercase phase set) includes `"quarantined"` — treated as live exposure that blocks a same-family re-entry (consistent with quarantined rows retaining real exposure). | B1 dies with T5 | family_exclusive_dedup.py:349,372,455 (same file) | position_current.phase |
| src/strategy/family_exclusive_dedup.py | 431-441 | `_TRADE_POSITION_BLOCKING_PHASES` (PositionPhase enum set) includes `PositionPhase.QUARANTINED.value` — same blocking-exposure semantics, sourced from canonical enum instead of literal set. | B1 dies with T5 | same call sites as above (byte-identical union) | position_current.phase |
| src/strategy/fill_up_wiring.py | 83-89 | Docstring: "A quarantine/void label is not proof the venue exposure is gone" — explains why `_live_position_phase_sql()` checks chain_shares in addition to phase. `_FILL_UP_BLOCKING_PHASES` set itself does NOT literally contain "quarantined" (checked, not present). | B4 | — | none (comment) | none |
| src/main.py | 5865-5868 | Comment on the `chain_mirror_reconcile` APScheduler job: "keeps position_current mirroring on-chain state so quarantined/stale rows do not accumulate forever." Describes chain_mirror_reconciler.py's drain backstop (see that file's entry above); no code here. | B4 | — | none (comment) | none |
| src/observability/status_summary.py | 53-62 | `_LEGACY_JSON_INACTIVE_POSITION_STATES = TERMINAL_STATES ∪ {"closed","exited","quarantined"}` for legacy JSON dashboard read. | B1 dies with T5 | status_summary.py:1107 | position_current.phase (legacy JSON mirror) |
| src/observability/status_summary.py | 80 | `_TERMINAL_ENTRY_PHASES = TERMINAL_STATES ∪ {"quarantined"}` for entry-command dedup display. | B1 dies with T5 | status_summary.py:872 | position_current.phase |
| src/ops/monitor_cadence.py | 22 | `NON_MONITOR_CHAIN_RISK_PHASES = frozenset({"quarantined","voided"})` — positions in these phases are excluded from the MONITOR_REFRESHED cadence-freshness check (read-only evidence collector, not a gate). | B1 dies with T5 | monitor_cadence.py:267 (same file) | position_current.phase |
| src/risk_allocator/governor.py | 36-44 | `ExposureState` Literal type alias includes `"QUARANTINED"` mirroring `position_lots.state` vocabulary; not branched on anywhere in this file (actual classification delegated to src.state.canonical_projections, out of scope). | B1 dies with venue_command_repo.py's B2 lot-state rename (type alias must track it) | none in this file (type-only) | trade.position_lots.state |
| src/riskguard/riskguard.py | 352-454 | **T3 target.** `_load_riskguard_portfolio_truth()`: an unparseable canonical row raises ValueError inside `_portfolio_position_from_loader_row`, caught and appended to a local `quarantined` list (excluded from `positions`), logged ERROR (L391-398). Then `canonical_known_count = len(positions)+len(quarantined)` is compared to `loader_position_count` and `consistency_lock` is set `"pass"` (not "degraded") whenever they match (L431-449) — i.e. a KNOWN exclusion is scored identically to a fully-healthy tick. Confirmed disease: `consistency_lock` value is **never actually consumed** anywhere downstream (grepped riskguard.py fully — only written into the returned dict, never read to gate risk_level; not even surfaced into `details_json` at L2654-2660) — so today it's not just "reports healthy," it's inert instrumentation that neither blocks nor even logs into the audit trail. Real exposure from the excluded row(s) vanishes from `PortfolioState.positions` and from all downstream risk math (Kelly sizing, exposure caps) with zero YELLOW/DATA_DEGRADED effect. | **B1 DIES with T3** — target form per doc: excluded row → `consistency_lock="degraded"` (never "pass"), feed into DATA_DEGRADED risk level | `_load_riskguard_portfolio_truth` called once, riskguard.py:2116 (`tick()`); tests: tests/test_riskguard.py `test_loader_quarantines_unloadable_row_instead_of_failing_whole_tick` (def at L1344, assertion `consistency_lock=="pass"` at L1411) and `test_loader_quarantine_logs_one_summary_for_multiple_bad_rows` (def at L1414) — **doc's cited test line numbers (:1327,:1388) are stale by ~15-25 lines**, same two tests confirmed present, same assertions | trade.position_current (read-only source), risk_state.db risk_state.details_json (consistency_lock NOT currently written here — gap) |
| src/riskguard/riskguard.py | 484-521 | `_active_position_equity_usd()` SQL fallback path (sqlite3.Error branch) excludes `phase in {"settled","voided","quarantined","admin_closed"}` from active-equity sum — same "quarantined = closed-enough" assumption as collateral_ledger.py, applied to the exception-fallback branch only (primary path uses raw SQL `phase IN ('active','day0_window','pending_exit')` which already excludes quarantined implicitly by omission). | B1 dies with T5 | same function, fallback branch only | position_current.phase |
| src/riskguard/riskguard.py | 1653, 2428-2457, 2591-2597 | `localized_orange_quarantine` — Brier-strategy-localization ORANGE-tier gate name; unrelated to position/settlement quarantine (a durable-gate severity label, "strategy quarantined behind a confirmed durable gate"). Distinct concept reusing the word. | B4 (rename term; functionally it's a durable-gate/kill-switch tier, not the disease pattern — no permanent-without-resolution behavior found: comment L1653 says "already quarantined behind a confirmed durable gate are removed" implying an existing removal path) | riskguard.py internal (brier_strategy_localization dict consumers, same file) | risk_state.db details_json key `localized_orange_quarantine` (bool) |

## Summary

Total distinct mechanisms/hits classified: **38** (table rows above; several files
contribute multiple distinct mechanisms, several rows are pure B4 comment noise).

Bucket counts (primary classification per row; a few rows are genuinely mixed —
db_writer_lock.py and ledger.py:88-115 carry both a B4 comment-noise component and a
B1/B2 live-artifact component, counted once under their dominant bucket):
- B1 (dies with a T-target): **16** — canonical_write.py:170-192, chain_mirror_reconciler.py (multi-hit), chain_state.py:43-49, collateral_ledger.py:866-889, ledger.py:88-115 (dies-with T2+T6), ledger.py:309 (dies-with T7), projection.py:257-266, projection.py:267-279/548-564/592-595, family_exclusive_dedup.py:197-240, family_exclusive_dedup.py:431-441, status_summary.py:53-62, status_summary.py:80, monitor_cadence.py:22, governor.py:36-44, riskguard.py:352-454 (**T3 core**), riskguard.py:484-521
- B2 (RESHAPE-AND-RENAME): **11** — domains.py:69, market_topology_repo.py:15-30 (SOURCE_CONTRACT_STATUSES), v2_schema.py authority tri-state ×4 tables (97-110, 260-273, 862-869, 1166-1173, 1303-1310) + settlement_writers.py:197 default (same family, 5 sites total but counted per schema site = 5 rows here), venue_command_repo.py position_lots QUARANTINED ×3 sites (216-226, 330-359, 3284-3334)
- B3 (dead code): **0** found in this file set — every hit has a live caller, live schema consumer, or describes live (if mischaracterized) behavior
- B4 (text-only): **11** — AGENTS.md:50-54, attribution_drift.py:208, edge_observation.py:160-164, db_writer_lock.py (audit-label comments, dominant), v2_schema.py:133-140, v2_schema.py:1509-1519, settlement_writers.py:108-118, ws_poll_reaction.py:211-214, fill_up_wiring.py:83-89, main.py:5865-5868, riskguard.py:1653/2428-2457/2591-2597 (`localized_orange_quarantine` — unrelated durable-gate term, not disease pattern)

No B3 (dead-code) hits in this file set — every quarantine reference in these 23 files
has at least one live caller or live schema consumer, or is a comment describing live
behavior (however stale/wrong the comment's characterization, e.g. AGENTS.md calling
quarantined "Terminal").
