Verdict: GO-WITH-CHANGES for Phase A shadow mode only; NO-GO for Phase B/C until the blockers below are fixed. Confidence: 0.82.

The three-layer shape is directionally right, but the current design is too clock-driven and too prompt-governed for a live-money repo. The superior shape is a hybrid heartbeat + event/cursor + verification-gated queue: launchd may wake the loop, but L2 should run on “new verified settlement/attribution rows since cursor” with a daily catch-up; L1 should run only when there is a safe queue item whose class, path, evidence cursor, and verifier contract pass machine gates; L0 should stay a read-only health sentinel. Zeus’s own repo law says decisions are cyclic and settlement is the final learning boundary, and the live machine re-evaluates continuously until settlement; that supports fresh sessions and different cadences, but not ungated hourly mutation pressure. GitHub+1

The smallest actionable recommendation: start Phase A only after making it technically read-only at the wrapper level. The current skeleton uses --permission-mode acceptEdits; that is inconsistent with a shadow phase that claims DECIDE/ACT are off. For Phase A, use a phase file plus shell/tool enforcement: no write tools except append-only loop/state/journal, no repo edits, no live DB write permission, no deploy/launchctl/network side effects, and a wall-clock timeout.

The strongest counterargument I considered is that hourly autonomous cleanup is useful because much of the queue is docs/registry/test noise, not settlement-dependent money logic. I agree with that for triage and proposal generation. I do not agree for autonomous merge unless the runner proves the touched path is outside the transitive runtime surface. Zeus’s deployment tooling itself treats src/, config/, scripts/, and deploy/launchd/ as runtime surfaces because daemon plists and operator flows execute from the live checkout. GitHub

[BLOCKER] phase-gating/security — loop/bin/tick.sh:launch skeleton §2/§11 — Phase A is described as observe-only, but the skeleton launches Claude with acceptEdits, so a prompt mistake or prompt injection can turn shadow mode into repo mutation — enforce PHASE=A in tick.sh, run with read-only tools/permission mode, write only to loop/state and journal, and fail closed if git diff --name-only contains anything outside that allowlist — verify locally: run a Phase A tick with an attempted edit to src/main.py and confirm the wrapper exits non-zero before Claude can persist the diff.

[BLOCKER] governance escape — loop/prompts/*.md / loop_design.md:§6/§9 — The C0–C4 autonomy ladder is currently prose, not a capability boundary; the loop can edit its own prompts under C2 and thereby weaken future interpretation of the ladder without changing the table text — move immutable law/class table/methodology into an operator-owned, hash-pinned constitution outside loop/**; allow the meta-loop to generate prompt-diff proposals only, and require operator signature or a separate verifier job that cannot write prompts.

[BLOCKER] indirect money-path mutation — loop_design.md:§6 C1/C2 — C1/C2 “non-money-path” auto-merge is under-classified because scripts, config, launchd plists, registries, and some generated artifacts can alter the next live boot or operator deploy even when no src/** money code changes — define money-path reachability as src/**, config/**, scripts/** executed by plists/operator flows, deploy/launchd/**, DB ownership/schema registries, AGENTS/governance that controls future agents, and any generated artifact consumed by live; demote all such paths to C3/C4 prepare-only unless a machine classifier proves non-consumption. Zeus’s root contract identifies live runtime entry points and canonical DB/proof surfaces, and its deploy tool gates runtime dirtiness across src/, config/, scripts/, and deploy/launchd/. GitHub+1

[BLOCKER] dead-code proof — loop_design.md:§6 C1 “已证零活引用” — Grep-zero caller plus tests can miss dynamic imports, CLI/launchd entrypoints, registry-loaded symbols, reflection, string-dispatch, and runtime-only side-effect callers, so “proven-dead” can delete code a daemon reaches only at boot or under exception recovery — require trace-first proof over live entrypoints, import graph, CodeGraph/runtime reachability, registry/script-manifest consumers, string-symbol search, and a boot/import smoke; money-path-adjacent deletions should be C3 prepare-only. This is not theoretical in this repo: the settlement attribution schema explicitly notes a prior “unregistered table crash-loops daemons” incident and says table registry absence can fatal at boot. GitHub

[HIGH] approach shape — loop_design.md:§1/§8 — A daily settlement clock is better than a pure while-true loop, but “hourly L1 improvement” still creates mutation pressure on stale evidence and “daily L2 at UTC 12:00” misses late, backfilled, or quarantined settlement arrivals — make launchd a heartbeat only; L2 advances from a durable settlement/attribution cursor, and L1 advances from queue depth plus evidence freshness, with “no new cursor/no safe queue item” as the default. The repo’s settlement tables encode authority='VERIFIED', settlement provenance, and settled_at, which are natural cursor surfaces. GitHub+1

[HIGH] budget/stop control — loop_design.md:§2/§4 — flock, --max-turns, a daily token file, and 3-strikes prevent some overlap and cost blowups but do not bound wall-clock hangs, subprocess loops, unbounded grep/test runs, runaway logs, or huge diffs — wrap each tick in a process-group timeout, ulimit, max log bytes, max subprocess duration, max diff files/lines, branch TTL, and post-run kill of child process group. Anthropic documents --max-turns as an agentic-turn limit, and its Claude Code GitHub Actions guidance separately recommends workflow-level timeouts to avoid runaway jobs. Claude Platform Docs+1

[HIGH] live-DB boundary — loop_design.md:§4/§5 — The loop needs to read settlement/trade truth, but the current design does not state a hard DB read-only mechanism; relying on prompt text is unsafe around scripts that default-write or require apply flags — run evidence queries through a narrow read-only adapter using SQLite mode=ro/immutable where possible, deny raw sqlite3.connect writes by wrapper policy, and block all scripts with manifest dangerous_if_run, target_db, or write targets unless explicitly whitelisted read-only. Zeus’s own settlement-graded EV script models the right posture: live DBs read-only, no venue calls, no daemon restart, no live DB writes. GitHub+1

[HIGH] hypothesis ledger epistemology — loop/state/hypothesis_ledger.yaml §3/§5 — min_n=30 per hypothesis is not enough when many hypotheses remain open, cells are repeatedly sliced, and the agent can choose when to test/stop; this permits p-hacking, optional stopping, and family-wise false discovery even while each individual claim satisfies N — add hypothesis_family, preregistered query/window, frozen decision rule, effect size with CI, multiple-comparison control/FDR or alpha-spending, negative controls, and a “no promotion from exploratory slice” rule.

[HIGH] mechanism/statistical bifurcation — loop_design.md:§5 — “Mechanistic finding can act at any n” is correct only for invariant-preserving code proofs; it can be abused to label economic behavior as mechanistic and bypass settlement evidence — require mechanistic claims to cite exact executable path, invariant violated, counterexample/test, and proof that the fix does not change risk posture or admission economics; anything about EV, calibration, city selection, sizing, or market behavior remains statistical unless it is a pure safety fail-closed bug.

[HIGH] ledger lifecycle — loop/state/hypothesis_ledger.yaml §3/§8 — “Never delete refuted” preserves institutional memory, but without expiry/supersession it will bloat prompts and turn old regime-specific refutations into false law — keep an append-only raw event log, but materialize a compact active view with regime_key, supersedes, superseded_by, expires_at, last_revalidated_at, and prompt_load=false for cold refutations; stale confirmed claims should automatically downgrade to needs_revalidation after code/config/source-regime changes.

[HIGH] fresh-session memory spine — loop/state/* §3 — Fresh sessions avoid context rot, but the proposed disk spine is too thin for safe multi-day work: it lacks typed cursors, branch inventory, verifier failure reasons, outstanding reverts, queue item attempt history, touched-path class decisions, and “why skipped” records — add tick_cursor.yaml, settlement_cursor.yaml, branches.yaml, verification_failures.yaml, class_decisions.jsonl, revert_registry.yaml, and a schema/lint gate before every tick.

[HIGH] diff circuit breaker — loop_design.md:§4/§6 — Single-commit atomicity is not a blast-radius limit; a single commit can modify a large registry, prompt, config, or script surface and indirectly change live behavior — enforce max changed files, max added/deleted lines, forbidden glob list, no binary/generated artifact edits, no config/risk/launchd/script-manifest edits under auto-merge, and automatic C3 demotion when the diff touches runtime/admission/probability/governance surfaces.

[MEDIUM] concurrency/ordering — loop/bin/tick.sh §2 — flock -n prevents overlapping ticks of the same layer, but it does not order L0/L1/L2 around shared state files or settlement cursor updates; L1 can read a half-written L2 ledger/queue update — write state through atomic temp-file + rename, maintain per-file schema versions, and use a shared state lock for ledger/queue/cursor writes, not only per-layer locks.

[MEDIUM] rollback/migration — loop_design.md:§6/§9 — “single commit revert” is insufficient for prompt changes, registry moves, archived docs, generated files, and branch/worktree state because the live daemon/operator may consume changed files between merge and revert — require pre-merge rollback notes that include consumer restart/reload behavior, artifact regeneration/undo steps, and whether reverting source is enough or whether generated/runtime files must be restored.

[MEDIUM] L0 sentinel authority ambiguity — loop_design.md:§1/§3 — L0 says “zero analysis zero modification,” but the design also allows the loop to touch LOOP_HALT in disasters; that is an unbounded actuation authority unless mechanically scoped — define exact mechanical halt predicates, e.g. forbidden write detected, live DB write attempt, deploy command attempt, budget corruption, or stale-heartbeat threshold; otherwise L0 only appends digest/escalation.

[MEDIUM] verifier independence — loop_design.md:§7 — “fresh context” is not enough when the verifier reads the same lead-written done-claim and same stale ledger; it can inherit framing and rubber-stamp path classification — verifier input should be machine-generated from diff, touched paths, class decision, tests run, and forbidden-surface classifier, with no prose conclusion from the builder except the claimed acceptance criteria.

[MEDIUM] settlement/event boundary cases — loop_design.md:§8 — Daily settlement analysis assumes settlement truth arrives cleanly once per day, but this repo has quarantine drains, source/payment disagreements, verified-unit triggers, and attribution categories for stale/unattributable decisions — L2 must handle late verified rows, revised outcomes, quarantine-to-verified transitions, duplicate idempotent rows, and attribution category rebuilds as first-class cursor events. The settlement schema and quarantine-drain manifest show verified authority, quarantine handling, and fail-closed settlement repair semantics. GitHub+1

[MEDIUM] premortem gap — loop_design.md:§10 — The premortem misses “docs/governance-only” changes that alter future agent behavior and thereby future money-path commits; AGENTS and prompts are not runtime code, but they are control-plane code for the loop — classify prompt, AGENTS, skills, class tables, route manifests, and review doctrine as governance C3/C4 unless operator-approved.

[MEDIUM] premortem gap — loop_design.md:§10 — The premortem misses config-template/LaunchAgent/environment drift: changing a plist, env var, or launchd working directory can point live to a different checkout or alter boot behavior without touching Python money code — forbid autonomous edits to deploy/launchd/**, ~/Library/LaunchAgents/**, and any file that influences WorkingDirectory, env, interpreter, or launch labels; require operator-only C4 proposals.

[MEDIUM] premortem gap — loop_design.md:§10 — The premortem misses registry YAML as executable law: a registry row can cause boot failure, route future agents away from live code, or make dangerous scripts appear safe — treat architecture/** registries and DB ownership manifests as runtime-governance surfaces, not C1 cleanup, unless a verifier proves they are not consumed by boot, preflight, or agent routing. Root AGENTS says unregistered files are invisible to future agents and table ownership is machine-checked. GitHub

[LOW] reporting/ops visibility — loop_design.md:§4/§10 — “No direct messages; morning report only” is fine for normal output but weak for emergency stops and loop death — add an external heartbeat/alert contract outside the loop repo: last successful tick, last attempted tick, last skipped reason, and emergency halt reason.

[LOW] source reproducibility — supplied pinned source URL — The commit-pinned URL 293f06b159b8 returned 404 in my web session, so I could not verify the exact pinned snapshot and used the public main tree plus raw source where available — before acting on source-line-specific findings, confirm the target checkout and commit exist locally — verify locally: git cat-file -e 293f06b159b8^{commit} && git checkout 293f06b159b8.

Concrete Phase A gate before launch: change tick.sh so Phase A cannot call ACT by construction, not by prompt. Use a separate OS user or filesystem ACL that cannot write live DBs or runtime surfaces; use read-only Claude permissions/tools; add timeout --kill-after or macOS gtimeout around the whole Claude process; atomic-write only loop/state/journal; postflight assert clean git diff outside loop state; and record settlement cursor observations without updating hypotheses to confirmed/refuted.

My answer on the approach: keep the three named layers, but change their semantics. L0 remains periodic and read-only. L2 becomes settlement-event/cursor-driven with daily catch-up. L1 becomes queue-depth and verification-gated, not “hourly single improvement” in spirit. A fresh session per tick is good, but only if the disk spine becomes a typed state machine rather than a few markdown/YAML files.

Sources used: the supplied inline design; public GitHub fitz-s/zeus main tree because the pinned URL failed; repo README/domain overview; root AGENTS.md; scripts/deploy_live.py; scripts/check_live_restart_preflight.py; src/state/schema/v2_schema.py; src/state/schema/settlement_attribution_schema.py; src/analysis/settlement_skill_attribution.py; src/decision/qlcb_reliability_guard.py; src/decision/city_skill_gate.py; src/decision/selection_calibrator.py; scripts/qkernel_settlement_graded_ev.py; architecture/script_manifest.yaml; Anthropic Claude Code CLI/GitHub Actions docs for --max-turns and timeouts. I could not read the commit-pinned tree at 293f06b159b8; if that commit differs materially from main, re-run only the source-line-sensitive parts of this review.

Load-bearing assumptions: live daemons/operator deploys use the same checkout or a checkout updated from autonomous merges; no external sandbox/path allowlist exists beyond the design text; Phase A would otherwise run the shown acceptEdits skeleton; and the loop’s state files are new, not already backed by a stricter implementation. If there is already an out-of-band sandbox that makes src/, config/, scripts/, deploy/launchd/, live DBs, launchctl, and prompt-governance files unwritable to the loop, the blockers downgrade but the event/cursor-gating and ledger fixes still stand.