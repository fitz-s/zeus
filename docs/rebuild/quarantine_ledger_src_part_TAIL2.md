# Quarantine Ledger — src/data tail-2 batch (13 files)

Read-only census per `docs/rebuild/quarantine_excision_2026-07-11.md`. No edits made.

| File | Lines | Mechanism | Bucket | New name if B2 | Consumers | DB artifacts |
|---|---|---|---|---|---|---|
| anchor_cross_check.py | 296, 376 | log text "city STAYS quarantined" — city fails receipt-verify vs run-pinned API, falls through to lower-fidelity serve rungs (1-2). No stored quarantine state; positive whitelist (`state/anchor_cross_check.json`) is the real mechanism. | B4 | wording: "city remains unwhitelisted, falls through to rungs 1-2" | same-file `resolve_bucket_serve_method`/`city_is_bucket_whitelisted`; `src/data/replacement_forecast_production.py` calls the cross-check cycle fns | none (file receipt JSON, not DB) |
| bayes_precision_fusion_history_provider.py | 20, 139 | comment-only: SQL WHERE `s.authority='VERIFIED'` excludes UNVERIFIED/QUARANTINED settlement rows from walk-forward train (no-leak provenance gate). Boundary READ filter, mints nothing. | B1 | consumer of settlement `authority='QUARANTINED'` minted by `src/execution/harvester.py:1476-1559` + `src/ingest/harvester_truth_writer.py:449,562` (outside this file set — likely a separate T8 target, not T1-T7 named) | `src/data/replacement_forecast_materializer.py`, `bayes_precision_fusion_capture.py`, `bayes_precision_fusion_download.py`, `src/engine/event_reactor_adapter.py` | zeus-forecasts.db `settlement_outcomes.authority` (CHECK incl. `'QUARANTINED'`, `src/state/db.py:1739,1802,3633,3138`) |
| hourly_instants_append.py | 129, 161, 164-165 | `quarantine_count` counts unparseable per-hour timestamps in one fetch chunk; raises `ValueError("Quarantine threshold exceeded...")` if >10% of the chunk fails to parse. Scoped to this fetch call, no persisted state, self-heals on retry. | B2 | `unparseable_count` / `"Unparseable-timestamp threshold exceeded for {city}: {n} failures"` | `_fetch_with_retry` (same file, wraps as RuntimeError); `scripts/backfill_hourly_openmeteo.py` (duplicate fetch layer, same pattern) | none (in-memory counter) |
| ingest_status_writer.py | 76-106, 191, 198 | `_last_quarantine_reason` queries `availability_fact.failure_type LIKE '%quarantine%' OR ='QUARANTINED'` — **dead branch**: no writer in src ever sets that literal (`log_availability_fact` callers use `UnitConsistencyViolation`/`UnknownCityViolation`/etc, confirmed by grep). Live fallback queries `data_coverage.status='FAILED'`. Surfaces (does not hide) fact — not disease itself, but named/shaped around a state nothing mints. | B2 | fn `_last_ingest_failure_reason`; JSON key `last_ingest_failure_reason` | `write_ingest_status` (same file; feeds `state/ingest_status.json`) → `src/ingest_main.py`, `tests/test_ingest_status_v2_rollup.py` | zeus-world.db `availability_fact.failure_type` (read-only, no literal ever written here) |
| ingestion_guard.py | 589, 596, 613, 650, 608-671 (`ProvenanceGuard` class) | `_VALID_AUTHORITIES = {VERIFIED, UNVERIFIED, QUARANTINED}`; `ProvenanceGuard.validate_write()` validates authority is one of the three. **Zero production callers** (`grep -rn "ProvenanceGuard("` outside this file hits nothing in src/scripts). | B3 | — | only `tests/test_ingest_provenance_contract.py` | none (class never wired to a real write path) |
| openmeteo_ecmwf_ifs9_bucket_transport.py | 583 | docstring: "verified by neither stays quarantined (None)" describing `resolve_bucket_serve_method` fallthrough (same whitelist family as anchor_cross_check.py). No stored state. | B4 | wording: "verified by neither serves via lower rungs (None)" | `src/data/anchor_cross_check.py`, `src/data/replacement_forecast_production.py` | none |
| polymarket_client.py | 1046 | `logger.warning("Quarantining token %s due to malformed metrics...")` — one malformed API position item is `continue`-skipped from the returned list. Per-call, no persisted state, self-heals every fetch. | B4 | log text: "Skipping malformed position token %s" | `get_positions_from_api` consumed by `src/runtime/bankroll_provider.py`, `src/state/chain_mirror_reconciler.py`, `src/engine/cycle_runtime.py` | none |
| replacement_cycle_advance_trigger.py | 251 | SQL literal `COALESCE(phase,'')='quarantined'` — reads `position_current.phase` enum to include quarantined-but-chain-confirmed families in "held" set for re-materialization priority. | B1 | consumer of T5 (QUARANTINED lifecycle phase retirement, `src/state/lifecycle_manager.py`) | `_held_position_families` used at line 625 → `src/events/reactor.py`, `src/data/replacement_forecast_production.py` | zeus_trades.db `position_current.phase` CHECK (enum member `'quarantined'`) |
| replacement_forecast_live_materialization_queue.py | 182-193 (`_quarantine_stale_lock`), 184 (`quarantined_stale_locks/` dir), 196-235 (`_queue_lock` caller at 220) | Orphaned lock-file self-heal: on `FileExistsError`, probes recorded holder PID; if dead/unparseable, moves the lock file into `quarantined_stale_locks/` (audit trail, never silent-delete) and steals the lock. **This is the mission-doc-flagged materialization-queue orphaned-lock move.** | B2 | fn `_archive_orphaned_lock`; dir `orphaned_lock_archive/` | only caller `_queue_lock` (same file, line 220); `tests/test_materialization_queue_stale_lock.py:57` asserts dir name literally `quarantined_stale_locks` — must update in same packet | none (filesystem dir move, not DB) |
| replacement_forecast_refit_gate.py | 22 | string literal `"emos_cells_product_keyed_or_quarantined"` — one required-evidence-flag NAME in `REQUIRED_REFIT_EVIDENCE` tuple; membership-only check (`_missing()`), no behavior tied to the word itself. | B4 | literal: `"emos_cells_product_keyed_or_excluded"` | `REQUIRED_REFIT_EVIDENCE` used at line 104 (same file) and passed through `src/data/replacement_forecast_refit_handoff.py:257`; `tests/test_replacement_forecast_refit_gate.py` | none |
| source_time.py | 24 (docstring), 116 (`LateArrivalPolicy.QUARANTINE = "quarantine"`) | Enum member declared as one of 5 late-arrival dispositions ("hold out of the live chain"). **Unwired**: `default_late_arrival_for()` never returns it; no consumer reads `.QUARANTINE` specifically anywhere in src/tests (only generic full-enum iteration in `tests/test_source_temporal_policy.py:120`). Declared for a future SourceContract layer per module docstring. | B2 (latent/unused — rename now, no live consumer to re-point) | `HOLD_FOR_REVIEW` or `DEFER_LIVE_ADMISSION` | `TemporalPolicy.late_arrival_policy` type slot (line 177, always `None` today); `tests/test_source_temporal_policy.py` iterates full enum | none |
| substrate_observer.py | 781 | SQL literal `phase IN ('quarantined', 'voided')` — includes quarantined chain-confirmed positions in EDLI live-refresh scope (same pattern as replacement_cycle_advance_trigger.py). | B1 | consumer of T5 (QUARANTINED lifecycle phase retirement) | `_edli_current_held_position_scope_rows` → `tests/money_path/test_edli_market_substrate_warm_cycle.py` | zeus_trades.db `position_current.phase` CHECK (enum member `'quarantined'`) |
| v2_city_floors.json (data file, not code) | 452 | `_workstream_a_resync` field (underscore-prefixed metadata annotation) contains historical prose: "...747,150 LFPG QUARANTINED pairs deleted...". Describes a completed 2026-05-03 one-time data cleanup event. Zero code consumers (grep confirms only the JSON file itself matches). | B4 | prose: "...747,150 LFPG excluded/invalid pairs deleted..." | none | none (JSON annotation field, no schema role) |

## Totals

- Distinct mechanisms classified: **13** (one per file; anchor_cross_check.py's two log sites and bucket_transport's docstring counted as one mechanism each since they're the same whitelist-fallthrough pattern repeated).
- Bucket counts: **B1 = 3** (bayes_precision_fusion_history_provider.py, replacement_cycle_advance_trigger.py, substrate_observer.py — all consumers of quarantine state minted elsewhere: settlement authority in harvester.py/harvester_truth_writer.py, and T5 lifecycle phase in lifecycle_manager.py), **B2 = 4** (hourly_instants_append.py, ingest_status_writer.py, replacement_forecast_live_materialization_queue.py, source_time.py), **B3 = 1** (ingestion_guard.py ProvenanceGuard — dead code, zero production callers), **B4 = 5** (anchor_cross_check.py, openmeteo_ecmwf_ifs9_bucket_transport.py, polymarket_client.py, replacement_forecast_refit_gate.py, v2_city_floors.json).
- Files with zero real hits: none — all 13 files had genuine quarantine-word hits (unlike a typical "no hits" file).
