# live_entry_health_repair -- Live repair packet

Date: 2026-07-10
Branch: `agent/runtime-throughput-first-principles`
Status: active

## Objective

Restore truthful live entry admission after the global auction reached a real winner. Repair only current, independently proven defects; never weaken live-health, forecast freshness, probability identity, exit lifecycle, or venue gates.

## Current truth

- Live PID 98439 is loaded at `16cec04f6`; entries are not paused.
- Chain-mirror repair succeeded: `6be10bfa-f2f` is canonical `voided/closed_exited`.
- Reactor reached winner claim, then selected winner identity `e664824…`.
- Entry authority currently fails on `entry_q_version`, `forecast_event_bridge`, and `pending_exit_release_loop`.
- A reactor cycle started at 2026-07-10T17:32:26Z and did not return through three following scheduled intervals, which were skipped by `max_instances_reached`.
- No new venue command, submit, ACK, or fill exists.

## Authority and boundaries

- Truth order: Chain/CLOB > canonical DB/events > current health receipt > logs/projections.
- Zone: K0/K2 live state repair plus K1 protective-health diagnosis.
- Invariants: INV-03 append-first authority, INV-06 point-in-time truth, INV-08 single transaction boundary, INV-29 command identity grammar.
- Required reads: root/scoped AGENTS, current architecture/delivery law, execution lifecycle reference, script manifest, `repair_active_entry_q_versions.py`, `src.control.live_health`, and exact live DB/health rows.
- Forbidden: DB backup/copy, venue submit/cancel from a repair, manual SQL, q recomputation from hindsight, health-gate bypass, stale posterior acceptance, lifecycle phase rewrite, schema change.
- Code files are not yet authorized to change. If diagnosis proves a code defect, amend scope and planning evidence before editing.

## Slice A -- Active ENTRY q_version

- Dry-run found exactly one active missing row: Seoul position `656594aa-af2`, command `d671dcc6142a4152`.
- Existing FinalIntentCertificate reconstructs exactly one posterior identity: `5aa5d7d02c36827aa1f6d11fb5da81ad79d02f855f49aab4156e871a3cea5597`.
- Apply may update only that command's empty `venue_commands.q_version` through the registered repair script.
- Acceptance: applied_count=1, subsequent dry-run candidate_count=0, no share/phase/state change, no venue action, no backup.
- Executed evidence: apply reported `active_missing_count=1`, `candidate_count=1`, `applied_count=1`, `blocked=0`, `db_backup_created=false`, `venue_action=false`; the post-apply dry-run reported `active_missing_count=0`, `candidate_count=0`.
- Canonical re-read: command remains `FILLED`, position remains `day0_window/synced` with `shares=chain_shares=18.44`; only its empty `q_version` became the uniquely reconstructed certificate hash. No new `venue_commands` row was created.

## Slice B -- Remaining blockers

1. Determine whether `forecast_event_bridge` is producer lag, wrong identity routing, or health-probe mismatch. No stale posterior may become executable.
2. Determine whether Seoul's `pending_exit_projection_regression` is a real illegal phase regression or a health query that failed to recognize canonical `EXIT_RETRY_RELEASED` evidence.
3. Locate the stuck reactor call from current process/log/DB evidence; distinguish bounded slow solve from deadlock or unbounded queue work.
4. Any repair must be one minimal independently reviewed slice; otherwise remain fail-closed with an exact blocker.

## Slice B2 -- Released exit projection health

- Canonical sequence for Seoul `656594aa-af2`: `EXIT_ORDER_REJECTED` seq 663 left `pending_exit`; `EXIT_RETRY_RELEASED` seq 666 legally transitioned `pending_exit -> day0_window`; held `MONITOR_REFRESHED` starts at seq 667.
- Defect: the projection-regression query selects the latest event only from `EXIT_INTENT`, `EXIT_ORDER_REJECTED`, and `EXIT_ORDER_POSTED`. It excludes `EXIT_RETRY_RELEASED`, so every legal held monitor after release is compared against the older rejection and mislabeled as a regression.
- Minimal repair: make release part of the latest exit-transition ordering and exclude a position when that latest transition is `EXIT_RETRY_RELEASED`. A new exit intent/rejection after release remains blocking because it becomes latest again.
- Forbidden: change position phase, suppress a genuine held projection after an unreleased exit, weaken any exit submit/runtime gate, or mutate live DB state.
- Acceptance: unreleased rejection + held projection still fails; release + held projection clears; current Seoul false positive disappears after deploy.
- Verification: full `tests/test_run_mode_failure_surfaces.py` passes 86/86. The antibody proves release -> held clears and release -> newer intent/rejection -> held fails again. Independent SQL review found no P0/P1 and judged the slice deploy-safe.

## Slice B3 -- Forecast bridge family identity

- Live proof after B2: the latest FSR names Taipei `2026-07-12/high` identity `0a8e736...`, whose matching and latest same-family posterior both computed at `17:49:44Z`. Health compares it to the global max `18:07:16Z` from unrelated Buenos Aires/Shenzhen families and reports a false `latest_newer_by=1052s`.
- Defect: posterior supersession is meaningful only inside the same probability authority identity family. A posterior for another city/date/metric cannot supersede the FSR's causal posterior.
- Minimal repair: preserve the global posterior-to-event timing bridge, but compute identity supersession against the latest live posterior for the same `product_id × city × target_date × metric`, using the same source-cycle/computed ordering as FSR selection.
- Forbidden: accept a superseded identity inside the same family, remove global bridge timing, weaken posterior/FSR age budgets, or change event production.
- Acceptance: same-family newer posterior remains blocking; different-family newer posterior does not supersede; current live false positive clears after deploy.
- Verification: bridge tests pass 7/7 and full live-health file passes 87/87. A read-only HEAD evaluation on current DB resolves the latest event identity to the exact same-family latest posterior with lag `0.0s` while preserving global posterior-to-FSR timing. Independent review found no P0/P1 and judged the slice deploy-safe.
- Carry-forward P2: the health family query additionally scopes by `source_id`, while producer supersession currently relies on one canonical source per replacement product. This is true in current materialized live rows; if a product later admits multiple live source IDs, producer and health need one shared executable family-key contract.
- Post-deploy review erratum: an adversarial counterexample proved the identity-match success return skipped the pre-existing global producer-stall condition. Current runtime was not stalled (`latest FSR created after global latest posterior`), but the shape could false-green if an unrelated-family posterior became globally newest and no later FSR existed. Corrective slice B3.1 is mandatory immediately.

## Slice B3.1 -- Preserve global producer stall under identity match

- Required conjunction: identity health has two independent axes. Same-family latest ordering proves the FSR's causal identity is not superseded; global posterior/event ordering proves the producer bridge has not stopped altogether.
- Minimal repair: evaluate `posterior newer than latest FSR by > budget AND posterior age > budget` before an identity match can return healthy. Keep same-family supersession and payload-age checks unchanged.
- Acceptance: unrelated-family newer posterior plus a newer FSR remains healthy; unrelated-family posterior with no later FSR becomes `FORECAST_TO_EVENT_BRIDGE_STALLED`; same-family newer posterior remains `IDENTITY_SUPERSEDED`.
- Verification: bridge tests pass 8/8 and full live-health file passes 88/88. Independent re-review confirms the original P1 is closed: identity and non-identity branches now share the global stall predicate, and no healthy return precedes it.

## Slice B1 -- Post-trade leaked-thread recovery

- Current evidence: the collateral heartbeat succeeded through 18:33:17 local; from 18:34:35 onward every 30-second attempt exceeded its 25-second deadline and logged `thread leaked, daemon should restart`.
- The daemon wrapper currently records FAILED and returns, so the next cadence leaks another thread. Its concurrent chain-sync job has also remained in-flight since 18:33:40, holding downstream DB users behind it.
- Rejected design: terminating the whole sidecar on collateral timeout could interrupt a concurrent Safe WRAP/APPROVE after the chain transaction but before its local receipt commit. It also could not distinguish a truly leaked deadline thread from an inner `TimeoutError`. This design was not deployed.
- Change boundary: `src/ingest/post_trade_capital_daemon.py` only. Run the existing pUSD-only refresh in a one-shot child interpreter with an outer deadline. A hang is killed and reaped at the child boundary; wrap/redeem/harvester jobs remain alive in the parent.
- Safety basis: collateral refresh performs venue balance/allowance reads plus a short SQLite snapshot write; it submits no external transaction. Killing its child can roll back an in-flight DB write but cannot create an external-action/local-receipt tear.
- Forbidden: change collateral truth, extend stale acceptance, terminate the parent sidecar, restart the order daemon from inside the sidecar, or alter chain-sync/venue logic.
- Test: child command/deadline is exact, timeout becomes FAILED scheduler health without parent exit, nonzero child outcome is not false-green, and existing success behavior remains.
- Verification: P4/collateral suite passes 24/24; a real sleeping child is killed and reaped by the outer deadline. Independent adversarial re-review found no P0/P1 and ruled the slice `DEPLOY-SAFE`.
- Carry-forward P2: a custom collateral deadline can make `deadline+2s` exceed the 30-second cadence. The default is 27 seconds and `max_instances=1` prevents overlap; deploy observation must verify no skip/restart loop, and a later bounded-config contract may make the relationship unconstructable.

## Verification

- Re-run current health composite after every mutation.
- Re-sample loaded SHA/PID, open positions, q identity, posterior/FSR identity, reactor completion cadence, and venue command/event counts.
- Actual order proof requires separate `venue_commands`, submit event, venue ACK/order ID, fill/trade fact, and capital change lines. A candidate or health-clear signal is not an order.

## Slice B4 -- Bounded live working-set reads

- Current runtime proof: the reactor run started at `18:46:50Z`, completed `pending_prune` at `18:46:57Z`, and then emitted no `forecast_snapshot_build` completion for more than ten minutes. That stage spans `_edli_pending_entity_keys` plus the forecast builder, so the log anchors alone do not isolate one call. The pending-key query had no SQLite progress deadline and its plan allowed an unbounded status scan, per-row event PK lookup, and temporary DISTINCT tree. `sqlite_stat1` was stale (2,520,044 estimated rows); a later exact read found 10,801,165 processing rows but only 1,018 pending and 12 processing. Hot read-only timing was 179ms for the old query and 94ms for the bounded query; this is a structural I/O amplifier, not proven as the sole ten-minute root. The separately budgeted forecast builder was 52ms hot after recovery.
- Current exit-monitor proof: the run started at `18:48:20Z` and did not reach the cadence-watchdog result until about `18:56:20Z`. The watchdog's `MAX(occurred_at) WHERE event_type='MONITOR_REFRESHED'` has no event-type-leading index and scans the 85.9GB trade DB. It had not entered `PolymarketClient`; this is not venue latency.
- Minimal reactor repair: read only a bounded newest active processing working set per valid active status using the existing `(consumer_name, processing_status, updated_at)` index, then join event provenance and deduplicate entity keys. Apply a SQLite deadline to this pre-build read and restore connection handlers/timeouts on every path. The cap is the existing reactor prune batch limit; no new trading threshold is introduced.
- Minimal monitor repair: enumerate canonical current non-terminal positions, then use the existing `(position_id, event_type, sequence_no DESC)` index to read each position's latest `MONITOR_REFRESHED`. This preserves event-proof semantics and removes the unindexed global event scan; do not replace it with projection `updated_at`.
- Forbidden: add a schema/index migration, delete/archive rows, weaken event freshness or probability gates, bypass global auction revalidation, change order eligibility, or perform venue actions.
- Acceptance: query plans are bounded/indexed; a large irrelevant event history does not change the result or runtime shape; the current 1,030 active processing rows are all below the configured 5,000-per-status bound; active-position latest monitor is detected; closed-only historical monitor rows cannot false-green the watchdog; existing busy timeout/progress handlers are restored; full affected suites pass; independent live-money critic finds no P0/P1.
- Verification: affected suites pass 85/85 twice; target modules compile; direct current-DB read-only timing was 94ms for the bounded pending-key read and 1ms for the indexed watchdog; live `EXPLAIN` proves MATERIALIZED bounded active IDs -> event PK lookups and current-position phase index -> position/event/sequence lookups. Independent review found no P0/P1 and returned `DEPLOY-SAFE WITH P2 FOLLOW-UPS`.
- Carry-forward P2: the per-status cap is applied before event-type filtering, so a future active set above 5,000/status can omit older FSR keys when newer other-type rows dominate and cause bounded queue churn; current active rows are below the cap and submit idempotency/revalidation remain intact. The watchdog still returns no alert for a current position that has never emitted any `MONITOR_REFRESHED`. The helper clears its own progress handler but cannot restore an unknown handler installed before it; the current call site has no nested handler.

## Slice B5 -- Preserve global-winner claim ownership across transient epochs

- Current proof: the live queue contains a chain of freshly materialized `global_auction_winner_target:*` events for Moscow `2026-07-10/high`. A target is correctly ranked first while `last_error='GLOBAL_WINNER_TARGETED_CLAIM'`; when that claimed target encounters `GLOBAL_EPOCH_SUPERSEDED` or another typed transient, generic requeue overwrites `last_error`. It then loses the target lane, so a later stable global auction often selects the same unclaimed family again and materializes another target. Orders remain zero while the target chain grows.
- First-principles invariant: the global optimizer may act only through a claimed event, but once it selects an unclaimed winner, that target owns the next legal claim until one of three events occurs: successful/terminal disposition, semantic horizon expiry, or a newly selected target supersedes it. A transient q/book/wealth epoch change must re-run all proofs but must not erase claim ownership.
- Minimal repair: when an event whose source is `global_auction_winner_target:*` takes the existing transient requeue path, preserve the existing `GLOBAL_WINNER_TARGETED_CLAIM` processing marker. Keep the real transient reason in the ReactorResult/log. `prioritize_global_winner` already expires an older marked target when a new economic target is materialized, so no new lifecycle or DB state is introduced.
- Forbidden: skip global scope/book/probability/wealth revalidation, act without a claim, submit from the event package, keep a target after terminal/horizon disposition, change YES/NO economics, or bypass idempotency.
- Acceptance: target + transient remains first pending claim next cycle; a new target supersedes the old one; ordinary transient events retain their real `last_error`; terminal/horizon paths remain terminal; no venue calls are added. Independent live-money review must find no P0/P1 before deploy.
- Verification: reactor plus global-auction integration suites pass 95/95. Independent review found no P0/P1 and returned `DEPLOY-SAFE WITH P2 FOLLOW-UP`; it independently passed 8 targeted tests and `git diff --check`.
- Carry-forward P2: preservation recognizes the canonical immutable `global_auction_winner_target:*` source prefix rather than additionally re-reading the pre-claim processing marker. Only `_next_claim_carrier` currently mints that prefix, and prefix priority cannot bypass the global auction, claim, probability/book/wealth revalidation, one-submit bound, or idempotency. A future writer that can mint the prefix should be required to prove the processing marker too.
- Live verification correction: five completed cycles proved the same target retained claim priority through four typed transients (`CURRENT_WEALTH_POSITION_CHAIN_EXPIRED`, `GLOBAL_EPOCH_SUPERSEDED`, `GLOBAL_FEASIBLE_SET_SUPERSEDED_BEFORE_SELECTION`, `DAY0_REMAINING_DAY_MEMBERS_UNAVAILABLE`) with zero claim-lock bounces. However, a later fresh selection created a target-of-target and superseded the old target. B5 therefore fixes retry-priority loss but does not by itself eliminate target-chain growth or produce an order. Exact window deltas were zero commands, submit events, ACKs, fills, trade facts, and envelopes for both YES and NO.

## Slice B6 -- Reuse exact local token topology before Gamma fallback

- Current live proof: B5 keeps one Moscow target claim-owned across retries (`attempt_count` 1 -> 3), but the complete global epoch takes 35--41 seconds. Each retry repeats dozens of Gamma event requests solely to fill missing NO token identities, then fails on `GLOBAL_EPOCH_SUPERSEDED` or `GLOBAL_FEASIBLE_SET_SUPERSEDED_BEFORE_SELECTION`. No command, submit, ACK, or fill is produced.
- First-principles invariant: a Polymarket condition's YES/NO token binding is topology, not a price. The canonical executable snapshot already stores the exact `condition_id`, `yes_token_id`, and `no_token_id`; current CLOB books and snapshot invalidation separately prove execution truth. Re-fetching the same topology from Gamma on every decision adds observation latency without adding probability or price information.
- Minimal repair: when a probability witness lacks token identities, bind exact condition matches from `executable_market_snapshot_latest` first only when their execution metadata is still current at the decision instant (`captured_at <= checked_at <= freshness_deadline`). Fetch Gamma for families with any condition unresolved or stale. Keep Gamma metadata overrides for fallback families; locally bound families continue through the existing snapshot invalidation, current complete-book capture, scope, probability, wealth, and submit-time revalidation gates.
- Files authorized: `src/engine/global_auction_universe.py`, `src/engine/event_reactor_adapter.py`, and `tests/integration/test_w3_solve_seam_g3.py`.
- Forbidden: infer a token from label/price/order, accept a partial binding, bypass Gamma when no exact local condition row exists, relax epoch/scope/quote/wealth gates, change YES/NO scoring, or submit from the topology binder.
- Acceptance: a complete exact fresh local binding causes zero Gamma calls and preserves q content; a stale or partial local binding falls back to Gamma; conflicting local rows fail closed; book capture still reads all native YES and NO books and current metadata/invalidation; affected suites pass; independent live-money review finds no P0/P1 before deploy; live observation shows the same target either advances to a typed economic decision/submit or exposes the next exact blocker.
- Current read-only coverage: across current 2026-07-10..12 market rows, 1,540/1,683 conditions have one exact local token pair, but only 88 are fresh at the measured instant and only 8/153 families are fully fresh-local; every other family must retain Gamma fallback. Zero conditions are locally ambiguous. These are current coverage measurements, not eligibility or profitability claims.
- Verification: global-book, solver, and reactor suites pass 146/146; target modules compile and `git diff --check` passes. Independent re-review found no P0/P1 after the stale-metadata correction and returned `DEPLOY-SAFE WITH P2 FOLLOW-UP`.
- Carry-forward P2: local freshness is checked at the book-provider entry instant; a snapshot can cross its deadline before book capture begins. The later invalidation and submit-time gates remain fail-closed, but capture should eventually recheck `freshness_deadline` at its own `started_at` to eliminate this narrow TOCTOU edge.

## Slice B7 -- Preserve the terminal local-hour forecast interval

- Current proof: the B6 live optimizer reached a unique actuation identity, then the Moscow `2026-07-10/high` winner failed on `DAY0_REMAINING_DAY_MEMBERS_UNAVAILABLE`. The expected bundle is not missing: fresh `ecmwf_ifs` was captured at `20:17:28Z` with Moscow-local points `23:00=22.1C` and next-day `00:00=22.0C`. At decision time around local 23:17, `remaining_day_extremes_c` rejects 23:00 because it is before `now` and rejects 00:00 because its calendar date is next day, leaving an empty vector.
- Semantic proof: Moscow's current-fact source docs are expired and cannot authorize a settlement/source-routing change. This slice makes none. Ogimet/UUWW remains the observed-so-far Day0 fact; ECMWF IFS hourly vectors remain a forecast feature only, never settlement truth. HIGH continues to use the observed maximum floor; LOW continues to use the observed minimum ceiling.
- Minimal repair: when the target local day is still open but no target-day grid point remains at/after `now`, admit the latest target-day point only when both the next local midnight and that point are no more than one hour away. These two proofs distinguish the true terminal interval from a truncated midday vector. A wider gap or more than one hour remaining in the day stays unavailable. Existing remaining-window extrema, expected-model completeness, bundle skew, source provenance, running-extreme clamp, and unseen-peak uncertainty remain unchanged.
- Files authorized: `src/data/day0_hourly_vectors.py`, `tests/test_day0_remaining_day_pricing.py`, and this packet.
- Forbidden: use settlement observations as forecast members, accept stale/wrong-day vectors, extend a grid point beyond one hour, remove expected-model completeness, collapse HIGH/LOW causality, or convert a near-day-end forecast into certain settlement.
- Acceptance: high and low both retain a same-hour terminal anchor; a point older than one hour contributes nothing; after-day-end remains empty; existing Day0 pricing suite passes; independent live-money review finds no P0/P1 before deploy; live replay of the exact current Moscow vector returns a member and still clamps/uncertainty-prices through the existing path.
- Verification: the complete Day0 remaining-day pricing suite passes 51/51; module compilation and `git diff --check` pass. An exact read-only Moscow replay at local 23:24 reads the fresh expected `ecmwf_ifs` vector and returns `[27.0]` after the existing observed-high clamp instead of unavailable. Independent adversarial review's midday-truncation P1 was fixed with the local-midnight proof; re-review found no P0/P1 and returned `DEPLOY-SAFE`.
- Live verification: three completed clean post-deploy cycles took 65.824s, 99.773s, and 72.756s; each retried 12 with zero processed/proof-accepted and zero claim-lock bounces. The terminal-hour missing-member reason did not recur. Moscow's targeted refresh proved fresh and reused cached topology in the two cycles that reached JIT preparation, but both ended at `prepared_family_missing`; the middle cycle correctly failed earlier on feasible-set supersession. Global Gamma fan-out remained 116--124 requests per cycle. Command, submit, ACK, fill, and trade-fact deltas were all zero. A fourth cycle began only after the B8 source edit triggered the expected deployment-freshness pause and is excluded.
- Unrelated repo-wide check: `topology_doctor.py --fatal-misreads --json` remains red on five pre-existing registry/path issues in untouched fatal-misread entries. This slice does not edit or rely on those broken references; its relevant antibodies were applied directly.

## Slice B8 -- Name the JIT global-preparation empty scope

- Current proof: after B7 deployment, two fresh live cycles independently returned `GLOBAL_ACTUATION_PREPARE_FAILED:prepared_family_missing` for the claim-owned Moscow winner. B7 removed `DAY0_REMAINING_DAY_MEMBERS_UNAVAILABLE`; the JIT refresh itself reports fresh cached topology, but `_selection_scoped_proofs` returns an empty tuple before the qkernel can construct `global_family`. The generic fallback erases which fail-closed filter removed the proofs.
- First-principles invariant: no executable order may be created without a current family, but a rejection must preserve the exact epistemic boundary that made the family non-executable. Observability may name a gate; it may not alter that gate or convert absence into eligibility.
- Minimal repair: collect typed, count-bounded rejection classes while the existing selection scope is evaluated, and use that diagnostic as `global_prepare_reason` only when the scope is empty. Preserve proof order, filter order, return values, qkernel inputs, global probability/book/wealth revalidation, and all venue behavior.
- Files authorized: `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: admit a proof rejected by execution-price, semantic admission, lock, held-position, strategy-policy, or final-floor gates; reuse a previously prepared family; submit from diagnostic state; alter YES/NO probability or scoring; add a DB write or network call.
- Acceptance: an empty scope reports its exact stage and bounded rejection-class counts instead of `prepared_family_missing`; a non-empty scope is byte-for-byte identical and emits no diagnostic; affected tests pass; independent live-money review finds no P0/P1; live deploy exposes the next exact blocker or advances to an independently evidenced order.
- Pre-deploy verification: the complete trusted global-auction seam file passes 32/32; module compilation and `git diff --check` pass. The new antibody proves a non-empty scope returns the identical ordered proof tuple with no diagnostic and an all-blocked scope returns the same empty tuple plus bounded stage/class counts.
- Live correction: after the restart's existing 300-second processing lease reclaimed the interrupted claim, the next clean cycle again returned bare `prepared_family_missing` and emitted no `SELECTION_SCOPE_EMPTY`. Therefore the scope was non-empty. The remaining shape is an early typed spine no-trade before `_prepare_global_family`: `SpineDecisionResult.no_trade_reason` is populated, while its optional `global_prepare_reason` remains unset because the bridge returned before global materialization.

## Slice B8.1 -- Preserve early spine no-trade at the global seam

- Minimal repair: when global preparation captures a spine result with no `global_family`, use its existing `global_prepare_reason` first and otherwise its existing typed `no_trade_reason`. This changes only the outer rejection label; the selected proof, family, qkernel decision, and all submit behavior remain unchanged.
- Files authorized: `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: treat a spine no-trade as a prepared family, synthesize q or candidate topology, reuse an earlier family, or change any YES/NO admission/score/submit gate.
- Acceptance: early `SPINE_INPUTS_UNAVAILABLE`, lead-bucket, and wiring outcomes surface under `GLOBAL_ACTUATION_PREPARE_FAILED` instead of `prepared_family_missing`; a successful prepared family remains unchanged; live observation exposes the exact early reason.
- Pre-deploy verification: the complete trusted global-auction seam file passes 33/33; module compilation and `git diff --check` pass. The antibody proves explicit global-prepare failure has priority, otherwise an early typed spine no-trade is preserved, while a successfully prepared family emits no failure reason.
- Live verification: the first complete post-deploy claim cycle returned `GLOBAL_ACTUATION_PREPARE_FAILED:SPINE_INPUTS_UNAVAILABLE:SERVED_JOINT_SAMPLES_MISSING`. The target remained claim-owned and requeued. Command, submit, ACK/order-id, fill/trade-fact, envelope, and collateral-reservation deltas were all zero.

## Slice B9 -- One probability authority across global selection and actuation

- Current proof: global selection prepares every family through `_prepare_current_global_probability_family`, which builds a replacement-posterior row-simplex witness and, for Day0, requires active conditioning plus current observation HWM. JIT actuation instead enters the ordinary Day0 branch in `_live_yes_probabilities`, which rebuilds a legacy canonical point-q path and never serves joint samples. The same selected family therefore has a valid current `JointOutcomeProbabilityWitness` at selection but cannot reconstruct it at actuation.
- First-principles invariant: the zero-sum family simplex is one immutable decision certificate, not two probability calculations. At actuation, current canonical truth must independently prove that the certificate's economic content is still current; after that proof, the original certificate remains the identity bound to the selected order. A new observation timestamp must not manufacture a different authority certificate, and an old witness must never be accepted without current content revalidation.
- Minimal repair: for global actuation, independently rebuild `_prepare_current_global_probability_family` at JIT and compare its content fields (`family/q/topology/posterior/source/band/sample matrix`) to the actuation witness. Retain the original witness only after equality, so the order identity remains the one selected. Route Day0 global preparation through the same replacement bundle builder that supplies current coherent samples; ordinary non-global Day0 behavior remains unchanged. Do not ask the local spine to mint a second global witness during actuation.
- Critic correction: the first draft was not deploy-safe. The claimed target payload could still carry an older observation time even when the current replacement posterior had advanced its remaining-day conditioning. Equal observed extreme is insufficient because `max(observed, X_remaining)` / `min(observed, X_remaining)` changes when the remaining window advances. Revised repair atomically binds the current posterior's `day0_conditioning` to the canonical observation HWM: exact observation time, extreme after native-unit conversion, sample count, source family, station, source ID, and settlement unit must agree. Only then may an ephemeral execution payload feed qkernel; the persisted target event remains claim identity and is never rewritten. Observation rows imported after decision time are excluded.
- Critic correction 2: the conditioning field `observed_extreme_c` is Celsius, while `day0_conditioning.unit` records the native settlement unit. Requiring that provenance unit to be `C` would reject every Fahrenheit family. The binding now requires `conditioning.unit == resolution/fact native unit` and separately compares `observed_extreme_c` to the exact native fact converted to Celsius; the antibody covers `80.6F == 27C` and a carrier/canonical source-family change at the same station.
- Critic correction 3: selecting the correct extreme row is insufficient if HWM/count/availability aggregate a different station or unit. The canonical visible set is now filtered before aggregation to the executable Day0 station, configured source family, native settlement unit, local date/DST geometry, and decision-time availability. A KJFK row cannot advance KLGA's remaining window; a Celsius KLGA row cannot advance a Fahrenheit family. Day0 monitoring station identity remains distinct from final settlement-source identity (for example HKO vs HKO_HQ); the executable trigger's `_expected_station_for_city` contract owns this plane. Full atomic binding is evaluated during global selection as well as actuation, so a contaminated posterior cannot distort the winner and merely fail later.
- Critic correction 4: row time is not possession time. `observation_instants` without `imported_at` is unavailable to the live observation lane, and rows imported after the decision cannot enter its HWM. Unknown source types fail closed; known WU/NOAA/HKO lanes use exact configured raw sources before aggregation, while live fast events use an explicit known-source allowlist plus current station/unit/rounding/authority checks. Among visible event facts, observation HWM wins rather than whichever older observation arrived last.
- Critic correction 5: the current observation payload now travels into the Day0 authority certificate. Its source clock is the canonical fact availability; agent-received and persisted clocks are the current decision/read boundary. The stale target remains only the causal-event parent. Certificate construction enforces `source_available <= agent_received <= persisted <= decision`, preventing a verifier-accepted impossible chronology.
- Files authorized: `src/data/replacement_forecast_current_target_plan.py`, `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, `tests/test_replacement_forecast_current_target_plan.py`, `tests/test_replacement_forecast_seed_discovery.py`, and this packet.
- Forbidden: reuse an unvalidated witness; compare only scalar q; accept stale observation/posterior/book; skip Day0 HWM; use legacy canonical q for a global actuation; alter YES/NO complement law, global objective, stake, or venue submit gates.
- Acceptance: changed current probability content rejects before proof binding; equal current content preserves the original actuation witness identity; a stale target carrier cannot feed its old Day0 observation into actuation; posterior conditioning and canonical observation state must match atomically; global Day0 builds coherent joint samples from the replacement posterior; ordinary Day0 is unchanged; selected YES and NO remain native columns/complements of the same row-simplex; independent review finds no P0/P1; live proceeds to the next typed gate or an independently evidenced order.
- Pre-deploy verification: 61/61 focused global-Day0/current-target tests pass, including stale carrier, Fahrenheit conversion, mixed station, mixed unit, future/missing import clock, unknown source type, 8+1 later-arriving older events, and full Day0 certificate chronology. Current live DB scan of every latest active Day0 posterior is 57/57 exact on observation time, sample count, native unit, Celsius extreme, and source basis. Independent critic's final and incremental reviews found no P0/P1 and judged the complete slice deploy-safe.

## Slice B10 -- Project only decision metadata from the global book read model

- Current proof: the first post-B9 reactor cycle entered `process_pending` at `21:51:08Z` and had not returned after four minutes; scheduler ticks, heartbeat, health, redecision, Day0 refresh, and exit monitor were skipped behind it. A live `SIGUSR1` all-thread dump pinned the reactor at `capture_current_global_book_epoch -> _global_book_snapshot_rows`. That query joins the compact 18,072-row latest table to the 9,709,963-row append table and selects `s.*`, including historical orderbook depth and raw evidence fields that are never consumed by the current-book decision. The canonical trade DB is about 85.9GB. This is unnecessary evidence transport on the decision clock, not additional economic proof.
- First-principles invariant: the current public CLOB response owns price/depth for this auction epoch. The persisted snapshot row is consulted only for current topology, execution metadata, fee/tick/minimum constraints, and invalidation identity. Reading any other append payload cannot improve the decision and must not consume its freshness window.
- Minimal repair: make `_global_book_snapshot_rows` select only the exact fields consumed by token binding, invalidation, executable-status evaluation, and curve construction. Preserve the same latest-row join, condition filter, row ordering, fail-closed missing metadata, invalidation check, YES/NO native books, fee/tick/minimum constraints, and complete-universe requirement.
- Files authorized: `src/engine/global_auction_universe.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: read probability or price from the compact projection, skip append-row identity, accept missing metadata, weaken snapshot invalidation, reduce the family/token universe, change YES/NO economics, extend quote TTL, or add any venue side effect.
- Acceptance: an SQL authorizer that denies every unneeded append column still permits a complete global book epoch; denying any required projected column fails closed; affected suites pass; independent live-money review finds no P0/P1; post-deploy reactor reaches a typed auction outcome inside the live quote/cycle budget instead of pinning the scheduler.
- Pre-deploy verification: the complete trusted global-auction seam suite passes 40/40. The authorizer antibody permits every YES/NO native book while denying unused append depth and fails closed when required fee metadata is denied. A read-only current-DB query of the stricter all-18,072-row narrow projection completed in 6.256s. Module compilation and `git diff --check` pass. Independent review found no P0/P1, confirmed both helper consumers are fully covered by the 15-column projection, and returned `DEPLOY-SAFE`; live cadence improvement remains a post-deploy proof obligation.

## Slice B11 -- Hydrate only open positions in the status read model

- Current proof: after `process_pending` returned in 29.817s, the old-SHA cycle still failed to emit its cycle-result/status pulse for more than 119s. The canonical trade DB currently has 989 `position_current` rows but only one open-exposure row: 749 voided, 215 settled, 17 admin-closed, 7 economically closed, and 1 Day0-held. `query_position_current_status_view` loads all 989 rows and constructs all trade IDs before `_query_transitional_position_hints` performs one indexed event query per ID; only afterwards does the Python loop discard terminal phases. This also keeps the world writer busy long enough that the official deploy guard cannot be written within its 30s deadline.
- First-principles invariant: a current open-position status view is a projection of canonical open exposure, not a scan of terminal history. Terminal rows remain canonical and queryable in `position_current`; excluding them from this derived open-status query changes no lifecycle, event, PnL, reconciliation, or settlement truth.
- Minimal repair: push the existing `OPEN_EXPOSURE_PHASES` predicate into the `position_current` SQL query before transitional/fill hydration. Retain the Python phase check as a defensive boundary. Do not change `query_portfolio_loader_view`, canonical rows, lifecycle grammar, or status output fields.
- Files authorized: `src/state/db.py`, `tests/test_db.py`, and this packet.
- Forbidden: delete/archive/mutate any position, hide quarantined chain risk from a surface that currently owns it, change open-phase membership, alter fill/exit hint semantics, change status counts, or weaken derived-export authority labels.
- Acceptance: terminal rows never enter either hydration helper; every `OPEN_EXPOSURE_PHASES` row still does; returned open-position content/counts are unchanged; current live read returns one position without 988 terminal event-query loops; affected DB/status tests pass; independent critic finds no P0/P1.
- Pre-deploy verification: five focused status-view semantics tests pass, covering active, Day0 held, pending-exit intent, retry release, and the new open-only hydration boundary. The current canonical trade DB returns the exact one open position in 0.142s through the changed function. Module compilation, `git diff --check`, and the planning-lock gate pass. The full legacy `tests/test_db.py` run is 80 passed / 20 failed on unrelated existing drift (removed legacy event helpers, stale settlement fixtures, missing projection fields, and one pre-existing active-position exit-state expectation). The latter traverses the same function but supplies an active row that remains selected by this diff; it conflicts with the pre-existing `phase != pending_exit -> none` rule, not the SQL phase filter.
- Independent review found no P0/P1 and returned `DEPLOY-SAFE`. It separately proved a relaxed-schema `unknown` row remains visible, terminal rows were already excluded, and quarantined chain-risk ownership remains intact in `query_portfolio_loader_view(runtime_exposure_only=True)` rather than this derived open-status surface.

## Slice B12 -- Freeze the global selection epoch; revalidate only its winner at submit

- Post-deploy truth for B10/B11: loaded SHA `2f8269799d8cb15c1beb01e5d469247b56662886` is exact, RiskGuard is GREEN, entries are unpaused, the forecast bridge is healthy, chain collateral is fresh at `$1,253.433838`, and no active reservation exists. B10 reduced a clean global-book construction to about 2.6 seconds and B11 let the cycle/status pulse complete, proving both deterministic I/O amplifiers were real. The live tail is still unstable: completed cycles took about 28.6s, 45.3s, and 152.3s, followed by another cycle exceeding 128s.
- Actual-order proof remains absent. Since deploy, deltas are zero on all five independent lines: `venue_commands`, submit-request events, venue ACK/order IDs, `venue_trade_facts`/fills, and submission envelopes. Health is DEGRADED only on `CANDIDATES_ONLY_NO_TRADE_NO_CAPITAL_FLOW`; risk, capital, forecast bridge, and submit authority are not the blocker.
- Root cause: the batch reads a complete probability-ready scope and captures a complete native YES/NO book epoch, then repeatedly redefines the scope at a later wall-clock time and requires every losing family's q carrier to remain unchanged through selection and again through actuation. A continuous posterior producer plus non-zero full-universe latency therefore has no finite completion guarantee. Live outcomes alternate between `GLOBAL_FEASIBLE_SET_SUPERSEDED_BEFORE_SELECTION` and `GLOBAL_EPOCH_SUPERSEDED`; detailed candidate rejection maps are currently discarded, but timing and identity evidence exclude venue, quote-TTL, and wealth supersession and isolate probability/scope churn.
- First-principles decision law: a global optimum is defined only over one immutable information vector. Zeus can certify `CUT_TIME_GLOBAL_OPTIMUM` over the complete family scope, every immutable probability witness, the complete native YES/NO book epoch, and one wealth witness observed for that selection epoch. It cannot certify a literal submit-time optimum over a continuously changing distributed universe without an atomic joint snapshot that the venue and probability authorities do not provide.
- Submit law: cut-time selection is not venue authority. The selected winner must still rebuild and match its current probability content, exact token/side/full-depth curve, and free cash in the existing actuation path. If any winner fact changes, submit is zero and the next cycle recomputes the complete epoch. A loser update belongs to the next epoch; it must not invalidate the already proved cut-time ranking or silently promote a runner-up.
- Minimal repair boundary: use the existing `CurrentGlobalAuctionScope` plus immutable probability/book/wealth witnesses as the selection manifest; require every probability witness to share the scope cut and bind the scope carrier; use those frozen authorities during the complete ranking; remove later all-loser scope/q revalidation; preserve complete scope admission, typed ineligibility, native YES+NO books, full candidate scoring, one-order maximum, winner q/book/free-cash JIT, idempotency, and every venue gate. The receipt/provenance language must say cut-time, never submit-time global optimum.
- Files authorized: `src/engine/global_batch_runtime.py`, `src/engine/global_single_order_auction.py`, `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet. `src/solve/solver.py` may change only if the existing frozen-authority seam cannot express the invariant without ambiguity.
- Forbidden: submit an old runner-up, skip winner probability/book/cash JIT, omit a family or native side to get a result, treat a moving/latest query as a snapshot, weaken quote freshness, mutate q, change Kelly/robust objective, fabricate order evidence, or claim guaranteed profit.
- Acceptance: a loser q update after the cut does not block one unchanged winner; winner q, book, or free cash change yields zero submit; mixed-cut probability witnesses fail closed; a family appearing after the cut is deferred to the next complete epoch; missing YES or NO remains whole-epoch no-trade; focused and affected suites pass; an independent live-money critic finds no P0/P1; official deploy reaches either a truthfully scored YES/NO winner or a specific economic no-trade reason, not moving-epoch livelock.
- Implemented cut: production opens one owned read transaction on the independent read-only forecast connection before naming `scope_at`. Readiness, posterior, topology, Day0 observation/HWM, and token binding all use that exact connection and snapshot. A caller-owned transaction is rejected rather than reused. World `opportunity_events` remain a separate append-only carrier scan constrained by both possession clocks at or before `scope_at`; they are included in the full-scope identity but are not treated as a second probability snapshot. The forecast snapshot is released before winner actuation so JIT revalidation sees post-cut truth.
- Implemented certificate: `selection_epoch_identity` binds the q/scope cut time, full scope, eligible scope, every eligible family witness, and every typed exclusion. `GlobalSingleOrderActuation` separately carries `selection_cut_at_utc` and the later `decision_at_utc`; both, plus the epoch identity, enter its hash and the required receipt economics fields. The receipt labels the claim `CUT_TIME_GLOBAL_OPTIMUM`, never submit-time global optimum.
- Pre-deploy verification: full W3 global seam passes 42/42; solver plus reactor suites pass 115/115; modules compile; `git diff --check` and the planning-evidence gate pass. The WAL antibody proves a concurrent readiness update remains invisible throughout selection and becomes visible only after release before winner JIT; caller-open transactions fail closed; mixed timestamp/carrier manifests fail closed. Independent live-money re-review found no P0/P1 and returned `DEPLOY-SAFE`. Carry-forward P2: production passes the required snapshot connection but the generic helper parameter defaults empty; cleanup does not specially catch `KeyboardInterrupt/SystemExit`; typed ineligibility taxonomy remains broad but is now snapshot-bound and certificate-audited; a future test should assert the three global selection fields in the final serialized durable receipt.

## Slice B13 -- Materialize an unclaimed winner past the current batch lease

- B12 live proof: exact SHA `59217ea25` loaded under PID 18365, entries unpaused, risk/infrastructure GREEN, fresh CHAIN collateral `$1,253.433838`, and zero active reservations. The first fully post-release epoch completed in 35.596s, reached a real winner/next-claim carrier, and failed before persistence with `GLOBAL_BATCH_FAILED:RuntimeError:GLOBAL_WINNER_TARGET_NOT_PENDING`. Order proof deltas remained zero on command, submit, ACK, fill/trade-fact, envelope, and live-order-event lines.
- Root cause: `_queue_global_winner_for_claim` runs before the current claimed batch is finalized. `EventStore.prioritize_global_winner` rejects whenever any same-family processing lease exists, including a lease owned by the exact current batch that is guaranteed to be finalized immediately after target materialization. With a bounded reactor page, a current processing carrier can represent the selected family even when the selected scope event itself was not the actuation owner. The target insert rolls back, then every claimed event receives the coarse batch failure; the selected economics are lost and no later claim can actuate.
- First-principles ownership law: a processing lease outside the current batch must continue to block a new target. Current-batch authority is the exact `(event_id, claimed_at)` generation minted before each claim and returned only after that claim and its pre-submit gate unit commit successfully, never the reusable event ID alone. In its own `BEGIN IMMEDIATE`, target materialization first proves every capability row still exists as `processing` at that exact generation, then proves every same-family processing row belongs to that capability set. A stale reclaim, requeue, terminal disposition, or missing row invalidates the old batch automatically.
- Crash contract: target commit precedes claimed-row finalization. A crash after commit can therefore leave fresh processing rows plus a pending target, and `fetch_pending` may return that target before the old leases become stale. This ordering is safe but is not processing-first: `GlobalBatchSubmitResult` makes `next_claim_event` mutually exclusive with both a winner and any venue submit, so the interrupted epoch performed no venue side effect; the target still needs a fresh claim, a new global epoch, and winner JIT before any submit. Old processing rows recover after lease expiry. The implementation must not claim otherwise.
- Minimal repair: capture the exact current claim-generation map, thread it into `_queue_global_winner_for_claim` and `prioritize_global_winner`, and allow only exact generation matches. Default callers pass no capability and preserve the existing lease block. Keep append-only event rows, one pending target, old-target supersession, stale-processing priority once a lease is actually stale, one-submit-per-epoch, and winner JIT unchanged.
- Files authorized: `src/events/event_store.py`, `src/events/reactor.py`, `tests/events/test_reactor.py`, and this packet.
- Forbidden: ignore an unrelated processing lease, mutate event facts, submit before the target is claimed, mark a target processed, bypass stale-processing recovery, broaden the allowset beyond the current batch, or infer order proof from target persistence.
- Acceptance: same-family processing outside the capability still blocks; an ABA stale reclaim invalidates the old capability; exact current-batch generations allow exactly one pending targeted carrier; a commit-then-crash may expose the target before fresh old leases but the interrupted epoch is structurally side-effect-free; the next epoch must reclaim/JIT the target independently; stale processing keeps priority once reclaimable; no venue call is introduced by materialization; focused reactor tests and independent critic find no P0/P1; live proceeds to winner actuation or its exact JIT rejection.

## Slice B14 -- Bound the live decision epoch to current executable work

- B13 live proof: exact SHA `978a1a942` loaded under PID 79888; active entry pause rows zero; runtime and four required sidecar SHAs match; live-health process/runtime/boot-prerequisite surfaces are healthy. The first post-release epoch materialized a target and returned `GLOBAL_WINNER_AWAITS_CLAIM` instead of `GLOBAL_WINNER_TARGET_NOT_PENDING`. The next epoch claimed targeted carriers and reached Wellington/2026-07-12/high JIT refresh, but `process_pending` took 327.754s and the following status pulse remained in-flight for more than five minutes. Command, submit-request, ACK/order-id, fill/trade-fact, envelope, and live-order-event deltas remained zero.
- Current-data diagnosis, not historical strategy evidence: the 80GB live world DB contains 32,208 Day0 event carriers, while only 416 have `target_date` inside the conservative UTC calendar +/-1-day envelope needed to cover every city-local current day. `_current_day0_events` reads and sorts the full event-type history before applying city-local current-day logic. The live trade DB contains 35,599 venue-order facts inside an 86GB file; each status pulse performs two full-history `ROW_NUMBER` windows even though only the current command set can appear in the derived open/conflict views. Concurrent `live_tick` recovery also omits its existing `live_tick_scope=True` narrowing when reconciling maker-fill economics.
- First-principles latency law: a live decision epoch may read every fact that can change the current choice, but no settled/obsolete carrier or terminal order history earns entry into the hot path merely because it shares a table. SQL must first construct a conservative current superset, then existing semantic code makes the final timezone/lifecycle decision. Derived observability must be driven by current command identities and point-read only their latest facts; it cannot delay the money path. Live recovery must use its already-defined live scope.
- B14-A: constrain Day0 carrier SQL by payload `target_date` between UTC decision date -1 and +1 day using the existing target-date index. Preserve the existing per-city timezone/current-local-day final predicate and append-only event identity.
- B14-B: pass `live_tick_scope=True` to the existing recorded-maker-fill-economics reconciliation only for scheduled `scope='live_tick'`; boot/restart scopes remain unchanged.
- B14-C: replace both status-pulse all-history fact windows with current-command-driven latest-fact point lookups or an equivalent indexed current-command CTE. Preserve output schema and terminal-event-vs-fact semantics. Add elapsed stage visibility around pulse subreads if it does not alter authority.
- B14-D: commit the reactor's canonical event/finalization unit immediately after `process_pending` and before any derived status read/write. A status slowdown must never hold target disposition invisible or retain the world writer while opening a second status connection. Status remains fail-soft after the commit.
- Files authorized: `src/engine/global_auction_universe.py`, `src/events/reactor.py`, `src/execution/command_recovery.py`, `src/observability/status_summary.py`, `tests/integration/test_w3_solve_seam_g3.py`, `tests/test_command_recovery.py`, `tests/events/test_reactor.py`, the narrow existing status-summary tests, and this packet.
- Forbidden: change q, side selection, FDR/Kelly, settlement semantics, city-local Day0 definition, command/fill truth, execution gates, venue calls, order state, or canonical DB rows; treat a faster no-trade as an order; infer profitability from latency alone.
- Acceptance: current Day0 semantic output is unchanged on the UTC +/-1-day envelope and cannot read older dates; current command/open/conflict status output is byte-equivalent on fixtures while latest-fact reads are index-backed; live-tick recovery does not scan unrelated historical maker fills; focused tests and independent review find no P0/P1; deployed epoch completes with explicit target/JIT economics or typed rejection and preserves all five separate order proof lines.

## Slice B15 -- Make certified replacement YES/NO bounds side-symmetric

- B14 live proof: exact SHA `526f39f91` is loaded and healthy. Three complete post-release epochs finished in 22.5--30.3 seconds, so the prior status/commit stall is gone. A fresh Wellington/2026-07-12/high target repeatedly reaches live candidate admission with `BUY_NO`, `yes_posterior=0.347921`, `no_q_lcb=0.617584`, and price `0.320000`, then is rejected only by `ADMISSION_BUY_NO_CONSERVATIVE_EVIDENCE_MISSING` because its bound provenance is `FORECAST_BOOTSTRAP` and settlement coverage is `INSUFFICIENT_DATA`. All five actual-order lines remain zero: no command/final intent, no `SUBMIT_REQUESTED`, no ACK/order ID, no fill/trade fact, and no envelope/live-order event.
- First-principles probability law: each condition is a binary zero-sum claim. For one immutable replacement posterior, `q(NO)=1-q(YES)`. Its conservative lower bound is not `1-LCB(YES)`; it is exactly `LCB(NO)=1-UCB(YES)` from the same bootstrap distribution. The live replacement builder already computes those two identities and the direction-specific settlement-backward coverage layer already shrinks a refuted NO lower bound before admission. A further source-name/coverage waiver applied only to `BUY_NO` is duplicate uncertainty control and breaks side symmetry; `BUY_YES` has no homologous third gate.
- Authority diagnosis: `FORECAST_BOOTSTRAP` identifies the bound construction inside the single `replacement_0_1` authority; it is not a legacy probability authority. Regime U1 requires the replacement posterior plus its certified `q_lcb_json/q_ucb_json` to be the one forecast authority. Treating the provenance label as evidence that NO lacks a native bound resurrects era layering and contradicts the executable construction.
- Minimal fail-closed repair: preserve the material-bin gate for canonical/legacy probability paths. Exempt only a replacement selected leg carrying a hashed bound certificate bound to the posterior identity, family/topology, selected condition/bin, live q-mode, joint-sample hash, and fused-center bootstrap basis. Both the candidate gate and receipt-level twin must verify finite `[0,1]` values, `q_no == 1-q_yes`, raw `no_q_lcb == 1-yes_q_ucb`, and served `no_q_lcb <= raw no_q_lcb` after any direction-specific settlement shrink. A probability-authority string, coverage status, source name, or boolean alone is insufficient. Missing, malformed, mismatched, or non-replacement evidence continues to reject.
- Files authorized: `src/strategy/live_inference/live_admission.py`, `src/engine/event_reactor_adapter.py`, `src/events/candidate_evaluation.py`, `src/events/reactor.py`, `src/events/no_submit_receipts.py`, `src/decision_kernel/certificates/execution.py`, `src/decision_kernel/verifier.py`, `tests/test_live_buy_no_material_admission.py`, `tests/strategy/test_settlement_coverage_admission_licensing.py`, `tests/engine/test_same_bin_yes_posterior_receipt_wiring.py`, `tests/events/test_candidate_evaluation.py`, `tests/events/test_opportunity_selector.py`, `tests/decision_kernel/test_actionable_trade_certificate.py`, the narrow q-kernel overlay/final-intent relationship tests, and this packet.
- Forbidden: complement price instead of probability; derive NO lower bound from YES lower bound; waive FDR, Kelly, direction law, execution-quality, RiskGuard, quote freshness, winner JIT, or venue checks; admit a generic `FORECAST_BOOTSTRAP` carrier; infer an order from a target/intent; claim guaranteed profit.
- Acceptance: certified replacement BUY_NO and its binary-equivalent BUY_YES representation face the same probability-evidence gate; a one-bit/scalar mismatch in authority, posterior ID, bound basis, YES point, YES UCB, NO point, or NO LCB fails closed at both twin sites; legacy `FORECAST_BOOTSTRAP + INSUFFICIENT_DATA` remains rejected; receipt JSON preserves the certificate inputs; focused and affected suites pass; an independent live-money critic finds no P0/P1; official deployment proceeds to the next actual economic/execution verdict and all five order-proof lines are re-sampled independently.
- Critic gate 1 returned `NO-GO`: a pre-qkernel certificate incorrectly required equality with the post-qkernel LCB, so a legitimate q-kernel tightening could be re-rejected at the receipt twin. The repaired invariant must be `qkernel_execution_lcb <= coverage_served_lcb <= raw_no_lcb == 1-yes_ucb`; equality across the first seam is forbidden. The certificate must also bind to the DecisionKernel forecast/belief parent identities, survive the final-intent whitelist into pre-submit/execution, and be verifier-checked there. Self-signed identity strings are not canonical authority.
- Gate inventory correction: `CandidateEvaluation` is explicitly a receipt/display projection of the already-materialized proof, but its `admitted` property recomputed the BUY_NO evidence gate without the canonical forecast parent or bound certificate. That third, non-authoritative evaluation could reject a certified replacement leg after the construction gate had accepted it. It is reduced to projecting the upstream typed `missing_reason`; candidate construction and the receipt money-path twin remain the two fail-closed certificate validators.
- Pre-deploy verification: the final certificate binds the selected leg to independently hashed canonical `q/q_lcb/q_ucb` maps, a content-hashed posterior topology, the topology-resolved condition bin, and the actual pre-qkernel served LCB. The q-kernel may only tighten `final_lcb <= served_lcb <= raw_no_lcb == 1-YES_UCB`. Coherently rehashed identity, sibling-bin, selected-scalar, topology-content, and unshrunk-bound forgeries fail closed. Receipt, ActionableTrade, final intent, pre-submit, command hash, and verifier closure are covered. The affected suite passes `248/248`; two excluded Day0 tests have pre-existing fixture drift at missing `remaining_day_q_source` before B15 logic. Module compilation, metadata freshness, planning evidence, and `git diff --check` pass. Independent final live-money review found no P0/P1/P2 and returned `DEPLOY-SAFE` for B15; this is not an order/fill claim.

## Slice B16 -- Emit the readiness-certified replacement carrier

- B15 live proof: exact SHA `ec2a07299` loaded, restart preflight passed, the exact deploy restart guard expired, and the former `ADMISSION_BUY_NO_CONSERVATIVE_EVIDENCE_MISSING` did not recur. Four complete post-deploy epochs instead requeued all 12 claims on `GLOBAL_PROBABILITY_EPOCH_CARRIER_MISMATCH:edli_family_82b3625374443a931653f7d6`; all five order-proof deltas stayed zero.
- Current-data root cause: the family is Panama City/2026-07-11/high. The replacement FSR scan emits latest posterior `30136` (`49b6…`), while the canonical READY dependency explicitly binds `soft_anchor_posterior=30223` (`daa8…`). Global preparation correctly reads the readiness-certified posterior, so the later equality gate must reject every epoch. A posterior row can exist before its readiness certificate is durable; the trigger comment claiming otherwise is false.
- First-principles law: a forecast event used as a probability carrier must name the exact posterior licensed by the current readiness certificate. Recency alone is not authority. The downstream carrier-equality gate remains fail-closed and unchanged.
- Minimal repair: make FSR and every reactor probability read select the same latest strategy-scope readiness as of decision time, with exact metric data-version and exact soft-anchor dependency identity. The bundle reader independently revalidates dependency source/product/data/status/time and fails closed on expired readiness or an over-age forecast cycle. Preserve one-row-per-family ordering, phase filters, coverage fairness, event identity, and every downstream q/book/wealth/JIT gate.
- Files authorized: `src/events/triggers/forecast_snapshot_ready.py`, `src/engine/replacement_forecast_hook_factory.py`, the three existing `event_reactor_adapter.py` consumers, `src/data/replacement_forecast_bundle_reader.py`, narrow trigger/carrier/staleness/event-reactor tests, and this packet.
- Forbidden: remove or weaken `GLOBAL_PROBABILITY_EPOCH_CARRIER_MISMATCH`; synthesize a readiness row; select an unready latest posterior; reuse an expired/wrong-scope certificate; serve stale truth as current; alter q, YES/NO bounds, ranking, Kelly, or venue submission.
- Acceptance: when a newer uncertified posterior exists, FSR emits the older exact READY-bound posterior and the global witness matches; once readiness advances, FSR advances atomically; missing/malformed/wrong-scope dependency emits no live replacement carrier; focused tests and independent critic find no P0/P1; official deploy advances to the next exact economic/execution verdict or a real order with all five proof lines.
- Pre-deploy verification: trigger, reactor selector, and bundle reader bind the same strategy scope, metric data-version, decision-time readiness, and exact posterior dependency. UTC-normalized SQL rejects a lexically earlier but physically future offset timestamp; expired/over-age/malformed/wrong-scope/wrong-source/forged dependencies and missing/non-live/training schema rows fail closed. Current read-only DB proof selects Panama City/high readiness `8ea832…` and posterior `30391 / 710faf…` in both selector and bundle. Focused affected suite passes `101/101`; independent broader review passes `114` focused and `164` broader tests with two known baseline LOW-fixture failures and one expected xfail. Independent live-money critic returns `GO` with no P0/P1; this is not an order/fill claim.
- Pre-deploy verification: the original independent critic's P1 counterexamples are now explicit antibodies: wrong/global scope, future or expired readiness, HIGH/LOW mismatch, wrong posterior source, malformed dependency, string posterior id, and wrong dependency source/product/data/status/available-at all fail closed. FSR and all three reactor consumers share the same point-in-time selector; the bundle independently revalidates the certificate. Affected suite passes `109/109`; compilation, freshness metadata, planning, map maintenance, and `git diff --check` pass. Current read-only DB proof produced `100/100` carrier/readiness identity matches; Panama City current certified bundle is READY on posterior `30391` with 11 complete q/q_lcb/q_ucb bins. Claude was unauthenticated and Gemini stalled, so no external final verdict was produced; this is disclosed and is not represented as independent approval. Main Tier-0/Tier-1 review found no remaining P0/P1/P2 on the B16 slice.

## Slice B17 -- Preserve the global winner callback contract at actuation

- B16 live proof: loaded SHA `64f380753`; two completed epochs contained zero carrier mismatch. Epoch one advanced to `GLOBAL_WINNER_AWAITS_CLAIM`; epoch two reached global actuation and all 12 claims failed on `TypeError: _full_depth_native_side_candidate_from_proof() takes 0 positional arguments but 1 was given`. All five new-order proof counters remained unchanged.
- Root cause: the builder is deliberately keyword-only and every spine materialization call supplies `family_key` plus `proof`; `_global_actuation_selected_proof` alone called it with one positional proof and omitted the family identity. This is a call-shape defect, not an economic rejection.
- Minimal repair: bind `family_key=candidate.family_key` and `proof=proof` at the actuation revalidation call. Keep the keyword-only builder, full-depth requirement, global winner identity comparison, q witness, book curve, JIT, risk, Kelly, and venue gates unchanged.
- Files authorized: `src/engine/event_reactor_adapter.py`, `tests/engine/test_s3_native_side_candidate_materialization.py`, and this packet.
- Acceptance: an AST antibody rejects any positional call to the keyword-only full-depth builder; the native-side/global-auction affected suite passes; official deploy advances to the next exact verdict or a real order with all five proof lines.
- Pre-deploy verification: the actuation call now supplies the same `family_key/proof` keyword contract used by spine construction; no probability, book, sizing, or submission code changed. The AST callback antibody plus global-auction affected suite pass `55/55`; module compilation, freshness metadata, planning evidence, and `git diff --check` pass.

## Slice B18 -- Split global actuation supersession evidence

- B17 live proof: loaded SHA `d54ad4aa1`; callback TypeError is gone. Two consecutive epochs select the same Wellington family, refresh its substrate as fresh, then fail closed on the composite `GLOBAL_ACTUATION_EXECUTION_BINDING_SUPERSEDED`; order proof deltas remain zero.
- First-principles diagnostic: identity drift and executable-curve drift have different causes and remedies. A single reason erases the distinction and makes any attempted fix guesswork.
- Minimal change: preserve both rejection branches and expose only the mismatched identity field names or the two execution-curve identity hashes in the typed reason. Do not change comparison inputs, equality, requeue behavior, q, book refresh, sizing, or submission.
- Files authorized: `src/engine/event_reactor_adapter.py` and this packet.
- Acceptance: affected suite remains green; official deploy identifies which exact fail-closed branch is active without creating a command/order.
- Pre-deploy verification: both former comparisons remain byte-for-byte strict; only typed evidence is refined. Global-auction affected suite passes `55/55`, module compilation and `git diff --check` pass.

## Slice B19 -- Bind JIT revalidation to the current global optimum

- B18 live proof: loaded SHA `13aad6cec8`; the exact deploy guard is expired and two complete post-guard epochs reach the Wellington winner before failing on the curve branch. The selected direct-CLOB curve changes (`c2f47f...`, then `53fc37...`) while the purported current curve stays fixed at `76f0ea...`. Identity, probability, token, side, and family rebinding all passed. All five new-order proof counters remain unchanged.
- Current-path root cause: the global auction captures a fresh complete CLOB book epoch, but winner actuation asked the targeted refresher for the trigger event's condition, which can differ from the global winner's sibling condition. The refresher also treated an older TTL-fresh row as sufficient without a network recapture. Both violate FC-03 for the actual winner.
- First-principles law: the selected shares are an argmax over the complete native ask curve, not merely a fillable prefix. A deeper ask change can leave the old order's cost/VWAP unchanged while making a larger order the new capital-growth optimum. Therefore prefix equality is insufficient; every economic curve field must remain identical or the full auction must run again.
- Minimal repair: force a real CLOB recapture for `global_actuation.decision.candidate.condition_id`, require a newer distinct snapshot row for that exact native token, and bind both later pre-submit reads to its venue book hash. Align only evidence carriers (`snapshot_id`, full-book hash, TTL) before comparing curves; token, side, every native ask level, fee, tick, and minimum order remain byte-identical. Any economic curve drift requeues for a new global auction.
- Side symmetry: YES and NO both compare their own complete native ask curve through the same identity function; no price complement or side waiver exists.
- Files authorized: `src/engine/event_reactor_adapter.py`, `src/events/reactor.py`, `src/data/substrate_observer.py`, narrow existing global-auction/reactor/substrate tests, and this packet.
- Forbidden: accept prefix equality as optimality proof, refresh the trigger instead of the winner, resize without a new global auction, compare YES and NO through complement prices, relax q/FDR/Kelly/RiskGuard/quote freshness/current-book checks, reuse an expired snapshot, or infer an order from this gate.
- Acceptance: a trigger/winner sibling test proves only the winner is recaptured; a lock-busy or unchanged snapshot fails closed; book-hash-only churn with an identical full native ask curve may proceed; any ask price/size, fee, tick, or min-order change re-auctions; final intent preserves the already-existing exact taker shares/limit and depth/VWAP checks; independent re-review finds no P0/P1 before deploy.
- Critic correction: independent review rejected the first `9dd29aba3` implementation because exact-prefix equality did not preserve the current argmax and refresh targeted the trigger row. Its third concern about final materialization was disproved by executable source: the global path already passes `exact_taker_shares/limit`, rejects quantization changes, re-sweeps exact shares, and verifies final-intent equality. The corrective slice removes the unsafe prefix gate rather than weakening execution.

## Slice B20 -- Carry forced recapture through the producer candidate gate

- B19 live proof: exact SHA `9dd29aba3` loaded and the deploy guard expired. The selected Wellington winner reached the new forced path, but the outer observer reported `status=refreshed` while the producer reported `selected_token_already_fresh=2`, `attempted=0`, `inserted=0`, `prefetched_orderbook_count=0`. Actuation therefore still compared the new global book to the old persisted curve and correctly rejected `exact_order_drift=cost,max_spend`. All five order-proof counters remained unchanged.
- Root cause: B19 bypassed the observer's family and outcome freshness filters, but `refresh_executable_market_substrate_snapshots` independently applies a selected-token freshness skip and hydrates local feasibility before network prefetch. The force identity stopped one layer too early, and the outer `refreshed` label did not prove a network recapture.
- Minimal repair: pass the exact forced condition set into the producer. Only those conditions may bypass `selected_token_already_fresh`, and they must bypass local feasibility hydration so both native books come from the CLOB network prefetch. The producer returns the exact forced-condition count independently of whether an old row was fresh; the callback then requires that scope plus real prefetch, attempts, and inserts for both sides. Ordinary callers retain every cache skip.
- Files authorized: `src/data/market_scanner.py`, `src/data/substrate_observer.py`, `src/events/reactor.py`, narrow producer/observer tests, and this packet.
- Forbidden: broadly disable cache freshness; treat a status label as I/O proof; reuse price-channel feasibility as FC-03; change probability, selection, sizing, YES/NO, or submit gates.
- Acceptance: an already-fresh exact condition produces two forced candidates, two network-prefetched native books, and two new snapshot rows; missing/locked/zero-attempt capture fails closed; ordinary fresh conditions remain zero-attempt skipped; official deployment advances past old-DB drift or yields the next typed economic/execution verdict.
- Critic correction: the first B20 callback incorrectly required two `forced_selected_token_recapture` override facts. Those facts exist only when old rows were already fresh; stale rows legitimately produce zero overrides. The corrected proof requires `forced_condition_count == exact requested scope` plus `status=refreshed`, `attempted>=2`, `inserted>=2`, and `prefetched_orderbook_count>=2`, so both fresh and stale prior states prove real recapture without false-green lock/zero-attempt results.
- Final verification: both-stale, one-fresh, and both-fresh producer cases pass; winner sibling targeting, full-curve argmax preservation, JIT book binding, exact taker materialization, and global auction suites pass `268/268` with 14 existing NumPy warnings. Compilation and `git diff --check` pass. Independent final re-review returned `GO`; the only residual is future-only multi-condition force aggregation, while the live global-winner call is exactly one condition.

## Slice B21 -- Use the existing bounded Gamma concurrency ceiling

- Current live proof: four consecutive complete 42-family epochs spent 5.212s, 5.833s, 7.423s, and 5.244s in `token_bind`, with 880--902 metadata rows. The caller requests only 8 workers although the binder already caps and supports 16. Total `process_pending` time ranged from 27.126s to 62.052s; the selected family changed between the target claim and the next global auction, producing repeated `GLOBAL_REAUCTION_WINNER_AWAITS_CLAIM` and zero submit attempts.
- First-principles invariant: independent Gamma family reads are required current metadata, but their wall time is the maximum completion wave, not the sum of avoidable eight-request waves. Increasing the already-bounded fan-out changes no fact, ordering, freshness, probability, book, sizing, claim, or submit semantics.
- Minimal repair: request the binder's existing maximum of 16 workers from the live global-book path. Keep one request per unresolved family, the same timeout, all-result requirement, conflict checks, mutable metadata reads, current CLOB capture, and winner JIT.
- Files authorized: `src/engine/event_reactor_adapter.py`, the existing global-auction seam tests, this packet, and its scope sidecar.
- Forbidden: cache active/closed/accepting-orders state, extend metadata freshness, accept a partial Gamma result, change the global feasible set, alter retry/claim behavior, or add a venue side effect.
- Acceptance: the 42-family synthetic fan-out uses no more than 16 workers and materially lowers bind wall time versus 8 workers; global-auction behavior tests remain green; compilation, planning-lock, and `git diff --check` pass. Deployment and live re-sampling remain operator-only.
- Verification: a five-run 42-task equal-latency fan-out benchmark improved from 0.7449s median at 8 workers to 0.3754s at 16 workers (1.98x). The complete global-auction seam passes `54/54` with 14 existing NumPy warnings; planning-lock, module compilation, and `git diff --check` pass. Ruff has no finding on the B21 line; the concurrently modified full adapter file is not baseline-clean. No deploy or venue action was performed.

## Slice B22 -- Claim the cut-time winner before its JIT in the same epoch

- Current live proof: a targeted Busan winner was legally claimed in the next cycle, but that cycle opened a new complete auction and selected Guangzhou instead. Consecutive cycles therefore spent 27--62s rebuilding the same 42-family epoch, returned `GLOBAL_REAUCTION_WINNER_AWAITS_CLAIM` for all 12 claimed rows, and produced zero submit attempts. This is a structural claim gap, not a lock bounce or missing market signal.
- First-principles invariant: B12 already defines one immutable cut-time global optimum and permits only the selected winner's q/book/free-cash JIT to cross into side effects. A loser update belongs to the next epoch; it must not erase a still-unacted cut-time certificate merely because the winner was outside the queue page. Conversely, no venue call may begin until the exact targeted carrier has a committed processing claim.
- Minimal repair: inject one bounded `claim_unpaged_winner` capability from the reactor into the global batch. Complete the existing side-effect-free whole-universe fence first. Then, in one `BEGIN IMMEDIATE`, validate every current batch claim generation, materialize the deterministic fence winner target, and claim it. Rebind only the actuation's event identity/hash to that target, then continue winner preflight, q/book/cash JIT, one-shot token, and submit. Add every same-epoch target claim to the batch finalize set so any exception or fallthrough is recovered normally.
- Files authorized: `src/engine/global_batch_runtime.py`, `src/events/reactor.py`, `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, `tests/events/test_reactor.py`, this packet, and its scope sidecar.
- Forbidden: submit before target claim commit, reuse a prior winner without current JIT, skip the whole-universe fence, accept a changed winner q/book/cash fact, weaken claim-generation checks, create a second venue submit, change q/FDR/Kelly/risk semantics, or serialize mutable Python authority into DB payloads.
- Acceptance: an unpaged cut-time winner is claimed and reaches JIT in the same epoch; a claim-generation race preserves the old await-next-claim behavior with zero venue calls; an exception after target claim finalizes/requeues that target; loser updates do not force a second global auction before the cut-time winner's JIT; winner drift at the existing fence remains fail-closed/reselected; all receipts cover exactly every committed claim; focused and affected suites, planning-lock, compile, Ruff, and `git diff --check` pass. Deployment remains operator-only.
- Verification: the new antibodies prove fence-before-claim, same-epoch materialize+claim, exact generation-race refusal, post-claim exception recovery, deterministic already-claimed target reuse, economic-identity-specific final target binding, exact receipt coverage, and one venue-call delta. A fence economics change still creates only the final target, eliminating the initial loser claim. Reactor/claim/horizon suites pass `109/109`; global-auction plus live-qkernel suites pass `152/152` with 14 existing NumPy warnings. Planning-lock, compilation, and `git diff --check` pass. Temporary Ruff reports 45 existing/concurrent full-file findings, none on B21/B22 added lines. Cross-module checks have two unrelated existing failures in `src/state/day0_metric_fact_store.py` raw-local-hour lint and monitor stale-edge behavior. No deploy, DB mutation, or venue action was performed.

## Slice B23 -- Capture independent CLOB book chunks concurrently

- Current live proof: complete 35--42 family epochs carry 704--855 native assets, so the 500-token `/books` limit creates two sequential chunks. Initial book capture measured 3.334--3.819s and the fence 3.342--4.787s, consuming 6.7--8.6s per complete epoch after token binding. The 16-family/325-asset path remains one chunk and gains nothing from concurrency.
- First-principles invariant: each chunk is an independent partition of the same required token universe. The epoch is valid only when every token returns inside the existing capture window; parallel observation changes neither the complete set nor any price, metadata, side, fee, tick, minimum, hash, or freshness rule. HTTPX 0.28.1 explicitly permits one `Client` to be shared between threads.
- Minimal repair: add an opt-in bounded `book_fetch_workers` parameter to `capture_current_global_book_epoch`, defaulting to one. Partition tokens exactly as today, submit at most two live chunk reads concurrently, consume results in original chunk order, and retain the same complete-response and elapsed-window checks. The live adapter opts into two workers; all other callers keep sequential behavior.
- Files authorized: `src/engine/global_auction_universe.py`, `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, this packet, and its scope sidecar.
- Forbidden: reduce the token universe, accept partial results, reuse a prior book, extend quote TTL, change chunk size, infer tradability from book presence, change fee/tick/minimum metadata, weaken winner JIT, or create a venue side effect.
- Acceptance: two or more chunks overlap in flight with a maximum of two workers; one chunk remains synchronous; result assets/states/hashes are equivalent; a missing token or raised chunk still rejects the whole epoch; the existing capture window spans all concurrent reads; focused and affected suites, planning-lock, compile, changed-line Ruff, and `git diff --check` pass. Deployment remains operator-only.
- Verification: two-chunk overlap, one-chunk synchronous execution, complete-window clock consumption, raised-chunk rejection, missing-token rejection, and sequential/parallel witness-identity equivalence are covered. A five-run two-chunk equal-latency benchmark improved from `0.2109s` serial median to `0.1059s` with two workers (`1.99x`) while preserving identity. Global-auction and live-qkernel suites pass `155/155`; reactor/claim/horizon suites pass `109/109`; compilation, planning-lock, and `git diff --check` pass. Ruff reports 33 existing full-file findings and none on B23 added lines. No deploy, DB mutation, or venue action was performed; live p50/p99 and monitor cadence remain post-deploy proof obligations.

## Slice B24 -- Collapse the now-side-effect-free duplicate book fence

- Current-path proof: the preflight lane always performs a complete initial book capture and global selection, then unconditionally performs a second complete book capture and selection. B22 moved winner claim after the second selection, so no claim, durable mutation, provisional reservation, or venue call exists between those two epochs; the first winner is discarded. Current 704--855-asset runs therefore pay a redundant 3.342--4.787s book read plus another full ranking before winner JIT.
- First-principles invariant: a global winner needs one complete current probability/book/wealth cut before claim, followed by candidate JIT before submit. It does not need two identical whole-universe cuts with no intervening authority-changing side effect. The single retained cut is the fence: it occurs after family preparation, binds every native YES/NO book, reacquires wealth at selection, and only then permits target claim.
- Minimal repair: in the preflight lane, treat the existing first complete book epoch and selection as the cut-time fence and remove the unconditional second provider call and selection. Preserve the non-preflight path, candidate-local fallthrough, curve-supersession overlay re-auction, exact target claim, JIT binding, and all venue-count guards.
- Files authorized: `src/engine/global_batch_runtime.py`, `tests/integration/test_w3_solve_seam_g3.py`, this packet, and its existing scope sidecar.
- Forbidden: reuse a prior cycle book, skip the complete universe, claim before the retained fence selection, remove winner JIT, accept q/book/wealth drift, weaken candidate-local exclusions, change sizing/risk semantics, or create an additional venue call.
- Acceptance: the preflight lane performs exactly one whole-universe provider call and one initial selection before claim; non-preflight behavior is unchanged; curve overlay and blocked-family fallthrough each trigger only their necessary re-selection; all existing target/fence/JIT/venue antibodies pass; affected suites, planning-lock, compile, changed-line Ruff, and `git diff --check` pass. Deployment remains operator-only.
- Verification: the retained cut is logged as the fence, selects once before target claim, and supplies the exact economic identity used by that claim. Stable preflight uses one book/wealth/selection; curve supersession and blocked-family fallthrough each add only their required re-selection while preserving one whole-universe provider call. Global-auction plus live-qkernel suites pass `155/155`; reactor/claim/horizon suites pass `109/109`; compilation, planning-lock, and `git diff --check` pass. Ruff reports three existing full-file findings and none on B24 added lines. Based on the old loaded daemon's measured second book and selection stages, the removed work was approximately `5.4--7.2s` per complete epoch; this is a pre-deploy structural estimate, not new-HEAD runtime proof. No deploy, DB mutation, or venue action was performed.
- Post-deploy runtime proof: loaded SHA `dce7e022` produced exactly one `book_epoch_fence` and one `select_fence`; no initial/fence duplicate pair remained. A comparable 22--23-family epoch reached the first preflight in `6.973s` versus `11.202s` on loaded B23, a `4.229s` reduction (`37.8%`) at that boundary. This is one observed pair, not a p99 claim.

## Slice B25 -- Stop candidate fallthrough on batch-wide authority loss

- Current live proof: on loaded B24, one 23-family epoch reached its first winner in `6.973s`, then every preflight returned the same batch-wide `live_health_entry_authority`/`entries_paused` failure. Runtime misclassified that as candidate-local, excluded nine families, and repeated full selection nine times for another `8.974s` before action-set exhaustion. No candidate could possibly pass the unchanged global control gate.
- First-principles invariant: candidate-local infeasibility may fall through to the next ranked family; batch-wide authority loss cannot. Re-evaluating another candidate against the same global deny state changes no feasible outcome and only ages data, consumes CPU/DB reads, and delays monitor work.
- Minimal repair: extend `GlobalWinnerPreflight` with typed `BATCH_BLOCKED`. The live adapter emits it only for explicit global submit prerequisites and control-plane prefixes; global batch runtime immediately rejects the epoch fail-closed. Existing `BLOCKED` retains candidate-local fallthrough semantics.
- Files authorized: `src/engine/global_batch_runtime.py`, `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, this packet, and its existing scope sidecar.
- Forbidden: treat family/market/q/book/strategy failures as batch-wide, allow submit after any global block, weaken existing control or health gates, change winner ranking, or alter candidate-local fallthrough.
- Acceptance: one batch-wide block causes exactly one preflight and zero fallthrough selections/venue calls; each claimed receipt carries the same typed batch rejection; candidate-local `BLOCKED` and curve supersession tests remain unchanged; affected suites, planning-lock, compile, changed-line Ruff, and `git diff --check` pass. Deployment remains operator-only.
- Verification: explicit global-control reasons map to `BATCH_BLOCKED`, while candidate-local strategy reasons remain `BLOCKED`; a batch-wide block performs one book cut, one selection, one preflight, zero fallthrough selections, and zero venue calls. Global-auction plus live-qkernel suites pass `162/162`; reactor/claim/horizon suites pass `109/109`; the declared capital evaluator passes `242/242`; compilation and `git diff --check` pass. No push or deployment was performed because the independent T5-core lifecycle merge on the main worktree still lacks its T5+T6 hard-gate completion.
- Verification: explicit adapter classification covers `entries_paused`, `live_health_entry_authority`, missing durable outbox, missing executor, and missing operator arm; a strategy-local block remains `BLOCKED`. The batch antibody proves one book, one selection, one preflight, zero fallthrough, and zero venue calls for `BATCH_BLOCKED`; candidate-local and curve-reauction antibodies remain green. Global-auction plus live-qkernel suites pass `162/162`; reactor/claim/horizon suites pass `109/109`; compilation, planning-lock, and `git diff --check` pass. Ruff reports 35 existing full-file findings and none on B25 added lines. The measured loaded-B24 waste removed by this slice was nine fallthrough selections totaling `8.974s`; live validation awaits a clean exact-HEAD reload.

## Slice B26 -- Pre-aggregate immutable allocator exposure indexes

- Current live proof: loaded B24 selected across 40 families/809 assets in `2.397s`. An equivalent 40-family/320-candidate scorer without the live cap resolver takes `54.6ms` median, while the canonical wealth snapshot hot-read takes `0.87ms` median. The remaining live-only work is `current_global_entry_capacity_usd` per candidate. `RiskAllocator.entry_capacity` currently scans every immutable exposure lot once for market and three more times for event, resolution, and correlation, producing `O(candidates * lots)` work.
- First-principles invariant: an allocator instance is built from one immutable tuple of current lots and one cap policy. Market/event/window/correlation exposure sums are deterministic functions of that tuple and policy, so computing them once at construction is exactly equivalent to re-scanning the tuple for every candidate.
- Minimal repair: build four private aggregate indexes in `RiskAllocator.__init__`; `_market_exposure` and `_remaining_capacity` become constant-time lookups. Preserve active/optimistic/closed predicates, optimistic weighting and rounding, every cap, global state, unknown-side-effect, reduce-only, and submit-time recheck.
- Files authorized: `src/risk_allocator/governor.py`, `tests/test_risk_allocator.py`, this packet, and its scope sidecar.
- Forbidden: cache across allocator refresh generations, omit optimistic exposure, change rounding, combine distinct scope keys, bypass submit-time allocation, or alter cap/kill-switch semantics.
- Acceptance: indexed and reference-scan decisions are field-for-field identical across mixed lot states and all scopes; `with_lots` rebuilds indexes; a production-shaped candidate/lots benchmark materially reduces capacity-query wall time; risk allocator, global auction, reactor, planning-lock, compile, changed-line Ruff, and `git diff --check` pass. Deployment remains operator-only.
- Verification: the indexed allocator matches an embedded reference-scan implementation field-for-field for confirmed, optimistic, exit-pending, settled, missing-key, and correlation-fallback cases; `with_lots` rebuilds and changes capacity. At 1,000 lots and 809 candidate queries, five-run median fell from `267.766ms` to `2.481ms` (`107.9x`). Current canonical lots are 126, so this primarily removes future scale growth rather than claiming a 2-second current-cycle win. Focused allocator/scope tests pass; global-auction plus live-qkernel pass `162/162`; reactor/claim/horizon pass `109/109`; compilation, planning-lock, and `git diff --check` pass. Ruff reports 35 existing full-file findings and none on B26 added lines. The broad allocator file has six unrelated failures introduced by concurrent T5/executor test-contract drift; none reaches the indexed methods or new oracle test.
## Slice B28 -- Require a conservative majority-win terminal-wealth certificate

- First-principles correction: a binary claim has exactly two settlement payoffs: lose `-cost` or win `shares-cost`. For a 13% claim bought at 1 cent, `+$0.12` per share is an expectation, not realized capital gain; the conservative terminal loss probability is 87% and the median payoff is `-$0.01`. Zero-sum fixes the payoff vector but does not erase uncertainty before settlement.
- Objective contract: retain robust expected log growth for multiplicative sizing only after a lexically prior terminal-outcome constraint. A candidate is entry-eligible only when lower-tail-CVaR win probability is strictly above one half, equivalently conservative loss probability is strictly below one half. This is a positive-median-payoff requirement, not a configurable hit-rate heuristic. YES and NO consume the same rule over native payoff-side q; labels receive no waiver.
- Minimal repair: add a typed `ROBUST_MAJORITY_LOSS` rejection, reject before sizing, and bind win/loss probabilities plus the exact 0/1 payoff branches into selection, actuation identity, and the current-state execution receipt. Keep EV only as explicitly diagnostic evidence; never name it realized gain.
- Files authorized: `src/solve/solver.py`, `src/engine/global_single_order_auction.py`, `src/engine/event_reactor_adapter.py`, `src/decision_kernel/canonicalization.py`, narrow solver/integration tests, the two W3 design authorities, this packet, and its scope sidecar.
- Forbidden: delete probability from a pre-settlement decision; treat positive EV as a realized profit; special-case YES or NO; infer a 0.5+ YES probability when current evidence does not provide it; weaken current q/book/wealth/JIT/RiskGuard/venue gates; submit, cancel, or mutate a live DB from this isolated branch.
- Acceptance: an otherwise current 13%-at-1-cent order is rejected specifically for majority loss; a mirrored YES/NO pair at identical payoff q and book receives identical certificates; a qualifying order exposes win/loss probabilities summing to one, exact lose/win payoffs, positive median payoff, and no wording that calls EV capital gain; receipt validation rejects forged or missing certificate fields; affected and declared evaluator suites pass.
- Verification: current 13%-at-1-cent and the exact 0.5 boundary reject as `ROBUST_MAJORITY_LOSS`; 0.500001 passes only when the robust log/EV envelope also passes. A current 13% longshot cannot outrank a 65% native NO order. YES/NO mirror certificates expose identical branch probabilities and exact `lose=-cost`, `win=shares-cost`, positive median payoff. JIT rejects a cut-time 60% decision if the current effective bound falls to 49%. Missing/forged probability, payoff, median, or semantics fields fail the sealed current-state grammar. After clean integration onto mainline `727f9a0a1`, the extended evaluator passes `352/352`; compilation, `git diff --check`, and bounded-file Ruff pass. The two large runtime files retain pre-existing full-file Ruff findings, with none on the added lines. No deployment, DB mutation, or venue action was performed.

## Slice B26 -- Prevent point-estimate family value from vetoing a robust exit

- Current live proof: non-Day0 held positions exposed monitor point probabilities of `0.995586--0.999999997`, while the same current posterior rows carried side-correct lower bounds of only `0.872727--0.942952`. Individual exit evaluation consumes fresh CI and fee/time/crowding-aware hold value, but the later family overlay recomputes `shares * q_point` without those costs and may turn a statistical exit back into a hold.
- First-principles invariant: a lower-authority point expectation is diagnostic evidence only. It cannot veto an already-approved higher-authority CI/cost-aware exit or promote a sale over a robust hold. Either override requires a coherent current family distribution and the same fee/time/crowding objective; this surface has neither.
- Minimal repair: preserve statistical exits after the Day0 maturity gate, preserve robust holds, and stamp any conflicting family point-value comparison as a diagnostic counterfactual with no actuation authority.
- Files authorized: `src/engine/cycle_runtime.py`, `tests/test_live_safety_invariants.py`, this packet, and its existing scope sidecar.
- Forbidden: weaken Day0 maturity, quote freshness, exit evidence, or RED/ORANGE behavior; invent q bounds from entry width; change entry selection or venue actuation; treat point probability as certainty.
- Acceptance: family point value cannot suppress a statistical exit or promote a sale over a robust hold; receipts name diagnostic-only authority, preserve the robust reason, and expose the conflicting point suggestion; Day0 maturity behavior remains unchanged; affected tests, the capital evaluator, compilation, planning-lock, and `git diff --check` pass. No deployment until the main worktree's independent T5+T6 hard gate is complete.
- Verification: the existing robust HOLD/EXIT decision remains the sole actuator; family point value is receipt-only in both directions. The receipt carries the evaluated leg's side-correct current `q_lcb/q_ucb` and `shares * q_lcb`, refuses a sibling's transient bound, clears a missing next-cycle CI instead of reusing it, and proves the same rule for `buy_yes` and `buy_no` at a live-shaped `q_point=0.999999997`. Focused tests pass `15/15`, live-safety passes `188/188`, and the declared capital evaluator passes `235/235`; compilation and `git diff --check` pass. Full-file Ruff reports 17 pre-existing findings and none on B26 added lines. No deploy, DB mutation, or venue action was performed; mainline T5+T6 remains a separate deployment blocker.

## Slice B27 -- Value robust HOLD at the current held-side lower bound

- Current-path proof: `Position.evaluate_exit` arms its CI gate from the current held-side bootstrap interval, but then compares executable sale proceeds with `shares * q_point`. A live-shaped held leg carried `q_point=0.999999997246481`, current `q_lcb=0.942952047639557`, and an executable bid near `0.95`; point value therefore forces HOLD even though the same current posterior's conservative terminal value is below executable sale value.
- First-principles invariant: HOLD and SELL are competing capital actions over the same current information cut. When the current held-side CI is valid, robust HOLD value is the lower confidence bound of terminal wealth, with the same fee, time, and correlation-crowding costs as the sale alternative. YES and NO require no branch: both inputs are already expressed as held-side probability. A point estimate cannot stand in for certainty, and a missing/malformed CI cannot be reconstructed from price, entry width, labels, or an older cycle.
- Minimal repair: inside the current-CI decision lane, bind every HOLD/SELL comparison to the finite in-range current held-side lower bound and record that probability basis. Preserve legacy no-CI behavior unchanged; reject malformed current CI as unavailable evidence instead of silently substituting the point probability.
- Fee ownership correction: immediate SELL cash is `shares * (bid - taker_fee)`; hold-to-settlement is `shares * q_lcb - time_cost - correlation_crowding`. Charging the immediate taker fee to HOLD and comparing against gross SELL reverses decisions near the boundary, so all three portfolio exit seams reuse one comparison with the fee assigned only to SELL.
- Lifecycle ordering: RED and independent whale/flash/vig exits retain priority. A malformed current CI cannot suppress those authorities; near settlement keeps the legacy safety exit when no valid current CI exists, while a valid current cut obeys strict net-SELL versus robust-HOLD dominance. Day0 robust sell dominance must pass its existing consecutive-confirmation gate and emits the maturity registry's existing `EDGE_REVERSAL` trigger for both YES and NO.
- Files authorized: `src/state/portfolio.py`, `tests/test_live_safety_invariants.py`, this packet, and its scope sidecar.
- Forbidden: complement price instead of probability; derive a lower bound from `q_point`, entry CI width, or a stale receipt; weaken quote freshness, Day0 maturity, risk, lifecycle, or venue gates; claim guaranteed profit or deploy before the independent T5+T6 hard gate closes.
- Acceptance: a live-shaped near-one YES leg and the identical held-side NO leg both sell when net executable value exceeds `q_lcb`-based robust HOLD value after configured fee/time/crowding costs; a valid bound that still dominates sale continues to HOLD; fee-boundary decisions charge SELL exactly once; missing current CI preserves the existing no-CI path without fabricated or stale probability/cost breadcrumbs; malformed CI fails closed without suppressing independent exits; Day0 preserves consecutive confirmation and maturity authority symmetrically; focused state/exit/live-safety tests, the declared capital evaluator, compilation, planning-lock, and `git diff --check` pass; independent review finds no P0/P1.
- Verification: live-shaped YES/NO, ordinary/near-settlement/Day0, fee-boundary HOLD/SELL, malformed/missing CI, stale receipt provenance, and both no-CI-to-q_lcb and q_lcb-to-no-CI confirmation transitions are covered. Live-safety passes `212/212`, HoldValue exit-cost tests pass `40/40`, and the declared capital evaluator passes `235/235` with 42 existing NumPy warnings. The wider lifecycle/exit/Day0 batch passes `225` and has four baseline-reproduced failures (three stale strategy-label expectations and one pre-existing collateral-preflight ordering test); none intersects the B27 diff. Compilation, planning-lock, and `git diff --check` pass. Full-file Ruff reports 19 pre-existing findings and none on added lines. Independent adversarial review found no P0/P1 and returned GO for B27 while explicitly denying whole-portfolio global-optimality or deployment claims. No deploy, DB mutation, or venue action was performed; mainline T5+T6 remains a separate hard gate.

## Slice B29 -- Re-auction on submit-time probability tightening

- Adversarial counterexample: a cut-time q of `0.70` can size 80 shares at 0.50 against wealth 100, but a submit-time effective q of `0.55` changes robust log growth to approximately `-0.0448`. Majority-win and positive unit edge still pass, so rewriting only the terminal probability/EV while retaining cut-time shares, payoff branches, and delta-log can submit a negative-growth stale order.
- First-principles invariant: probability, book, wealth, sizing, and global winner identity are one optimization cut. A lower submit-time q changes both the candidate's optimum size and its ordering against every other family and cash; local certificate rewriting cannot prove the old winner remains globally optimal. The current certificate must therefore be discarded and globally re-auctioned, not locally resized in isolation or submitted with stale utility.
- Minimal repair: after preserving typed majority-loss and non-positive-edge reasons, reject any effective payoff `q_lcb` strictly below the cut-time terminal q with `GLOBAL_CURRENT_STATE_PAYOFF_Q_TIGHTENED_REAUCTION_REQUIRED`. Classify that reason and the tighter q's more specific majority-loss/non-positive-edge reasons as `BATCH_BLOCKED`, so crossing a threshold cannot let the same stale global cut fall through to a runner-up; the recurring event loop then supplies a fresh full-universe re-auction. Exact equality remains admissible; a looser later bound cannot override the cut because the effective q remains the minimum.
- Files authorized: `src/engine/event_reactor_adapter.py`, `tests/engine/test_event_reactor_live_qkernel_gate.py`, this packet, and its scope sidecar.
- Forbidden: reuse cut-time shares/utility under a changed q; resize one candidate without re-ranking the full current universe; loosen q; relabel a stale certificate; bypass JIT book/wealth/RiskGuard/venue gates; deploy or touch live state.
- Acceptance: mirrored YES/NO q tightening above 0.5 fails with the exact re-auction reason, including a concrete negative-log counterexample; an exact current q passes and preserves sealed terminal/payoff identity; majority loss keeps its more specific typed reason; the combined B26+B27+B28+B29 evaluator, receipt/JIT, live-safety, compile, planning-lock, and diff checks pass; independent review finds no P0/P1.
- Verification: exact q passes; q tightening that remains majority/positive-edge emits the re-auction reason, while threshold crossings retain typed majority-loss or non-positive-edge reasons. All three are batch-wide: with a tradeable runner-up, the stale cut performs one selection, one preflight, zero venue calls, and stamps every receipt `GLOBAL_PREFLIGHT_BATCH_BLOCKED:<exact reason>`. The concrete wealth-100, 80-share, q `0.70 -> 0.55` negative-log counterexample and its YES/NO mirror are covered. Combined entry/evaluator/receipt/JIT tests pass `360/360` with 42 existing NumPy warnings; live-safety plus HoldValue pass `244/244`; compilation, planning-lock, AST, and `git diff --check` pass. Full-file Ruff reports 32 pre-existing findings and none on B29 added lines. Independent adversarial review found no P0/P1 and returned GO, with the explicit boundary that synchronous same-call re-auction is not yet implemented. No deployment, DB mutation, or venue action was performed.

## Slice B30 -- Count an entry fill as unprojected exactly once

- Current proof: canonical account equity reports `$428.77` of `unprojected_entry_fill_equity_usd`, while every contributing command already has a `position_current` row. The bucket includes the current Guangzhou entry cost `$29.11`, Wellington's original `$107.58` fill despite only `0.00818` share remaining, and terminal positions. It therefore double-counts already-projected conditional-token wealth.
- First-principles invariant: an entry fill is unprojected only before any canonical lot or position projection exists. Projection state may later become active, pending exit, released, voided, economically closed, or settled; none of those transitions make the original cash-to-token conversion unprojected again. Command `position_id` is the stable identity; venue order id is a fallback because replacement may change the position's current order id.
- Minimal repair: exclude a filled entry command when any `position_lots` row cites its command or any `position_current` row matches its position id or venue order id. Preserve canonical per-trade fill dedup and the genuinely pre-projection bridge.
- Acceptance: multi-fill commands still sum once per trade id before projection; any canonical position phase and any terminal lot state suppress the bridge; the live read-only query drops the false current bucket to zero; RiskGuard and capital evaluator regressions pass. No DB write or venue action is part of this slice.
- Verification: focused bridge antibodies pass `4/4`; full RiskGuard passes `106/106`; the merged terminal-wealth, receipt/JIT, live-safety, and HoldValue set passes `604/604` with 42 existing NumPy warnings. Applying the repaired query read-only to the current canonical trade DB returns `$0.00` instead of the loaded daemon's `$428.77`. Three broader on-chain-bankroll tests fail identically on the pre-change main branch because they still expect the retired trailing-wallet loss basis; they do not reach this query. Compile, planning-lock, and `git diff --check` pass; Ruff is unavailable in the configured virtual environment.

## Slice B31 -- Remove exit telemetry from global entry authority

- Current proof: exact-HEAD daemon `cb17d70ec` has healthy runtime, venue, risk, collateral, forecast, and execution-capability surfaces. New entry is nevertheless globally blocked because `pending_exit_release_loop` reports a Guangzhou exit rejection emitted under an already-superseded deployment mismatch.
- First-principles invariant: an exit incident is evidence about that position's exit lifecycle. It may block or escalate that exit, reserve its exposure, and remain visible to operators; it is not authority to veto an independently executable new entry. Global entry denial belongs only to facts that invalidate every candidate's source, probability, capital, runtime, venue, risk, or submit authority. A true global deployment failure is already covered by `runtime_code` and `process_code`, so coupling the exit telemetry adds no safety invariant.
- Minimal repair: keep `pending_exit_release_loop` in composite live health and operator diagnostics, but remove it from `_ENTRY_LIVE_HEALTH_REQUIRED_SURFACES`. No event is deleted, aged out, rewritten, or relabeled. Recent exit failures remain DEGRADED telemetry while independent entry evaluation proceeds through all real global gates.
- Acceptance: a composite failing only on pending-exit telemetry does not block ENTRY; runtime/venue/risk/probability/execution failures still fail closed; exit monitoring and projection tests remain unchanged; no live state or venue action is performed by the code change.
- Verification: exit-only degradation passes entry authority while `runtime_code` degradation still returns `failing_surfaces=runtime_code`; focused authority/global-batch tests pass `9/9`. The broader money-path, run-mode, global-auction, and qkernel batch passes `384` with one baseline-reproduced Day0 fixture failure (`DAY0_ONE_BIN_EDGE_FRAGILE`) on untouched code. The declared capital evaluator passes `254/254` with 42 existing NumPy warnings; compile, planning-lock, and `git diff --check` pass. Aggregate diff from loaded `cb17d70ec` is one runtime-line removal plus tests/docs; B31 cutoff code is fully absent.

## Slice B32 -- Require evidence before portfolio Brier can actuate risk

- Current proof: live `risk_state` is RED and entry capability is reduce-only although daily/weekly realized loss, collateral, portfolio consistency, source, venue, and execution-quality surfaces are healthy. The sole RED input is portfolio Brier `0.815948` from one probability-identity-ready settlement; the same row is already marked `thin_sample_no_verdict=true` at strategy scope.
- First-principles invariant: a score is telemetry; a verdict additionally requires evidence. One Bernoulli outcome cannot prove that every independently evaluated current candidate is unsafe. Runtime learning may use settled outcomes, but a pooled Brier level below the existing minimum evidence floor cannot change RiskGuard behavior.
- Minimal repair: preserve the raw Brier score and raw threshold level, but set the authoritative portfolio Brier level to GREEN while `0 < n < 10`, matching the existing strategy evidence floor. Current realized loss, exposure, collateral, settlement quality, source freshness, venue, and submit authority remain behavior-changing.
- Acceptance: a single high-confidence loss records raw RED telemetry without producing RED risk, reduce-only entry capability, or a strategy gate; at `n=10` the existing Brier thresholds bind unchanged; no DB, process, or venue mutation is part of this code slice.
- Verification: the focused evidence-floor class passes `5/5`, including ticks proving `Brier > 0.8` records raw RED but remains authoritative GREEN at `n=1`, while the same threshold actuates RED at the exact `n=10` boundary. Full RiskGuard passes `108/108`; the declared capital evaluator passes `254/254` with 42 existing NumPy warnings. Eight failures in a wider allocator/invariant/monitor batch are outside the aggregate diff, and representative schema/monitor failures reproduce on untouched live `cb17d70ec`.

## Slice B33 -- Batch current Gamma market metadata by condition

- Current live proof: since loaded SHA `c43565260`, 115 global-auction epochs over about 94 families and 1,704 assets have `token_bind` p50 `6.279s`, p95 `7.606s`, max `9.789s`. It is the largest core stage; `prepare_families` p50 is `4.304s`, `book_capture` `3.910s`, and `select_fence` `0.640s`. The binder issues one Gamma `/events?slug=` request per family even though the current metadata needed for ranking is keyed by condition.
- First-principles invariant: the auction still requires current active/closed/accepting-orders, fee, token, and event identity for every condition in the complete feasible universe. Request count is not authority. Gamma's official `GET /markets` accepts a `condition_ids[]` filter, so the same current facts can be fetched in bounded complete batches without weakening freshness or winner JIT revalidation.
- Minimal repair: add an optional condition-batch reader to `bind_current_global_probability_tokens`; the live adapter chunks at 100 conditions, fetches chunks concurrently, verifies exact condition coverage plus one internally consistent full event payload per family, then feeds the unchanged token/metadata parser. Missing, duplicate, malformed, metadata-conflicting, or HTTP-failed rows reject the whole epoch fail-closed. The existing per-event path remains for callers that do not provide the batch seam.
- Files authorized: `src/engine/global_auction_universe.py`, `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, this packet, and its existing scope sidecar.
- Forbidden: reuse prior-cycle Gamma metadata, accept partial condition coverage, infer missing token or event identity, shrink the global universe, weaken active/closed/accepting-orders/fee checks, skip current book capture, alter ranking/sizing, or bypass winner JIT.
- Acceptance: batch and per-event paths produce identical witnesses and metadata for the same current Gamma payload; one missing condition fails closed without per-event fallback; live code emits at most `ceil(conditions/100)` Gamma requests; global-auction, qkernel, planning-lock, compilation, changed-line Ruff, and `git diff --check` pass. Deployment remains operator-only.
- Pre-change benchmark: live p50 `6.279s` for 94 per-family requests. A read-only Gamma probe returned 100/100 requested conditions in `1.675s`; 1,100 conditions in 11 concurrent 100-condition chunks completed in `1.368s` with 1,056/1,100 current Gamma rows returned, correctly exposing 44 stale/nonexistent conditions rather than fabricating coverage. This is endpoint capability evidence, not post-change runtime proof.
- Verification: batch and per-event fixtures produce identical witness, binding, sample-matrix, event-level `market_end_at`, and metadata identities; missing/malformed/duplicate/unexpected conditions plus conflicting event identity or parent metadata fail closed without per-event fallback. Independent review first found and repaired loss of parent metadata; a second verifier then found that empty Mapping rows and missing/non-list/empty-id parent events were still skipped, allowing sibling metadata to spread across a family. The final parser now requires every requested market to carry exactly one complete, identical parent event and the permanent counterexamples pass. W3/global-auction passes `95/95` with 19 existing NumPy warnings; planning-lock, compilation, bounded-file Ruff, AST parse, and `git diff --check` pass. A post-fix read-only Gamma probe returned 100/100 conditions in `1.677s`, with 10 parent event identities, zero metadata conflicts, and zero malformed rows. One wider qkernel test remains failed on a concurrent strategy price-floor expectation before reaching B33. No deploy, restart, DB write/copy, or venue action was performed.

## Slice B34 -- Vectorize exact Day0 settlement Monte Carlo

- Current proof: the latest 120 live cycles have `prepare_families` p50 `4.615s`, p95 `5.876s`. A production-shaped read-only profile over 117 current families prepared 92 and spent `7.365s/7.951s` in 40 Day0 probability rebuilds; `p_raw_vector_from_maxes -> bin_counts_from_array` executed 400,000 times and consumed about `4.0s`. With the production read transaction active, SQLite execution was only `0.429s`, so DB HWM reads are not the remaining prepare bottleneck.
- First-principles invariant: preserve the exact seeded Monte Carlo experiment: identical normal draw sequence, per-market instrument/residual sigma, `SettlementSemantics.round_values`, inclusive integer-bin membership, count normalization, and output ordering. Do not substitute the analytic diagnostic model, reduce `n_mc`, weaken topology completeness, or change Day0 probability authority.
- Minimal repair: draw the same `n_mc * n_members` normal sequence once into a two-dimensional NumPy array, apply settlement rounding once, flatten the measured outcomes, and count every bin once. The Day0 bootstrap sampler likewise replaces its per-bin scalar count loop with the existing all-bin counter without reordering draws. This removes 10,000 Python loop iterations per family plus repeated per-bin masks while retaining the same simulated sample multiset, RNG order, and integer counts.
- Files authorized: `src/signal/ensemble_signal.py`, `tests/test_ensemble_signal.py`, this packet, and its scope sidecar. Existing B33 files remain authorized for the preceding slice.
- Acceptance: an explicit scalar oracle with independently seeded identical generators is bit-identical to the vectorized function for WMO and non-WMO rounding, residual sigma, incomplete bin masks, and multiple `n_mc` sizes; existing signal/Day0/global-auction suites pass; a fixed-input microbenchmark materially reduces median latency; planning-lock, compilation, Ruff on changed files, and `git diff --check` pass. Deployment remains operator-only.
- Pre-change benchmark: fixed 51-member, complete NYC family, `n_mc=10,000`, eight warm calls: median `0.097996s`, min `0.096479s`, max `0.100532s` per probability vector.
- Verification: the vectorized function is bit-identical to an independently implemented scalar oracle across WMO/floor rounding, residual sigma, incomplete bin masks, and `n_mc` 1/257/503. The same 51-member microbenchmark fell to `0.018644s` median (`5.26x`). A production-shaped current-DB warm preparation pass fell from `5.334s` over 117 families to `1.435s` over the then-current 118-family set (about `73%`, with the one-family scope drift stated); signal/calibration coverage passes `150/150`, Day0-required coverage `105/105`, and W3/global-auction `95/95`. Compilation, bounded-file Ruff, planning-lock, and `git diff --check` pass. This is local/read-only evidence; the loaded daemon remains on old code.

## Slice B35 -- Fill the existing CLOB book-chunk I/O window

- Current proof: the latest 120 loaded-runtime cycles have `book_capture` p50 `4.295s`, p95 `5.678s`, max `9.465s`. Current complete auctions carry about 2,046 native YES/NO tokens, so the unchanged 500-token `/books` contract produces five independent chunks while the live path permits only two in flight. A production-shaped read-only probe returned the identical complete 2,046-token set with two workers in `3.690s` median and four workers in `2.153s` median, a `41.7%` reduction.
- First-principles invariant: all book chunks are independent partitions of one mandatory token universe. The epoch remains valid only after every requested token returns inside the same end-to-end capture window; concurrency changes neither prices nor metadata, fee, tick, minimum, hash, freshness, ranking, sizing, or winner JIT semantics.
- Minimal repair: raise the existing bounded `book_fetch_workers` contract from two to four and let the live adapter request four workers. Preserve the 500-token chunk size, one-chunk synchronous path, complete-response check, capture-window check, current Gamma overlay, and all downstream fail-closed gates.
- Files authorized: `src/engine/global_auction_universe.py`, `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, this packet, and its existing scope sidecar.
- Forbidden: shrink or sample the token universe, accept partial or stale books, reuse a prior epoch, extend quote TTL, alter CLOB timeout or retry behavior, infer tradeability from book presence, change ranking/sizing, or create a venue side effect.
- Acceptance: four chunks overlap with at most four workers; one chunk remains synchronous; parallel and sequential asset state plus witness identity remain equal; missing or raised chunks reject the entire epoch; focused and affected suites, planning-lock, compilation, changed-line Ruff, and `git diff --check` pass. Deployment and live re-sampling remain operator-only.
- Verification: the four-chunk barrier reaches exactly four concurrent worker threads, the one-chunk path remains on the caller thread, workers above four are rejected, and missing/raised chunks plus capture-window and sequential/parallel identity checks all pass. A five-run four-chunk equal-latency benchmark improved from `0.176327s` median with two workers to `0.090940s` with four (`1.94x`). The current 2,046-token read-only venue probe retained complete coverage and improved from `3.690s` to `2.153s` median (`41.7%`). W3/global-auction passes `95/95`; compilation, bounded-file Ruff, planning-lock, and `git diff --check` pass. No deploy, restart, DB write/copy, or venue action was performed.

## Slice B36 -- Drive unready-FSR maintenance from the active queue

- Current proof: the loaded daemon spends a stable `6.15--6.32s` in `pending_prune` and repeatedly exhausts its six-second maintenance budget while expiring zero rows. A current canonical-DB read-only profile isolated the active-RMF gate at `15.338s` and its candidate read at `2.097s`; SQLite chose an event-type expression index, scanned historical FSR facts, then probed processing by PK merely to find 815 active rows. The actual latest-posterior and raw-member reads together took about `23ms`.
- First-principles invariant: unready-FSR pruning is defined over this consumer's mutable `pending` set, not over historical immutable event volume. The same exact event IDs, causal snapshots, family keys, latest posterior, and member-count rules must be evaluated; only the join driver may change.
- Minimal repair: require both active-count and candidate reads to start from the existing `idx_opportunity_event_processing_status` index on `(consumer_name, processing_status, updated_at)`, then join each active row to the immutable event PK. No schema, cadence, threshold, status transition, forecast authority, or fail-soft behavior changes.
- Files authorized: `src/events/reactor.py`, `tests/events/test_continuous_redecision_emit.py`, this packet, and its existing scope sidecar.
- Forbidden: skip an active pending event, inspect fewer families, cache forecast readiness, change event status or retention semantics, weaken replacement spine readiness, mutate schema/config/live DB, or create a venue side effect.
- Acceptance: production-shaped `EXPLAIN QUERY PLAN` uses processing status then event PK; current read-only timings become proportional to the active queue rather than historical FSR count; expiry/keep/chunk/gating semantics remain covered; affected event suites, planning-lock, compilation, Ruff, and `git diff --check` pass. Deployment and live re-sampling remain operator-only.
- Verification: on the same current canonical DB and 821 active rows, the active gate fell from `15.338s` to `0.0032--0.0062s`; the full candidate read fell from `2.097s` to `0.0049--0.0059s`. `EXPLAIN QUERY PLAN` now reports `idx_opportunity_event_processing_status` followed by `sqlite_autoindex_opportunity_events_1`. After the live queue naturally drained to 115 rows, nine warm read-only passes measured `0.69ms` median for the gate and `0.88ms` for candidates. Event/redecision/prune coverage passes `113/113`, reactor prune-focused coverage `2/2`, planning-lock, compilation, and `git diff --check` pass. Full-file Ruff has seven pre-existing findings outside B36 lines and none on the two SQL or added test lines. No deploy, restart, DB write/copy, schema change, or venue action was performed.

## Slice B37 -- Stop repeating a converged legacy processing-row repair

- Current proof: the latest 120 loaded-runtime `repair_missing_processing_rows` sweeps have p50 `2.435s`, p95 `3.256s`, max `3.882s`, and all 120 repaired zero rows. Source search finds one runtime writer for `opportunity_events`: `EventStore.insert_or_ignore`, which writes the immutable event and its mutable processing row on the same SQLite connection before the caller can commit. The repair exists for older writer bugs, not current steady-state event production.
- First-principles invariant: recovery must run until the bounded historical window is proven free of missing rows; after that proof, repeating the identical anti-join cannot discover a new orphan created by the sole atomic writer. Process restart must discard the proof and recheck; any repair exception or positive repair count must keep recovery active.
- Minimal repair: add one process-local convergence latch around the existing repair call. Start false, continue scanning after every positive repair batch, set true only on an exact zero result, skip later steady-state calls, and naturally reset on daemon restart. Preserve the 48-hour window, event-type filter, batch limit, inserted processing status, and all surrounding prune ordering.
- Files authorized: `src/events/reactor.py`, `tests/events/test_continuous_redecision_emit.py`, this packet, and its existing scope sidecar.
- Forbidden: delete the repair method, persist the latch across process restarts, converge after an exception or positive repair, change event insertion/transaction semantics, skip current pending events, alter DB schema/config, or create a venue side effect.
- Acceptance: zero converges and later calls do no DB work; positive batches continue until a later zero; exceptions remain retryable; restart/reset re-enables the exact existing repair; prune ordering and all event-store/redecision suites remain green; planning-lock, compilation, changed-line Ruff, and `git diff --check` pass. Deployment remains operator-only.
- Verification: direct transition tests prove zero converges, positive counts continue, and an exception remains retryable. Event/redecision/prune coverage passes `115/115`; W3/global-auction passes `95/95`; compilation and `git diff --check` pass. Full-file Ruff retains seven pre-existing findings outside the changed lines. No deploy, restart, DB write/copy, schema change, or venue action was performed.

## Slice B38 -- Remove completed terminal orders from the live recovery read set

- Current proof: the current canonical trade DB gives `live_tick` recovery 41 point-order IDs. Nineteen are positions already in a hard-terminal phase and already carry one or more positive durable `venue_trade_facts`; all nineteen still enter matched-order priming, and recent runtime logs repeatedly issue `get_order` for them. The candidate builder also executes `_fill_trade_fact_count` once per candidate, then ranks all 35,651 historical order facts for 921 commands even though only eligible recovery command states can produce a result.
- First-principles invariant: a hard-terminal position plus a positive durable fill fact is completed local recovery evidence for a high-cadence tick. Re-reading that historical order cannot reopen, settle, or improve the terminal position. Missing positions, open phases, review-required commands, terminal commands without a positive durable fact, and partial remainders remain unresolved and must keep their current recovery behavior. Full and restart reconciliation remain unchanged.
- Minimal repair: first scope canonical order-fact ranking to commands in the existing recoverable state/intent set, then fold positive-fill existence and current position phase into that query, eliminating its per-row SQL. Add a `live_tick`-only filter for candidates whose position phase is `settled`, `voided`, or `admin_closed` and whose positive durable fill fact already exists. Use the exact same filter for snapshot priming and apply so no required key can be omitted from the captured venue snapshot.
- Files authorized: `src/execution/command_recovery.py`, `tests/test_command_recovery.py`, this packet, and its scope sidecar.
- Forbidden: interpret an empty or failed venue response as absence; skip `economically_closed`, `pending_exit`, `active`, `day0_window`, missing-position, review-required, zero-fill, or partial-remainder work; change command/lifecycle state; weaken full/restart reconciliation; write the live DB; or perform any venue side effect.
- Acceptance: candidate construction performs one command-scoped query rather than ranking unrelated order history plus one fill-count query per row; live-tick priming and apply both omit only positive-fact hard-terminal candidates; full/boot behavior still includes them; open/review/missing-fact candidates remain included; focused recovery tests, planning-lock, compilation, changed-line Ruff, and `git diff --check` pass. Deployment and live re-sampling remain operator-only.
- Verification: on the current canonical DB, matched candidates remain `20` for full reconciliation but fall to the one runtime-open EXIT for `live_tick`; the nineteen skipped rows are all `settled/voided` with positive durable trade facts. Candidate median fell from about `212ms` to `29.1ms`, and SQL trace contains no per-row fill-count query. Missing-position, missing-trade-fact, review-required, `economically_closed`, and `day0_window` counterexamples remain included.

## Slice B38.1 -- Prime only venue reads consumed by the live recovery apply set

- Current proof: after B38, current `live_tick` priming still returns 21 order IDs. Sixteen come only from `_active_venue_command_priming_rows`; every one is a `FILLED EXIT` whose position is already `settled` or `voided`. None belongs to the exact local-orphan, terminal-point, matched-fact, filled-entry-projection, or partial-remainder candidate sets, and no live-tick apply pass can request it. The broad active query itself costs about `216ms` median before causing sixteen unused point reads.
- First-principles invariant: snapshot priming is a dependency closure over actual apply candidates plus commands that an earlier pass can turn into a later candidate in the same cycle. `ACKED`, `POST_ACKED`, and `PARTIAL` therefore need transition-closure priming even before their later fact/projection exists. A `FILLED` command cannot re-enter those command states; it should be primed only when an exact filled-entry or remainder candidate consumes it.
- Minimal repair: for `live_tick`, narrow broad active-command priming to `ACKED`, `POST_ACKED`, and `PARTIAL`, then continue harvesting every exact client-taking pass candidate, including partial remainders and runtime-open filled ENTRY rows. Preserve broad boot/full behavior and every apply function unchanged.
- Files authorized: `src/execution/command_recovery.py`, `tests/test_command_recovery.py`, this packet, and its scope sidecar.
- Forbidden: remove transition-closure or exact candidate harvest, suppress `SnapshotMissError`, change candidate/apply semantics, drop account-level open-order/trade reads, alter boot/full/restart behavior, write the live DB, or perform venue side effects.
- Acceptance: current read-only priming falls from 21 order IDs to five (four partial remainder plus one matched recovery); the sixteen active-only terminal EXIT IDs disappear; a pre-projection ACKED order remains primed; all runtime-open/remainder tests remain included; full recovery suite, planning-lock, compilation, changed-line Ruff, and `git diff --check` pass. Deployment and live re-sampling remain operator-only.
- Verification: current read-only priming falls from `21` order IDs to `5`; the removed sixteen are exactly `FILLED EXIT` commands whose positions are `settled/voided` and which belong to no apply candidate set. The initial broad-removal test correctly failed on a pre-projection ACKED transition; the final implementation retains `ACKED/POST_ACKED/PARTIAL` transition closure while excluding only broad `FILLED` over-priming. Boot/full still include the terminal EXIT fixture.

## Slice B38.2 -- Seek canonical order truth only for live partial candidates

- Current proof: after B39, four live partial-remainder candidates still cost about `242ms` median because their query ranks all 35,651 historical order facts. A read-only keyed-seek prototype returns the identical four fact IDs through `idx_venue_commands_state`, `idx_order_facts_command`, and the fact PK in about `73.6ms` median (`3.3x`).
- First-principles invariant: canonical proof strength is per command. A candidate query needs the strongest/current fact only for commands that satisfy its current state/intent predicates; ranking unrelated commands cannot affect the result. The proof-rank order `600/550/400/300/200/100` and local-sequence tie-break remain exact.
- Minimal repair: extract the existing proof-rank CASE into one SQL-expression helper. Keep the global CTE behavior unchanged for other callers, but let live partial-remainder selection use a correlated indexed seek for each candidate command and join the selected fact by PK.
- Files authorized: `src/execution/command_recovery.py`, `tests/test_command_recovery.py`, this packet, and its scope sidecar.
- Forbidden: change proof ranks, fact authority, tie-breaking, partial/remainder/phase predicates, full/boot/restart queries, command state, lifecycle, or venue behavior; add schema/index; write the live DB; or perform venue side effects.
- Acceptance: keyed and global reducers select identical facts across stronger-earlier/weaker-later histories; current read-only partial query materially improves; live priming remains the exact five-ID union; full recovery suite, planning-lock, compilation, changed-line Ruff, and `git diff --check` pass. Deployment and live re-sampling remain operator-only.
- Verification: current read-only partial selection remains exactly four rows while median falls from `241.6ms` to `83.1ms`; total live priming reaches `111ms` median versus about `507ms` before B39/B40. The permanent stronger-full-match/later-RESTING counterexample proves the keyed seek retains canonical proof authority. Recovery passes `243/243`; snapshot-contract coverage passes `37/37`; planning-lock, compilation, and `git diff --check` pass. Ruff reports 13 pre-existing full-file findings and none on changed lines. No deploy, restart, DB write/copy, schema change, or venue action was performed.

## Slice B39 -- Put exact held-position SELL and CASH in the global capital auction

- Current proof: the family solver already prices ledger-bound `sell_holding` actions from the held token's native bid ladder, but `PreparedGlobalFamily` drops the holdings snapshot and the cross-family auction materializes only BUY candidates from ask curves. A valid capital-releasing exit therefore cannot compete with BUY YES, BUY NO, or CASH; it is lost before global ranking and later actuation remains entry-shaped.
- First-principles invariant: at one current decision epoch the feasible action set is BUY native YES, BUY native NO, SELL an exact verified native holding, and CASH. CASH has zero incremental log wealth. A BUY pays its current ask-depth cost; a SELL gives up the held claim and receives fee-deducted current bid-depth proceeds. Every non-cash action must bind the same current probability simplex, book epoch, and ledger generation, and only strictly positive robust `delta log wealth` plus positive robust EV may beat CASH.
- Minimal repair: preserve the exact `NativeHoldingsSnapshot` on each prepared family; capture side-native bids beside asks; materialize only full-position SELLs whose exact ledger token/shares fit current bid depth and venue precision; score their two relative-to-HOLD terminal branches with the same lower-CVaR probability tail; compare BUY/SELL decisions in one deterministic global ordering. Keep BUY sizing and its majority-win contract byte-compatible. Before any entry-only gate, route a selected SELL through an independent current probability/wealth/position/BID preflight and the existing reduce-only exit lifecycle with its selected exact minimum limit; persist the capital certificate on `EXIT_INTENT`. Treat every possible FAK fill prefix as executable only after proving positive robust delta-log and EV at each piecewise BID boundary.
- Files authorized: `src/solve/solver.py`, `src/engine/qkernel_spine_bridge.py`, `src/engine/global_auction_universe.py`, `src/engine/global_single_order_auction.py`, `src/engine/global_batch_runtime.py`, `src/engine/event_reactor_adapter.py`, `src/execution/exit_lifecycle.py`, `src/execution/executor.py`, `tests/solve/test_solver_properties.py`, `tests/integration/test_w3_solve_seam_g3.py`, this packet, and its scope sidecar.
- Forbidden: infer a holding from outcome wealth; synthesize NO price as `1-YES`; score a sell from asks; sell more than exact current chain shares; let entry pause or entry health block a reduce-only exit; let a SELL fall into the BUY executor; accept stale probability/book/wealth identity; place, cancel, deploy, restart, or unpause while this slice is under test.
- Acceptance: deterministic counterexamples prove SELL beats a positive BUY when releasing the holding has higher robust capital growth, BUY beats SELL when holding remains superior, CASH beats every non-positive action, YES/NO label mirrors preserve the winner and economics, insufficient bid depth and stale ledger/book identities fail closed, and no SELL can enter the BUY submission path. Entry pause/health never block reduce-only SELL, current position/token/full shares and BID economics are revalidated twice, worse BID curves batch-block without entering the BUY overlay, exact limit is audited and cannot be silently tick-rounded, and every possible FAK prefix remains strictly positive. Focused solver/global-auction/adapter/exit/reactor suites, planning-lock, compilation, and `git diff --check` pass before any deployment decision.

## Slice B40 -- Make recurring command recovery yield on DB contention

- Current proof: the current exact-head recovery sweep completed in about `8.8s` without a lock collision, but current-day logs also contain `database is locked` recovery failures and a historical `process_pending` stage above `113s`. The recurring `live_tick` path takes blocking WORLD+TRADE legacy flocks, then uses SQLite's default `30s` busy timeout, and retries each failed pass after `2s`, `5s`, and `10s`. Excluding the unbounded flock wait, one pass can therefore occupy roughly `4 * 30 + 17 = 137s` before failing.
- First-principles invariant: recurring reconciliation is subordinate to active money-path writes. Its durable inputs remain pending and the scheduler retries them on the next tick, so waiting or retrying inside the same tick cannot create truth; it only occupies an executor, holds writer-intent flocks, ages venue evidence, and amplifies contention. Restart preflight and operator/full recovery retain their existing completion-oriented wait policy.
- Minimal repair: add an opt-in non-blocking mode to the existing canonical WORLD+TRADE flock context; for `live_tick` APPLY connections only, acquire both flocks non-blocking and set `PRAGMA busy_timeout=0`. On the first flock or SQLite contention, record the blocked pass, perform no sleep, skip the remaining APPLY work in that tick, and retry through the next scheduled tick. Read snapshots and venue I/O continue to occur without a writer connection.
- Files authorized: `src/state/db.py`, `src/execution/venue_sync_contract.py`, `src/execution/command_recovery.py`, `tests/execution/test_venue_sync_contract.py`, `tests/test_command_recovery.py`, `tests/test_db_writer_lock.py`, this packet, and its scope sidecar.
- Forbidden: change command/reconciliation grammar, treat contention as success, drop durable work, weaken submit recovery, change non-`live_tick` retry behavior, alter schema/lifecycle, mutate a canonical DB, deploy, restart, or perform a venue action.
- Acceptance: a contended `live_tick` takes one non-blocking writer attempt, sleeps zero seconds, records the exact deferred pass, and performs no later APPLY probes; a free `live_tick` uses a zero SQLite busy timeout; default/full/restart/boot callers remain blocking and preserve the `2/5/10s` retry policy; focused recovery/venue-contract tests, planning-lock, compile, changed-line lint, and `git diff --check` pass.
- Verification: isolated worktree command recovery, venue sync, writer-lock, reactor, and runtime-failure coverage passed `494/494`. After migration, current HEAD passed reactor `83/83`, command-recovery plus venue-sync `302/302`, and writer-lock plus runtime-failure plus architecture contracts `227/227` (`612/612` total across the three batches).

## Slice B41 -- Prevent live money-path SQLite scan stampedes

- Current proof: twelve consecutive exact-head `scope_scan` stages completed in `0.171--0.587s`. The next cycle overlapped the redecision screen, chain mirror, exit monitor, Day0 refresh, and checkpoint jobs; `scope_scan` rose to `54.822s`, the reactor to `323.299s`, redecision to about `332s`, exit monitor to about `305s`, and chain mirror to about `87.5s`. After those jobs drained, later scope scans returned to `0.875s` and `2.395s`. Native sampling showed concurrent SQLite page reads and a default-pool writer waiting in `btreeBeginTrans`; the reactor's scope scan itself contains no executor or write transaction.
- First-principles invariant: current entry selection, price-driven redecision, held-position exit, and ten-minute mirror reconciliation are distinct semantic jobs, but their broad SQLite traversals must not run concurrently against the same world and forecast files. Redecision and mirror are retryable lower-priority scans. Exit monitoring is higher priority and must run after the current reactor without sharing the reactor executor, because its venue HTTP may block.
- Minimal repair: make redecision skip a tick when the entry reactor is active; make chain mirror defer behind reactor, redecision, or held-position monitoring; and make exit monitoring claim its priority Event before waiting up to 30 seconds for the current reactor lock. New reactor ticks then yield to the claimed monitor, while exit venue I/O remains on the default executor and cannot starve the dedicated reactor pool.
- Forbidden: drop durable events, weaken exit/entry/reconciliation truth, run venue side effects from a new path, serialize exit HTTP onto the reactor executor, mark a timed-out monitor as completed, change DB contents, deploy, restart, or issue a venue action.
- Acceptance: overlapping lower-priority scans never enter their bodies; an idle chain mirror still runs; exit claims priority before waiting, runs only after reactor handoff, and clears the claim on timeout; existing continuous-redecision, mirror, exit, runtime-failure, and scheduler contracts remain green.

## Slice B42 -- Stop repeated side-effect-free Window-B lock waits

- Current proof: one live global-batch cycle ended with three `WORLD_WRITE_LOCK_BUSY_POST_SUBMIT` results and nine JIT-refresh deferrals. Window B runs for every receipt, including `submitted=false`, `venue_call_started=false`, and `side_effect_status=NO_SUBMIT`; each lock failure then immediately attempted `requeue_pending` with the same busy timeout and repeated that pair of waits for every claimed receipt.
- First-principles invariant: a receipt whose venue call started is must-settle and outranks all side-effect-free projection work. A `NO_SUBMIT` loser is durable through its processing lease and may retry after contention clears; repeating the same known-busy wait cannot produce new truth. One global batch permits at most one venue call.
- Minimal repair: finalize the actual venue winner first; bound side-effect-free mutex/SQLite acquisition to the existing claim timeout; after the first Window-B contention, leave all remaining `NO_SUBMIT` receipts on their processing leases without another lock attempt; and make the immediate requeue path a zero-wait probe instead of a second full wait. Real or uncertain venue side effects retain the completion-oriented wait path.
- Forbidden: weaken venue-command durability, treat an unknown side effect as `NO_SUBMIT`, reorder more than the one proven global winner, drop an event, change q/book/sizing/risk/submit semantics, mutate a canonical DB manually, deploy, restart, or issue a venue action.
- Acceptance: a venue-started winner finalizes before every loser; a failed winner prevents loser lock churn; a successful winner permits at most one bounded loser wait; the immediate requeue attempt uses `busy_timeout=0`; all skipped events remain `processing` for stale-lease recovery.

## Slice B43 -- Make the exit-monitor handoff gate the reactor

- Current proof: after the coordination code was first loaded, an exit monitor remained active while a new entry reactor still started. The reactor called `_defer_for_held_position_monitor("edli_event_reactor")` twice, but `_HELD_POSITION_MONITOR_DEFER_JOBS` omitted that exact key, so both checks were runtime no-ops.
- First-principles invariant: once exit monitoring claims priority, a new entry cycle may not start a second broad traversal of the same world/forecast/trade state. This is admission control, not a DB lock: the lower-priority job must be rejected before opening connections or reading pages.
- Minimal repair: add the exact reactor key to the existing monitor-defer contract and keep the post-active-lock recheck, so a monitor claim racing between the first check and reactor-lock acquisition still wins. No other targeted EDLI job is newly deferred.
- Acceptance: an active held-position monitor makes the real helper return true for `edli_event_reactor`; source order retains checks both before and after the reactor active-lock claim; exit handoff, chain mirror, redecision, and unrelated run-mode behavior stay green.

## Slice B44 -- Replace majority-loss wall with the positivity objective

- Supersedes Slice B28's fixed-0.5 acceptance criterion: the lower-tail-CVaR win-probability-above-one-half gate that B28 added is removed; the downstream robust Δlog-wealth-and-EV positivity objective is now the sole economic gate.
- Rationale: (1) the Kelly marginal condition is `q > price`, not `q > 0.5`, and the fee-inclusive positive-robust-growth objective already computed in `solver.py` enforces exactly that condition, so the 0.5 wall's only marginal effect was to reject positive-growth sub-majority orders that the positivity objective would already have accepted on their own terms; (2) the known miscalibration class -- tail overconfidence that overvalues high-`q` NO bets -- passes the 0.5 wall untouched because it lives above 0.5, so the wall supplied zero protection against that bias while suppressing the tail-YES region where the same bias is conservative rather than overconfident; (3) the terminal-wealth certificate chain (median-branch coherence in the certificate constructor, the current-state adapter gate, and qkernel canonicalization) is now made coherent for `win_probability_lcb <= 0.5`, so a sub-majority certificate correctly carries the loss-branch median instead of an always-win-branch median that no longer matches the accepted regime.
- Receipt-measured fact: `ROBUST_MAJORITY_LOSS` was 76.1% of candidate-weight rejections in the 2026-07-15 window.
- Minimal repair: delete the `robust_q > 0.5` gate in `_score_global_single_order` (BUY path); make `_binary_terminal_wealth_certificate`'s `median_payoff_usd` branch-coherent (win branch above 0.5, loss branch below, mirroring the existing SELL-side pattern); leave the `ROBUST_MAJORITY_LOSS` taxonomy entry and its all-rejections-collapse branch in place as inert historical-receipt vocabulary since the emission no longer fires; make the current-state adapter's median-payoff validation branch-coherent instead of win-branch-only and delete both of its independent `<= 0.5` hard-probability gates (`GLOBAL_CURRENT_STATE_ROBUST_MAJORITY_LOSS`, renamed at its remaining incoherence-check use to `GLOBAL_CURRENT_STATE_TERMINAL_CERTIFICATE_INCOHERENT`), leaving `GLOBAL_CURRENT_STATE_ECONOMICS_NON_POSITIVE` as the sole downstream positivity gate; make qkernel canonicalization's terminal-probability and terminal-payoff identity checks branch-coherent for `win_probability_lcb <= 0.5` the same way.
- Files authorized: `src/solve/solver.py`, `src/engine/event_reactor_adapter.py`, `src/decision_kernel/canonicalization.py`, `tests/solve/test_solver_properties.py`, `tests/engine/test_event_reactor_live_qkernel_gate.py`, this packet, and its scope sidecar.
- Forbidden: change the positivity objective itself (`robust_delta_log_wealth > 0 and robust_ev_usd > 0`); special-case YES or NO; delete the `ROBUST_MAJORITY_LOSS` taxonomy entry or its historical-receipt collapse logic; submit, cancel, or mutate a live DB from this isolated branch.
- Acceptance: a current 13%-at-1-cent order is accepted with a loss-branch median payoff and positive robust Δlog-wealth/EV; selection among eligible sub- and supra-majority candidates ranks by robust Δlog-wealth, not by majority status; a positivity-boundary test (not a 0.5 boundary test) is strict; a genuinely incoherent certificate (sub-0.5 win probability pinned to a win-branch median) is refused by the renamed adapter check; focused solver, event-reactor qkernel-gate, W3 seam, and decision-kernel suites pass with no new regressions beyond pre-existing baseline failures.
- Verification: `tests/solve/` and `tests/engine/test_event_reactor_live_qkernel_gate.py` pass in full; `tests/integration/test_w3_solve_seam_g3.py` and `tests/decision_kernel/` pass with the same pre-existing failure set as unmodified `HEAD` (four decision-kernel failures and four qkernel-gate live-entry-price-floor failures, none touched by this slice). No deployment, DB mutation, or venue action was performed.

## Slice B45 -- Give one weather family one correlation identity

- Current proof: canonical trade truth carries `$498.589984` of open Seoul 2026-07-16 HIGH exposure across two sibling NO positions. Both share one stable `weather_family_id`, but their 37 submit requests carry 37 event/token-specific correlation keys. The `$500` per-event cap, not the `$1000` correlation cap, stopped further allocation; correlation accounting is therefore not describing the economic family.
- First-principles invariant: correlation identity names the common terminal random variable, not the discovery event, intent, token, or order. Every native YES/NO bin for one `(city,target_date,metric)` family must share the exact stable `weather_family_id`; a refreshed forecast or another sibling token cannot create a new risk bucket.
- Minimal repair: use the candidate `family_key` for global selection capacity; translate final-intent certificates to `FinalExecutionIntent.correlation_key=family_id`; and normalize active position exposure from canonical `city/target_date/temperature_metric`, preferring an explicit stable family id for new/unprojected lots and falling back to legacy allocation identity only when the family cannot be reconstructed. Existing market, event, resolution-window, Kelly, log-wealth, and submit checks remain unchanged.
- Files authorized: `src/engine/event_reactor_adapter.py`, `src/engine/event_bound_final_intent.py`, `src/risk_allocator/governor.py`, `tests/integration/test_w3_solve_seam_g3.py`, `tests/decision_kernel/test_execution_command_certificate.py`, `tests/test_risk_allocator.py`, this packet, and its scope sidecar.
- Forbidden: lower or add a dollar cap, mutate/copy a canonical DB, infer family identity from token price, merge distinct date/metric families, clear `entries_paused`, force an order, weaken Fractional Kelly, or change BUY/SELL/HOLD/CASH ranking.
- Acceptance: sibling YES/NO and refreshed-event candidates resolve to one family key at selection and submit; active legacy positions with fragmented stored keys reload under one reconstructed family key; legacy rows without enough family truth retain their prior fail-safe fallback; allocator capacity counts all sibling exposure together; the declared capital evaluator, allocator and final-intent suites, planning-lock, compilation, and `git diff --check` pass before a paused exact-SHA deploy.
- Verification: canonical read-only `position_current` plus chain-confirmed exposure reload maps both Seoul 2026-07-16 HIGH sibling NO positions to `edli_family_d22dadc28739aab3b940f64e`, with two lots and exact total exposure `$498.589984`. Focused family-selection/final-intent/allocator tests pass `5/5`; the declared capital evaluator passes `586/586` with 46 existing NumPy warnings. The wider allocator/final-intent/W3 batch adds three passing tests and retains the exact eight failures reproduced on detached clean `3c1477c2d`; none intersects this slice. Planning-lock, compilation, and `git diff --check` pass.

## Slice B46 -- Preserve current book proof when the auction has zero evaluations

- Current proof: schema-v12 receipts currently set native-side coverage to `UNAVAILABLE` whenever `candidate_evaluations` is empty, even when the same epoch carries a fresh complete book capture and typed native-side states.
- First-principles invariant: an empty candidate set is an economic result; it is not missing observation. A complete current book capture must persist every native YES/NO state. If any non-excluded `EXECUTABLE` side lacks a BUY evaluation, receipt creation must fail closed instead of erasing the mismatch behind `UNAVAILABLE`.
- Minimal repair: make book capture completeness, not evaluation count, control native-side receipt validation and persistence. Preserve the existing `UNAVAILABLE` shape only when no complete book capture exists.
- Files authorized: `src/engine/global_batch_runtime.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: synthesize evaluations, treat `NO_ASK` as executable, weaken exact key equality, change auction ranking, mutate a canonical DB, clear `entries_paused`, or trigger a venue side effect.
- Acceptance: a fresh book epoch containing only typed non-executable sides persists `COMPLETE` coverage with zero evaluations; a fresh epoch containing an unmaterialized executable side still raises the existing materialization mismatch; focused integration tests, the declared evaluator, planning-lock, compilation, and `git diff --check` pass before paused exact-SHA deploy.
- Verification: the zero-evaluation fresh-book antibody persists two typed non-executable native sides as `COMPLETE`, while the existing executable-side mismatch antibody remains fail-closed. This test is included in the `586/586` declared evaluator result above. Full-file Ruff reports only three pre-existing findings in `global_batch_runtime.py` at untouched lines 884, 885, and 1047.

## Slice B47 -- Keep operator pause outside the economic opportunity set

- Current proof: commit `af00e67c2` moved `entries_paused` ahead of scope/probability/book evaluation, restricted the auction to canonical held families, and skipped the entire scope when no holding existed. The first live process that imported this dirty shape scanned one family, prepared zero, emitted `GLOBAL_AUCTION_NO_CURRENT_PROBABILITY_FAMILY`, and produced no complete post-deploy receipt.
- First-principles invariant: an operator pause governs whether a new-risk BUY may create a venue side effect; it cannot decide which zero-sum opportunities exist or alter the BUY/SELL/HOLD/CASH counterfactual. Selection-time economics remain complete and current, while winner preflight and executor independently block entry actuation.
- Minimal repair: revert only the paused-auction scope pruning and early BUY policy from `af00e67c2`, preserving its history and the independent deploy/watchdog serialization commit. The existing preflight/executor pause authority remains unchanged.
- Files authorized: `src/engine/event_reactor_adapter.py`, `src/engine/global_batch_runtime.py`, `tests/integration/test_w3_solve_seam_g3.py`, `docs/rebuild/schema_packets/w3_solve_design_packet_2026-07-03.md`, and this packet.
- Forbidden: clear or weaken `entries_paused`; force an order; remove reduce-only SELL; reuse stale book/probability truth; mutate/copy a canonical DB; or change robust log-wealth, Fractional Kelly, final-holding, correlation, or exposure-cap law.
- Acceptance: while entries remain paused, the live route still produces one complete current receipt over all executable BUY/SELL/HOLD/CASH candidates, then records a `BATCH_BLOCKED` preflight with venue submit count unchanged. The declared evaluator, planning-lock, compilation, and `git diff --check` pass before exact-SHA restart.
- Verification: the four reverted code/test/doc surfaces are byte-identical to the previously tested `b75d871ee` versions, while later deploy/watchdog serialization remains present. The declared evaluator passes `586/586` with 46 existing NumPy warnings; planning-lock, compilation, staged/unstaged `git diff --check`, and the existing schema-v12 zero-evaluation/current-side antibodies pass.

## Slice B48 -- Bind capital capacity to the decision epoch

- Current proof: after each of two clean live-daemon restarts, the first complete auction receipt rejected every one of 1,619/1,620 BUY candidates as `CAPITAL_CONSTRAINT_UNAVAILABLE` even though the immediately preceding allocator refresh reported configured and an offline read of the same canonical exposure produced `$250` capacity for every candidate. The first immutable-pair deployment reproduced the rejection: it had frozen transient `heartbeat STARTING` actuation state into selection. The next cycle selected the same positive NO candidate normally after heartbeat health advanced.
- First-principles invariant: one auction compares one immutable economic information vector. Selection-time capital authority is current exposure plus concentration caps; transient heartbeat/risk/WS/kill-switch/collateral/pause authority governs JIT actuation, not the economic BUY/SELL/HOLD/CASH ranking. Another thread's later exposure refresh may govern a later epoch, but startup ordering must not erase the current opportunity set.
- Minimal repair: publish an immutable `AuctionCapitalAuthority` containing the refreshed allocator's exposure-and-cap witness only; inject it into the cycle-scoped live adapter; and resolve all BUY capacity from `auction_capacity`. Existing `entry_capacity` and submit-time allocator/risk/heartbeat/WS/collateral/pause checks remain dynamic and unchanged.
- Files authorized: `src/risk_allocator/governor.py`, `src/risk_allocator/__init__.py`, `src/events/reactor.py`, `src/engine/event_reactor_adapter.py`, `tests/test_risk_allocator.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: synthesize capacity, retain a snapshot across reactor cycles, weaken any cap, ignore a failed refresh, clear `entries_paused`, force an order, mutate/copy a canonical DB, or replace submit-time JIT authority with the selection snapshot.
- Acceptance: a captured authority remains internally coherent if the process-global pair changes later; the live adapter uses the injected cycle authority for every candidate; a missing capture fails the live lane closed before auction construction; declared evaluator, focused allocator/global-auction tests, planning-lock, compilation, and `git diff --check` pass; exact-SHA paused deployment produces repeated complete receipts without restart-first-cycle `CAPITAL_CONSTRAINT_UNAVAILABLE` and with zero venue side effects.

## Slice B49 -- Make submit-time capital authority atomic

- Independent verification found a live-money race left outside B48: allocator refresh publishes allocator and governor state under one lock, but executor-facing entry, reduce-only, and order-type readers dereferenced the two globals separately. A concurrent refresh could therefore combine a newly published exposure allocator with an older health state, or the inverse, at the final pre-submit gate.
- First-principles invariant: selection and actuation intentionally use different time authorities, but each authority must be internally atomic. Selection freezes structural exposure/caps for one auction epoch; every submit/JIT check re-reads the latest allocator+governor pair exactly once under the publication lock.
- Minimal repair: centralize current actuation reads in one coherent pair snapshot; route entry allocation, generic submit, current capacity, and order-type selection through it; make the operator summary capture the same pair under the lock. Do not reuse the auction snapshot for submit.
- Files authorized: `src/risk_allocator/governor.py`, `tests/test_risk_allocator.py`, and this packet.
- Acceptance: a deterministic partial-publication counterexample blocks the reader until the complete pair is visible; an auction authority captured before a later exposure increase cannot authorize submit; focused risk/executor tests, the declared evaluator, planning-lock, compilation, and `git diff --check` pass before paused exact-SHA deploy.
- Verification: both new atomicity/current-recheck antibodies pass; the declared capital evaluator passes `589/589` with 47 existing NumPy warnings; planning-lock, compilation, and `git diff --check` pass. Independent read-only verification found no P0/P1/P2 in B49 and confirmed executor submit reads the latest coherent pair. The broader allocator/bridge batch passes 39 tests and has five unrelated stale-test failures (retired DB helper, blocked legacy live intent, newer collateral refresh contract, and two incomplete legacy SQLite schemas); none reaches the B49 readers or new counterexamples.

## Slice B50 -- Isolate probability transitions to their unavailable families

- Current proof: the live auction already excludes families that cannot produce a current decision-time q certificate and receipts every typed reason. A separate `REPLACEMENT_RAW_INPUT_HWM` special case nevertheless rejected the entire auction when any one family was between raw-input publication and posterior materialization, even when other families held current q, current native books, and positive robust capital-growth candidates.
- First-principles invariant: the economic action set contains only claims whose current settlement probability and executable book are both provable at the selection cut. A family without current q is not a zero-valued candidate and must never reuse a stale posterior; it is explicitly outside that cycle's admissible set and is reconsidered on the next reactor cycle. Its temporary authority loss cannot erase independent current opportunities in other families. Global optimality therefore means the best BUY/SELL/HOLD/CASH action over the complete current admissible set, with the full scanned scope and every exclusion persisted.
- Minimal repair: remove only the transition-specific whole-auction rejection. Keep typed current-q preparation fail-closed per family, rebuild the auction scope from prepared families only, persist the full-scope identity plus `probability_ineligible_by_family`, reject when no family has current q, and leave probability, book, wealth, Kelly, concentration, preflight, and executor gates unchanged.
- Files authorized: `src/engine/global_batch_runtime.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: use a prior posterior after an HWM transition, synthesize q, omit an excluded family from the receipt, select when the admissible set is empty, clear `entries_paused`, force an order, mutate/copy a canonical DB, or weaken submit-time authority.
- Acceptance: one transitioning family cannot block a different current-q family from winning; the transitioning family is excluded with its exact typed reason and no stale fallback; an all-transitioning scope selects nothing and performs no venue call; focused global-batch tests, the declared capital evaluator, compilation, planning-lock, and `git diff --check` pass before an exact-SHA paused restart.
- Verification: focused global-batch coverage passes `28/28`; the declared capital evaluator passes `589/589` with 47 existing NumPy warnings; compilation passes. The live process remained on the previous SHA during verification, `entries_paused` remained effective, and venue command/order/trade counts did not change.

## Slice B51 -- Overlap unknown-scope full-book refresh with Gamma binding

- Current proof: loaded-runtime receipts repeatedly took about `5.2--7.4s` in the book stage with `prefetched=False`: Gamma token/metadata binding consumed about `1.8--2.6s`, followed by a serial full CLOB capture of about `3.2--4.9s`. The cache was valid, but `refresh_family_keys=None` means the triggering events cannot be safely narrowed and therefore require a full refresh. That branch fell through without launching the existing speculative full-book fetch.
- First-principles invariant: Gamma tradeability/token binding and public-CLOB depth are independent current authorities. When a full refresh is already required, their I/O should overlap; neither result becomes authority until current Gamma tokens exactly match the prefetched token set and the captured books pass the existing current-epoch validation.
- Minimal repair: treat an unknown refresh scope exactly like other known full-refresh cases when deciding whether to launch speculative book prefetch. Preserve the post-bind token equality check, discard stale token hints on mismatch, and retain the serial authoritative fallback.
- Files authorized: `src/engine/event_reactor_adapter.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: reuse stale books, accept last-seen tokens as authority, skip Gamma binding, weaken freshness, batch stale orders, clear `entries_paused`, force an order, or mutate/copy a canonical DB.
- Acceptance: with a hot cache and unknown refresh scope, full-book I/O starts before Gamma completes and the matching current token set is consumed at its original capture cut; a stale speculative hint remains discarded and current Gamma tokens are fetched authoritatively. Focused W3 coverage, the declared evaluator, planning-lock, compilation, and `git diff --check` pass before a paused exact-SHA deploy.
- Verification: focused speculative-refresh and transition coverage passes `12/12`; the declared capital evaluator passes `589/589` with 47 existing NumPy warnings; compilation and `git diff --check` pass. No canonical DB was copied or mutated, `entries_paused` remained effective, and venue command/order/trade counts remained unchanged during verification.

## Slice B52 -- Keep collateral allowance out of auction cash endowment

- Current proof: canonical collateral rows at `19:16:42Z`, `19:17:12Z`, and `19:17:42Z` held the same `$1415.214725` pUSD balance while allowance moved `max -> 0 -> max`. Receipt `32447` consumed the middle row and rejected all `1618` BUY candidates as `CAPITAL_CAPACITY_EXHAUSTED`; the next cycle recovered without any balance or exposure change.
- First-principles invariant: pUSD balance is owned selection-time capital; ERC20 allowance is revocable submit-time permission. A permission read cannot erase the economic opportunity set. Every BUY may be ranked from current balance, probability, book, Kelly, and structural caps, while the executor must synchronously refresh allowance and fail closed against the exact quantized order notional before command persistence or SDK contact.
- Minimal repair: build `PortfolioWealthWitness.spendable_cash_usd` from current pUSD balance rather than `min(balance, allowance)`. Keep allowance in the bound ledger generation and leave the existing JIT collateral refresh/preflight unchanged.
- Files authorized: `src/engine/global_auction_universe.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: reuse allowance to authorize a side effect, ignore insufficient balance, count legacy USDC.e as pUSD BUY cash, weaken reservations/in-flight ambiguity, clear `entries_paused`, force an order, or mutate/copy a canonical DB.
- Acceptance: a zero-allowance/current-positive-balance witness preserves the owned pUSD cash endowment; executor tests prove a fresh zero allowance forces synchronous refresh and a still-insufficient result blocks before side effects; focused W3/collateral tests, the declared evaluator, planning-lock, compilation, and `git diff --check` pass before a paused exact-SHA restart.
- Verification: current-wealth focused coverage passes `11/11`; collateral refresh/preflight coverage passes `76/76`; the declared capital evaluator passes `588/588` with 47 existing NumPy warnings. Planning-lock, compilation, and `git diff --check` pass; no DB mutation/copy, pause change, or venue side effect was performed.

## Slice B53 -- Restore symmetric priced entry admission

- Current proof: post-deploy receipt `32464` contains a complete 1,618-candidate auction over 1,738 native side states. All 869 executable YES candidates were rejected as `ROBUST_MAJORITY_LOSS`; every one of the 14 scored/selected candidates was NO. Source blame shows commit `eb0643871e` reintroduced `robust_q > 0.5` in the solver and two matching submit gates after `8c36a93af` had removed them. Separately, the current-state path bypassed `forecast_qkernel_entry.min_entry_price=0.10`, allowing the complained-of 0.004/0.01 longshot class into global ranking.
- First-principles invariant: a binary claim's Kelly marginal condition is conservative win probability greater than its fee-inclusive executable unit cost, not greater than 0.5. The current owner strategy first defines the admissible native price set; inside it, CASH beats a candidate unless exact current terminal wealth has strictly positive robust delta-log wealth and strictly positive robust EV. YES and NO are labels over native claims; the same strategy floor, cost, q, payoff, depth, wealth, and Fractional Kelly equations govern both.
- Minimal repair: remove only the three price-independent 0.5 admissions; restore branch-coherent median payoff certificates and canonicalization; bind each prepared family to its current owner event and apply that strategy's `min_entry_price` to the current native best ask before scoring. Retain current q, current native book, positive edge, positive robust objective, Fractional Kelly final-holding target, concentration caps, full BUY/SELL/HOLD/CASH comparison, and JIT probability/book/wealth recapture.
- Files authorized: `src/solve/solver.py`, `src/solve/AGENTS.md`, `src/engine/event_reactor_adapter.py`, `src/decision_kernel/canonicalization.py`, `architecture/strategy_profile_registry.yaml`, `docs/rebuild/order_engine_first_principles_design_2026-07-02.md`, `docs/rebuild/schema_packets/w3_solve_design_packet_2026-07-03.md`, `tests/solve/test_solver_properties.py`, `tests/engine/test_event_reactor_live_qkernel_gate.py`, `tests/integration/test_w3_solve_seam_g3.py`, and this packet.
- Forbidden: special-case YES or NO, let a strategy-subfloor BUY enter scoring, weaken positive robust growth, remove minimum-size/depth/Kelly gates, use stale probability or books, clear `entries_paused`, force an order, mutate/copy a canonical DB, or call expected value realized profit.
- Acceptance: a current 13% claim at the live strategy floor is admitted only when positive under the production robust objective and receives a loss-branch median; a 0.004 BUY is rejected before ranking for both native labels while reduce-only SELL remains eligible; the winner between sub- and supra-majority admissible claims is determined by the global capital objective; the strict economic boundary is robust positivity rather than q=0.5; both native labels pass the same JIT grammar; an incoherent sub-majority win-branch median fails closed. Focused solver/adapter/canonicalization tests, W3 relationship coverage, the declared evaluator, compilation, and `git diff --check` pass before a paused exact-SHA restart.
- Verification: full SOLVE coverage passes `212/212`; current-state JIT/certificate coverage passes `132/132`; W3 global-auction relationship coverage passes `142/142`; the declared capital evaluator passes `589/589` with 47 existing NumPy warnings. Compilation and `git diff --check` pass. No canonical DB was copied or mutated, `entries_paused` was not changed, and no venue action was performed.

## Slice B54 -- Resolve posterior member coverage by exact current carrier IDs

- Current proof: complete current posterior provenance already binds the exact `raw_model_forecast_ids` that produced the served q. The event trigger nevertheless falls back to a family/cycle `COUNT(DISTINCT model)` scan, which can count unrelated rows and repeats a broader query than the certificate requires.
- First-principles invariant: the probability carrier identity is the exact causal member set, not every row sharing city/date/metric/cycle labels. Resolve those IDs directly at the decision cut; use the family scan only when exact provenance is absent or cannot be verified current.
- Minimal repair: parse the certified raw IDs once, batch one bounded `IN (...)` lookup against current `source_available_at`, and reuse exact counts per posterior row. Preserve the existing fallback and fail-closed missing-count behavior.
- Files authorized: `src/events/triggers/forecast_snapshot_ready.py`, `tests/events/test_forecast_snapshot_ready.py`, `architecture/test_topology.yaml`, and this packet.
- Forbidden: treat provenance count alone as current availability, count unrelated family rows, bridge a missing exact ID with stale-as-fresh data, mutate/copy a canonical DB, clear `entries_paused`, or change q.
- Acceptance: complete exact-ID provenance counts only the referenced current rows and performs no family-wide count scan; missing/partial exact IDs retain the existing current family fallback or typed block; the full trigger suite, declared capital evaluator, compilation, planning-lock, and `git diff --check` pass before a paused exact-SHA restart.
- Verification: the complete trigger suite passes `30/30`, including an exact three-ID posterior whose sibling family row is not counted and whose SQL trace contains no family-wide `COUNT(DISTINCT model)` scan. The declared capital evaluator passes `589/589` with 47 existing NumPy warnings; EventStore plus RiskGuard passes `147/147`; compilation, planning-lock, and `git diff --check` pass. No canonical DB was copied or mutated, `entries_paused` was not changed, and no venue action was performed.

## Slice B55 -- Rebuild a stale taker plan as a fresh passive maker

- Current proof: live repeatedly selected positive robust-utility winners with `STABLE` preflight, then requeued them as `SUBMIT_ABORTED_MODE_FLIPPED:proof_mode=TAKER:fresh_mode=MAKER`; no venue command was persisted after `2026-07-22T07:14:24Z` despite continued current winners. The same submit validator already rebuilt `MAKER -> TAKER`, but terminally discarded the safer `TAKER -> MAKER` transition.
- Invariant references: preserve INV-24's executor-only venue gateway, INV-25's no-submit-on-failed endpoint preflight, and INV-28's command/event persistence before every side effect. This slice harmonizes the existing `SUBMIT_ABORTED_MODE_FLIPPED` grammar with the already-authoritative fresh rest-then-cross policy; it supersedes no probability, sizing, lifecycle, DB, or executor authority surface.
- First-principles invariant: proof mode is a time-indexed execution plan, not an immutable fact. At submit, the same rest-then-cross policy must choose from the fresh book. A valid fresh passive-maker verdict may replace a stale taker plan only by rebuilding the final intent and passing all existing maker-specific quote identity, post-only price, duplicate-order, capital, and final JIT witness checks before command persistence.
- Minimal repair: make the existing validator symmetric for two valid modes, returning the fresh policy verdict for either direction. Keep missing or unknown proof/fresh modes fail-closed. Do not alter q, q_lcb, robust utility, sizing, strategy floors, book freshness, or executor walls.
- Files authorized: `src/engine/event_reactor_adapter.py`, `tests/engine/test_final_submit_mode_authority.py`, `tests/engine/test_mode_flip_and_recapture_semantics.py`, `tests/engine/test_quote_feasibility_wrong_book_identity_wall.py`, `tests/integration/test_w3_solve_seam_g3.py`, `architecture/test_topology.yaml`, `architecture/source_rationale.yaml`, this packet, and its scope sidecar.
- Forbidden: force a venue action, relax a probability/economic gate, submit a stale taker price, bypass post-only crossing checks, mutate/copy a canonical DB, or change the `MAKER_TAKER_FORBIDDEN` no-trade verdict.
- Acceptance: both valid mode transitions adopt the fresh mode and pass global winner preflight into the final mode-specific builder; `TAKER -> MAKER` derives context and limit from the same fresh JIT bid/ask even when an old quote has the same midpoint but different touches; missing/unknown modes still raise the typed abort and candidate-locally fall through; focused relationship tests, compilation, planning-lock, and `git diff --check` pass; exact-SHA live deployment is followed by current process/heartbeat, decision/preflight, command/event, and rejection-reason verification.
- Verification: focused mode/price/identity coverage passes `65/65`, the global-preflight parameter cases pass `2/2`, and exact-maker-share preservation passes `2/2`. The load-bearing same-midpoint counterexample spans fresh-book context, final-intent builder, full execution-command verification (including PRE_SUBMIT revalidation), and executor fresh recapture: stale `0.51/0.52` plus fresh `0.10/0.93` yields a non-crossing `0.11` post-only order with exactly `8` shares through recapture. The broader worktree slice passes `479` tests with the same `16` pre-existing failures as current live (`14` stale qkernel/Day0 fixtures and `2` EventStore fake-connection fixtures). Compilation, `git diff --check`, and changed-surface planning/map/freshness gates pass; no canonical DB or venue side effect was touched.

## Slice B56 -- Honor held-monitor guaranteed progress after the positive cap

- Current proof: exact SHA `d4c95fbd1` loaded under PID `98361`, but the standard deploy proof failed because only 5 of 22 open positions received a post-restart `MONITOR_REFRESHED` event. The live loop repeatedly refreshed the selected Day0 tranche while 17 positions, including three new confirmed fills, remained outside the post-start proof window. `execute_monitoring_phase` reserves rotating ordinary local/network positions and reports them as `held_monitor_budget_guaranteed_positions`, but its two-success cap breaks before those reserved positions when two urgent monitors already succeeded.
- First-principles invariant: urgency orders the first work, while a reservation is a hard progress guarantee inside the bounded monitor budget. A throughput cap may truncate only the unreserved tail; it cannot cancel a reservation it just granted. Deadline and urgent-wake preemption remain independent fail-closed boundaries.
- Minimal repair: when the positive-progress cap is reached, continue only for positions in the already-computed `budget_guaranteed_position_ids`; keep the cap unchanged for every unreserved position. Do not change probability, exit criteria, position state, order submission, budget duration, or urgent-wake preemption.
- Files authorized: `src/engine/cycle_runtime.py`, `tests/test_live_safety_invariants.py`, this packet, and its scope sidecar.
- Forbidden: clear `entries_paused` before the standard post-restart proof, force an order, alter Wellington, weaken probability/quote freshness, extend the monitor deadline, mutate/copy a canonical DB, or let an unreserved tail bypass the positive cap.
- Acceptance: two successful urgent monitors no longer prevent an already-reserved active local/network position from receiving its monitor attempt; the first unreserved tail position still stops on `positive_budget_progress_limit`; existing deadline/preemption/round-robin tests remain green; compilation, planning-lock, and `git diff --check` pass before a standard exact-SHA restart.
- Verification: the new counterexample proves two urgent successes are followed by all four reserved active positions while the fifth unreserved active position remains deferred by the unchanged positive cap. The complete live-safety invariant file passes `268/268`; compilation, planning-lock, and `git diff --check` pass. No pause, canonical DB, Wellington, or venue state was changed during worktree verification.

## Slice B57 -- Schedule live monitor reservations from persisted progress

- B56 deployment proof: the exact hotfix SHA improved post-restart coverage from 5/22 to 14/22, but eight open positions still lacked a post-start `MONITOR_REFRESHED` before the standard timeout. The process cursor advances when a position is merely reserved, including attempts later skipped, preempted, or superseded; therefore it can wrap without any durable progress and repeatedly select fresher positions while never selecting the oldest missing proof.
- First-principles invariant: monitor fairness is a statement about durable redecisions, not attempted scheduling. On the canonical live path, the least-recently persisted `MONITOR_REFRESHED` position owns the next lane reservation; a failed/non-persisted attempt remains first until it produces evidence. The process cursor remains only for noncanonical fallback callers that have no durable progress timestamp.
- Minimal repair: include the latest monitor event timestamp in the existing indexed canonical position read, project it onto the runtime position view, and let each lane reserve the oldest persisted refresh first with stable identity as its tie-break. No event, phase, probability, quote, exit, budget, or venue semantics change.
- Acceptance: two reservation calls without new persisted evidence choose the same oldest position; after its persisted timestamp advances, the next-oldest position is selected. Existing fallback cursor rotation remains unchanged; complete live-safety coverage, compilation, planning-lock, and `git diff --check` pass before another standard exact-SHA restart.
- Verification: the durable-progress counterexample passes and the complete live-safety invariant file passes `269/269`; compilation, changed-surface planning-lock/navigation, and `git diff --check` pass. No canonical DB, control override, Wellington position, or venue state was changed during worktree verification.

## Slice B58 -- Fall through a family-scoped Day0 anomaly pause

- Live evidence after B57: the standard restart proved `22/22` post-start monitors and cleared its own guard, but Dallas Jul22 NO repeatedly won the complete auction and then failed JIT with `LIVE_INFERENCE_INPUTS_MISSING:DAY0_ORACLE_ANOMALY_PAUSED:Dallas:2026-07-22`. The generic classifier treated this known family-scoped source dispute as a batch-wide authority loss, so one deliberately untradeable family monopolized every epoch and no runner-up could reach preflight.
- First-principles invariant: an oracle anomaly must continue to block every order in its own city/date family, but it conveys no information about another family's probability, book, or wealth witness. Therefore the frozen complete epoch may exclude exactly that family, derive a new exclusion-bound epoch identity, and compare the remaining BUY/SELL/HOLD/CASH candidates without venue side effects.
- Minimal repair: classify the already-typed `DAY0_ORACLE_ANOMALY_PAUSED` JIT reason as family `BLOCKED`, using the existing exclusion/re-auction path. Do not clear the anomaly, reuse its q, change Kelly, alter the price band, or convert unknown inference failures into fallthrough.
- Acceptance: the exact Dallas reason classifies family-local; the end-to-end global-batch counterexample excludes the paused winner, persists a different epoch identity, preflights the runner-up, and performs exactly one venue call; unknown/malformed inference reasons remain `BATCH_BLOCKED`. Focused tests, compilation, planning-lock, and `git diff --check` pass before an exact-SHA standard restart.
- Verification: focused scope/fallthrough coverage passes `32/32`. The full W3 solve seam passes `261` tests and reproduces two unrelated stale fake-connection fixture failures byte-for-byte on current live `0ae21a075`; neither reaches preflight classification or re-auction. Compilation, changed-surface planning-lock, and `git diff --check` pass before deployment.

## Slice B59 -- Project authenticated entry fills in the ingest transaction

- Live proof after B58: command `9bd083d376334765` received one authenticated `CONFIRMED` REST trade fact for `47.1` Munich NO shares at `0.06`, but remained `ACKED`; position `1605bc27-b62` remained `pending_entry` with zero shares and zero cost. No duplicate command was persisted. The continuous fill synchronizer had durably observed the fill, while recurring recovery repeatedly deferred its first unrelated `REVIEW_REQUIRED` pass under auction DB contention. More fundamentally, no recovery pass consumes the ordinary `ACKED + CONFIRMED trade fact` shape: matched-order recovery explicitly stays when a positive trade fact already exists, and filled-position repair requires the command to be `FILLED` first.
- First-principles invariant: an authenticated fill changes owned terminal wealth at the instant its canonical trade fact commits. The command event and position projection derived from that exact fact must commit in the same transaction; an asynchronous poll is crash recovery, not the normal capital truth path. The reducer must validate exact command/order identity, authenticated source, confirmed state, cumulative size, submitted limit, and legal command grammar before projecting either partial or full exposure.
- Minimal repair: add one idempotent canonical reducer in command recovery for `ACKED`/`POST_ACKED`/`PARTIAL` BUY entries backed by exact `CONFIRMED` `REST`/`WS_USER` trade facts. The fill synchronizer invokes that reducer for each newly appended local command before advancing its watermark, inside its existing `BEGIN IMMEDIATE`; boot/live recovery invokes the same reducer as a DB-only backstop. Do not duplicate lifecycle grammar in ingest, perform venue I/O, or alter selection/probability/Kelly.
- Files authorized: `src/execution/command_recovery.py`, `src/ingest/fill_synchronizer.py`, `tests/test_command_recovery.py`, `tests/test_fill_synchronizer.py`, this packet, and its scope sidecar.
- Forbidden: manual SQL repair, canonical DB copy, force an order, clear an operator pause, alter Wellington, accept non-confirmed or foreign trades, accept a worse-than-limit fill, silently clamp overfill, or treat ex-ante utility as realized profit.
- Acceptance: an exact full authenticated fill atomically advances `ACKED -> FILLED` and `pending_entry -> active` with command-bound shares/cost; a partial fill advances to `PARTIAL` without duplicate projection and a later cumulative fill completes it; replay is idempotent; mismatched order, unauthenticated source, worse-than-limit price, or overfill performs no semantic advance; any projection failure rolls back the newly appended trade fact and watermark. Focused fill-sync/recovery tests, relevant execution contracts, compilation, planning-lock, and `git diff --check` pass before exact-SHA standard deployment and live venue/canonical reconciliation.

## Slice B60 -- Resolve cancel/full-fill ordering without losing owned exposure

- Live proof after B59 deploy: Munich's exact `47.1 @ 0.06` `CONFIRMED` REST fact and chain position are current, but a later rest-cancel request moved command `9bd083d376334765` to `CANCEL_PENDING` before the new backstop ran. The reducer therefore scanned zero; the command remained nonterminal and live health reported a terminal-command/venue-fact conflict. This is an event-order race, not a new economic decision.
- Invariant: a cancellation can terminate only unfilled remainder. An exact authenticated cumulative full fill equal to the submitted BUY size leaves no remainder for cancel to own and must terminalize as `FILLED`; a partial fill while cancel is pending remains governed by the existing terminal-remainder reconciliation.
- Minimal repair: admit `CANCEL_PENDING` only when the same B59 reducer proves a complete exact-order authenticated fill. Use the existing legal grammar `CANCEL_PENDING -> REVIEW_REQUIRED -> FILLED` inside the reducer's savepoint; do not add a grammar transition, infer a cancel result, accept a partial fill, or touch the venue.
- Acceptance: full fill wins the cancel race and atomically projects command/position/execution truth; partial fill stays `CANCEL_PENDING`; all B59 exclusions, idempotency, full live-safety, planning-lock, compilation, and diff checks remain green before another standard deployment.

## Slice B61 -- Keep an owned current HTTP response after unrelated circuit movement

- Live proof after B60 deploy: once the restart guard cleared, a complete 1,869-candidate auction could no longer reach the selected positive-utility winner. One full metadata refresh exceeded its six-second deadline; a later current CLOB `/books` batch received a 2xx response but `PolymarketRequestGovernor.request()` discarded it as `POLYMARKET_REQUEST_LEASE_LOST` because another request changed the host generation while it was in flight. The failed epoch then forced more full I/O, reactor overlap, and scheduler starvation.
- First-principles invariant: the governor owns HTTP-attempt admission, deduplication, and retry circuits; it does not own the truth of a response already returned by the venue. A transport/429 fact from another request must remain in the persistent circuit, but it cannot retroactively erase an independently owned exact request's 2xx payload. A same-identity higher-priority replacement or an expired lease still fences the older response.
- Minimal repair: when success bookkeeping loses only generation authority, release the still-owned exact lease neutrally and return the 2xx response for normal downstream identity/completeness/freshness validation. If the exact lease itself is no longer owned, preserve `POLYMARKET_REQUEST_LEASE_LOST`. Do not cache partial books, bypass request admission, clear a circuit/embargo, extend freshness, alter the auction objective, force an order, or touch Wellington.
- Acceptance: an owned request whose sibling changes endpoint generation returns its received 2xx while preserving the failure circuit; an exact same-identity preemption remains fenced; governor coverage, W3/current-book coverage, live-safety, compilation, planning-lock, and `git diff --check` pass before standard exact-SHA deployment.

## Slice B62 -- Spend the available monitor cycle on the full held book

- Live proof: current exit-monitor receipts repeatedly carried 38-39 candidates but stopped after 14-19 successful monitors in 14-19 seconds, deferring 20-25 positions as `positive_budget_progress_limit` despite a 75-second budget. The Miami loss path therefore waited multiple cycles for fresh probability and quote evidence even while the monitor lane was idle.
- First-principles invariant: every held position is re-decided from a fresh `ExitContext` every normal monitor cycle. Urgency and rotating reservations determine ordering and degraded-deadline survival; successful work is not a reason to stop while budget remains. A real monotonic deadline and urgent-wake preemption remain the only cycle truncation boundaries.
- Minimal repair: remove the positive-progress stopping condition while retaining the 75-second monotonic deadline, urgent ordering, batch book prefetch, durable-progress rotation, and one-third reservations for deadline-degraded cycles. Do not change probability, exit thresholds, sizing, lifecycle, price-band law, or venue submission.
- Acceptance: a positive-budget cycle with time remaining scans the complete held book, including the unreserved tail; an elapsed deadline still defers safely after persisted progress; zero-budget reservation and urgent-preemption behavior remain unchanged. Complete live-safety coverage, compilation, planning-lock, and `git diff --check` pass before exact-SHA deployment and live receipt verification.
- Verification: focused full-sweep/deadline/reservation coverage passes `7/7`; the complete live-safety file passes `285/285`; compilation, freshness, map-maintenance, planning-lock, and `git diff --check` pass. Ruff reports the same 17 pre-existing findings as current live and no new finding from this slice.

## Slice B63 -- Execute reserved monitor thirds after deadline expiry

- Independent refutation of B62: with nine held positions and a positive budget that expired before the first refresh, the scheduler selected three coverage reservations but stopped after the first successful monitor. Selection-time attempt bookkeeping then rotated reservations that had never produced a `MONITOR_REFRESHED` event, so sustained slow I/O could still starve the book.
- First-principles invariant: the monotonic deadline ends unreserved work; it does not revoke the bounded tranche already reserved to guarantee degraded coverage. Every reserved position receives one finite attempt, then the unreserved tail defers. Urgent-wake preemption remains absolute.
- Minimal repair: after deadline expiry, continue only for `budget_guaranteed_position_ids`; remove the successful-first-position exception. Add a three-cycle antibody that advances the monotonic clock before work and proves every selected third receives an actual refresh attempt; the existing bounded urgent/local/network reservations may add progress but never admit the unreserved tail.
- Acceptance: a nine-position book under sustained deadline expiry actually executes every selected three-position tranche and those tranches cover all nine over three cycles; normal-budget full sweep, zero-budget reservations, urgent preemption, batch prefetch, and finite iteration remain unchanged. Full live-safety, compilation, planning-lock, freshness, map-maintenance, and diff checks pass before follow-up exact-SHA deployment.
- Verification: the adversarial expired-before-first-refresh case executes every selected third and covers all nine positions over three cycles; the complete live-safety file passes `286/286`. Compilation, planning-lock, freshness, map-maintenance, and `git diff --check` pass; Ruff remains identical to live at 17 pre-existing findings.

## Slice B64 -- Admit the official HKO LOW forecast and reseed immediately

- Live proof: `raw_model_forecasts` contains 1,071 `hko_fnd/high` rows through the current HKO cycle and zero `hko_fnd/low` rows, although the same HKO payload and parser expose `forecastMintemp`. `config/station_forecast_sources.json` and dispatcher kwargs select only `metric=high`. The availability poll then returns on an unchanged Open-Meteo clock without reseeding after successful station writes.
- First-principles invariant: a settlement-aligned provider payload is one causal issue containing distinct HIGH and LOW facts; source admission must preserve both typed metrics without a second network fetch. Any newly persisted source fact must invalidate and rematerialize affected probability authority independently of unrelated providers' clocks.
- Minimal repair: configure HKO for both metrics; fetch once, parse both typed fields, and persist the union through the existing idempotent writer. After any positive station write count, run the existing fusion-upgrade reseed before the unchanged-Open-Meteo early return. Preserve CWA HIGH-only behavior and per-source fail-soft isolation.
- Files authorized: `src/data/station_forecast_adapter.py`, `src/data/replacement_fusion_upgrade_trigger.py`, `src/ingest_main.py`, `src/state/schema/v2_schema.py` comment contract, `config/station_forecast_sources.json`, `tests/test_station_forecast_live_ingest_wiring.py`, `tests/data/test_fusion_upgrade_trigger.py`, `architecture/db_table_ownership.yaml` ownership note, this packet, and its scope sidecar.
- Forbidden: synthesize the missing historical Jul24 LOW row, mutate a canonical DB manually, weaken freshness/current-shape gates, treat current observation as final settlement, change YES/NO side law, change exit thresholds or the absolute venue price band, force an order, or perform a second HKO request per metric.
- Acceptance: one HKO fetch persists HIGH and LOW rows with distinct typed identities; invalid/duplicate metrics fail closed; CWA remains HIGH-only; every completed due station poll runs one idempotent fusion-upgrade reconciliation even when Open-Meteo is current, narrowing to changed providers when rows were committed and scanning globally when canonical rows already exist; a not-due poll does not reseed; focused tests, compilation, planning-lock, and `git diff --check` pass before exact-SHA deployment. Post-deploy canonical proof requires new `hko_fnd/low` rows, a rematerialized Hong Kong LOW posterior whose provenance consumes the official source, and fresh held-position monitor receipts.
- Verification: station ingest/dispatcher/reseed and current-value serving coverage passes `30/30`; the four source-clock station-admission/materializer antibodies pass `4/4`; compilation, test freshness, map maintenance, planning evidence, JSON validation, and `git diff --check` pass. Independent review found no P0/P1 and its P2 value-identity gap was closed by asserting distinct HKO HIGH `33/34` and LOW `28/27` values from one payload. Full legacy materializer coverage remains baseline-drifted on current live (10 Paris/frozen-scheme fixture failures); the focused station authority tests are green on both semantics and current code.
- Post-deploy corrective finding: live wrote 9 HKO LOW rows and invoked the upgrade trigger, but Hong Kong Jul25 LOW remained on its pre-station posterior. The trigger intersected `changed_sources` with the old posterior's configured sources, making a newly admitted station source unobservable even though the shared current-value reader already returned it. Corrective acceptance: both commit-scoped and periodic comparisons treat capturable `cwa_*`/`hko_*` sources as relevant; the first exact raw row signals one input revision enqueue, an unchanged pending revision is durably coalesced, a newer raw row may enqueue once, and a posterior that consumed the exact row returns to no-op.
- Corrective verification: focused trigger/station/current-serving coverage passes `46/46`, including the mixed positive/zero provider report; the real enqueue-path antibody proves simultaneous family growth plus HKO revision emits one seed and two durable markers, an identical retry emits zero seeds, and a new HKO raw id emits one seed while retaining the family marker. Compilation, Ruff, planning/freshness/map checks, YAML parsing, and `git diff --check` pass. Independent re-review found zero P0/P1/P2 and judged the behavior deploy-safe; no Python LSP/type checker is installed in this environment, so `compileall` plus Ruff are the disclosed static substitutes.
- Live mixed-report finding: the first corrective restart returned `cwa_township=6, hko_fnd=0`; narrowing reconciliation only to positive writers seeded Taipei and still omitted Hong Kong. A due poll proves each reported provider completed, not only the ones that inserted a new row. Final acceptance therefore passes every reported provider key (including zero-row HKO) to the exact-source comparison; only a not-due `None` suppresses reconciliation.

## Slice B65 -- Use current-evidence bounds in held-position redecision

- Live proof: Singapore Jul24 HIGH 32 NO consumed WU 32C only at `05:04:07Z`, 6m25.790s after the canonical WU write. The overlapping full-book monitor ran `04:56:10Z -> 05:02:31Z` for 381.535s and could yield to the urgent Day0 wake only after its current position refresh. Non-Day0 held refresh still recomputes a legacy bootstrap CI even though every current live held family has canonical `forecast_posteriors.q_lcb_json/q_ucb_json` coverage.
- First-principles invariant: the source-clock replacement posterior and its current-evidence q bounds are one probability authority. Held redecision must consume those persisted bounds in native held-side space; a diagnostics-era bootstrap cannot replace them or occupy the single monitor lane while a newer settlement fact waits.
- Minimal repair: extend `ReplacementBelief` with the exact matched YES and held-side lower/upper bounds, apply the same observed-extreme transport to point and bounds, and let `refresh_position` derive its edge interval directly from those bounds. Missing, malformed, or incoherent bounds make the replacement belief unavailable; the existing fail-closed reseed/read-through path remains the fallback.
- Files authorized: `src/engine/position_belief.py`, `src/engine/monitor_refresh.py`, `tests/engine/test_position_belief_authority.py`, `tests/engine/test_belief_observed_floor.py`, `tests/engine/test_monitor_held_belief_readthrough.py`, `tests/test_monitor_refresh_nowcast_wiring.py`, and this packet.
- Forbidden: alter the point q, fabricate bounds, reuse stale entry CI as current replacement authority, weaken probability/quote freshness, change BUY/SELL/HOLD/CASH economics, change the venue price band, force an exit, mutate/copy a canonical DB, or bypass lifecycle/executor law.
- Acceptance: YES and NO held-side bounds convert symmetrically; observed HIGH/LOW transport preserves exact impossible-bin facts and coherent `lcb <= q <= ucb`; fresh replacement refresh never invokes legacy bootstrap; missing/incoherent bound vectors fail closed; focused authority/observed-floor tests, live-safety monitor tests, compilation, planning-lock, and `git diff --check` pass before exact-SHA deployment. Live proof requires a standard monitor run with no regression in order execution and materially lower full-book/urgent-family latency.
