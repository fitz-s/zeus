# Goal

Live trading runs continuously and produces settlement-graded real profit. Not completion: process liveness, a green healthcheck, an order, a merged PR.

The only score that counts is the decision-certificate × settlement join (`settlement_skill_attribution`, six categories — SKILL_WIN is evidence, LUCKY_WIN is a MISS).

Current work state lives in `docs/operations/current/plans/hourly_capital_gains_improvement_loop.md` (forward journal) and `plans/INDEX.md`. This file carries no blocker narrative — blockers rot; read the journal.
