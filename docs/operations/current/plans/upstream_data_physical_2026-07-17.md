# Upstream data physical — issuance shock / mixed-cycle fusion / staleness isolation (2026-07-17)

Goal: full upstream forecast-layer体检 + redesign: per-source publication bias vs fusion, issuance-shock handling (probability jumps on new cycles), per-city best-source fusion form, latency quantification, staleness degrade ladder (per-city isolation instead of stale-q trading), non-blocking pipeline under source outage. First principle / zero-sum / act-faster-than-market. NO market-price backtests (walk-forward only; only settlement-source accuracy backtest allowed).

## Ground evidence (live DB, measured 2026-07-17)
- Publication latency (available-cycle, single_runs 7d): ukmo 7.4h avg, ecmwf_ifs 6.5h, icon_global 3.7h, nbm 3.1h, jma max 14h. previous_runs ~8h uniform. ENS ~8.3h.
- Mixed-cycle fusion: instruments served at cycle ages up to 12h apart in one fusion (anchor 18z + ukmo 12z); ENS shape 12z vs anchor 18z → delta_ens partially = cycle skew.
- Issuance shock (14d, n=8108 successive live posterior center diffs): avg |dμ| 0.265C, 15% >0.5C, 4.7% >1C, 0.5% >2C, max 4.04C.
- Posterior compute delay after source_available: avg 25min, max 11.7h (tail!). CONUS intra-target refresh gaps up to 30-37h.
- Current staleness now: Hong Kong low 10.7h old, Shanghai low 7.8h; rest ~1.5h.

## Workstreams
- W1: latency/issuance measurement + per-source cycle clock truth (explorer: exp-ingest)
- W2: issuance-shock propagation + re-decision handling (explorer: exp-redecision)
- W3: fusion weights / per-city best source / market-definition alignment (explorer: exp-fusion)
- W4: staleness degrade ladder + per-city isolation (design; consult REQ-20260717-032202-ec62d2 pending)
- W5: non-blocking queue architecture (head-of-line risk)
- Execution: worktree implement → git-master merges to live branch promptly (parallel co-workers consume commits).

## Status
- [x] Boot; all explorer reports in; exp-starve CONUS root cause DEFINITIVE (degF members_unit bug, fixed 305897168 on 07-14; the 30h darkness = missing alerting + blind receipts + fingerprint churn)
- [x] Consult v2 answered (/tmp/cgc/answer_REQ-20260717-040148-1c678b.txt) — verdict folded below
- [x] impl-ensage DONE + cherry-picked to live branch 3c532675d (ENS shape 30h age bound; 3 tests green on live)
- [ ] impl-lowens (quarantine≠missing + floor alignment) — running
- [ ] impl-receipts (typed block sub-reasons + cycle-scoped fingerprint) — running
- [ ] impl-watchdog (posterior-starvation alert before TTL) — running
- [ ] git-master loop: cherry-pick each as it lands; live branch advances continuously

## Phase 2 EXECUTION (operator go 2026-07-17: "开始执行实现 不采取任何需要大量时间累积验证的内容")
Selection filter: nothing requiring long accumulation windows. EXCLUDED for now: ENS mirror 30-day equivalence capture (P2-F), 28-day basket freeze cycles (start applies immediately w/ existing history), gamma_g shape-age sigma term (needs hybrid fit job). DISPATCHED (worktrees):
- W1 impl-renorm: fixed_weight_center_from_values renormalize-over-present (basket membership never a readiness requirement; kills CONUS incident class structurally). Floor: present configured weight sum >=0.25 else None.
- W2 impl-transport: ENS anomaly transport increment 1 — stale-shape (lag>0) members translated onto fresh center, delta_ens operational 0 (raw kept as provenance), preimage hits from translated members, semantics_revision ensemble_anomaly_transport_v1 on affected rows only; same-cycle path byte-identical. Authority doc §1d append. NO sigma age term yet.
- W3 impl-weights: scripts/fit_source_clock_city_weights.py — walk-forward versioned weight artifact (reuses center.py math; greedy+2SE+0.05C threshold, cap 4; region fallback <60 dates, global core <30) + ACTIVE.json pointer + consumer switch in scheme_for_city (legacy CSV fallback). First artifact generated from existing settlement history immediately — no accumulation wait.
Merge protocol: cherry-pick to live branch on each completion (git-master = main thread), tests verified on live tip before landing.
W1/W2/W3 LANDED + verified (26 green; wiring failures == baseline). First artifact ACTIVE (city_weights_20260717, servable-only, 56/3/49 tiers, -0.057C vs frozen).

## Phase 3 (operator go post-compact: 继续执行剩余内容和数据拟合改进真实世界模拟)
- [x] F3 metric threading: scheme_for_city/fixed_weight_center_from_values now take metric; materializer + live queue pass the track metric — LOW track reads its own artifact bucket (was silently serving HIGH weights). Landed 58503f359, 28 green, live pickup verified (Chicago low → 3-model basket vs high → nbm 1.0).
- [x] F1 staleness variance + between-spread cohort LANDED 00ee1968c (one keep-both manifest conflict). Lag = CYCLE-lag (selected cycle − served_cycle), not capture-lag — a promptly-captured stale cycle carries full issuance error. Fit per (model,metric,lead), v=cummax(max(0,m2−m2_freshest)); serving adds v to raw m2 in precision weights; between-spread restricted to ±3h freshest cohort, fail-open, provenance only when active (shape_hash untouched). Verification: failure sets pre/post cherry-pick IDENTICAL (8=8, remaining 6+2 wiring fails pre-existing; F1 also REPAIRED 11 baseline test failures). ARTIFACT ACTIVE (sha 452babc4, 155961 settled cells, 17 models): per-6h increment ~0.05-0.06C (high) / ~0.05-0.12C (low) — same order as the 0.099-0.168 paired anchor (paired-vs-pooled scale difference expected). Loader verified: ecmwf_ifs high 0h→0, 49h→0.38C², monotone; unknown model→0.
- [x] F2 CP effective-n LANDED 403fc974e (fitter reuses _anova_icc; Kish design effect inside _finite_evidence_binomial_ucb; both consumers incl Day0; 63 green on live tip; conftest pins artifact-absent identity for tests). First fit: rho_high=0.294 (n_targets=3152), rho_low=0.329 (n=247) → n_eff≈3.2/2.9 — zero-hit UCB 0.057→~0.60, a MASSIVE widening. Artifact QUARANTINED (state/ens_member_dependence/QUARANTINE_20260717/, loader verified rho=0 fallback = byte-identical serving). COVERAGE MEASUREMENT DONE (scratchpad/cp_coverage_report.md, 3171 settled targets, target-clustered bootstrap): rho=0 violated ONLY at k∈{0,1} (r(0)=0.066 vs UCB 0.057); coverage-calibrated rho: high=0.0051, low=0.0580 (thin n=247), pooled=0.0040; walk-forward split (rho_train=0.0066) → ZERO test failures. ICC 0.294/0.329 = correlation, NOT the bound's operational quantity — activating it would blow zero-hit UCB 0.057→0.60. Coverage-calibrated fitter LANDED eaf016ead (46 green on live tip; rho_icc demoted to provenance, schema v2, seeded-bootstrap deterministic, thin-metric max(own,pooled) floor). ARTIFACT ACTIVE (sha aad91cee): rho_high=0.00464 (n_eff 41.4, n=3152), rho_low=0.05396 (n_eff 13.8, n=247, pooled-floored); loader verified live. Zero-hit UCB 0.057→0.070 (high) / 0.195 (low) — honest, evidence-preserving widening vs the rejected ICC 0.60. Quarantined ICC artifact deleted.
- Explorer facts feeding these: served_current carries age_hours/served_cycle per instrument but only value reaches the center (materializer:2042); between-spread has NO cycle filter (:1268-1281); CP uses len(members) independent trials, no dependence correction (:2537,:2624); no staleness/age-variance artifact exists anywhere.

## Phase 4 (operator go 2026-07-17: "除了需要累积的内容都执行 使用数学和统计就能证明一切 不需要时间")
Unlocks the two previously operator-pending authority changes + everything provable from existing settled history. Still excluded (genuine accumulation): ENS faster-mirror 30d equivalence capture (P2-F); 28d basket freeze governance (auto-applies from 2nd artifact).
WAVE 1 (dispatched, worktrees locked):
- [x] impl-ladder LANDED 0cacf2ab8 (feat) + 3e3b52a4c (authority §4a). Boundaries DERIVED from n=1479 settled targets (fresh serving age p50 7.19h/p99 15.75h): GREEN≤18h (covers p99), AMBER (18,24h] fitted sigma inflation, RED (24,30h] OR newer-cycle-detected, EXPIRED≥30h unchanged. AMBER v fitted high 0.367 degC² (p=6.3e-8, n=1011) / low 0.244 (n=156) = 13-19% of base variance, spread-dominated → honestly priceable; center-error penalty lag-6/12/18 = +0.069/0.161/0.277°C all SIG. RED justified by signed drift |bias| 0.12/0.32/0.47°C (past 24h bias is uncorrectable-by-variance). RED = entry-scoped bundle-reader BLOCKED + maker cancel; monitor/exit read independent paths (structural test proves untouched) — the operator's per-city isolation ask, entry-only. Artifact ACTIVE (sha 33cb2234, 1481 targets), loader verified GREEN→0/AMBER→0.367/unknown→0. DELIBERATE DEVIATION from consult sketch: did NOT implement "AMBER relaxes two-provider→1provider+ENS" — weakens a completeness gate with no settlement proof, violates never-weaken-a-gate + prove-with-stats; deferred to own derivation. AMBER keeps provider-completeness.
- [x] impl-simcov: NO-CHANGE-WITH-PROOF LANDED b39a21153 (docs/evidence + property tests, zero serving change). Math: max(CP_rho, Cantelli) of two valid one-sided 95% UCBs is itself a valid 95% UCB (strictly more conservative); plug-in (mu,sigma) optimism is MASKED by the max (F>=CP_rho pointwise → composed floor's marginal coverage bounded below by the calibrated CP term); the decision seam (q_ucb_yes → q_lcb_no, §7 ranker picks ONE primary/family/decision) needs marginal per-bin coverage NOT joint — Bonferroni alpha-split would destroy EV to buy a guarantee never consumed. Measured (3172 targets, 36039 cells): floor over-covers 2-6x; Cantelli binds 46%(high)/23%(low) and over-covers where it binds. HAIRLINE (recorded, NOT actioned): high k=0 boot_upper 0.0710 vs floor 0.0698 = 1.1pp — but point estimate r(0)=0.068 IS covered; this is the member-dependence rho calibration boundary (CP term, not Cantelli), sample/bootstrap noise (fitter walk-forward 3152 vs measure 3172; true rho_high 0.0046-0.0055 ±0.001), fitter self-consistent on its own sample, walk-forward split test-fold ZERO failures. Chasing 1pp inside CI = over-engineering; left as-is with evidence.
- [x] impl-gammag LANDED 87b3d5f4f (+ test-isolation fix 1468dacc3). gamma_g fitted = STATISTICAL ZERO both metrics (high slope -0.126 p~0 n=59967/47clusters; low slope -0.074 p=0.057 n=2138) → clamped 0. PROOF (docs/evidence/upstream_physical_2026_07_17/shape_age_sigma_fit.md): after anomaly transport recenters aged shape, CRPS is flat-to-improving with shape age and coverage does not deteriorate → transport already removes the priced error, no widening warranted. Dormant machinery landed fail-open (transported-branch sigma = sqrt(within²+between²+gamma·lag/6), gamma=0 today; provenance shape_age_sigma_term_c2 stamped only when >0; shape_hash untouched). Artifact ACTIVE (gamma 0.0/0.0), future refit can activate zero-code. NB: exposed a latent test-isolation gap — F1's staleness_variance absent-case used delenv→default dir; once I generated its live artifact the byte-identity test read it. Fixed with symmetric conftest autouse isolation.
## Phase 4 waves 2-3 (operator go 2026-07-17b: "除了需要累积的内容都执行"; then "最优化的正确性 不要担心over-engineering ... 判断是否影响正确结算/运行 不要过度删除")

WAVE 2 — Day0 obs assimilation. VERDICT: consult rank-1 CDF-level conditioning ALREADY LIVE (_build_fused_q_bounds:3406-3421, Y=max(obs,X~N), no-leakage walk-forward, on q_point/q_lcb/q_ucb). Member-level within-shrink would DOUBLE-COUNT obs — deliberately NOT done. But ONE REAL LIVE DEFECT confirmed by orchestrator direct read (:3959-3963: final q_ucb = max(mixed_ucb, q_point, finite_evidence_floor); the CP finite-evidence floor _current_evidence_tail_ucb_floors:2865 is NOT day0-aware, so a below-observed-max bin day0 zeroed gets its q_ucb lifted back to a positive Cantelli/member floor — asserts positive probability for an observation-EXCLUDED settlement outcome, prior measurement ~18.6% of Day0 HIGH targets, max ~0.14 leaked mass).
  - RESOLVED — NO LIVE DEFECT, NO CODE FIX. impl-day0floor2 (clean re-dispatch) + orchestrator direct read CONTRADICT the first (lost) agent's 18.6% claim. DECISIVE CODE FACT (self-verified :3700): the finite-evidence floor computation is gated `if _current_shape is not None and _day0_obs_extreme_c is None:` — `_finite_evidence_member_count` (grep-verified sole assignment :3707) is NEVER set on the Day0 path, so `_finite_evidence_ucb_floor_by_bin` stays None and the max at :3959-3963 reads ({}).get(bid,0.0)=0.0. FINITE-EVIDENCE FLOOR and DAY0 CONDITIONING ARE MUTUALLY EXCLUSIVE (gate added d8053c27e9 2026-07-11). MEASURED LIVE: 0/15,583 Day0 rows carry any finite-evidence floor; 1,476/2,000 already serve impossible bins at q_ucb==0. The first agent's 18.6% was a FUNCTION-ISOLATION ARTIFACT (it measured _current_evidence_tail_ucb_floors standalone — which IS day0-unaware, 0.45/0.59 floors — but that function is never invoked on Day0). Landing the day0-aware fix would have been DEAD CODE (byte-identical, no settlement effect) — failing the operator's own correctness test. LANDED ab3df2e18: evidence doc day0_finite_evidence_consistency.md (mutual-exclusion proof), verified HEAD-advanced + file-present. NO production code touched.
  - OPEN MODELING QUESTION (operator's call, needs own settlement-graded validation — NOT a defect): Day0 POSSIBLE bins currently get NO finite-evidence/member-count humility floor (only the day0-conditioned fused-center bootstrap). Could a tight Day0 bootstrap be overconfident on a possible bin with few effective members? That would need enabling a Day0-appropriate humility floor + validating it doesn't miscalibrate — a modeling change, not a leak fix. Flagged, not acted.

WAVE 3 — P2-A. critic-p2a (real) verdict: full two-table CAS rewrite NOT justified; (b) separate persistence deferred (no correctness failure mode). ONE candidate hole: bundle_reader:553 (computed_at DESC) vs event_reactor_adapter:19117 (source_cycle_time DESC) different monotonicity keys. trace-readiness-mono (real) verdict H2 (0.82): the hole is LARGELY illusory — _seed_source_cycle_regresses_current_posterior (queue:1197) refuses an older-center seed before it materializes, so an older-center posterior is never written/certified/served; monotonicity enforced at admission not certificate. Residual caveat: non-atomic admission→readiness-upsert window under concurrent same-scope materialization.
  - measure-wave3 VERDICTS (scratchpad/wave3_ac_report.md, n=1800 targets 2026-06-15..07-16):
    (a) REFUTED — DO NOT BUILD shape-precompute. New-target wait median 3.44h / p90 8.11h (the "7.5h"). Binding leg is CENTER/carrier 97.3%, ENS shape only 2.7%. The ENS cycle the first posterior used pre-existed entry by median 4.95h (A≈100%, B≈0%), but precompute pre-warms a leg that is NEVER the bottleneck → removes ≤2.7%, cannot touch the center-bound wait. Consult's "per-target on-demand shape = the 7.5h wait" premise is EMPIRICALLY FALSE. Real new-target-latency lever = the CENTER/carrier publication path (OM anchor possession, publishes after entry in 96.4%) — a SEPARATE future investigation, not P2-A.
    (c) BELT-AND-SUSPENDERS (not load-bearing) — but BUILD under correctness-first. Normal path already monotone: single-writer `_queue_lock` (O_CREAT|O_EXCL :348-387) held across the serial batch subprocess (scripts/materialize_replacement_forecast_live.py:431-448) + commit-time `_cycle_monotone_block_reasons` (materializer:673-736). ONLY residual: crash-steal (daemon SIGKILL mid-batch → orphan child commits while stale-lock self-heal lets a new daemon steal the lock), EQUAL-cycle only (strict-older caught by :673). The :673 guard does NOT cover equal-cycle wall-clock last-writer-wins on the readiness cert; the new guard does.
  - [x] impl-readymono LANDED 15b3677df (cherry-picked from 7ced3449b). Guard `_serving_key_strictly_newer` + reason READINESS_CERT_CYCLE_REGRESSION at materializer:~4976 (before write_readiness_state); refuses replacing a cert whose bound posterior sorts strictly ahead on (source_cycle_time DESC, computed_at DESC) — matching event_reactor_adapter:19117 / reactor.py:6326 / forecast_snapshot_ready.py:1116 / staleness_cancel.py:364 serve key. Fail-open on every gap (no incumbent, unparseable, missing ts, equal keys); blocks only strict regression; never sole darkening gate. Single-source-of-truth `_compose_scope_key` extracted in readiness_repo.py so guard-read and upsert can't disagree on the conflict key. VERIFIED real: HEAD-advanced + helper present (3 hits) + test file present + 32 green (10 new monotone incl crash-steal both insert orders, + cycle_policy + schema). provenance-audit: both edited files CURRENT_REUSABLE. NOTE: agent's first empty-anchor slipped into the MAIN tree leaving harmless empty commit 28c7dc1f5 on live history (verified truly empty, no tree change — left as-is, revert would only add noise).
  - WAVE-3 NET: only the (c) guard builds. (a) refuted by data. (b) deferred (no correctness failure mode). Full P2-A two-table CAS rewrite NOT done — consult's BLOCKER ranking was blind to the latent architecture + the center-bound (not shape-bound) latency reality. This is the data-driven answer to the consult, recorded as the considered response.
WAVE 3 (last, biggest refactor surface): P2-A CenterState/ShapeState full decoupling.

### WAVE 3 TERRAIN (explore-centershape first-hand, materializer 4882 lines):
COUPLING TODAY: center μ* (raw diagonal precision mean, :1824/:2124) + shape (_read_current_evidence_shape :1503, ONE ENS snapshot per (city,target,metric) call) computed one pass; shape's between/delta terms vs same-pass center; ENS cycle bounded [carrier-30h, carrier]. Post-anomaly-transport (4b4481d21): "same-cycle WITHIN shape, any-cycle BETWEEN center+shape" is ALREADY live (recenter X'_j=center+(member_j-member_mean), delta zeroed, gamma·lag/6 added).
LATENT = already present, reusable as the decoupled pieces:
  - CenterState: replacement_current_value_serving.py already serves newest-per-model event-by-event (exact single_runs > exact previous_runs > newest prior; station own-cycle override).
  - assembler join semantics: anomaly transport already licenses newest-center + older-licensed-shape.
  - shape-age catastrophic guard: 30h bound :1530-1533.
  - monotone substance: append-only + newest-served (bundle_reader :558) + readiness-certificate binding (:526-580, "don't serve uncertified newer row") + semantics-revision convergence (cycle_policy :128-144). Consult verdict (e) itself: existing monotone-cycle+TTL "already gives 80%".
MUST-BUILD (genuinely absent): (a) shape PRECOMPUTE for all tradable targets on ENS arrival — today per-target on-demand = the measured 7.5h new-target ENS wait (the real, quantified problem); (b) separate center/shape persistence — today both folded in forecast_posteriors.provenance_json.bayes_precision_fusion.current_evidence_shape; (c) componentwise monotone (c',s')>=(c,s) CAS.
MIGRATION HARD CONSTRAINT: center+shape are NOT columns — inside provenance_json, parsed by bundle_reader (:547-580 SELECT *) + event_reactor_adapter (:19117) + executable_forecast_reader + current_target_plan; a split needs an assembler reproducing the exact provenance_json shape + q/q_lcb/q_ucb contract.
PRELIMINARY ARCHITECTURE JUDGMENT (to validate, NOT yet committed): the measurable win is (a) alone. (b)(c) full two-table CAS rewrite has a large migration blast radius for goals already ~80% met by existing append-only+newest-served+readiness-binding+revision-convergence — candidate for minimal-machinery restraint (build (a); prove (b)(c) redundant rather than rewrite). This is exactly the "act on the consult by reasoning why the obvious alternative doesn't hold HERE" pattern — needs a consult/critic check before finalizing, since consult ranked full P2-A a BLOCKER from outside (blind to the latent-architecture facts above).

## Consult v2 verdict (adopted/rejected — reasoning recorded)
ADOPTED (non-authority-changing, implementing now or next):
- (e) INVARIANT: "adding/discovering/failing optional evidence may never reduce availability of the last validated active posterior" — the class behind both incidents. Minimal Zeus form: last-good posterior already serves until 30h TTL; the gaps were alerting/receipts/retry-waste (3 impl slices running). Full candidate/active-pointer CAS architecture deferred — existing monotone-cycle + TTL already gives 80% of it.
- (a) Post-release fast lane mandatory; pre-release sigma widening REJECTED (issuance risk ≠ settlement uncertainty — polluting meteorological sigma smears bins, false edge); jump-distribution-as-q-calibration REJECTED (information arrival ≠ forecast error). Pre-positioning = machinery only (queue priority, poll hazard windows), never directional inventory.
- (f) CP-on-51-members treats dependent members as independent → overdispersion; beta-binomial or effective-n before CP is the statistically honest form. ALSO: CP+Cantelli max() composition needs simultaneous-coverage accounting. Both = follow-up math slice, settlement-graded walk-forward only.
- (b) Mixed-cycle: correct unit is ERROR VARIANCE not age haircuts: v_m(cycle-lag) from strictly-prior settlements added to diag(M); between-spread only over freshest coherent cohort (±3h); delta_ens only within one coherent ENS cycle recentered on fused center. Minimal first step: exclude stale-vintage instruments from between-spread (they may still enter center via inflated variance).
- (c) Frozen weights: nightly refit / weekly-or-7-settled-dates activation, 120d window 60d half-life, immutable versioned artifact keyed by exact model id (NEVER positional), candidates = {equal, diagonal precision, LW+bb^T simplex} activated only on out-of-sample settlement CRPS win. New model = weight zero until 25 prior settlements (kills the 0.766-cold-start class).
REQUIRES OPERATOR DECISION (authority-law changes):
- (d) Degrade ladder GREEN(<=18h)/AMBER(<=24h, settlement-fitted sigma inflation, 1-provider+ENS OK)/RED(>24h or newer-cycle-detected-not-active: no new entries, cancel makers, monitor only)/EXPIRED(30h) — contradicts current binary fail-closed + two-provider law in replacement_final_form authority doc. Trades more availability for fitted-inflation complexity.
- ENS member partial handling: interpolate single missing step + coverage-effective n_cov=(Σa)²/Σa² instead of null-member — touches leakage law (R-AH/R-AJ).
REJECTED (with reasoning):
- Same-cycle-only fusion: recreates the outage under asynchronous publication; dominated by variance-penalty form.
- Shadow-mode migration: operator law forbids; consult agrees cutover-in-one-transaction works.

## Key findings so far (file:line in explorer reports, verified spot-checks)
1. AUTHORITY DRIFT: T2/Ledoit-Wolf fusion center computed then DISCARDED (materializer:1765-1847). Live center = RAW diagonal precision mean OR frozen per-city CSV weights (51/54 cities, snapshot 2026-06-25, never re-fit). fused.sd survives only as non-source-clock fallback. Authority doc describes a dead path.
2. LIVE BUG (found via DB): low-track ENS members nulled per-scope -> MISSING_EXPECTED_MEMBERS blocks low posterior refresh for ~51 cities' far dates; run-level says COMPLETE so no retry lane fires. HK low 10.7h stale while high 1.5h.
3. Mixed-vintage fusion: per-model serving picks newest available cycle per model (up to 12h skew); between-spread computed over mixed cycles; no per-instance staleness down-weight.
4. Issuance shock real: cycle-boundary q TV distance p90=0.206, max 0.49; center jumps 4.7%>1C. Zeus posterior lags source_available by avg 25min (tail 11.7h); market can reprice first (adverse selection window).
5. Publication-lag blind spot: source_available_at - source_cycle_time persisted but never monitored; 30h TTL rests on it.
6. Isolation skeleton exists: FAMILY_ENTRY_BLOCKED (entry-only, family-scoped) never touches monitor lanes; staleness currently only expires posteriors (30h) — no graded per-city isolation tied to freshness.
7. Resting orders: BELIEF_REPRICE_DELTA=0.03 hysteresis + Q_VERSION_STALE zero-magnitude cancel (5min lane) + 20min rest deadline. Held: CI-separation + 2-cycle confirmation. New entries: no issuance-aware guard.
8. INCIDENT 2026-07-13/14 (measured): ALL CONUS cities' 07-14 posteriors starved 30-37h (Atlanta 37.3h, Seattle 32.1h, Chicago/Austin/LA 30.5h, NYC hi+lo 30.4h), all recovered same instant 07-14T14:42Z (Day0 lane). 277 failed materialize subprocesses for Chicago alone (~5min cadence, fingerprint suppression NOT effective), all BLOCKED REPLACEMENT_LIVE_POSTERIOR_REQUIREMENTS_NOT_MET. Onset exactly at fusion_upgrade instrument_set_expansion 07-13T08:15:39 when gem_hrdps_continental (CMC) first became capturable for the target (Chicago frozen scheme weight 0.766!). Raw values/anchors/ENS all present throughout. Root-cause chain: exp-starve tracing (suspect: frozen scheme completeness/serving ceiling -> center None -> live_eligible False).
9. Second live bug ROOT-CAUSED (exp-lowens, full file:line report): LOW-track per-member boundary-ambiguous quarantine nulls member values (extract_open_ens_localday.py:574-580: value=None when boundary_min < inner_min — cross-midnight window colder); downstream treats ANY null member as hard block (ingest_grib_to_snapshots.py:386-396 missing_member_value_for_contract_extrema -> contributes_to_target_extrema=0). Nulled count == ambiguous_member_count exactly (Amsterdam low 07-19: amb=10 -> 41/51). HIGH immune structurally: extract:513-522 has NO quarantine (daytime max stays inside local day). Introduced by d13d93c37 (2026-06-10, #403): relaxed snapshot-level quarantine to 26/51 majority but left per-member nulling + downstream any-null block — three-layer semantic inconsistency. BINDING BLOCK = contributes=0 (EXECUTABLE_FORECAST_NON_CONTRIBUTING_EXTREMA, reader :826-834,:1430 — floor-40 CANNOT save it; floor only cures MISSING_EXPECTED_MEMBERS). Run-level COMPLETE vs scope PARTIAL is by design (ecmwf_open_data.py:887-905) but means NO retry lane fires. Test blind spot: tests/test_opendata_observed_members_aggregation.py covers amb=51 and genuine-partial only, not minority-quarantine. FIX = option 1: ingest distinguishes 'quarantined by boundary rule' from 'genuinely missing' — minority-ambiguous scope keeps contributes=1 using remaining members' min; + regression test. Producer floor alignment alone (option 2) is INSUFFICIENT. Must pass leakage law (R-AH/R-AJ) review.
9b. Impacted live markets NOW: HK low 07-17 stale 11.5h; ZERO posteriors: Shanghai low 07-17+07-18, HK low 07-18, London low 07-18, Miami low 07-17, Paris low 07-18 — each w/ 11 live tokens.
9c. CONFIRMED third defect (DB proof, posterior 46741 London low 07-18): live FUSED_NORMAL_FULL posterior on carrier 07-16T18Z serves bpf.current_evidence_shape from ENS snapshot 1206110 cycle 2026-07-12T12Z — 4.5 DAYS old — as "current evidence" (within=1.30C, delta_ens=0.86C computed against stale member mean). Materializer ENS query (materializer:1339-1362) enforces causality (ENS cycle <= carrier) but NO age floor; quarantine bug starves newer contributing rows, so the query walks back indefinitely. Silent stale-as-current on the sigma path — violates root time law in spirit while passing every gate. Fix: ENS shape age bound (e.g. same 30h law or <= 2 cycles behind carrier) -> else shape=None -> non-live-grade (existing downstream handles it).
10. Retry-loop waste: 277 subprocess spawns x 240s cap on single-worker lane; SKIPPED_UNCHANGED_BLOCKED_INPUT fingerprint (queue:845-971) did not suppress (fingerprint churned or gate bypassed) — verify + fix.

## Phase 2 — first principles (operator redirect 2026-07-17): failsafe必须用不到
Operator: 防范只是 failsafe;第一性 = 永远拿最新数据做准确融合。数据-时间关系、增删数据的价值、最优组合 — 全部实测,不拍脑袋。

### Measured data-time law (settlement-graded, paired, previous_runs archive)
- 6h staleness cost (paired, all SIG): ukmo +0.168C, nbm +0.157, icon_eu +0.128, gem +0.121, gfs +0.113, icon_global +0.107, ecmwf +0.099, jma +0.074. ~0.1-0.17C per 6h cycle — freshness is worth real °C, uniformly.
- 12h: ecmwf +0.077, icon_global +0.135, gfs +0.108 (n=369, mostly SIG). Slope roughly linear-ish; no model is "staleness-flat" once paired (earlier unpaired flatness was selection bias).
- LAG DECOMPOSITION: provider publication lag avg 7.96h DOMINATES; Zeus machinery fast (avail->posterior p50 4.8min). p90+ tail = scopes waiting for the SLOW ENS-baseline leg (ecmwf opendata 00/12Z publishes ~08:07Z; 19 cities' new-day posteriors waited 7.5h with all instruments fresh).
- => Binding constraint on "always newest": the ENS baseline leg's 2/day 6-8h-late publish. Everything else already refreshes at 6h cadence with minutes of Zeus lag.

### Combo experiments DONE (exp-combo; full report scratchpad/combo_experiments_report.md; all paired-SE, walk-forward, settlement-graded, n=2797 eval instances)
- E2 leave-one-out: global backbone (icon_global/ukmo/ecmwf/gfs) carries real weight (removal raises MAE SIG). jma_seamless HURTS pooled/EU/Asia (-0.02C on removal, SIG — validates its 2026-06-17 live drop); dmi HURTS EU; ukmo HURTS EU yet is top Asia carrier (+0.052 SIG) — region-dependent value.
- E3: full precision pool beats frozen CSV scheme +0.08C (honest peek-free); ALL-models worse than selected subset by +0.18C — beyond ~3-4 good models MORE HURTS; greedy picks 2.7 models avg (peeky upper bound +0.26C).
- E4: EXCLUDING stale vintages HURTS (+0.012C pooled, +0.152C on affected instances, SIG) — consult confirmed: precision-downweight stale, never exclude.
- E5: staleness monotone: +0.096C/cycle-back (daily archive), +0.183C at 3 back. Consistent with my 6h paired slopes (+0.1-0.17C/6h on live cycles).
- E6: cwa/hko untestable in previous_runs archive (single_runs only).
- SYNTHESIS: model SELECTION is a bigger lever than freshness (both ~0.1C scale); frozen scheme over-includes hurting models in some regions and under-uses the pool in others → per-region settlement-driven precision pool over 3-4 vetted models indicated. Weekly walk-forward refit artifact (consult (c)) is the delivery vehicle.
### Consult follow-up verdict (0.94 conf; full text /tmp/cgc/answer_REQ-20260717-121403-d627dd.txt — archive before /tmp cleanup)
**P2-A CENTER/SHAPE DECOUPLING [BLOCKER-ranked]**: consult REVISES its own earlier same-cycle stance — same-cycle coherence is required WITHIN an ENS shape, never BETWEEN center and shape legs. Design: immutable CenterState (updates event-by-event on any newer capturable instrument) + ShapeState (one coherent ENS cycle, precomputed for ALL tradable-horizon targets on ENS arrival — kills the new-target ENS wait entirely); assembler joins newest center to newest licensed shape; componentwise monotone CAS (c',s')>=(c,s) with one strict. 30h bound stays as catastrophic guard that should never fire on routine 00/12Z schedule. Migration additive (two tables, backfill, pointer switch, atomic rollback).
**P2-B ANOMALY TRANSPORT MATH [HIGH]**: stale-shape reuse licensed ONLY as location-shape transport: translated members X'_j = mu_t + A_(j,e) (anomalies from ONE coherent cycle recentered on fresh center); recompute preimage hits on translated members; sigma_t^2 = max(sigma_min^2, a_g + b_g*S_e^2 + gamma_g*age/6) fitted walk-forward on "fresh center + then-available shape" (actual availability timestamps, never retrospective newest-pairing). Operational recentered delta_ens = 0 — do NOT retain delta_ens_center^2 (double-counts the new center); raw D_t kept as provenance only. Deterministic staleness slopes do NOT estimate shape staleness — gamma_g needs its own hybrid fit. Verify: availability-replay CRPS/PIT/coverage by shape-age bucket.
**P2-C AVAILABILITY-AWARE SELECTION [HIGH]**: score R_m = E[L(f at newest run available_at<=tau, Y)] on an exogenous decision schedule — publication timing enters the loss itself, no manual timing bonus. One basket per city×metric; serving-time age penalty + renormalize-on-absence (membership NEVER a readiness requirement — the incident-class antibody). Activate only when full posterior non-inferior on CRPS, not center MAE alone.
**P2-D BASKET GOVERNANCE [HIGH]** (converts exp-combo deltas into baskets w/o noise-mining): paired daily loss deltas w/ target-date-clustered/block-bootstrap SE; EB shrinkage toward alpha_(m,metric,lead); add only if P(delta < -eps_c)>=0.95, eps_c=max(0.05C, 0.03*MAE_B); smallest basket within max(eps,1SE) of best on outer rolling-origin eval; reject if CRPS-degradation UCB>0.02C. Min-n: 60 paired dates for city-specific, 30-59 pooled, <30 global core; pooled source decision >=300 pairs/20 cities/30 dates. Membership recompute after 28 new settled dates (monthly max, 28d freeze); weights/age-penalties refit weekly inside frozen basket. New product = new identity, zero inherited weight.
**P2-E DATA TYPES RANKED [MEDIUM]**: 1. hourly settlement-station obs assimilation into MEMBERS on Day0 (X'_j=max(O_<=t, F_j,>t)) — first by wide margin, feed exists, constrains settlement support mechanically (not center-move); 2. exact-station MOS/LAMP as candidate center instrument (valid-period must match local-calendar-day operator); 3. ERA5-Land only as hierarchical prior where settlement residuals sparse; 4. radar/satellite nowcast last (0-6h convective regime, heavy engineering).
**P2-F ENS FASTER MIRROR [BLOCKER on identity]**: never alias open-meteo ensemble to ecmwf_open_data product id (interpolated hourly + elevation downscaling + land-cell defaults = different product). Prefer their 3-hourly min/max extrema variables if conformance probe passes; disable elevation adjustment; match extraction operator. Two-stage licence: (i) transport equivalence 60 paired cycles/30 days, cluster by cycle, 100% member mapping, median |ext diff|<=0.05C p99<=0.25C, bias CI +-0.05C, spread ratio [0.95,1.05], recentered bin-prob MAD<=0.01; (ii) settlement equivalence: recentered CRPS UCB<=0.03C, coverage degradation<=2pp, no shoulder-bin degradation. Pass -> transport-equivalent carrier inherits calibration; fail -> new product (90 settled dates pooled, 60 city-specific). Whole-cycle fallback, never mix members across transports.
Alternative rejected by consult: joint availability-aware stacking/EMOS over everything (cleaner but needs far more history + parameter/migration risk). Reversal fact for P2-A: availability-replay showing recentered stale-shape loses more CRPS than fresh-center gains within the age range.

## Constraints (operator law)
- Fail-closed freshness; no stale-as-fresh. No shadow modes. No gate accretion / over-engineering. Minimal machinery.
- Only settlement-source accuracy backtest allowed; strictly walk-forward otherwise.
- Live branch advances continuously; merge worktree results promptly to main tree.

## T0-1 FIX DESIGN (2026-07-18, Fable session; CONFIRMED tier0 per audit §7 erratum)
Defect: Day0 served q integrates whole-day Normal; post-peak P(new-extreme-beyond-obs)
served 0.314 vs realized 0.070 (4.50x), LOW eve 0.409 vs 0.000. Evidence:
day0_mechanism_first_principles_audit.md §7 + day0_percity_diurnal_timing.md.
Design (slice 1, remaining-window CENTER correction, DB-only, fail-open):
- Data: day0_hourly_vectors (model='ecmwf_ifs', anchor family; times_json UTC + temps_c_json),
  latest causal capture <= computed_at for (city, target_date).
- delta_high = max(h over whole local day) - max(h over remaining hours >= computed_at);
  mu_r = mu*_corrected - delta_high. LOW symmetric: delta_low = min_rem - min_whole;
  mu_r = mu* + delta_low. delta >= 0 by construction; clamp negatives to 0.
- Apply mu_r ONLY when _day0_obs_extreme_c is not None, to BOTH _build_scaled_normal_uniform_q
  (point q) and _build_fused_q_bounds (mu_star) AND the finite-evidence floor mu (one
  probability world). Sigma unchanged (slice 2 = persisted remaining members narrowing).
- Fail-open: vector absent/stale/unparseable/timezone error -> delta=0 (byte-identical).
- Provenance: stamp day0_remaining_center_delta_c + vector_id + hours_remaining when fired.
- Non-day0 path byte-identical. HK oracle_truncate preimage untouched (transform handles).
Status: implementer dispatched (worktree). Baseline: tests/test_finite_evidence_probability_symmetry.py
+ test_staleness_variance_serving.py + materializer suites green at ceb55a796/cf2405d9f.
