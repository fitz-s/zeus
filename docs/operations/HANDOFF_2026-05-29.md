# Session Handoff — ENS bias / FT-domain canonical refit / live-unshadow mission

- Created: 2026-05-29
- Author: session 866db2ea (Opus), worktree `ens-bias-hierarchical`
- Purpose: single resume reference after repeated compaction lost task detail. Read this + the two docs in §1 before acting.
- Branch: `feat/ft-ship-64` (worktree HEAD `d56101ab9e`), **18 commits behind origin/main** (PR #359 stack unmerged).
- Live state at write: FT flag `false` (SHADOW), fills 4h `0`, daemons alive (live-trading 74152, forecast-live 85201, riskguard 85199). Disk 70Gi free / 93% used.

---

## 0 — Original goal directive (verbatim, for release)

The recurring stop-hook goal that drove this session, quoted exactly:

> **advance properly to live can restart correctly on right data, on right mode, no trading = not working, you must make sure that live can place order and it's making profit.**

Three sub-conditions the hook checks: (1) live restarts on right data + right mode — ✅ satisfied (daemons alive, OpenData today's run, order path wired); (2) live placing orders — ✗ blocked by shadow gate (correct, intentional); (3) making profit — ✗ no fills yet. This goal is **on hold by design** pending the T-series statistical gate (below). Operator is releasing it 2026-05-29.

---

## 1 — Where the authoritative docs live

| Doc | Path | What it holds |
|---|---|---|
| **Mission context (PRIMARY)** | `docs/operations/ENS_B_BLOCKERS_AND_M_SERIES_CONTEXT_2026-05-28.md` | §0 directives, §1 HB1-HB8→B-fix map, §3 full task ledger, §12 domino chain D1-D17, §13 verification probes, **§14 = CURRENT plan (T-series supersession)**. Read §0 + §14 first. |
| Pipeline provenance audit | `docs/operations/PIPELINE_PROVENANCE_AUDIT_2026-05-28.md` | 5-subagent data-source map; defects D1-D4 + §8b my verification. |
| Pipeline empirical detail | `docs/operations/PIPELINE_EMPIRICAL_DETAIL_2026-05-28.md` | Measured per-product/per-lead numbers; E1 (3h vs 6h 8.6°C disagreement), E2/D5 (lead mismatch), defect ledger §6, pre-rebuild order §7, my guesses §8. |
| SD3 statistics validation | `docs/operations/SD3_STATISTICS_VALIDATION_2026-05-28.md` | Test A (11/20 buckets correction-wins-OOS) + Test B + diagnosis. Has CORRECTION banner → product-lineage report. |
| SD3 product-lineage | `docs/operations/SD3_PRODUCT_LINEAGE_VALIDATION_2026-05-28.md` | **Decisive**: TIGGE bias does NOT transfer to live OpenData (mean −1.00°C). Only SF MAM has real live bias. |
| Row-action manifest (sd3) | `docs/operations/ROW_ACTION_MANIFEST_2026-05-28.csv` | 87-row reproducibility audit under sd3 gate. Closes BL-A/HB8. |
| Task files (OMC) | `~/.claude/tasks/` + TaskList tool | Task #17-#160 ledger. |
| Memory index | `~/.claude/projects/-Users-leofitz--openclaw-workspace-venus-zeus/memory/MEMORY.md` | Cross-session lessons. |
| Validation evidence CSVs | `docs/operations/sd3_validation_evidence/` | ENS_RESIDUAL_EVIDENCE_12CITY_HIGH.csv, product_stratified_high.csv, score_12city_high.csv, phase2_oos_bias_high.csv. |

---

## 2 — Background: what this mission is

Goal: bring Zeus live trading (Polymarket weather) onto the **full_transport (FT) bias-correction** error model — but only if the correction is statistically proven to beat the raw forecast out-of-sample against settlement. The system trades temperature-bin probabilities ~2 days out.

**The arc:**
1. Found ensemble forecast had cold bias (phantom edges) → built hierarchical FT bias estimator (#50-#56).
2. Hardened the producer→MC→promote pipeline with SD1-SD6 structural decisions + B1-B7 hard-blocker fixes + BL-A-E (#126-#149). All shipped, 65/65 B-tests green at `3ce02984b3`.
3. Wired FT into live entry + monitor (F1-F6, flag-gated, fail-OPEN shadow) (#94-#99).
4. Ran the MC rebuild at sd3 gate (`deabf8f64bde27b7`) on isolated staging `/private/tmp/scratch_ens_fit.db` — HIGH (71 rows, 14.8M pairs) + LOW (8 cities, 329k pairs). Verified via domino chain D1-D12 all GREEN.
5. **Operator pivot (post-D12):** the MC-promote-then-unshadow plan is SUPERSEDED. New rule: *bias_c training target not proven equal to the live correction target.* No unconditional FT correction. Raw baseline dominates unless a candidate wins OOS proper-score vs **settlement**. → T-series gate (§14 of primary doc).
6. **Pipeline audit (this session)** found two upstream defects that gate everything:
   - **D1 (HIGH):** live OpenData 3h product (`mx2t3`) is extracted with `STEP_HOURS=6` (a constant inherited from the TIGGE 6h product). Value-level corruption up to 8.6°C on same city/date. **Only the LIVE product is affected — the historical TIGGE ledger (mx2t6, 6h, correctly handled) is clean.** Bug file is `extract_open_ens_localday.py` in `51 source data/scripts/` — OUTSIDE this repo.
   - **D5 (HIGH):** the residual/calibration ledger is 0-12h-lead-dominated, but markets trade ~48h out. Bias is fit at the wrong lead.
   - Conclusion: live OpenData is already near-unbiased at trading lead; the big historical biases are short-lead + TIGGE + window artifacts. Promoting sd3 as-is = systematically over-warm bets = losing money. **HARD-HOLD confirmed.**

---

## 3 — Current status of every open thread

### CURRENT PLAN = T-series (supersedes D13-D17 live chain)

| T | Subject | Status | Notes |
|---|---|---|---|
| T1 (#156) | 00z/12z window test | ✅ DONE | H1 confirmed Seoul/HK/Jakarta/Busan/Shanghai (12z contaminated); refuted SF/Jeddah (station gap). |
| T2+T3 (#157/#158) | cycle-strict + unit-normalized residual ledger | ✅ DONE | `scripts/build_ens_residual_evidence.py` (`89e1aec9d1`). HIGH→0z, LOW→12z; settlement °F→°C via members_unit. |
| **T4 (#159)** | candidate scorer, blocked-fold OOS proper-score vs settlement | 🔵 IN PROGRESS | Accept-gate built: `scripts/score_error_model_candidates.py` + `tests/test_t4_selection_rule_invariants.py` (10/10 green). Scoring engine `scripts/audit_refit_proper_scores.py`. **NOT YET run as the lead-stratified candidate selection.** Must bake in D5: score AT trading lead (~48h), not pooled. |
| T5 (#160) | 12-city small-sample MC smoke, pass conditions A-E | ⏸️ after T4 | Pre-full-MC gate. |

Candidate set per (city×metric×season): {raw identity, scale-only, prior-bias, live-bias, transported, hierarchical-fallback}. **Accept a correction only if** beats raw on ≥2/3 proper scores (LogLoss/RPS/Brier) AND bootstrap LCB(improvement)>0 AND no catastrophic cohort regression. Else raw.

### Pre-rebuild fix order (from pipeline audit §7) — the real remaining work

1. **D1 fix** (operator-gated, touches live data path outside repo): make `STEP_HOURS` product-derived (3 for mx2t3, 6 for mx2t6) or read GRIB startStep-endStep in `extract_open_ens_localday.py`; re-extract live mx2t3 since 2026-05-06. Relationship test: mx2t3 vs mx2t6 same-issue daily-max agree within ~1°C.
2. **D5 fix** (in-repo, safe): lead-stratify the residual ledger + bias fit; fit/serve at the trading lead (~48h), not the freshest snapshot.
3. **D3:** split Platt by data_version. **D4:** lead-bucket the bias dimension.
4. Re-run product-stratified Test A / transfer / consensus-sanity / small clean MC on the corrected, lead-matched base.
5. THEN T4/T5 → consider unshadow.

### Live chain (all blocked, do NOT fire autonomously)

| Domino | Status | Blocker |
|---|---|---|
| D13 #359 merge | OPEN, external-gated | Stacked PR #355→#357→#359→main. Not autonomously mergeable. |
| D14 / M1 (#152) rebase worktree onto main | blocked by D13 | Big rename diff (6,438+/11,543−). B1-B7 patches must carry over to new paths. |
| D15 / M3 (#151) trace wiring | ✅ GREEN | trace writer healthy on world.db (173 rows/2h). Retraction recorded — was querying legacy zeus_trades.db. |
| D16 / M4 (#154) bin-bias before/after | SUPERSEDED by T4/T5 | open-meteo is another forecast, not the trade target. Arbiter = settlement. |
| D17 / M5 (#155) unshadow live | **HARD-HOLD** | until T4/T5 pass on D1+D5-corrected base. |

### Misc OPEN tasks (pending/in_progress only — verified against TaskList 2026-05-29)

- #64 (in_progress) REBUILD PARENT: full_transport canonical refit — parent of M2+R-series; closes when T-gate resolves.
- #105 (pending) F7b: trace writer must tag `p_raw_domain` (all world.db rows NULL). Should land before unshadow. Gated by settlement_write capability → needs operator ARCH_PLAN_EVIDENCE.
- #109 (in_progress) P0: market scanner ingests 1 of 11 sub-markets per event — affects candidate coverage / shadow trace completeness.
- #110 (pending) per-outcome HTTP overhead in capture_executable_market_snapshot — perf, not correctness.
- #121 (pending) portfolio_quarantined blocker (RiskGuard) — required for M5 fill path.
- #124 (in_progress) post-restart verify candidates+trace+first order — subsumed by M3 (#151, done); can close.
- #63 (pending) contingent refinement roadmap — post-T-gate follow-up.
- #131-#139 R-series — subsumed by M-series / operator-gated (R4 #134 done; rest pending).
- #152 (pending) M1 rebase; #154 (pending) M4; #155 (pending) M5 — live chain, see table above.
- #159 (in_progress) T4; #160 (pending) T5 — current gate, see T-series table above.

(Already COMPLETED, do NOT redo: #107 promote conn.commit fix, #116 test fixture, #29 test_market_phase_dispatch, #134 R4 iso producer+MC.)

---

## 4 — Hard invariants (do not violate)

- **Never write production DBs from the producer** — `_refuse_prod_db` antibody. Run on isolated staging only.
- **No partial calibration on new cities** — full historical ingest → calibration once at depth → Platt → 14-day shadow.
- **Cross-DB writes = INV-37** (ATTACH + SAVEPOINT, never independent connections).
- **DB authority:** zeus-world.db (canonical), zeus-forecasts.db (calibration+ensemble), zeus_trades.db (trades).
- **Daemon liveness:** `launchctl list` STATUS = last exit code, NOT current health. Verify via PID + ps + log byte-delta.
- **Multi-session collision:** live root shared; always `git rev-parse HEAD` before any live daemon restart.
- **Stats locked** (changing any re-bumps gate hash): MIN_PRIOR_N=5, MIN_PAIRED_N=5, DEFAULT_MIN_LIVE_N=20, CONSERVATIVE_RESIDUAL_FLOOR_C=3.0, gate `ftgate-2026-05-28-sd3` = `deabf8f64bde27b7`.
- **First live order must complete programmatically** — no manual completion ever.

---

## 5 — Session-mode state at handoff

- 2 heartbeat crons deleted (`905fce6f`, `5aeea3e5`) — scheduler empty.
- autopilot / ralph / ultrawork / skill-active state cleared (`state_clear` + legacy sweep).
- `#359` PR monitor (`bvo7624iz`, persistent) — still armed unless operator kills it.
- Goal stop-hook ("advance properly to live…") is OMC/openclaw-layer injected, not a local file — operator releases on their side using the §0 verbatim text.

---

## 6 — Completed-task ledger (do NOT redo — verified against TaskList 2026-05-29)

Grouped by phase. Full per-task detail in the TaskList tool; this is the survival summary.

- **Deploy + live-gate foundation (#17-#28):** safe-start sequence, deploy origin/main GATED, temp alignment verify, enable trading, P0-2/P1-2 hardening, IOC Fix B/C forward-port, P0-3 day0 window, P0-D sanity telemetry, P0-E quarantine, strategies 6.1-6.6 promoted, live-fill observation, ECMWF 00z ingest root-cause.
- **Pipeline audit + obs/probes (#29-#48):** forecast→p_raw→p_cal→edge map, LIVE-PROB-P0 shape gate, forecast-run selection, FIX-3/4/5 (day0 gate, frontier telemetry, obs-coverage job), HKO ingest, schema-34/35 collision, Probes 1-6 (Amsterdam reject, FP=0, DAY0-P1, rails/governance), PR-1/2/3 merged, deploy+hotfix reconcile, market-discovery-collapse root-cause.
- **City onboarding + cold-bias discovery (#49-#62):** jinan/zhengzhou onboard, daily-max timezone extraction fix (phantom edges), extraction-window vs model-bias discrimination, market-price-biased decision, hierarchical ENS bias estimator (TDD), PR#334/#335/#336 predictive-error layer, city-onboarding doc, data-parity audit, DDD scoping + fixes, authority-doc pointers.
- **FT canonical refit pipeline (#65-#88):** proper-score audit harness, ft pairs preserve+promote, model_bias_ens_v2 writer fix+schema, fit ft posteriors, HIGH Platt/identity, sentinel reader gate, monitor_refresh wiring, world.db promote+pin+schema37, ROOT-FIX-A window selection, ship-readiness CI gate, replay-equivalence harness, producer STEP1-6, transport n_paired gate, SIGNAL_QUALITY false-reject fix, calibrator coverage.
- **Live FT wiring F1-F6 (#94-#99):** entry path, world.db schema boot, promote CLI, read_bias_model STAGING filter, settings.json doc-code align, db_table_ownership.yaml register.
- **Live-block fixes (#101-#123):** position_drift suppression, freshness gate, ws_gap_guard, opening_hunt filter, forecast-live status=1, promote conn.commit (#107), evaluator isoformat fix, East-Asia wrong-direction remediation (#111), invariant audit (#114), row-lineage trace, test fixture (#116), PR#349 merge + live align, branch purge, trace-writer-silent investigation (#120→retracted, healthy), row-reproducibility audit, PR#354 salvage, small-sample rebuild.
- **SD1-SD6 structural decisions (#126-#130):** coverage=evidence, insufficient-prior→wide-σ+MIN_PRIOR_N=5, selective driver→manifest, pair-batch domain manifest, producer preflight + P0-P8 MC entry gate.
- **BL-A-E hard-blocker fixes (#140-#144):** ROW_ACTION_MANIFEST regen, season/month-scoped rebuild, pair-batch same-DB query, A-row production n_mc, producer never-writes-prod antibody.
- **B1-B7 pre-MC hard-blockers (#145-#149 + B6/B7):** hemisphere-aware season, cache_key+target_month, replay-uses-STAGING, training_cutoff threading, gate_changed-aborts. (Shipped `3ce02984b3`, 65/65 tests green.)
- **M-series done (#150,#151,#153):** M0 #359 monitor armed, M3 shadow verify (trace writer healthy), M2 rebuild+refit at sd3 (HIGH 71 rows/14.8M pairs + LOW 8 cities/329k pairs, domino D1-D12 GREEN).
- **R4 (#134):** small-sample iso producer + MC verified diff.
- **T1-T3 (#156-#158):** 00z/12z window test, residual evidence ledger, strict target-window extraction.

## 7 — Heartbeat-required reading (the file the heartbeat cron mandated)

The 2hr heartbeat cron prompt's STEP 1 mandated reading:

**`docs/operations/ENS_B_BLOCKERS_AND_M_SERIES_CONTEXT_2026-05-28.md`** — specifically §0 (operator standing directives), §12 (domino chain D1-D17), §13 (verification probes D2-D9), and §14 (T-series supersession = current authority). This is the same PRIMARY doc in §1 above. It is the single source of truth between heartbeat fires; this handoff summarizes it but does not replace it. Both heartbeat crons are now deleted.
