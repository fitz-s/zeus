# Full Handoff — 2026-05-30 (live pipeline + overdue backlog)

## LIVE STATE (now)
- Branch `main @ 6dbc71ce3e`. Daemon `com.zeus.live-trading` running SHADOW (`edli_shadow_no_submit`, `real_order_submit_enabled=false`). Wallet $185, untouched. `decision_events=0`, no fills.
- ALL session fixes UNCOMMITTED (working tree). Files touched: `src/engine/event_reactor_adapter.py`, `src/events/reactor.py`, `src/events/event_store.py`, `src/events/triggers/forecast_snapshot_ready.py`, `src/main.py`, `config/settings.json`, `scripts/ingest_grib_to_snapshots.py`, `src/state/db.py` (#34), tests (`tests/engine/test_event_reactor_no_bypass.py`, `tests/events/test_reactor.py`, `tests/events/test_forecast_snapshot_ready.py`, `tests/money_path/test_edli_live_canary.py`, `tests/money_path/test_edli_online_invariants.py`).
- **Milestone:** the EDLI q/FDR kernel now computes `q` in the LIVE reactor (`regret_with_q=2`) — was a multi-day silent halt at an unimplemented kernel. But the pipeline still LEAKS: families consumed at gates before a candidate. `decision_events` still 0.

## OVERDUE BACKLOG (NOT operator-gated — these are mine, behind schedule)
- **#10 Low rebuild (D1-LOW window fix + re-extract) — started 3 days ago, NOT done.** I did NOT work it this session. No code/extract this session. Needs: the LOW product-derive aggregation-window fix + full re-extract. STATUS UNKNOWN beyond "in_progress 3 days" — re-verify what's actually on disk before assuming any of it landed.
- **#27 Platt re-compete via OOS gate — started 1 day ago, NOT done.** OOS before/after numbers never produced (agents died on degF→F normalize). `identity-Platt` is the current live default. NOTE: the calibration **read** path is now fixed (#44) so the reactor can load existing Platt calibrators — but **re-competing/promoting** new Platt (the actual #27 work) is not done. Branch `fix/platt-oos-gate` (tip ~7f3107790b), harness 28/28; run `scripts/score_platt_candidates.py` → identity-vs-Platt LogLoss/RPS/bin-bias; improve→promote, regress→diagnose data-vs-process.

## WHAT WAS FIXED THIS SESSION (short: what / how / status). All UNCOMMITTED.
1. **#38 forecast 30h halt — DONE+verified.** ECMWF OpenData ingest write-refused (`_v1` data_version vs #362 allowlist). Fix: `scripts/ingest_grib_to_snapshots.py:~520` normalize `ecmwf_opendata_*_v1 → drop _v1` before `assert_data_version_allowed`. Forecast pipeline recovered.
2. **#35 freshness decouple — DONE+live-proven.** Entry/FDR gate required 30s PRICE-freshness → impossible for >30s family captures. Fix: `_latest_snapshot_rows_for_event_family` +`require_fresh` param; entry callers (gate + receipt) pass `require_fresh=False` (bind IDENTITY); price-freshness stays at submission (`assert_snapshot_executable`).
3. **#39 °C/°F unit — DONE+live-proven.** Bin unit defaulted to 'F' → all Celsius cities `EVENT_BOUND_MARKET_TOPOLOGY_INVALID`. Fix: `_bin_from_market_event` derives unit from `SettlementSemantics.for_city(city).measurement_unit` (new `_settlement_unit_for_payload_city`).
4. **#40 EDLI q/FDR kernel — DONE (107 tests, live-proven `q`).** Was a corrupted abandoned refactor dispatched to a fail-closed empty stub. Fixes (`event_reactor_adapter.py`): repoint dispatch stub→`_canonical_probability_and_fdr_proof`; reconstruct `_forecast_snapshot_row_for_event` (was corrupted `_canonical_probability_rows`); rename `_canonical_hypothesis_rows`→`_market_analysis_from_event_snapshot` (drop vestigial conn + dead code); `_generate_candidate_proofs` `canonical_*`→`generated_*`; module import `+edge_n_bootstrap,+settings`; **alpha-correctness:** `q_lcb` fed edge-LCB where robust_trade_score wants q-LCB → `q_lcb=edge_lcb+p_market`; buy_no optional (scan omits non-executable side, q from `analysis.p_posterior`); day0 vector-mask helper + arity. Stripped 44 stale `unauthored` xfail markers.
5. **#41 decision-first emission filter — DONE (17 tests).** Reactor budget starved by market-less LOW/far families (Polymarket lists none). Fix: `scan_committed_snapshots` only emits families with a `market_events` row (fail-open if table empty). Live: 1325→918 market-backed.
6. **Throughput — DONE.** `no_submit_proof_limit` 10→50 (`config/settings.json` + canary assert). Emission priority 10→50 (`build_forecast_snapshot_ready_event`) so market-backed families jump the backlog. Refresh family-scan ORDER BY priority (`_refresh_pending_family_snapshots`).
7. **#42 snapshot-retry — DONE (114 tests, live-confirmed reaching inference).** Reactor consumed a family on a TRANSIENT `EXECUTABLE_SNAPSHOT_BLOCKED` (snapshots not captured that exact cycle). Fix (`reactor.py`+`event_store.py`): `_process_one` returns `_EXECUTABLE_SNAPSHOT_RETRY`; `process_pending` `requeue_pending` until `MAX_EXECUTABLE_SNAPSHOT_RETRIES=8`→dead-letter; new `EventStore.attempt_count`/`requeue_pending`; `ReactorResult.retried`.
8. **#44 calibration K1 attach — DONE+verified.** `CALIBRATION_AUTHORITY_MISSING:calibration store unavailable`. Root: calibration authority K1-split — `platt_models` in zeus-world.db, `calibration_pairs` (48M) in zeus-forecasts.db; `get_calibrator` reads both; reactor's calibration_conn (`get_world_connection()`) had only `main` attached. Fix: `_edli_event_reactor_cycle` ATTACHes forecasts to the world conn (read-only). Verified: real Platt calibrator loads (Wuhan, level=3). **This is what let `q` compute live.**
9. **#43 SOURCE_CAPTURED retry — DONE (36 tests).** Forecast-source re-ingestion race (`source_available_at` updated after cycle decision moment). Fix: `_process_one` treats a money-path reason containing `SOURCE_CAPTURED_AFTER_DECISION_TIME` as transient → `_EXECUTABLE_SNAPSHOT_RETRY` (requeue). Reuses #42 machinery.
10. **#45 bankroll warm — APPLIED, UNCONFIRMED.** `KELLY_PROOF_MISSING:bankroll_provider_unavailable`. No-submit Kelly reads `bankroll_provider.cached()` which was None (EDLI process never called `current()`). Fix: `_edli_event_reactor_cycle` warms `bankroll_provider.current()` once/cycle. Log shows no "warm failed" (current() succeeded) but NO new family reached Kelly post-fix to confirm it passes.

## REMAINING TO FIRST LIVE CANDIDATE (live pipeline still leaky; `decision_events=0`)
Latest live regret (last 120), dominant leaks:
- **`FORECAST_READER_SNAPSHOT_MISMATCH` (25) — NOW THE DOMINANT INFERENCE LEAK.** The forecast reader (`src/data/executable_forecast_reader.py`, called in the receipt builder) selects the LATEST committed snapshot, which ≠ the event's `causal_snapshot_id` (a newer snapshot for the family was re-ingested). Consumes the family. FIX (investigate): reader should honor the event's causal snapshot, OR re-emit events keyed to the latest snapshot, OR treat mismatch as transient/retryable. This is the next gate to a candidate.
- **`EXECUTABLE_SNAPSHOT_BLOCKED` (64) — dead-lettered** families that exhausted 8 retries = genuinely uncapturable (the targeted refresh/Gamma isn't returning their market). Investigate which families + why capture misses (likely illiquid or Gamma slug miss). Lower priority (those families may not be tradeable).
- **`SOURCE_CAPTURED` (22)** — should now requeue (#43); residual count = either MAX-retried dead-letters or pre-restart rows. Re-verify after a clean window.
- **`CALIBRATION` (7)** — pre-#44-load rows (stale). Should not recur.
- **`KELLY/bankroll` (2)** — confirm #45 once a family passes the inference gates.
- After leaks closed: first family survives all gates → `decision_events>0`/`edli_no_submit_receipts>0` = FIRST CANDIDATE → **#24 bias** (shadow p_raw vs online ≤1) → operator unshadow (#12, irreversible) → fills → GOAL #36 (3 verified fills + 120min heartbeat + exit verification + alpha).

## OTHER PENDING TASKS (full list)
- **#24** forecast-website error / shadow p_raw-vs-online bias ≤1 — the unshadow gate; needs decisions flowing first.
- **#34** `init_schema_forecasts` fresh-build crash (world `market_events` ghost) — fix in worktree `.claude/worktrees/agent-af3da32d60f3ea852` (db.py `_WORLD_ATTACH_EXCLUDED={"market_events"}`); APPLIED to live src/state/db.py → fingerprint drift (computed 35342b7335 ≠ pin 1e667af0). Decide revert vs `--write-pin 35342b73`.
- **#11** gate-hash re-bump (retire sd3, mint new id — keying changed). Pre-unshadow.
- **#23** multi-surface live-launch readiness audit. **#30** Decision-1 harden (evidence pairing). **#32** A–J pre-live acceptance suite. **#9** P5 OOS selection (FT promotion gate). **#28** raise OpenData cap (future).
- **`position_current` missing `chain_avg_price` column** — Traceback in `src/execution/exchange_reconcile.py::_apply_exit_fill_projection_and_execution_fact` (exit/PnL schema drift; affects GOAL exit-verification). Add column/migration.

## DISCIPLINE / GOTCHAS
- NOTHING committed. Commit the session set as coherent units; opus critic on the kernel (#40, Tier-0 alpha) before unshadow.
- SHADOW stays on until #24 bias passes (operator unshadow is the only path to fills).
- 2 shadow-config tests (`test_edli_online_invariants` enabled-False asserts) + canary class still assert the pre-shadow contract — update to shadow contract.
- Tooling gotchas: Bash stdout MANGLES tokens (data_version/dataset_id/edge_n_bootstrap/etc) — verify via Read on /tmp files, not echo. `created_at` is stored with `T` separator; SQLite `datetime('now')` uses space and `'T'>' '` lexically → `created_at >= datetime('now','-Ns')` matches ALL of today (caused a false "bulk emitter" alarm). `Grep` tool absent this session — use Bash `rg`.
