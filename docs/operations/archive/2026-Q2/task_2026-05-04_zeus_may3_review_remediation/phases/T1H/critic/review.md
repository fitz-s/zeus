# T1H Critic Review (10-ATTACK)

Reviewer: critic (subagent `a03538fb0b5f999ed`), 2026-05-05.
HEAD at review: T1G closeout commit + T1H worktree changes.
Phase contract: `phases/T1H/phase.json` (5 invariants, K1 read-only diagnostic).
Phase scope: `phases/T1H/scope.yaml` (all src/** out_of_scope).

## Phase context

- Tracked diff: `architecture/script_manifest.yaml` (+29), `architecture/test_topology.yaml` (+27). Untracked: `scripts/state_census.py` (483 lines), `tests/test_state_census.py` (586 lines) — both in_scope. The two new files are expected untracked at critic stage; git-master stages on APPROVE.
- 0 deviations from executor; 18 tests pass (`18 passed in 0.12s`); 0 src/ files touched; LAST T1 phase, output feeds T2 admission.
- Cross-phase invariants intact: T1A (1 settlement_commands DDL match in `src/execution/settlement_commands.py:28`); T1F+T1BD+T1C+T1E+T1G (no diff in any prior-phase source surface).

## Attack table

| # | Attack | Verdict | Evidence (file:line) |
|---|--------|---------|---------------------|
| 1 | Read-only URI verification | PASS | `grep -n "mode=ro\|sqlite3.connect" scripts/state_census.py` returns 4 hits, ALL within `_open_read_only` at lines 100-110. Line 107: `uri = f"file:{db_path}?mode=ro"`. Line 108: `conn = sqlite3.connect(uri, uri=True)` — the `uri=True` flag is REQUIRED for SQLite to parse the URI mode parameter; without it the string would be treated as a literal filename and the read-only constraint would be silently dropped. Verified `uri=True` is present. There are zero other `sqlite3.connect` invocations in the file. Mental write-attempt: `conn.execute("INSERT INTO settlement_commands ...")` raises `sqlite3.OperationalError: attempt to write a readonly database` because SQLite enforces `?mode=ro` at the connection layer (verified by test_census_read_only at tests/test_state_census.py:105 which does exactly this and asserts the OperationalError). |
| 2 | data_unavailable distinct from no_redeem_queued | PASS | `_classify_redeem_truth` at scripts/state_census.py:128-153: line 145 `return "data_unavailable"` for the 0-rows path (no settlement_commands rows for the condition_id); line 149 `return "data_unavailable"` for the no-table path (settlement_commands table missing entirely); line 153 fallback uses `"data_unavailable"` as final default. Inspection of `_REDEEM_STATE_MAP` (lines 117-125) shows it maps SettlementState enum strings to lowercase classifier strings — and CRUCIALLY does NOT contain `"no_redeem_queued"` as either key or value. The string `"no_redeem_queued"` appears in scripts/state_census.py ONLY at lines 8 (docstring schema enumeration) and 132 (comment explaining the invariant: "never 'no_redeem_queued'. 'no_redeem_queued' is reserved for future use"). Test at tests/test_state_census.py:184 explicitly asserts `pos["redeem_truth"] == "data_unavailable"` and line 189 asserts `pos["redeem_truth"] != "no_redeem_queued"`. T1H-DATA-UNAVAILABLE-DISTINCT-FROM-NO-REDEEM satisfied. |
| 3 | Placeholder identity detection (case-sensitive + top-level anomalies) | PASS | `_classify_identity_truth` at scripts/state_census.py:243-267: line 243 `if condition_id and condition_id.startswith("legacy:")` — Python `str.startswith` is case-sensitive (matches T1F's `compatibility_placeholder_reason` predicate at venue_submission_envelope.py which also uses startswith). Line 264 same predicate against the envelope row's `condition_id`. Line 266 `if env_qid == "legacy-compat"` — equality also case-sensitive (would not match `"LEGACY-COMPAT"`). Anomaly path: line 391 `anomalies.append({...})` with `axis="identity_truth"`, `reason="placeholder"`, `detail=...` inside the per-position loop AND the `anomalies` list is a TOP-LEVEL key in the JSON output (line 410 `result["anomalies"] = anomalies` after the loop closes). The anomalies list IS at the top level, not nested under the position. Test at tests/test_state_census.py TestCensusDetectsPlaceholderIdentity (4 methods at lines 239/288/324/353) covers condition_id legacy:, question_id legacy-compat, live-bound (negative), and no-envelope (negative) — all asserted. T1H-DETECTS-PLACEHOLDER-IDENTITY satisfied. |
| 4 | Corrected-without-fill-authority constant import | PASS | scripts/state_census.py:74 `from src.state.portfolio import FILL_AUTHORITY_VENUE_CONFIRMED_FULL`. The constant is sourced from canonical truth: `src/state/portfolio.py:223 FILL_AUTHORITY_VENUE_CONFIRMED_FULL = "venue_confirmed_full"`. Lines 76-78 provide a fallback `"venue_confirmed_full"` ONLY for ImportError (sys.path / venv issues at script-direct invocation); the comment at line 77 explicitly cites "The string value is locked in src/state/portfolio.py:223". The detection at line 356 uses the imported constant: `and fill_authority != FILL_AUTHORITY_VENUE_CONFIRMED_FULL`. **Caveat C-1 LOW: the fallback string `"venue_confirmed_full"` IS a hard-coded duplicate that would drift if the canonical constant changes. Severity LOW because (a) it's only triggered on ImportError (rare — only direct-script-invocation outside a venv), (b) the comment explicitly flags the duplicate, (c) a follow-up could fail-closed instead of falling back. Not blocking.** Imported-from-src verdict: yes. |
| 5 | Provenance headers | PASS | `head -3 scripts/state_census.py`: lines 1-3 are `# Created: 2026-05-05`, `# Last reused or audited: 2026-05-05`, `# Authority basis: docs/operations/task_2026-05-04_zeus_may3_review_remediation/phases/T1H/phase.json`. Same three fields at lines 1-3 of `tests/test_state_census.py`. Both files satisfy the ~/.claude/CLAUDE.md Code Provenance section. T1H-PROVENANCE-HEADER-PRESENT satisfied for both files. |
| 6 | Script manifest registration | PASS | `architecture/script_manifest.yaml:622-649`. Verified: `class: diagnostic` (line 626 — read-only-appropriate, not 'repair' or 'execution'), `lifecycle: long_lived`, `status: active`, `authority_scope: diagnostic_non_promotion`, `read_targets: [state/zeus_trades.db]` (line 631), `write_targets: [stdout, temp]` (line 632 — only stdout/temp, no DB writes), `target_db: state/zeus_trades.db`, `dangerous_if_run: false` (line 634 — correctly false because read-only), `dry_run_default: true` (line 635), `apply_flag: null` (line 636 — explicitly absent as required by phase.json invariant), `external_inputs: []`, `promotion_barrier: "Diagnostic output cannot authorize canonical DB writes."`, `required_tests: [tests/test_state_census.py]` (line 649). Header provenance fields (Created/Last reused or audited/Authority basis) embedded in the manifest entry at lines 623-625. All required fields present and correct. T1H-PROVENANCE-HEADER-PRESENT for the manifest row also satisfied. |
| 7 | 18 tests vs 4 named invariants | PASS | Test enumeration via `grep "    def test_" tests/test_state_census.py` returns 18 methods across 5 classes. Mapping: `TestCensusReadOnly` (3): test_census_read_only (named invariant 1), test_census_run_does_not_create_db (legitimate — verifies mode=ro doesn't create files), test_main_requires_read_only_flag (legitimate — CLI flag enforcement). `TestCensusDataUnavailableOnEmptySettlementQueue` (3): test_census_data_unavailable_on_empty_settlement_queue (named invariant 2), test_census_redeem_truth_with_existing_row (legitimate — happy-path classification), test_classify_redeem_truth_no_table (legitimate — missing-table fallback). `TestCensusDetectsPlaceholderIdentity` (4): named invariant 3 plus condition_id/question_id paths + live-bound and no-envelope NEGATIVE cases. `TestCensusDetectsCorrectedRowWithoutFillAuthority` (3): named invariant 4 plus 2 negatives (corrected+locked-authority NOT flagged; not-corrected NOT flagged). `TestCensusJSONShape` (5): top-level-keys, six-axes-per-row, json-serializability, --json-out CLI, missing-DB warning. Spot-checked 3 helpers (test_census_run_does_not_create_db, test_main_requires_read_only_flag, test_census_corrected_with_locked_fill_authority_is_open) — all assert distinct, legitimate behaviors. NO theatrical or redundant tests. 14 helpers + 4 named = 18; matches phase.json minimum of 4 with substantive coverage above it. |
| 8 | JSON output schema completeness | PASS | scripts/state_census.py:316-413 builds the output dict. Top-level keys (line 320-324): `generated_at` (ISO-8601 from `datetime.datetime.now(datetime.timezone.utc).isoformat()`), `census_version: "T1H/v1"` (line 321 — versioned schema marker), `summary` (anomaly_count etc.), `positions` (per-position rows), `anomalies` (line 324 initialized as empty list, populated at lines 360/391, set as top-level at line 410). Per-position row schema documented in docstring at lines 36-43 enumerates all 6 axes. Test at tests/test_state_census.py:490 (`test_census_output_has_required_top_level_keys`) asserts all top-level keys present; test at line 515 (`test_census_position_row_has_all_six_axes`) asserts each position row has all 6 classification fields. No corners cut. |
| 9 | 0 src/ touch and prior-phase regression | PASS | `git diff src/` returns empty. `git diff --stat` shows only 2 modified files (architecture/script_manifest.yaml, architecture/test_topology.yaml) plus 2 untracked test/script files. T1A: `git grep -c "CREATE TABLE IF NOT EXISTS settlement_commands" src/execution/settlement_commands.py` = 1. T1F+T1BD+T1C+T1E+T1G: zero diffs in any prior-phase source surface. The script IMPORTS from src/state/portfolio (line 74) and src/state/db (line 90) read-only — these are imports, not modifications. |
| 10 | Cross-phase consumed-invariant check | PASS | The census's anomaly-detection logic CONSUMES (not undermines) prior-phase invariants: (a) T1F-ADAPTER-ASSERTS-LIVE-BOUND-BEFORE-SDK — the placeholder-identity classifier at lines 243-267 uses the SAME `legacy:` / `legacy-compat` predicates as T1F's gate (verified mirror at `src/contracts/venue_submission_envelope.py:95-105`), so a placeholder envelope flagged by the census matches one rejected by the live gate. (b) T1BD-CORRECTED-NO-CHAIN-MUTATION-ALL-SITES — the corrected-without-fill-authority detector at lines 346-367 reads the SAME `corrected_executable_economics_eligible` field that T1BD locks against chain mutation; the census reports anomalies but does not write, so it cannot regress T1BD. (c) T1A-DDL-SINGLE-SOURCE — census reads settlement_commands rows but does not redefine the DDL; T1A intact. (d) T1C-SETTLEMENT-NOT-REDEEM — census's redeem_truth axis classifies SettlementState rows but does not transition them; T1C boundary preserved. (e) T1E-LOCK-TIMEOUT-DEGRADE-NOT-CRASH — census uses read-only URI mode, so it cannot contend for the writer lock; orthogonal to T1E. (f) T1G-AUDIT-ARTIFACT-WRITTEN — census's identity_truth axis verifies envelope persistence outcomes that T1G audited; consumes T1G's audit conclusions. All 6 prior invariants preserved. |

## Caveats

- **C-1 (LOW)**: `scripts/state_census.py:78` provides a fallback `FILL_AUTHORITY_VENUE_CONFIRMED_FULL = "venue_confirmed_full"` for ImportError (when src/* is not importable, e.g., direct script invocation outside venv). This is a HARD-CODED DUPLICATE of the canonical constant at `src/state/portfolio.py:223`. If the canonical constant changes, the fallback would silently drift. Severity LOW because (a) the fallback only triggers on ImportError (rare in production), (b) the inline comment at line 77 explicitly flags the duplicate (`The string value is locked in src/state/portfolio.py:223`), (c) the failure mode is misclassification (false-negative on corrected-without-fill-authority detection), not silent corruption. Recommend a follow-up: replace fallback with `raise RuntimeError("src.state.portfolio not importable; T1H census cannot run without canonical fill_authority constant")` to fail-closed. Not blocking.

- **C-2 (LOW carry-forward from T1F-T1G)**: Same telemetry text-tap pattern caveat as prior phases. T1H does NOT introduce new counters (it is read-only diagnostic), so this is not a NEW concern; only noted for consistency. T2F typed sink will retroactively address all prior-phase counter emits.

## T1H Verdict

CRITIC_DONE_T1H
verdict: APPROVE_WITH_CAVEATS
caveats: ["C-1 LOW: scripts/state_census.py:78 ImportError fallback hard-codes FILL_AUTHORITY_VENUE_CONFIRMED_FULL=\"venue_confirmed_full\" duplicating src/state/portfolio.py:223 — would silently drift if canonical constant changes; severity LOW (fallback only triggers on ImportError; inline comment flags the duplicate; failure mode is misclassification not corruption); recommend fail-closed alternative", "C-2 LOW (carry-forward from T1F-T1G): consistent treatment with prior phases; T1H introduces no new counters so this is not a new concern"]
read_only_uri_used_everywhere: yes
data_unavailable_string_distinct: yes
placeholder_detection_case_sensitive: yes
fill_authority_constant_imported_from_src: yes
provenance_headers_present: yes
script_manifest_class_correct: yes
test_count_18_above_4_minimum: yes
helper_tests_legitimate: yes
json_schema_complete: yes
no_src_touched: yes
no_prior_regression: yes
ready_for_close: yes
