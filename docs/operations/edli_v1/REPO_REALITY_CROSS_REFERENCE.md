# EDLI v1 Repo Reality Cross-Reference

Created: 2026-05-24

| Claim / object | Reference spec source | Repo evidence | External evidence | Implementation action | Unknown / review required |
| --- | --- | --- | --- | --- | --- |
| PR328 semantic wiring readiness. | User-requested EDLI v1 final acceptance contract. | `docs/operations/edli_v1/PR328_DEEP_SEMANTIC_WIRING_REVIEW.md` records a NO-GO review: reactor uses old-cycle summary, FDR/Kelly are no-op placeholders at the EDLI adapter boundary, executable snapshot gate is not event-specific, live inference/TradeScore are not wired into reactor, Day0 trigger source is semantically contested, market-channel token metadata is incomplete, and online config is premature. | Polymarket market-channel/orderbook/order docs cited by the audit for public-data-vs-fill and native bid/ask cost semantics. | Treat PR328 as draft scaffold only; do not merge or reboot daemon until P0 semantic wiring blockers are resolved and CI/reviewer gates are green. | Required repair path: event-specific causal family hydration, p_live, native executable cost, TradeScore, full-family FDR, typed Kelly, RiskGuard, same-event final intent, executor. |
| EDLI event/evidence tables belong in world DB. | Section 7 ownership corrected from earlier package. | Root `AGENTS.md` defines three-DB split; `architecture/db_table_ownership.yaml` is canonical table ownership. | None. | Add world-owned schema modules and registry rows; add wrong-DB tests. | Schema version bump shape. |
| Event row must be immutable, processing mutable. | Sections 7-9. | `src/state/AGENTS.md` requires append-first discipline for truth surfaces. | None. | Separate `opportunity_events` and `opportunity_event_processing`; block updates to event rows. | Existing trigger style for append-only guard. |
| Market channel is public data, not fill truth. | Sections 12, 21, 24. | `src/execution/AGENTS.md` and `src/venue/AGENTS.md` preserve executor/venue boundary. | Polymarket market channel docs define public L2 data; user channel docs define authenticated order/trade updates. | Implement `market_channel_ingestor.py` as online quote/cache/evidence service only; never write fill truth. | User-channel reconciliation mapping remains existing authority. |
| BUY YES/NO uses native ask; SELL uses held-token bid. | Section 14. | `MarketAnalysis` already distinguishes native NO quote availability per spec. | Polymarket orderbook docs define BUY as best ask and SELL as best bid. | Implement native book-walk cost; forbid complement/midpoint/last-trade costs. | Token map freshness seam. |
| FOK/FAK live is not authorized by current execution law. | Sections 14-15, PR10 notes. | `src/execution/AGENTS.md` says limit orders only and Zeus provides liquidity on entry. | Polymarket create-order docs define FOK/FAK as immediate market order types. | Keep `taker_fok_fak_live_enabled=false`; collect feasibility evidence only. | Requires separate execution-law packet if changed. |
| Forecast COMPLETE may be live; partial is evidence/no-trade. | Sections 10, 20, 24. | `src/data/executable_forecast_reader.py` and source/coverage tables are current evidence surfaces. | ECMWF Open Data docs describe cycle and step availability including Cycle 50r1 change. | Reuse executable forecast reader/evidence for live eligibility; classify partial states without "shadow" terminology. | Exact existing API call shape. |
| Day0 hard fact must use source availability and settlement rounding. | Sections 11, 21, 24. | `Day0ObservationContext` and `SettlementSemantics` are current authorities. | None. | Trigger uses `observation_available_at`; absorbing boundary delegates rounding to `SettlementSemantics`. | Exact durable Day0 observation writer hook. |
| FDR/Kelly/RiskGuard are mandatory. | Sections 13-15. | `src/strategy/AGENTS.md` mandates full-family FDR; `src/riskguard/AGENTS.md` mandates enforcement. | None. | Reactor adapter must call existing paths and log rejections. | Exact adapter seam. |
| Online final config uses EDLI enabled/live but stale-book and FOK/FAK live off. | Prompt section 16. | `config/AGENTS.md` says `settings.json` controls runtime behavior. | None. | Add `edli_v1` config keys in repo style. | Daemon restart command discovery. |
| Live submit adapter must preserve existing money-path gates. | Prompt sections 13 and 21. | `src/engine/evaluator.py`, `src/engine/cycle_runner.py`, `src/engine/cycle_runtime.py`, `src/strategy/kelly.py`, `src/riskguard/riskguard.py`, and `src/execution/executor.py` own the current gate chain. | None. | Reactor gates source truth, executable snapshot freshness, RiskGuard GREEN, and then calls the existing cycle runner via `submit_existing_cycle_for_event()` so final-intent and executor side effects stay in the established money path. | FDR/Kelly/final-intent remain enforced inside the existing cycle runner rather than duplicated per event. |
| Replay correctness gate is required for closeout. | Prompt section 19. | `scripts/replay_correctness_gate.py` expects `state/zeus_trades.db`. | None. | Plain worktree invocation is blocked by absent local DB; canonical-main DB invocation with worktree-local baseline passes. | REVIEW_REQUIRED only for plain isolated-worktree default path; operator should rerun in live root before restart. |
| Forecast trigger must survive daemon reboot and catch up after commits. | Prompt section 8. | `source_run`, `source_run_coverage`, `ensemble_snapshots_v2`, and `src/data/executable_forecast_reader.py` are current forecast evidence surfaces. | ECMWF Open Data cycle/step contract remains external source authority. | Added `ForecastSnapshotReadyTrigger.scan_committed_snapshots()` and daemon catch-up call before reactor processing; live eligibility delegates to `read_executable_forecast_snapshot()`. | Scanner is the implemented reboot/catch-up path; direct ingest callback is optional future latency optimization. |
| Day0 trigger needs a durable catch-up path. | Prompt section 9. | Trade DB `settlement_day_observation_authority` exists and is the durable observation authority surface. | None. | Added daemon catch-up scanner over `settlement_day_observation_authority`; uses persisted `payload_json.observation_available_at` when present, otherwise `recorded_at` as durable availability evidence; uses `SettlementSemantics` resolver. | Direct `Day0ObservationContext` callback is optional future latency optimization. |
| Market channel must run as online data/quote/evidence service. | Prompt section 10. | `executable_market_snapshots` carries active YES/NO token IDs; `src/main.py` owns daemon scheduler jobs; `src/data/polymarket_client.py` has public `/book` orderbook fetch. | Polymarket public market channel is data-only; user channel/reconcile is fill truth. | Added quote cache, Polymarket REST seed/reconnect, public websocket loop, active weather token discovery, daemon thread scheduler job, and feasibility evidence conversion. | Live websocket connectivity smoke test remains after operator reboot; public market channel remains non-fill authority. |
| Every rejection writes NoTradeRegretLedger. | Prompt section 14 and A30. | `no_trade_regret_events` world table and compatibility `no_trade_events` exist. | None. | Reactor writes regret rows when a ledger is supplied; daemon hook supplies `NoTradeRegretLedger`. | Real final-intent rejection reasons depend on future live gate adapter wiring. |
| Day0 pilot live cap must survive daemon scheduler ticks. | Prompt section 21 A34. | `src/main.py` constructs a fresh reactor each scheduler tick, so in-memory caps are insufficient. | None. | Added durable `edli_live_cap_usage` world table and `EdliLiveCapLedger` enforcement before Day0 final submit. | Actual post-submit notional remains conservatively reserved at configured tiny cap because the existing cycle owns final order sizing. |
