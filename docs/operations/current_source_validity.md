# Current Source Validity

Source-validity posture is folded into the unified current-fact snapshot as of 2026-07-21:

→ **`docs/operations/current_data_state.md`**

That file carries the live forecast authority (fusion `openmeteo_ecmwf_ifs9_bayes_fusion_v1`), the retired sources (AIFS deleted commit `2764616bf`), and the active observation/settlement sources, all evidence-backed and expiry-bound.

(This file previously duplicated that content verbatim. Kept as a stable pointer so references to "source validity" still resolve — update the snapshot there, not here.)
