# Archived work packets

Completed work packets are historical records.

- `work_packets/` now keeps only the current live packet.
- Archived packets in this folder preserve prior packet intent, evidence shape, and acceptance history.
- Historical packet paths may still appear in older docs or comments; treat those references as archive-only unless a newer packet explicitly promotes them back into the live surface.
