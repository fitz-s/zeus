# T1G Critic Review (10-ATTACK)

Reviewer: critic (subagent `a03538fb0b5f999ed`), 2026-05-05.
HEAD at review: T1E closeout commit + T1G worktree changes.
Phase contract: `phases/T1G/phase.json` (4 invariants, K0 LIVE BOUNDARY, verification-first).
Phase scope: `phases/T1G/scope.yaml` (1 in_scope file: the new test).

## Phase context

- VERIFICATION-FIRST phase. Audit-only outcome confirmed: tracked diff is `architecture/test_topology.yaml` (+27 lines: 1 trust_policy row + 1 detailed test_metadata block). Untracked: `tests/test_final_sdk_envelope_persistence.py` (in_scope) + `docs/.../phases/T1G/audit/sdk_envelope_path_audit.md` (the primary phase artifact).
- 13 sites classified: 7 VERIFIED_PERSISTS, 0 NEEDS_FIX, 6 NOT_LIVE_PATH. Audit conclusion: source-edit scope is empty; T1G closes audit-only.
- Independent reproduction: `python -m pytest -q tests/test_final_sdk_envelope_persistence.py tests/test_executor_command_split.py tests/test_v2_adapter.py` returns `65 passed in 0.85s`.
- Cross-phase invariants intact (verified via direct grep): T1A (1 settlement_commands DDL match), T1F gate (audit re-asserts in dedicated section), T1BD/T1C/T1E (no diff in chain_reconciliation/portfolio/harvester/db.py).

## Audit verdict spot-checks

Independent traces of 4 audit verdicts (random sample):

**Site 1 (executor.py:1609, VERIFIED_PERSISTS) — TRACED + CONFIRMED**: Read executor.py:1481-1609. The flow is: line 1484 `client = PolymarketClient()` (try-block). On success, line 1525 `result = client.place_limit_order(...)`. On exception in 1525, control jumps to 1532 (M2 SUBMIT_TIMEOUT_UNKNOWN, NOT a SUBMIT_REJECTED — sites 11/12 territory). On success, control reaches 1609 `_persist_final_submission_envelope_payload(conn, result, command_id=command_id)`. The `result` dict carries `_venue_submission_envelope` per polymarket_client.py:_legacy_order_result_from_submit. Audit verdict CORRECT.

**Site 3 (executor.py:1495, NOT_LIVE_PATH) — TRACED + CONFIRMED**: Read executor.py:1484-1521. Line 1495 SUBMIT_REJECTED is INSIDE the `except Exception` block of `client = PolymarketClient()` at line 1486. The constructor raised before any SDK contact. Reason payload at line 1498 reads `pre_submit_client_init_failed` — explicit pre-submit semantics. Code comment at lines 1487-1489: "Constructor / credential / adapter setup failures happen before any venue submit side effect. They are safe terminal rejections, not M2 unknown-side-effect outcomes." No SDK response object exists in this scope. Audit verdict CORRECT.

**Site 7 (executor.py:2138, NOT_LIVE_PATH) — TRACED + CONFIRMED**: Read executor.py:2125-2157. Line 2126 `client.v2_preflight()`. Lines 2127-2157 are the `except V2PreflightError` block. v2_preflight is a read-only endpoint-identity check (per Phase 4 docstring at line 2083 "V2 endpoint-identity preflight"). It does NOT submit an order. The SUBMIT_REJECTED at line 2138 has reason `v2_preflight_failed` — no order was sent, no SDK submission response. Audit verdict CORRECT.

**Site 9 (executor.py:2342, VERIFIED_PERSISTS) — TRACED + CONFIRMED**: Read executor.py:2290-2367. Line 2291 `final_envelope_payload = _persist_final_submission_envelope_payload(conn, result, command_id=command_id)` runs BEFORE line 2331 `if result.get("success") is False:`. The success_false branch at line 2342 SUBMIT_REJECTED uses `**final_envelope_payload` spread at line 2347 — verified directly from source. The persisted SDK-returned envelope reference is threaded into the rejection event payload. Audit verdict CORRECT.

**Site 11 (executor.py:1568, NOT_LIVE_PATH) — TRACED + CONFIRMED**: Read executor.py:1532-1569. Line 1532 `except Exception as exc:` catches SDK exceptions from `client.place_limit_order(...)` at line 1525. The `result` variable is NOT BOUND in this scope (the assignment in 1525 raised before completing). Line 1541 appends `event_type="SUBMIT_TIMEOUT_UNKNOWN"`. Line 1568 sets `command_state="SUBMIT_UNKNOWN_SIDE_EFFECT"` on the OrderResult constructor (the planner-cited line). No `result` dict exists; therefore no `_venue_submission_envelope` exists; therefore persistence is not possible (and would be a design error if attempted). The recovery path is the idempotency_key embedded at line 1547. Audit verdict CORRECT. Cite-rot disclaimer in audit ("actual event is SUBMIT_TIMEOUT_UNKNOWN at 1541; SUBMIT_UNKNOWN_SIDE_EFFECTS at 1568 is the command_state field") is technically accurate.

4/4 spot-checks confirm audit reasoning. No hand-waving observed.

## Attack table

| # | Attack | Verdict | Evidence (file:line) |
|---|--------|---------|---------------------|
| 1 | Audit-doc honesty | PASS | Spot-checked Sites 1, 3, 7, 9, 11 by independent trace (see above). Each verdict in the audit doc is supported by literal source-line evidence; no hand-waving. The audit doc includes verbatim code excerpts with file:line annotations and traces the post-call flow (e.g., Site 13 traces the chain `submit() → _submit_result_from_response → polymarket_client._legacy_order_result_from_submit → _persist_final_submission_envelope_payload`). The 4 spot-checks would have caught any honest hand-wave at the verdict level. |
| 2 | NOT_LIVE_PATH justification | PASS | All 6 NOT_LIVE_PATH verdicts have explicit reasons: Sites 3+6 ("client init failed before SDK contact"; pre-submit local failure), Sites 7+8 ("v2_preflight is read-only, no order submitted"; preflight before submit), Sites 11+12 ("SDK exception, no result dict bound; SUBMIT_TIMEOUT_UNKNOWN event uses idempotency_key for recovery"). None says "out of scope" or hand-waves. Each reason maps to verifiable source evidence (e.g., Site 7 cites `client.v2_preflight()` at line 2126 — a read-only endpoint-identity check). |
| 3 | NEEDS_FIX claim is real | PASS | Spot-checked SUBMIT_REJECTED Sites 4 (executor.py:1662) and 9 (executor.py:2342). Both are inside `if result.get("success") is False:` branches that execute AFTER `_persist_final_submission_envelope_payload(...)` was called. Reading the source: line 1609 (Site 1 persistence call) happens BEFORE line 1662 (Site 4 SUBMIT_REJECTED on success_false), and line 2291 (Site 2 persistence call) happens BEFORE line 2342 (Site 9 SUBMIT_REJECTED on success_false). The `**final_envelope_payload` spread at lines 1667/1696/2347/2375 (Sites 4, 5, 9, 10) carries the persisted SDK envelope reference. No bypass path found. The 0 NEEDS_FIX result is not self-serving — it's structurally enforced by the persistence call placement BEFORE the success_false / missing_order_id branches. |
| 4 | Relationship test exercises real path | PASS | `tests/test_final_sdk_envelope_persistence.py` invokes real production functions: `_live_order(...)` at line 334 (Test 1), `_live_order(...)` at line 422 (Test 2), `execute_exit_order(...)` at line 508 (Test 3). Mocking is at the SDK boundary (`patch("src.data.polymarket_client.PolymarketClient")` at lines 319, 405, 494) — which is the correct level: the entire executor.py code path is exercised, not a toy adapter. The fake SDK returns a `_venue_submission_envelope` distinguishable from the pre-submit envelope by `raw_response_json` populated and `order_id=SDK-RETURNED-*` marker. ASSERTION 1 in each test counts `venue_submission_envelopes` rows where `raw_response_json IS NOT NULL` (i.e., post-submit rows) — this is exactly the structural antibody. |
| 5 | Test would FAIL on synthetic gap | PASS (mental simulation) | If `_persist_final_submission_envelope_payload(conn, result, command_id=command_id)` at executor.py:2291 were removed, then `final_envelope_payload` would be undefined at line 2347 (would raise NameError). Even if the line were replaced with `final_envelope_payload = {}`, the test would fail at ASSERTION 1 (`post_submit_count >= 1`) because no row with `raw_response_json IS NOT NULL` would be inserted. ASSERTION 2 (`final_submission_envelope_id` in event) would also fail because the empty spread would not contribute the field. Antibody is REAL — the test is not performative. Test 1 + Test 3 cover the ACK path; Test 2 covers the REJECTED-with-success_false path. All three would fail loudly on a synthetic-gap regression. |
| 6 | FOK/FAK coverage | PASS | Audit doc "FOK/FAK Coverage Section" (lines 290-303) is present and substantive. Verified from source: `polymarket_v2_adapter.py:362` passes `order_type=envelope.order_type` to `client.create_and_post_order(...)`, line 380 passes the same to `client.post_order(...)`. The `submit()` function does not branch on order_type — it routes all order types through the same persistence chain. The audit's claim "order_type does NOT affect the code path through submit()" is verified. The audit acknowledges that test fixture coverage of FOK/FAK specifically would require parametrizing the `_make_entry_intent` helper with `order_type="FOK"` — this is NOT done in the current test file. **Caveat C-1 LOW: the audit claims coverage via structural argument; no explicit FOK/FAK parametrized test fixture exists in `test_final_sdk_envelope_persistence.py`. Severity LOW because (a) the structural argument is sound (order_type is a string parameter passed to the SDK, not a code-branch selector), (b) FOK/FAK behavior at the venue layer is the SDK's responsibility, not the persistence layer's. Recommend a follow-up parametrized test if telemetry shows FOK/FAK orders are actually used in production.** |
| 7 | T1F-inherited gate section | PASS | Audit doc "T1F-Inherited Gate Section" (lines 307-321) is present and substantive. Identifies two gate layers: `polymarket_v2_adapter.py:315-328` (T1F primary — `assert_live_submit_bound()` before SDK call) and `polymarket_client.py:407-424` (mirror gate). Re-asserts `T1F-PLACEHOLDER-ENVELOPE-FAKE-SDK-COUNT-ZERO`. Cross-checked: T1F's adapter gate at line 315 + the placeholder envelope rejection at line 324-328 (verified during T1G unrelated grep) confirms the gate is intact. Audit correctly notes T1G test fixtures must use live-bound (non-placeholder) envelopes — which is what `_make_entry_intent` and `_make_exit_intent` produce. |
| 8 | Cite-rot drift handling | PASS | Executor's deviation note matches the audit's per-site "Line drift: None" entries (12/13 sites, with the 2 SUBMIT_UNKNOWN_SIDE_EFFECT sites carrying explicit semantic-site disclosures). Independent verification: `command_state="SUBMIT_UNKNOWN_SIDE_EFFECT"` at executor.py:1568 (matches planner cite — a field assignment on OrderResult constructor); `event_type="SUBMIT_TIMEOUT_UNKNOWN"` at executor.py:1541 (the actual append_event call). Same structure at 2225/2251. The SDK exception path enters the `except Exception` block where `result` is unbound — there is NO partial response dict because the assignment `result = client.place_limit_order(...)` raised before completing. Adversarial probe: even if the SDK raised AFTER returning a partial dict (e.g., a network error during dict construction), Python's exception model means the partial dict would not be assigned to `result`. NOT_LIVE_PATH classification is sound. |
| 9 | No source touched | PASS | `git diff src/` returns empty. `git diff` for each of the 5 critical surfaces (`src/execution/executor.py`, `src/venue/polymarket_v2_adapter.py`, `src/state/venue_command_repo.py`, `src/contracts/venue_submission_envelope.py`, `src/state/db.py`) returns empty. Only `architecture/test_topology.yaml` is modified (+27 lines: 1 trust_policy row + 1 detailed test_metadata block, format consistent with prior phases). T1G-NO-NEW-SDK-CALL-PATH and T1G-ONLY-MISSING-PATHS-PATCHED both satisfied: zero source-edit work. |
| 10 | No prior-phase regression | PASS | T1A: `git grep -c "CREATE TABLE IF NOT EXISTS settlement_commands" src/execution/settlement_commands.py` = 1 (intact). T1F+T1BD+T1C+T1E: all 65 tests in the audit-related suites (test_final_sdk_envelope_persistence + test_executor_command_split + test_v2_adapter) pass cleanly. No diff in any prior-phase source surface. Cross-phase consumed_invariants list (T1A-DDL-SINGLE-SOURCE, T1F-ADAPTER-ASSERTS-LIVE-BOUND-BEFORE-SDK, T1F-PLACEHOLDER-ENVELOPE-FAKE-SDK-COUNT-ZERO, T1BD-CORRECTED-NO-CHAIN-MUTATION-ALL-SITES, T1C-LIVE-PRAW-NOT-TRAINING-DATA, T1E-LOCK-TIMEOUT-DEGRADE-NOT-CRASH) all preserved. |

## Caveats

- **C-1 (LOW)**: FOK/FAK coverage is via structural argument in the audit (order_type is a parameter passed through, not a code-branch selector) — not via parametrized test fixture. The structural argument is sound (verified at `polymarket_v2_adapter.py:362` and `:380` — order_type is forwarded as a parameter, not branched on). No explicit `pytest.mark.parametrize("order_type", ["GTC", "FOK", "FAK"])` test exists. Severity LOW because the persistence path is order_type-agnostic by design. Recommend a follow-up parametrized fixture if FOK/FAK telemetry shows production usage.

- **C-2 (LOW carry-forward from T1F-T1E)**: T1G inherits T1F's existing C-1 LOW caveat (counter emits use `logger.warning("telemetry_counter event=...")` text-tap pattern, not test-asserted via caplog). T1G does not introduce new counters but consumes T1F's gate emit (`telemetry_counter event=placeholder_envelope_blocked_total`) at the audit boundary. Same deferred-to-T2F treatment. Not new in T1G; consistent.

## T1G Verdict

CRITIC_DONE_T1G
verdict: APPROVE_WITH_CAVEATS
audit_doc_honest: yes
not_live_path_justifications_sound: yes
no_missed_needs_fix: yes
relationship_test_exercises_real_path: yes
test_would_fail_on_synthetic_gap: yes
fok_fak_coverage_present: yes
t1f_gate_section_present: yes
cite_rot_handling_sound: yes
no_src_touched: yes
no_prior_regression: yes
caveats: ["C-1 LOW: FOK/FAK coverage is via structural argument in the audit (sound — order_type is a parameter passed through, not a code-branch selector) but no explicit parametrized test fixture exists in test_final_sdk_envelope_persistence.py; recommend follow-up if FOK/FAK telemetry shows production usage", "C-2 LOW (carry-forward from T1F-T1E): T1G inherits T1F counter emit text-tap pattern; not T1G-introduced; deferred to T2F typed sink (consistent treatment with prior phases)"]
ready_for_close: yes
