# HOOK_SCHEMA_CHANGE Evidence

date: 2026-05-06
override_id: HOOK_SCHEMA_CHANGE
phase: hook-redesign-Phase-2
authorized_by: operator (Fitz)
operator_signed_sentinel: evidence/operator_signed/HOOK_SCHEMA_CHANGE_2026-05-06.signed

## Scope of Changes

1. `.claude/hooks/pre-commit-invariant-test.sh` — +~25 LOC migration shim:
   - Adds STRUCTURED_OVERRIDE=BASELINE_RATCHET/MAIN_REGRESSION/COTENANT_SHIM as preferred bypass path
   - Legacy [skip-invariant] still accepted, emits migration_warning ritual_signal to hook_signal jsonl
   - Migration runway: 30 days (retire 2026-06-06)
   - All 4 existing escape-hatch behaviors preserved

2. `.claude/hooks/dispatch.py` — Phase 2 implementation (already written above this sentinel)

3. `.claude/hooks/registry.yaml` — catalog_size 11→12, pre_edit_hooks_protected added

4. `.claude/settings.json` — 7→12 hooks (parallel-shipping)

## Justification

Phase 2 of hook redesign per PLAN §3. This is an authorized architectural
change, not an agent attempting to self-disable gates. The pre_edit_hooks_protected
hook itself is new in this phase and its first act is to block further edits
to hook files — requiring this sentinel for the migration shim edit is correct behavior.
